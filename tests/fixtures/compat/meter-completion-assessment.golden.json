{
  "__note": "Deliberately changed in Phase 42 Plan 04. --outcome-value moved from the marker-derived 525.0 point estimate to the sidecar's 446.25 low bound (D-08: --outcome-value carries value_low, not value_base -- understates rather than overstates). --metadata's shape moved from the marker's frozen 9-key `assessment` summary to the job-assessments SIDECAR record (D-10: the sidecar is the only value/provenance source; the marker plays no part), gaining value_low/value_base/value_high/bounds_source/assessment_schema_version alongside the pre-existing provenance fields. This fixture is NOT one of the four immutable v1.x wire-contract goldens named in tests/test_compat_v1_4_meta.py's docstring (jobs-outcome, meter-completion, meter-completion-markerless, meter-tool-event) -- it is eligible for a deliberate, decision-traceable change like this one. WR-04 (Phase 42 code review): re-pinned to include taxonomy_version, prompt_version, policy_version and model -- classifier.py's _build_job_assessment populates all four unconditionally and Plan 42-05 made hermes-report.sh forward them, so the previous pattern described a wire shape production never sends. Derived from the reporter's actual emitted --metadata, not hand-written. Phase 43 Plan 01 (EGV-18, T-43-04): re-pinned again to add reportability_status (value 'reportable', immediately after evidence_class) -- the reportability decision now rides in --metadata so a withheld/candidate row is distinguishable on the tenant from a row dropped for bounds or schema reasons. Still derived from the reporter's actual emitted argv, not hand-written. Phase 44 Plan 01 (EGV-05, D-01/D-03): re-pinned again to insert economic_mechanism ('labor_substitution', immediately after reportability_status and before evaluator) -- the mechanism this plan makes selectable and forwardable now rides in --metadata too. Re-pinned in place rather than as a second golden, for the same four reasons WR-04 and reportability_status already established: this fixture is explicitly eligible for decision-traceable change, it has moved twice by exactly this route already, there is exactly one call site so a second golden would leave this pattern pinning a shape production no longer sends, and the earlier pattern's provenance survives in this note's history and in git. Still derived from the reporter's actual emitted argv, not hand-written. Phase 44 Plan 02 (EGV-14/EGV-15, D-06/D-07/D-08): re-pinned again to insert net_value, supplied_costs and cost_coverage immediately after economic_mechanism and before evaluator -- net_value subtracts every supplied cost category (not AI cost alone) and is withheld from a candidate row through the shared _strip_value_family stripper (D-07); supplied_costs and cost_coverage are operator input, not model output, so they ship unconditionally regardless of reportability_status. Re-pinned in place rather than as a second golden, for the same reasons this note's prior entries already established. Still derived from the reporter's actual emitted argv, not hand-written. Phase 44 Plan 03 (EGV-16, D-12/D-13): re-pinned a final time to insert double_counting_group ('g38-sid-002', immediately after cost_coverage and before evaluator) -- the group id marking that several jobs inferred from one session's transcript served one outcome now rides in --metadata too, closing the contiguous Phase 44 forwarder block this plan's own docstring says no later field in this phase follows. Like supplied_costs/cost_coverage, this is caller-supplied structural identity, not model output, so it ships unconditionally regardless of reportability_status. Scope, stated here so a reader of this fixture does not infer more than the field delivers: the id groups jobs inferred from ONE session's transcript ONLY (job inference runs only when root_sid == session_id) -- it does NOT resolve cross-session or root-plus-subagent attribution. Re-pinned in place rather than as a second golden, for the same reasons this note's prior entries already established. Still derived from the reporter's actual emitted argv, not hand-written. Phase 46 Plan 02 Task 4 (EGV-21, D-06/D-07, checkpoint decision ship-both): re-pinned a final time to insert inference_provider and inference_address_class immediately after model and before confidence -- classifier.py's _build_job_assessment populates both UNCONDITIONALLY on every record it builds (Phase 46 Plan 02 Task 2), and this plan makes hermes-report.sh forward both under two different disciplines: inference_provider on the isinstance + non-empty + 32-byte-slice shape evaluator/evaluator_version already use, inference_address_class on an allow-list-then-drop discipline mirroring _ECONOMIC_MECHANISMS (an out-of-set value is dropped silently, never passed through). Both belong to _PROVENANCE_FAMILY_META_KEYS (plan 46-01's two-tier drop), not the value-family tier and not the always-kept base-metering set -- a truncated record should still say where inference was configured to go. \"openrouter\"/\"public\" is the exact worked example from 46-RESEARCH.md Q3 (https://openrouter.ai/api/v1 -> public). Still derived from the reporter's actual emitted argv, not hand-written. Phase 50 Plan 01 (DECL-01/DECL-02/DECL-05, Task 1 checkpoint decision \"option-b\"): re-pinned a final time to insert evidence_class_authority ('evaluator', immediately after evidence_class and before reportability_status) -- classifier.py's new _evidence_class_precedence walk names WHICH of the four boundaries (evidence, valuation, classification, evaluator) decided evidence_class, and this plan makes hermes-report.sh forward it, gated on membership of that four-word set. 'evaluator' is the honest value for this fixture's own default-arm 'MODEL_ESTIMATED_DEMO' evidence_class: no boundary votes on an unconfigured install, so the walk falls through to _forced_evidence_class() with authority 'evaluator'. Still derived from the reporter's actual emitted argv, not hand-written.",
  "verb": [
    "jobs",
    "outcome"
  ],
  "exact_match_fields": {
    "__verb": "jobs",
    "__subcommand": "outcome",
    "__positional_args": [
      "assessment-golden-job"
    ],
    "--result": "SUCCESS",
    "--outcome-type": "CONVERTED",
    "--outcome-value": "446.25",
    "--outcome-currency": "USD",
    "--quiet": true
  },
  "pattern_fields": {
    "--metadata": "^\\{\"source\":\"test\",\"value_low\":446\\.25,\"value_base\":525\\.0,\"value_high\":603\\.75,\"bounds_source\":\"derived\",\"assessment_schema_version\":1,\"taxonomy_version\":1,\"prompt_version\":1,\"policy_version\":1,\"evidence_class\":\"MODEL_ESTIMATED_DEMO\",\"evidence_class_authority\":\"evaluator\",\"reportability_status\":\"reportable\",\"economic_mechanism\":\"labor_substitution\",\"net_value\":500\\.0,\"supplied_costs\":\\{\"human_review\":25\\.0\\},\"cost_coverage\":\\{\"included\":\\[\"human_review\"\\],\"unknown\":\\[\"rework_or_error\",\"handoff\",\"training_or_change\"\\],\"excluded\":\\[\"metered_ai_cost\"\\]\\},\"double_counting_group\":\"g38-sid-002\",\"evaluator\":\"llm\",\"evaluator_version\":\"v1\",\"model\":\"unknown\",\"inference_provider\":\"openrouter\",\"inference_address_class\":\"public\",\"confidence\":0\\.8,\"assumptions\":\\{\"estimated_hours_saved\":3\\.5,\"assumed_loaded_rate\":150\\.0\\}\\}$"
  },
  "forbidden_fields": [
    "--budget-id",
    "--alert-id"
  ]
}
