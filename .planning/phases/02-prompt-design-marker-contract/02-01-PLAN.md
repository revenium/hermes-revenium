---
phase: 02-prompt-design-marker-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/revenium/task-taxonomy.json
  - skills/revenium/references/task-taxonomy.md
  - examples/setup-local.sh
  - tests/test_repository.py
autonomous: true
requirements: [TAX-01, TAX-02, TAX-03, TAX-04, PROMPT-06, TEST-02]
tags: [taxonomy, json-fixture, reference-doc, install-time-copy, test-invariant]

must_haves:
  truths:
    - "The seed `skills/revenium/task-taxonomy.json` exists and contains exactly the 8 labels in D-06 order: research, analysis, generation, review, code_review, refactor, planning, debugging."
    - "Every taxonomy label key matches the regex `^[a-z][a-z0-9_]{1,47}$` and no label in the seed is in the D-12 blocklist (ack, acknowledgment, greeting, confirmation, hello, thanks)."
    - "`skills/revenium/references/task-taxonomy.md` documents the schema (top-level `labels` object; per-label `{description, examples}`), the label regex, the blocklist, the lookup-first mint policy, and the atomic write-to-tmp + os.rename + fcntl.flock pattern."
    - "`examples/setup-local.sh` performs a guarded install-time copy of the seed `task-taxonomy.json` into `${REVENIUM_STATE_DIR:-${HOME}/.hermes/state/revenium}/task-taxonomy.json` only if no live file already exists at that path (does not overwrite an existing live taxonomy)."
    - "`python3 -m unittest tests.test_repository.RepositoryTests.test_taxonomy_file_schema` and `tests.test_repository.RepositoryTests.test_expected_files_exist` both pass after this plan."
  artifacts:
    - path: "skills/revenium/task-taxonomy.json"
      provides: "Seed taxonomy fixture, 8 labels in D-06 order, schema {labels: {<name>: {description, examples}}}"
      contains: "research, analysis, generation, review, code_review, refactor, planning, debugging"
    - path: "skills/revenium/references/task-taxonomy.md"
      provides: "Cold-path reference doc: schema, label catalog, normalization rules, mint policy, atomic write pattern"
      min_lines: 60
    - path: "tests/test_repository.py"
      provides: "TEST-02 (test_taxonomy_file_schema); test_expected_files_exist extended with task-taxonomy.json and references/task-taxonomy.md"
      exports: ["test_taxonomy_file_schema"]
    - path: "examples/setup-local.sh"
      provides: "Install-time copy of seed task-taxonomy.json to ${STATE_DIR}/task-taxonomy.json (guarded — no overwrite of live file)"
  key_links:
    - from: "examples/setup-local.sh"
      to: "${REVENIUM_STATE_DIR:-${HOME}/.hermes/state/revenium}/task-taxonomy.json"
      via: "guarded cp (only if destination does not exist)"
      pattern: "task-taxonomy.json"
    - from: "tests/test_repository.py::test_taxonomy_file_schema"
      to: "skills/revenium/task-taxonomy.json"
      via: "Path read + json.loads + per-label regex/blocklist check"
      pattern: "SKILL / 'task-taxonomy.json'"
    - from: "tests/test_repository.py::test_expected_files_exist"
      to: "skills/revenium/task-taxonomy.json and skills/revenium/references/task-taxonomy.md"
      via: "expected list contains both new paths"
      pattern: "task-taxonomy"
---

<objective>
Ship the seed taxonomy fixture, the cold-path taxonomy reference doc, the install-time taxonomy copy, and the TEST-02 invariant. This plan is the lowest-blast-radius slice of Phase 2 — no SKILL.md edits, no marker-write code, no halt-block changes. After this plan, the live runtime taxonomy file is seeded by `setup-local.sh` on fresh installs, the seed-file shape is locked by an invariant test, and the agent has a reference doc to consult when making mint-vs-reuse decisions.

Purpose: Pin the agent ↔ cron taxonomy contract by shipping a checked-in fixture with deterministic byte order, a self-documenting schema reference, and a test invariant that fails fast on schema drift. Decouples taxonomy artifacts from the prompt-side work in plan 02-03.

Output: One new JSON fixture, one new reference doc, an extended `setup-local.sh`, and a new test method plus an extended file-existence list in `tests/test_repository.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-prompt-design-marker-contract/02-CONTEXT.md
@.planning/phases/02-prompt-design-marker-contract/02-RESEARCH.md
@.planning/phases/02-prompt-design-marker-contract/02-PATTERNS.md
@.planning/phases/01-path-foundation/01-01-SUMMARY.md
@CLAUDE.md
@skills/revenium/scripts/common.sh
@skills/revenium/references/setup.md
@tests/test_repository.py
@examples/setup-local.sh

<interfaces>
<!-- Phase 1 outputs (already shipped) — what this plan consumes from common.sh -->
From skills/revenium/scripts/common.sh:
```bash
TAXONOMY_FILE="${REVENIUM_TAXONOMY_FILE:-${STATE_DIR}/task-taxonomy.json}"
MARKERS_DIR="${REVENIUM_MARKERS_DIR:-${STATE_DIR}/markers}"
STATE_DIR="${REVENIUM_STATE_DIR:-${HERMES_HOME}/state/revenium}"
HERMES_HOME="${HERMES_HOME:-${HOME}/.hermes}"
```

From tests/test_repository.py (existing pattern to extend):
```python
ROOT = Path(__file__).resolve().parents[1]
SKILL = ROOT / 'skills' / 'revenium'

class RepositoryTests(unittest.TestCase):
    def test_expected_files_exist(self):
        expected = [...SKILL / 'references' / 'setup.md', SKILL / 'references' / 'troubleshooting.md', ...]
```

From PATTERNS.md — required taxonomy shape:
```json
{
  "labels": {
    "research":    {"description": "...", "examples": ["...", "..."]},
    "analysis":    {"description": "...", "examples": ["...", "..."]},
    "generation":  {"description": "...", "examples": ["...", "..."]},
    "review":      {"description": "...", "examples": ["...", "..."]},
    "code_review": {"description": "...", "examples": ["...", "..."]},
    "refactor":    {"description": "...", "examples": ["...", "..."]},
    "planning":    {"description": "...", "examples": ["...", "..."]},
    "debugging":   {"description": "...", "examples": ["...", "..."]}
  }
}
```

From PATTERNS.md — required TEST-02 method signature (RESEARCH.md Draft Test Signatures):
```python
def test_taxonomy_file_schema(self):
    """Seed task-taxonomy.json has correct schema and all labels match the regex."""
    import json, re
    taxonomy_path = SKILL / 'task-taxonomy.json'
    self.assertTrue(taxonomy_path.exists(), 'task-taxonomy.json missing from skill root')
    data = json.loads(taxonomy_path.read_text())
    self.assertIn('labels', data, 'taxonomy missing top-level "labels" key')
    labels = data['labels']
    self.assertIsInstance(labels, dict, '"labels" must be a dict')
    label_regex = re.compile(r'^[a-z][a-z0-9_]{1,47}$')
    forbidden = {'ack', 'acknowledgment', 'greeting', 'confirmation', 'hello', 'thanks'}
    expected_labels = ['research', 'analysis', 'generation', 'review',
                       'code_review', 'refactor', 'planning', 'debugging']
    self.assertEqual(list(labels.keys()), expected_labels,
                     'seed taxonomy labels must match D-06 order exactly')
    for label, schema in labels.items():
        self.assertRegex(label, label_regex, f'label "{label}" fails regex')
        self.assertNotIn(label, forbidden, f'forbidden label "{label}" in seed taxonomy')
        self.assertIn('description', schema, f'label "{label}" missing description')
        self.assertIn('examples', schema, f'label "{label}" missing examples')
        self.assertIsInstance(schema['description'], str, f'label "{label}" description must be str')
        self.assertIsInstance(schema['examples'], list, f'label "{label}" examples must be list')
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ship seed task-taxonomy.json (TAX-01, TAX-02, TAX-03)</name>
  <files>skills/revenium/task-taxonomy.json</files>
  <read_first>
    - .planning/phases/02-prompt-design-marker-contract/02-RESEARCH.md (Architecture Patterns Pattern 3 — required JSON shape; lines 286–301)
    - .planning/phases/02-prompt-design-marker-contract/02-PATTERNS.md (lines 23–55 for label order and JSON formatting convention)
    - .planning/phases/02-prompt-design-marker-contract/02-CONTEXT.md (D-06, D-07, D-08 — locked decisions on label list, schema, mint policy)
    - skills/revenium/scripts/budget-check.sh (line 83 pattern: `json.dumps(data, indent=2) + '\n'` — the project's canonical JSON-file write style)
  </read_first>
  <action>
Create a new file at `skills/revenium/task-taxonomy.json` containing a single top-level JSON object with one key, `labels`. The value of `labels` is an object with exactly 8 keys, in this exact insertion order (D-06 — locked): `research`, `analysis`, `generation`, `review`, `code_review`, `refactor`, `planning`, `debugging`. Each value is an object with exactly two keys: `description` (string) and `examples` (array of 2 strings). Use `json.dumps(..., indent=2, ensure_ascii=True)` byte order (2-space indent, ASCII-only) with a single trailing newline. Per D-07, the per-label schema is `{description, examples}` — no other keys (no aliases, no metadata, no version, no tags). Per TAX-03, every label key must match `^[a-z][a-z0-9_]{1,47}$` (the supplied 8 all satisfy this). Per D-12, none of the labels may be from the blocklist `{ack, acknowledgment, greeting, confirmation, hello, thanks}` (the supplied 8 do not collide). Write descriptions that are crisp enough for the lookup-first decision (≤ 25 words each); concrete suggestions for each label live in RESEARCH.md Pattern 3 (lines 286–301) — those are starting points, the exact wording is at executor discretion subject to constraints. The `examples` array for each label MUST contain exactly 2 short example phrases (e.g., `["find all usages of X", "what does this API return"]` for `research`). Do not exceed 2 examples per label — that pins the file's byte budget. File MUST NOT contain any of the legacy branding regex strings (`OpenClaw|openclaw|ClawHub|clawhub`) — none of the descriptions or examples should reference forked-tool product names.
  </action>
  <verify>
    <automated>python3 -c "import json; d=json.loads(open('skills/revenium/task-taxonomy.json').read()); labels=d['labels']; assert list(labels.keys()) == ['research','analysis','generation','review','code_review','refactor','planning','debugging'], list(labels.keys()); import re; r=re.compile(r'^[a-z][a-z0-9_]{1,47}$'); [r.match(k) or (_ for _ in ()).throw(AssertionError(k)) for k in labels]; [({'description','examples'} <= set(v.keys()) and isinstance(v['description'],str) and isinstance(v['examples'],list) and len(v['examples'])==2) or (_ for _ in ()).throw(AssertionError(k)) for k,v in labels.items()]; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `skills/revenium/task-taxonomy.json` exists and parses as JSON.
    - Top-level object has exactly one key: `labels`.
    - `list(data['labels'].keys())` equals `['research','analysis','generation','review','code_review','refactor','planning','debugging']` — order is byte-exact for the TEST-02 ordering assertion.
    - Every label's value is an object with exactly the keys `description` (str) and `examples` (list of 2 strings) — verified by `set(v.keys()) == {'description','examples'}` for every label.
    - Every label key matches `^[a-z][a-z0-9_]{1,47}$`.
    - No label key is in `{ack, acknowledgment, greeting, confirmation, hello, thanks}`.
    - `python3 -m unittest tests.test_repository.RepositoryTests.test_no_legacy_branding_left` does NOT add `skills/revenium/task-taxonomy.json` to the offenders list (run after creating the file).
    - File ends with a single `\n`.
  </acceptance_criteria>
  <done>The seed taxonomy JSON file is committed at `skills/revenium/task-taxonomy.json`, parses cleanly with the exact 8-label order, every label has the `{description, examples[2]}` shape, and adding the file does not introduce any legacy-branding regex match.</done>
</task>

<task type="auto">
  <name>Task 2: Ship references/task-taxonomy.md cold-path reference doc (PROMPT-06, TAX-02, TAX-03, TAX-04)</name>
  <files>skills/revenium/references/task-taxonomy.md</files>
  <read_first>
    - .planning/phases/02-prompt-design-marker-contract/02-PATTERNS.md (lines 58–94 — structural pattern from setup.md, content checklist for this doc, legacy-branding guard)
    - .planning/phases/02-prompt-design-marker-contract/02-RESEARCH.md (Architecture Patterns Pattern 2 — taxonomy mutation snippet under fcntl.flock; lines 239–282)
    - .planning/phases/02-prompt-design-marker-contract/02-CONTEXT.md (D-07, D-08, D-12, D-15 — schema, mint policy, blocklist, content split)
    - skills/revenium/references/setup.md (exact structural analog — H1 title, H2 sections, fenced code blocks, no frontmatter, declarative voice)
    - skills/revenium/references/troubleshooting.md (a second reference-doc analog to confirm the shape)
    - tests/test_repository.py (line 47 — legacy-branding regex; ensure new doc body does not introduce any matching string)
    - skills/revenium/task-taxonomy.json (the seed file shipped in Task 1 — the doc's label catalog mirrors its order)
  </read_first>
  <action>
Create a new file at `skills/revenium/references/task-taxonomy.md`. Follow the reference-doc structural pattern from `skills/revenium/references/setup.md` exactly: H1 title on line 1, H2 sections, no YAML frontmatter, no XML tags, fenced code blocks for JSON and Python, declarative third-person voice (not imperative — the agent reads this for understanding, not as instructions). File MUST end with a trailing newline. File MUST NOT contain any string matching `OpenClaw|openclaw|ClawHub|clawhub` (legacy-branding regex from `tests/test_repository.py:47`).

The doc MUST cover, in this section order:

1. `# Task Taxonomy` (H1 title).
2. `## What this is` — one paragraph describing the taxonomy file's role: agent-owned controlled vocabulary at `${TAXONOMY_FILE}` (mention the variable name from common.sh; do not inline the literal path), seeded from `skills/revenium/task-taxonomy.json` on fresh installs, mutated by the agent via the atomic write pattern documented below. The agent reads this file before classifying a substantive turn (lookup-first per D-08).
3. `## Schema` — describe the top-level shape (`{"labels": { <label>: { "description": str, "examples": [str, str] } }}`). Per-label schema is exactly `{description, examples}` — no other keys (D-07). Include a fenced JSON block showing the shape with one representative label.
4. `## Label normalization rules` — labels MUST be lowercase, snake_case, length 2–48 characters, matching the regex `^[a-z][a-z0-9_]{1,47}$` (TAX-03). State the rules verbatim. Include a fenced shell block showing the regex.
5. `## Blocklist` — list the 6 trivial labels the cron rejects: `ack`, `acknowledgment`, `greeting`, `confirmation`, `hello`, `thanks` (D-12 verbatim, in this order). State that the blocklist is a closed set for v1 — adding entries requires a release. Format as a bullet list.
6. `## Mint policy` — describe the lookup-first reuse discipline: read the taxonomy, reuse an existing label if any fits the turn's semantics, only mint a new label when no existing label clearly fits (D-08 verbatim). Bias toward reuse — fragmentation is permanent, oversized labels are recoverable.
7. `## Atomic write pattern` — describe the write-to-tmp + `os.rename` + `fcntl.flock` pattern (TAX-04). Include the Python snippet adapted from RESEARCH.md Pattern 2 (lines 239–282), showing: open with `r+`, `fcntl.flock(LOCK_EX)`, `json.load`, mutate dict, `tempfile.NamedTemporaryFile` in same directory, `json.dump(indent=2, ensure_ascii=True)`, `tmp.flush()`, `os.fsync()`, `os.rename(tmp, dst)`. State that `os.rename` is atomic only on the same filesystem — the temp file MUST be created in `os.path.dirname(taxonomy_path)`, not `/tmp`.
8. `## Label catalog` — for each of the 8 seed labels in D-06 order (`research`, `analysis`, `generation`, `review`, `code_review`, `refactor`, `planning`, `debugging`), an H3 subsection with the label name. Under each H3, prose describing when to use this label (matches the `description` field in the seed JSON), the 2 canonical examples from the seed file, and 1–2 sentences on disambiguation from adjacent labels (e.g., for `review` note that code-specific review goes under `code_review`; for `code_review` note that design-doc review goes under `review`).

Target length: 80–150 lines. Do not exceed 200 lines — this is a reference doc, not a tutorial.

Do not use the words `ABSOLUTE`, `FIRST`, or `NON-NEGOTIABLE` anywhere in this file (those are reserved per D-14 for the SKILL.md halt-check anchor; using them here dilutes the priority signal).
  </action>
  <verify>
    <automated>python3 -c "import re; t=open('skills/revenium/references/task-taxonomy.md').read(); assert t.startswith('# Task Taxonomy'), 'H1 title wrong'; assert t.endswith('\n'), 'no trailing newline'; assert not re.search(r'OpenClaw|openclaw|ClawHub|clawhub', t), 'legacy branding'; assert 'ABSOLUTE' not in t and 'NON-NEGOTIABLE' not in t, 'reserved keyword present'; [t.index(s) for s in ['## Schema', '## Label normalization rules', '## Blocklist', '## Mint policy', '## Atomic write pattern', '## Label catalog', '^[a-z][a-z0-9_]{1,47}$', 'fcntl.flock', 'os.rename', 'tempfile', 'research', 'analysis', 'generation', 'review', 'code_review', 'refactor', 'planning', 'debugging', 'ack', 'acknowledgment', 'greeting', 'confirmation', 'hello', 'thanks']]; print('OK')" 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - File `skills/revenium/references/task-taxonomy.md` exists.
    - File starts with `# Task Taxonomy` on line 1.
    - File contains H2 headings: `## What this is`, `## Schema`, `## Label normalization rules`, `## Blocklist`, `## Mint policy`, `## Atomic write pattern`, `## Label catalog`.
    - File contains a fenced code block with the regex string `^[a-z][a-z0-9_]{1,47}$`.
    - File contains all 6 blocklist labels as bullet items: `ack`, `acknowledgment`, `greeting`, `confirmation`, `hello`, `thanks`.
    - File contains H3 subsections for all 8 seed labels in D-06 order under `## Label catalog`.
    - File contains a Python snippet referencing `fcntl.flock`, `tempfile.NamedTemporaryFile`, `os.rename`, and `json.dump`.
    - File contains NO occurrence of `ABSOLUTE`, `FIRST` (as a standalone word), or `NON-NEGOTIABLE` (those are reserved for SKILL.md halt-check anchor).
    - File ends with a single `\n`.
    - `python3 -m unittest tests.test_repository.RepositoryTests.test_no_legacy_branding_left` does NOT add `skills/revenium/references/task-taxonomy.md` to the offenders list when run after this task.
  </acceptance_criteria>
  <done>The reference doc exists with all required H2 sections, the label catalog covers all 8 seed labels in D-06 order, the atomic write pattern Python snippet is present, and adding the file does not trigger the legacy-branding regex.</done>
</task>

<task type="auto">
  <name>Task 3: Install-time taxonomy copy in setup-local.sh + extend test_repository.py with TEST-02 and expected-files entries</name>
  <files>examples/setup-local.sh, tests/test_repository.py</files>
  <read_first>
    - examples/setup-local.sh (current 18 lines — the file shape to extend; uses `set -euo pipefail`)
    - skills/revenium/scripts/common.sh (lines 6–20 — env-var-driven STATE_DIR shape; the install-time copy must compute the destination path the same way without sourcing common.sh, since setup-local.sh runs before any skill source)
    - tests/test_repository.py (lines 11–28 — `expected` list pattern; lines 51–58 — `test_runtime_paths_are_hermes_native` pattern to model TEST-02 after)
    - .planning/phases/02-prompt-design-marker-contract/02-PATTERNS.md (lines 295–342 — TEST-02 signature, lines 367–373 — expected-list extension pattern)
    - .planning/phases/02-prompt-design-marker-contract/02-RESEARCH.md (Open Questions item 1, lines 712–715 — recommendation that setup-local.sh performs the install-time copy with a `[ -f ]` guard)
    - .planning/phases/02-prompt-design-marker-contract/02-CONTEXT.md (D-08 — lookup-first; the copy must NOT overwrite an existing live taxonomy)
  </read_first>
  <action>
**Part A — `examples/setup-local.sh`:** After the existing `chmod +x "${TARGET_DIR}/scripts/"*.sh` line (currently line 10) and BEFORE the existing `echo "Installed skill to ${TARGET_DIR}"` line (currently line 12), insert a guarded install-time copy of the seed taxonomy file. The block MUST:

1. Compute the destination path using the same env-fallback shape as common.sh — locally, since setup-local.sh runs before any `common.sh` source: `STATE_DIR_DEFAULT="${REVENIUM_STATE_DIR:-${HOME}/.hermes/state/revenium}"` and `TAXONOMY_DEST="${REVENIUM_TAXONOMY_FILE:-${STATE_DIR_DEFAULT}/task-taxonomy.json}"`. This mirrors the canonical `:-` fallback pattern (see common.sh lines 6–18) without duplicating the SSoT — the same env vars override it identically.
2. `mkdir -p "$(dirname "${TAXONOMY_DEST}")"`.
3. Copy ONLY if the destination does not already exist: `if [[ ! -f "${TAXONOMY_DEST}" ]]; then cp "${REPO_ROOT}/skills/revenium/task-taxonomy.json" "${TAXONOMY_DEST}"; echo "Seeded ${TAXONOMY_DEST}"; else echo "Taxonomy already exists at ${TAXONOMY_DEST}, not overwriting"; fi`.

This is the canonical "first-install seed; live file owned by agent after that" pattern (D-08 — lookup-first; never clobber agent-mutated taxonomy). Preserve `set -euo pipefail`. Quote all variable expansions. Use `[[ ... ]]` for the conditional, never `[ ... ]`. The whole block sits between the `chmod +x` line and the trailing `echo` block.

**Part B — `tests/test_repository.py`:** Make TWO changes inside `class RepositoryTests`:

1. **Extend `test_expected_files_exist`** (currently lines 11–28): add two new `Path` entries to the `expected` list, after the existing `SKILL / 'references' / 'troubleshooting.md'` entry (which is at line 18):
   - `SKILL / 'task-taxonomy.json'`
   - `SKILL / 'references' / 'task-taxonomy.md'`
   Preserve the exact existing pattern (sorted-ish by directory, one path per line, trailing comma). Do not reformat the rest of the list.

2. **Add a new test method `test_taxonomy_file_schema`** after `test_runtime_paths_are_hermes_native` (currently ends at line 59) and before `test_shell_scripts_have_valid_syntax` (currently starts at line 61). The new method MUST follow the signature shown in the `<interfaces>` block of this plan (sourced from PATTERNS.md lines 295–342). Specifically:
   - `def test_taxonomy_file_schema(self):` with docstring.
   - `import json, re` inside the method body (existing test style — see how `test_no_legacy_branding_left` does `import re` would be at module level but the project uses method-local imports for new tests per PATTERNS.md line 247).
   - `taxonomy_path = SKILL / 'task-taxonomy.json'`.
   - Assert the file exists, parses as JSON, has top-level `labels` key, `labels` is a dict.
   - Assert `list(labels.keys()) == ['research', 'analysis', 'generation', 'review', 'code_review', 'refactor', 'planning', 'debugging']` with the failure message `'seed taxonomy labels must match D-06 order exactly'`.
   - Compile `label_regex = re.compile(r'^[a-z][a-z0-9_]{1,47}$')` and `forbidden = {'ack', 'acknowledgment', 'greeting', 'confirmation', 'hello', 'thanks'}`.
   - Loop over `labels.items()`: assert each label matches the regex, is not in the blocklist, has a `description` (str) and `examples` (list) field per D-07.

Preserve `unittest.main()` at file end. Do not change `if __name__ == '__main__':` line. Do not introduce module-level imports of `json` (existing tests use method-local `import json` per PATTERNS.md line 247 — keep that pattern consistent).
  </action>
  <verify>
    <automated>bash -n examples/setup-local.sh && python3 -m unittest tests.test_repository.RepositoryTests.test_expected_files_exist tests.test_repository.RepositoryTests.test_taxonomy_file_schema -v 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n examples/setup-local.sh` exits 0 (preserves existing strict-mode shape).
    - `examples/setup-local.sh` contains the literal substring `task-taxonomy.json` exactly once in the install-time copy block.
    - `examples/setup-local.sh` contains the substring `REVENIUM_TAXONOMY_FILE` (env-override hook honored).
    - `examples/setup-local.sh` contains the substring `[[ ! -f` (the guard against overwriting a live taxonomy).
    - `examples/setup-local.sh` still starts with `#!/usr/bin/env bash` and `set -euo pipefail`.
    - End-to-end install test: setting `HERMES_HOME=$(mktemp -d) REVENIUM_STATE_DIR=$(mktemp -d) bash examples/setup-local.sh` exits 0 AND produces a file at `${REVENIUM_STATE_DIR}/task-taxonomy.json` that is byte-identical to `skills/revenium/task-taxonomy.json` AND the script's stdout contains `Seeded`. Running the script a second time leaves the destination file unchanged AND stdout contains `not overwriting`.
    - `python3 -m unittest tests.test_repository.RepositoryTests.test_expected_files_exist` passes (the two new Path entries resolve).
    - `python3 -m unittest tests.test_repository.RepositoryTests.test_taxonomy_file_schema` passes against the seed taxonomy created in Task 1.
    - `python3 -m unittest tests.test_repository.RepositoryTests.test_shell_scripts_have_valid_syntax` still passes — setup-local.sh edits do not break `bash -n` (note: this test scans only `skills/revenium/scripts/*.sh`, but setup-local.sh hygiene is independently checked above).
    - `python3 -m unittest tests.test_repository.RepositoryTests.test_runtime_paths_are_hermes_native` still passes (this plan does NOT modify common.sh).
  </acceptance_criteria>
  <done>`setup-local.sh` performs a guarded, env-overridable, one-time copy of the seed taxonomy; `test_expected_files_exist` enforces both new file paths exist; `test_taxonomy_file_schema` enforces the seed schema; end-to-end fresh-install copy works and is idempotent.</done>
</task>


<task type="auto">
  <name>Task 4: Add test_taxonomy_atomic_write_pattern — single-writer round-trip fixture for Phase 2 Success Criterion 5 (atomic write pattern)</name>
  <files>tests/test_repository.py</files>
  <read_first>
    - tests/test_repository.py (post-Task-3 state — `test_taxonomy_file_schema` and the extended `expected` list are present)
    - .planning/phases/02-prompt-design-marker-contract/02-RESEARCH.md (Pattern 2 — atomic write snippet under fcntl.flock, lines 239–282)
    - .planning/ROADMAP.md (Phase 2 Success Criterion 5 — atomic-write pattern produces no partial-read states)
  </read_first>
  <action>
Add a single new test method `test_taxonomy_atomic_write_pattern` to `class RepositoryTests` in `tests/test_repository.py`. Place it after `test_taxonomy_file_schema` (added in Task 3 of this plan) and before `test_shell_scripts_have_valid_syntax`. The method MUST be a deterministic single-writer round-trip — no sleeps, no flaky timing, no second process racing the writer. The test exercises the atomic-write pattern (`fcntl.flock(LOCK_EX)` + write-to-temp + `os.rename`) and asserts that a separate reader process sees ONLY a complete pre-state or a complete post-state, never partial bytes.

Implementation requirements:

1. The method is named `test_taxonomy_atomic_write_pattern`. Docstring (one line): """Atomic write pattern (flock + write-to-tmp + os.rename) never produces partial reads (Phase 2 SC5).""".
2. Method-local imports: `import json, os, shutil, subprocess, sys, tempfile`. (Match Task 3's method-local-import style.)
3. Build a small Python program as a string `reader_src` that:
   - Reads `sys.argv[1]` as the target path.
   - Opens it with `open(path, "rb")` and calls `f.read()` once.
   - Calls `json.loads(data)` and prints `OK:<n_labels>` on success or `PARTIAL:<error>` on JSON parse failure.
   - Exits 0 on success, 1 on parse failure.
4. Inside the test method:
   - Create a temporary directory with `tempfile.mkdtemp(prefix="gsd-atomic-")`. Use try/finally with `shutil.rmtree` for cleanup.
   - Seed `target = os.path.join(tmpdir, "task-taxonomy.json")` by writing a complete JSON object with 1 label (e.g., `{"labels": {"seed": {"description": "seed", "examples": ["a", "b"]}}}`) via the atomic pattern: open a temp file via `tempfile.NamedTemporaryFile("w", dir=tmpdir, delete=False, suffix=".tmp")`, `json.dump(...)`, `tmp.flush()`, `os.fsync(tmp.fileno())`, capture `tmpname = tmp.name`, then `os.rename(tmpname, target)`.
   - Spawn the reader BEFORE the second write — but only to confirm it can parse the pre-state. Use `subprocess.run([sys.executable, "-c", reader_src, target], capture_output=True, text=True, timeout=10)`. Assert `result.returncode == 0` and `result.stdout.startswith("OK:1")`.
   - Perform a SECOND atomic write that mutates the file: add a second label so the post-state JSON is `{"labels": {"seed": {...}, "minted": {"description": "minted", "examples": ["c", "d"]}}}`. Use the same `fcntl.flock(LOCK_EX)` on the existing target file plus tempfile-in-same-dir + `os.fsync` + `os.rename` commit pattern.
   - Spawn the reader AGAIN. Assert `result.returncode == 0` and `result.stdout.startswith("OK:2")`.
   - Critical assertion: NEITHER subprocess invocation produced `PARTIAL:` output. Both reads landed on a complete JSON document.
5. Document inline in a single-line comment ABOVE the method body: `# Single-writer round-trip per Phase 2 SC5; concurrent multi-writer fixture deferred to Phase 3 (RESEARCH.md note: v1 has single-writer-per-session).`
6. Do NOT introduce module-level imports (`fcntl`, `tempfile`, `shutil`, `subprocess`, `sys`, `json`). Keep them method-local for consistency with Task 3 of this plan and the future TEST-01 in plan 02-02.

The test deliberately uses `subprocess.run` with `sys.executable` (the current Python interpreter) so the reader is a real separate process — not just an inline `json.loads` call. This is what the ROADMAP language ("a separate reader process") demands. The test is deterministic because `os.rename` is atomic on POSIX: the rename either is or is not visible; there is no torn state.

Notes (also captured in the plan-level Notes section): the test does NOT spawn a concurrent multi-writer race fixture — RESEARCH.md (lines 388–390) explicitly notes v1 has a single writer per session and defers an empirical concurrent-write fixture to Phase 3. This task satisfies Phase 2 Success Criterion 5 by exercising the same atomic-write pattern used by the agent and asserting the reader-process observability invariant, without introducing flaky sleep-based synchronization.
  </action>
  <verify>
    <automated>python3 -m unittest tests.test_repository.RepositoryTests.test_taxonomy_atomic_write_pattern -v 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m unittest tests.test_repository.RepositoryTests.test_taxonomy_atomic_write_pattern -v` exits 0 with `OK`.
    - The new method calls `subprocess.run` at least twice (once after the seed write, once after the mutation write).
    - The new method uses `os.rename` (atomic replace) as the write commit step in BOTH writes; the temp file is created in the SAME directory as the target (no cross-filesystem `/tmp` use).
    - The new method asserts the reader's `returncode == 0` and `stdout.startswith("OK:")` for both reads — no partial-read state observed.
    - `python3 -m unittest tests.test_repository.RepositoryTests.test_taxonomy_file_schema` (from Task 3) and `test_expected_files_exist` still pass — this task does not regress earlier work.
    - The method body contains no `time.sleep()` call — determinism is preserved by relying on `os.rename` atomicity, not on timing.
  </acceptance_criteria>
  <done>Phase 2 Success Criterion 5 ("Concurrent agent writes to the taxonomy file under `fcntl.flock` produce no partial-read states observable to a separate reader process") is covered by `test_taxonomy_atomic_write_pattern`, executed against the same atomic-write pattern documented in `references/task-taxonomy.md`. The synthetic concurrent-write fixture is deferred to Phase 3 per RESEARCH.md (single-writer-per-session in v1).</done>
</task>

</tasks>

<notes>
## Notes

- **TAX-05 deferral.** `TAX-05` ("The cron tolerates a missing or malformed taxonomy file by falling through to the `unclassified` path with a warning logged") is Phase 3 cron-side work, not Phase 2 — plan 02-01 only establishes the precondition (seed file presence at the canonical path and `setup-local.sh` install-time copy). The requirement ID is removed from this plan's `requirements` frontmatter and will appear in a Phase 3 plan when the cron reader is implemented. The success-criteria map-back below has been updated accordingly.
- **Phase 2 Success Criterion 5 — option chosen.** ROADMAP Phase 2 Success Criterion 5 requires that concurrent agent writes to the taxonomy file under `fcntl.flock` produce no partial-read states observable to a separate reader process. RESEARCH.md (lines 388–390) notes that v1 has a single writer per session and recommends deferring an empirical concurrent-write fixture to Phase 3. To satisfy the success criterion without introducing flaky timing-dependent tests, this plan adds Task 4 (`test_taxonomy_atomic_write_pattern`) — a deterministic single-writer round-trip that exercises the same `fcntl.flock` + write-to-temp + `os.rename` pattern documented in `references/task-taxonomy.md` and asserts via a separate reader subprocess that every read lands on a complete JSON document (never partial). This is **option (a)** from the revision instructions: a tight, deterministic test, no sleep-based synchronization, leveraging `os.rename` POSIX atomicity. The empirical multi-writer race fixture is deferred to Phase 3 per RESEARCH.md's recommendation.

</notes>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent → taxonomy file | The agent mutates `${TAXONOMY_FILE}` via Python heredocs. Concurrent agent processes could race on minting the same label. The seed file is read-only after install; agent only adds/never overwrites — but a malformed write could corrupt the file. |
| Install-time host → live taxonomy | `setup-local.sh` writes the seed file into a user-controlled path resolved from `${HERMES_HOME}` / `${REVENIUM_STATE_DIR}`. If those env vars are operator-controlled (they are), the install copy honors them; this is a feature, not a vulnerability. |
| Repo file scanner → taxonomy.md | `test_no_legacy_branding_left` greps the new reference doc; new content must not reintroduce forbidden strings. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | T | task-taxonomy.json fixture | mitigate | TEST-02 (`test_taxonomy_file_schema`) pins the byte-shape: enforces the 8-label order, schema, regex, and blocklist on every test run — any tampering with the seed file fails CI. |
| T-02-01-02 | T | references/task-taxonomy.md | mitigate | `test_no_legacy_branding_left` greps the new doc; explicit acceptance criterion enforces no `ABSOLUTE`/`NON-NEGOTIABLE` strings to protect the SKILL.md halt-anchor priority signal. |
| T-02-01-03 | I | live taxonomy at `${TAXONOMY_FILE}` | mitigate | Defense in depth: `chmod 700 ${MARKERS_DIR}` shipped in Phase 1; `${TAXONOMY_FILE}` lives directly under `${STATE_DIR}`, which is `~/.hermes/state/revenium/` (user-private by default). No new permission widening in this plan. |
| T-02-01-04 | T | setup-local.sh install-time copy | mitigate | Guarded with `[[ ! -f ]]` — NEVER overwrites an existing live taxonomy. The agent's mutated copy is preserved across re-installs. End-to-end test in acceptance criteria verifies this. |
| T-02-01-05 | E | seed JSON with malformed labels (e.g., uppercase, hyphens) | mitigate | TEST-02 enforces `^[a-z][a-z0-9_]{1,47}$` on every label key; the 8 supplied labels all satisfy this; CI fails on any drift. |
| T-02-01-06 | I | reference doc could leak forked-tool branding | mitigate | Acceptance criterion runs `test_no_legacy_branding_left` post-task; doc author MUST consult the regex before writing. |
</threat_model>

<verification>
After all three tasks complete, run:

```bash
python3 -m unittest tests.test_repository -v
```

Expected outcome: 6 tests run (the original 5 + the new `test_taxonomy_file_schema`), all passing except `test_no_legacy_branding_left` (which is pre-existingly failing on `.planning/` files per Phase 1 deferred-items.md — this plan must not extend that offender list, only honor the existing failure).

Additional checks:

```bash
# 1. Schema invariant
python3 -m unittest tests.test_repository.RepositoryTests.test_taxonomy_file_schema -v

# 2. File-existence invariant
python3 -m unittest tests.test_repository.RepositoryTests.test_expected_files_exist -v

# 3. setup-local.sh syntax + behavior
bash -n examples/setup-local.sh
HERMES_HOME=$(mktemp -d) REVENIUM_STATE_DIR=$(mktemp -d) bash examples/setup-local.sh | grep -q "Seeded"
# Re-run idempotency
HERMES_HOME=$(mktemp -d) REVENIUM_STATE_DIR=$(mktemp -d) bash -c '
  D=$(mktemp -d)
  REVENIUM_STATE_DIR="$D" bash examples/setup-local.sh > /dev/null
  REVENIUM_STATE_DIR="$D" bash examples/setup-local.sh | grep -q "not overwriting"
'

# 4. SSoT discipline: task-taxonomy.json string appears only where expected
grep -rn 'task-taxonomy.json' skills/ tests/ examples/ docs/ --include='*.sh' --include='*.py' --include='*.md' --include='*.json'
# Expected hits: skills/revenium/task-taxonomy.json (the file itself), skills/revenium/references/task-taxonomy.md (the doc), skills/revenium/scripts/common.sh (the Phase 1 variable), tests/test_repository.py (the new TEST-02 + expected list), examples/setup-local.sh (the install-time copy). No other hits.
```
</verification>

<success_criteria>
- `skills/revenium/task-taxonomy.json` parses, has the 8 labels in D-06 order, schema `{description, examples[2]}` per label, every key matches `^[a-z][a-z0-9_]{1,47}$`, no blocklist labels.
- `skills/revenium/references/task-taxonomy.md` exists with all required H2 sections, label catalog covers all 8 seed labels in D-06 order, reserved keywords absent.
- `examples/setup-local.sh` performs a guarded install-time copy that is idempotent and never overwrites a live taxonomy.
- `tests/test_repository.py::test_expected_files_exist` resolves the two new paths.
- `tests/test_repository.py::test_taxonomy_file_schema` passes against the seed file.
- Map-back: TAX-01 (8-label seed) ✓, TAX-02 (`{description, examples}` schema + reference doc) ✓, TAX-03 (regex) ✓, TAX-04 (atomic write pattern documented in reference doc + behaviorally exercised by `test_taxonomy_atomic_write_pattern`) ✓, PROMPT-06 (`references/task-taxonomy.md` ships) ✓, TEST-02 ✓. **Deferred to Phase 3:** TAX-05 (cron tolerance for missing/malformed taxonomy — depends on Phase 3 cron reader work).
</success_criteria>

<output>
After completion, create `.planning/phases/02-prompt-design-marker-contract/02-01-SUMMARY.md` summarizing the seed taxonomy artifact byte shape, the reference doc structure, the install-time copy semantics, and the new test coverage. Include the exact `expected_labels` list from the TEST-02 method so downstream plans (especially Phase 3 cron-side enforcement) can copy-paste it.
</output>
