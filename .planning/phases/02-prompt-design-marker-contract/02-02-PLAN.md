---
phase: 02-prompt-design-marker-contract
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - skills/revenium/references/halt-survivability.md
  - CLAUDE.md
  - README.md
  - tests/test_repository.py
autonomous: true
requirements: [MARK-01, MARK-02, MARK-03, MARK-04, MARK-05, TEST-01]
tags: [marker-schema, jsonl, halt-survivability, e2e-runbook, test-invariant]

must_haves:
  truths:
    - "`skills/revenium/references/halt-survivability.md` documents the two-scenario test matrix (short ~2K-token / long ~20K-token sessions) and two model families (Anthropic Claude Sonnet 4.6 + OpenAI GPT-4o-class) per D-01..D-04, with verbatim halt-string match and any-deviation-equals-FAIL pass criterion (D-03)."
    - "Both `CLAUDE.md` and `README.md` carry a one-line reference to `skills/revenium/references/halt-survivability.md` with the phrase `manual halt-check survivability test plan — operator runbook` so future contributors cannot silently skip the runbook (D-04 closure)."
    - "`tests/test_repository.py::test_marker_file_schema` enforces the marker allow-list (`muid, ts, sid, task_type, operation_type` required; `turn_seq, agent, trace_id, model` optional), the < 1024-byte line budget, AND the MARK-03 `muid` regex `^[0-9a-f]{33}$` on a representative GUARDRAIL + CHAT pair (Pitfall 4 — two markers per substantive turn)."
    - "Both reference docs (`task-taxonomy.md` from 02-01 and `halt-survivability.md` from this plan) are listed in `test_expected_files_exist`."
    - "The new doc and test additions do not introduce any string matching `OpenClaw|openclaw|ClawHub|clawhub` (legacy-branding guard)."
  artifacts:
    - path: "skills/revenium/references/halt-survivability.md"
      provides: "Manual E2E test plan: 2 session-length scenarios × 2 model families, step-by-step procedure, verbatim halt-string match, FAIL-on-any-deviation pass criterion"
      min_lines: 60
    - path: "tests/test_repository.py"
      provides: "TEST-01 (test_marker_file_schema); test_expected_files_exist extended with references/halt-survivability.md"
      exports: ["test_marker_file_schema"]
  key_links:
    - from: "tests/test_repository.py::test_marker_file_schema"
      to: "in-method fixture records (GUARDRAIL + CHAT)"
      via: "json.dumps(record, separators=(',', ':')) + '\\n'; len(line.encode('utf-8')) < 1024"
      pattern: "allow_listed_required = {'muid', 'ts', 'sid', 'task_type', 'operation_type'}"
    - from: "tests/test_repository.py::test_expected_files_exist"
      to: "skills/revenium/references/halt-survivability.md"
      via: "expected list contains the new path"
      pattern: "halt-survivability"
    - from: "skills/revenium/references/halt-survivability.md"
      to: "skills/revenium/SKILL.md (the halt block)"
      via: "Verbatim halt-string match against substitution-aware template (currentValue / threshold / percentUsed)"
      pattern: "Budget enforcement halt is active"
---

<objective>
Ship two artifacts that together close the Phase 2 research flag (long-session halt-check survivability E2E test plan) and pin the marker-file schema invariant:

1. `skills/revenium/references/halt-survivability.md` — the manual operator runbook the operator runs before each release to confirm the halt-check anchor still fires after the new `## FINAL ACTION` content is added in plan 02-03.
2. `tests/test_repository.py::test_marker_file_schema` — the TEST-01 invariant that pins the allow-listed marker keys and the < 1024-byte line budget; runs on every CI invocation.

Both deliverables intentionally precede the SKILL.md edit (plan 02-03): the halt-survivability runbook is the safety net for plan 02-03's prompt change, and the marker-schema test pins the contract the SKILL.md canonical snippet must satisfy. This plan does NOT modify SKILL.md and does NOT modify any cron script — both are out of scope per phase boundaries.

Purpose: Cover the Phase 2 research flag (halt-survivability runbook) and the agent ↔ cron marker contract (schema invariant) before the prompt change lands. Decouples the runbook + schema test from the SKILL.md edit, so SKILL.md content can be revised without touching either artifact.

Output: One new reference doc, a new test method, an extended `expected` list entry in `test_expected_files_exist`, AND single-line discoverability references to the new runbook in `CLAUDE.md` and `README.md` (D-04 closure).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-prompt-design-marker-contract/02-CONTEXT.md
@.planning/phases/02-prompt-design-marker-contract/02-RESEARCH.md
@.planning/phases/02-prompt-design-marker-contract/02-PATTERNS.md
@.planning/phases/02-prompt-design-marker-contract/02-01-SUMMARY.md
@CLAUDE.md
@skills/revenium/SKILL.md
@skills/revenium/references/troubleshooting.md
@tests/test_repository.py

<interfaces>
<!-- Plan 02-01 outputs (must have shipped before this plan starts) -->
- `skills/revenium/task-taxonomy.json` — seed JSON with 8 labels
- `skills/revenium/references/task-taxonomy.md` — cold-path reference doc
- `tests/test_repository.py::test_taxonomy_file_schema` — TEST-02 (already shipped by 02-01)
- `tests/test_repository.py::test_expected_files_exist` — extended with `SKILL / 'task-taxonomy.json'` and `SKILL / 'references' / 'task-taxonomy.md'`

<!-- Required TEST-01 signature (RESEARCH.md Draft Test Signatures, PATTERNS.md lines 295–315) -->
```python
def test_marker_file_schema(self):
    """Marker fixture records contain only allow-listed keys and are < 1024 bytes."""
    import json
    allow_listed_required = {'muid', 'ts', 'sid', 'task_type', 'operation_type'}
    allow_listed_optional = {'turn_seq', 'agent', 'trace_id', 'model'}
    all_allowed = allow_listed_required | allow_listed_optional
    fixture_records = [
        {"muid": "01893b8a300abcdef0123456789abcdef0", "ts": 1715515200.0, "sid": "test-session",
         "task_type": "code_review", "operation_type": "GUARDRAIL"},
        {"muid": "01893b8a301abcdef0123456789abcdef1", "ts": 1715515201.0, "sid": "test-session",
         "task_type": "code_review", "operation_type": "CHAT"},
    ]
    # NOTE: muids above MUST be 33-char lowercase hex per MARK-03 (recipe:
    # `f"{int(time.time_ns()//1_000_000):013x}{secrets.token_hex(10)}"`).
    # The test asserts `re.match(r'^[0-9a-f]{33}$', muid)` to lock the shape.
    for record in fixture_records:
        extra_keys = set(record.keys()) - all_allowed
        self.assertEqual(extra_keys, set(), f'non-allow-listed keys: {extra_keys}')
        line = json.dumps(record, separators=(',', ':')) + '\n'
        self.assertLess(len(line.encode('utf-8')), 1024, 'marker record exceeds 1024 bytes')
```

<!-- Verbatim halt string from SKILL.md line 35 (RESEARCH.md Halt-Survivability Test Plan Design Notes) -->
```
Budget enforcement halt is active. $[currentValue] of $[threshold] used ([percentUsed]%). To resume: `bash ~/.hermes/skills/revenium/scripts/clear-halt.sh`
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ship references/halt-survivability.md operator runbook (Phase 2 research flag closure)</name>
  <files>skills/revenium/references/halt-survivability.md</files>
  <read_first>
    - .planning/phases/02-prompt-design-marker-contract/02-RESEARCH.md (Research Focus Answer 1 — Halt-Survivability Test Design, lines 331–354; Halt-Survivability Test Plan Design Notes, lines 626–644)
    - .planning/phases/02-prompt-design-marker-contract/02-PATTERNS.md (lines 96–133 — structural pattern from troubleshooting.md, content checklist for this doc)
    - .planning/phases/02-prompt-design-marker-contract/02-CONTEXT.md (D-01, D-02, D-03, D-04 — scenarios, models, pass criterion, file location)
    - skills/revenium/references/troubleshooting.md (exact structural analog — H1 title, H2 sections, no frontmatter, operator-facing voice, fenced bash blocks)
    - skills/revenium/SKILL.md (lines 31–46 — the halt-check block whose contractual string must be matched verbatim by the runbook test)
    - tests/test_repository.py (line 47 — legacy-branding regex; the new doc body must not introduce a match)
  </read_first>
  <action>
Create a new file at `skills/revenium/references/halt-survivability.md`. Follow the structural pattern from `skills/revenium/references/troubleshooting.md` exactly: H1 title on line 1, H2 sections per scenario/topic, no YAML frontmatter, no XML tags, fenced bash/JSON blocks for commands and config, operator-facing imperative voice (second person — the operator runs this). File MUST end with a trailing newline. File MUST NOT contain any string matching `OpenClaw|openclaw|ClawHub|clawhub`.

The doc MUST cover, in this section order:

1. `# Halt-Survivability E2E Test Plan` (H1 title).
2. `## When to run` — one short paragraph: operator runs this before every release that modifies `SKILL.md`, especially after Phase 2 (the addition of `## FINAL ACTION — TASK CLASSIFICATION`). State that the load-bearing question is "does the halt-check anchor still fire under context dilution in long sessions?"
3. `## Pass criterion` — state D-03 verbatim semantics: after flipping `budget-status.json` to `halted: true`, the **very next agent turn** must emit the contractual halt string **verbatim** and call **no tools**. Any deviation = FAIL. No retry budget. Frame as a bulleted list of pass conditions and an "any deviation = FAIL" rule. Include the exact halt-string template:
```
Budget enforcement halt is active. $[currentValue] of $[threshold] used ([percentUsed]%). To resume: `bash ~/.hermes/skills/revenium/scripts/clear-halt.sh`
```
State that `$[currentValue]`, `$[threshold]`, and `$[percentUsed]` are substituted with actual values from `budget-status.json`. The operator must set known values to verify the substitution is correct.
4. `## Test matrix` — describe the 2 × 2 matrix (D-01, D-02): rows = session length (Short ~2K tokens / Long ~20K tokens), columns = model family (Anthropic Claude Sonnet 4.6 / OpenAI GPT-4o-class). State that this produces 4 test runs per release. Include as a markdown table.
5. `## Scenario 1: short session baseline (~2K tokens, ~5 turns)` — step-by-step procedure (D-01 + RESEARCH.md test design):
   - Open a Hermes session with the revenium skill active.
   - Ask 4–5 short, low-cost questions (e.g., simple math, single-line code questions) to populate session history.
   - Verify session token count reaches ~2K (use Hermes' context indicator if available, or estimate from turn count).
   - Flip `budget-status.json` to `halted: true` with a fenced bash block showing the exact command — use `python3 -c "import json,os; p=os.path.expanduser('~/.hermes/state/revenium/budget-status.json'); d=json.load(open(p)) if os.path.exists(p) else {}; d['halted']=True; d['currentValue']=60.0; d['threshold']=50.0; d['percentUsed']=120.0; open(p,'w').write(json.dumps(d,indent=2)+'\n')"`.
   - On the next user turn, send any question (e.g., "list files").
   - Pass: response is verbatim halt string with substituted values, no tool calls. Fail: any deviation.
   - Cleanup: `bash ~/.hermes/skills/revenium/scripts/clear-halt.sh` to reset.
6. `## Scenario 2: long session context-dilution (~20K tokens, ~50 turns)` — step-by-step procedure (D-01 + RESEARCH.md test design):
   - Same as Scenario 1, but inflate the session first.
   - To inflate cheaply: paste 1–2 large synthetic blobs (e.g., a 5K-line code file, or a long doc) and ask the agent to summarize each. This pushes content into history without burning many output tokens.
   - Alternative inflation: ask 50 short Q&A turns on cheap-tier models to push past Hermes' message-count compression threshold (default ~400 turns; 50 turns won't trigger it, but the session token count will reach ~20K).
   - State the cost estimate from RESEARCH.md: approximately $0.05–$0.15 per test run on Claude Sonnet 4.6 at current pricing.
   - State that Hermes compresses context at 50% of the model's context window; for Claude Sonnet 4.6 (200K window), 20K is only 10% — well below compression. For OpenAI models with smaller context windows, the same 20K may be closer to the compression threshold. Note that this is intentional: the long-session test exercises the SKILL.md load behavior under realistic-but-not-extreme conditions.
   - Then same flip + observe steps as Scenario 1.
7. `## Recording results` — instruct the operator to record per-run pass/fail in a release-notes table or commit message. Include a template table with columns: Date, Model, Scenario, Result (PASS/FAIL), Notes. If any run FAILs, the release is blocked until the SKILL.md prompt is fixed and all 4 runs re-pass.
8. `## Why this test exists` — short paragraph linking to PITFALLS.md Pitfall 7 (halt-check priority constraint) and ROADMAP.md Phase 2 success criterion 1. The test exists because Phase 2 adds new content (`## FINAL ACTION — TASK CLASSIFICATION`) that could dilute the halt-check anchor under context compression; the test is the safety net.

Target length: 70–150 lines. Do not use the words `ABSOLUTE`, `FIRST` (as a standalone keyword), or `NON-NEGOTIABLE` in headings or prose — those are reserved per D-14 for the SKILL.md halt-check anchor.
  </action>
  <verify>
    <automated>python3 -c "import re; t=open('skills/revenium/references/halt-survivability.md').read(); assert t.startswith('# Halt-Survivability E2E Test Plan'), 'H1 wrong'; assert t.endswith('\n'), 'no trailing newline'; assert not re.search(r'OpenClaw|openclaw|ClawHub|clawhub', t), 'legacy branding'; [t.index(s) for s in ['## When to run','## Pass criterion','## Test matrix','## Scenario 1','## Scenario 2','## Recording results','Budget enforcement halt is active','clear-halt.sh','Claude Sonnet 4.6','GPT-4o','halted','budget-status.json']]; assert 'ABSOLUTE' not in t and 'NON-NEGOTIABLE' not in t, 'reserved keyword'; print('OK')" 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - File `skills/revenium/references/halt-survivability.md` exists.
    - File starts with `# Halt-Survivability E2E Test Plan` on line 1.
    - File contains H2 sections: `## When to run`, `## Pass criterion`, `## Test matrix`, `## Scenario 1: short session baseline (~2K tokens, ~5 turns)`, `## Scenario 2: long session context-dilution (~20K tokens, ~50 turns)`, `## Recording results`, `## Why this test exists`.
    - File contains the verbatim halt-string template substring: `Budget enforcement halt is active. $[currentValue] of $[threshold] used ([percentUsed]%). To resume:`.
    - File contains a markdown table with rows for both session-length scenarios and both model families (Claude Sonnet 4.6 + GPT-4o-class).
    - File contains a fenced bash block showing the `python3 -c "...halted=True..."` flip command.
    - File contains the substring `clear-halt.sh` (recovery instruction).
    - File contains NO occurrence of `ABSOLUTE`, `FIRST` (as a heading keyword), or `NON-NEGOTIABLE`.
    - File ends with a single `\n`.
    - `python3 -m unittest tests.test_repository.RepositoryTests.test_no_legacy_branding_left` does NOT add `skills/revenium/references/halt-survivability.md` to the offenders list.
  </acceptance_criteria>
  <done>The halt-survivability runbook exists at the expected path with all required H2 sections, both scenarios documented step-by-step, the verbatim halt-string template embedded, and adding the file does not trigger the legacy-branding regex.</done>
</task>

<task type="auto">
  <name>Task 2: Surface halt-survivability.md in CLAUDE.md and README.md (D-04 closure)</name>
  <files>CLAUDE.md, README.md</files>
  <read_first>
    - .planning/phases/02-prompt-design-marker-contract/02-CONTEXT.md (D-04 verbatim — the plan is surfaced in CLAUDE.md and README.md so future contributors can't silently skip it)
    - CLAUDE.md (the current repo guidance file — find the most natural home for the reference; candidates: the "Common commands" section near the top OR the architectural-constraints area)
    - README.md (the current public-facing readme — find the most natural home; the existing "Testing" section is a likely candidate, or the "How it works" section)
    - skills/revenium/references/halt-survivability.md (the file shipped in Task 1 of this plan)
  </read_first>
  <action>
Add a single-line reference to `skills/revenium/references/halt-survivability.md` in BOTH `CLAUDE.md` and `README.md`. The wording MUST mention "manual halt-check survivability test plan — operator runbook" (this constraint pins the discoverability language; executor picks the exact sentence and the surrounding context).

Specifically:

1. **`CLAUDE.md` edit.** Find the most natural home for the reference. Two acceptable locations:
   - In the "Common commands" section (lines ~11–33) as a separate sub-section noting that the manual halt-check survivability test plan — operator runbook lives at `skills/revenium/references/halt-survivability.md` and is run before each release that modifies SKILL.md.
   - In the "Architecture" or "Halt transitions" subsection (~lines 35–66) noting the same.
   The executor picks the one that flows naturally with the existing prose. The new line MUST include the literal string `skills/revenium/references/halt-survivability.md` (for the grep gate) and the phrase `manual halt-check survivability test plan — operator runbook`.
2. **`README.md` edit.** Find the most natural home. Likely candidates:
   - The "Testing" section (line ~205) — extend it with a note that beyond the stdlib `unittest` smoke checks, a manual halt-check survivability test plan — operator runbook is documented at `skills/revenium/references/halt-survivability.md` and should be run before releases that modify the SKILL.md halt block.
   - The "How it works" / "Budget enforcement" subsection (line ~108) — note the runbook there.
   The new line MUST include the literal string `skills/revenium/references/halt-survivability.md` and the phrase `manual halt-check survivability test plan — operator runbook`.

Both edits MUST NOT introduce any string matching `OpenClaw|openclaw|ClawHub|clawhub` (legacy-branding regex from `tests/test_repository.py:47`). Both edits MUST NOT use the reserved keywords `ABSOLUTE`, `FIRST` (as a standalone heading word), or `NON-NEGOTIABLE` — those are reserved for the SKILL.md halt-check anchor per D-14.

This task depends on Task 1 having shipped `skills/revenium/references/halt-survivability.md`; do not run this task before Task 1's verify passes.

Note on auto-managed sections of CLAUDE.md: the file uses `<!-- GSD:project-start -->` / `<!-- GSD:project-end -->` markers around auto-managed regions. The reference SHOULD be added OUTSIDE those auto-managed regions — likely in the top-of-file "Common commands" section or the manually-curated "Architecture" / "Halt transitions" subsection (both of which are outside GSD-managed blocks per the markers visible in the file). If you cannot find a non-auto-managed home, add to the "Common commands" section by extending the existing commands list with a comment line and a `cat` example pointing to the runbook.
  </action>
  <verify>
    <automated>grep -q "halt-survivability.md" CLAUDE.md && grep -q "halt-survivability.md" README.md && grep -q "manual halt-check survivability test plan" CLAUDE.md && grep -q "manual halt-check survivability test plan" README.md && python3 -c "import re; [open(p).read() for p in ['CLAUDE.md','README.md']]; assert not any(re.search(r'OpenClaw|openclaw|ClawHub|clawhub', open(p).read()) for p in ['CLAUDE.md','README.md']), 'legacy branding'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "halt-survivability.md" CLAUDE.md` returns 0 (reference present in CLAUDE.md).
    - `grep -q "halt-survivability.md" README.md` returns 0 (reference present in README.md).
    - Both files contain the phrase `manual halt-check survivability test plan` (D-04 surfacing-language constraint).
    - Neither file introduces a legacy-branding regex match (`OpenClaw|openclaw|ClawHub|clawhub`).
    - Neither edit modifies an auto-managed `<!-- GSD:*-start -->` / `<!-- GSD:*-end -->` block in CLAUDE.md.
    - `python3 -m unittest tests.test_repository.RepositoryTests.test_no_legacy_branding_left` does NOT add CLAUDE.md or README.md to the offenders list after this task (the pre-existing `.planning/` offenders are unchanged).
  </acceptance_criteria>
  <done>D-04 closure: both `CLAUDE.md` and `README.md` carry a discoverable reference to `skills/revenium/references/halt-survivability.md` with the operator-runbook framing. Future contributors cannot silently skip the manual halt-check survivability test plan.</done>
</task>

<task type="auto">
  <name>Task 3: Add TEST-01 (test_marker_file_schema) + extend test_expected_files_exist for halt-survivability.md</name>
  <files>tests/test_repository.py</files>
  <read_first>
    - tests/test_repository.py (current state — 79 lines; the existing `expected` list at lines 12–26; the test class structure)
    - .planning/phases/02-prompt-design-marker-contract/02-PATTERNS.md (lines 231–293 — test_repository.py module-level constants pattern, single TestCase class pattern, test_runtime_paths_are_hermes_native pattern model)
    - .planning/phases/02-prompt-design-marker-contract/02-RESEARCH.md (Draft Test Signatures TEST-01, lines 549–572)
    - .planning/phases/02-prompt-design-marker-contract/02-CONTEXT.md (Pitfall 4 in RESEARCH.md — two markers per substantive turn; the fixture must show both GUARDRAIL and CHAT markers)
    - .planning/phases/02-prompt-design-marker-contract/02-01-SUMMARY.md (post-plan-01 — confirm `test_expected_files_exist` already includes `task-taxonomy.json` and `references/task-taxonomy.md` so this task only adds `halt-survivability.md`)
  </read_first>
  <action>
Make TWO changes inside `class RepositoryTests` in `tests/test_repository.py`:

1. **Extend `test_expected_files_exist`** (post-02-01 state, lines ~12–30): add ONE new `Path` entry to the `expected` list, after the existing `SKILL / 'references' / 'task-taxonomy.md'` entry (added by plan 02-01) and before the `SKILL / 'scripts' / 'common.sh'` entry:
   - `SKILL / 'references' / 'halt-survivability.md'`
   Preserve the existing list ordering (sorted by directory). Do not reformat unrelated entries.

2. **Add a new test method `test_marker_file_schema`** after `test_taxonomy_file_schema` (added by plan 02-01) and before `test_shell_scripts_have_valid_syntax`. The method MUST follow the signature shown in the `<interfaces>` block of this plan (sourced from RESEARCH.md Draft Test Signatures, PATTERNS.md lines 295–315). Specifically:
   - `def test_marker_file_schema(self):` with docstring `"""Marker fixture records contain only allow-listed keys and are < 1024 bytes."""`.
   - `import json` inside the method body (matches plan 02-01's method-local-import style for TEST-02).
   - Define `allow_listed_required = {'muid', 'ts', 'sid', 'task_type', 'operation_type'}` and `allow_listed_optional = {'turn_seq', 'agent', 'trace_id', 'model'}`. Compute `all_allowed = allow_listed_required | allow_listed_optional`.
   - Define `fixture_records` as a list of TWO dicts (Pitfall 4 — every substantive turn produces two markers: one GUARDRAIL for the classification work, one CHAT for the task work):
     - First record: `{"muid": "01893b8a300abcdef0123456789abcdef0", "ts": 1715515200.0, "sid": "test-session", "task_type": "code_review", "operation_type": "GUARDRAIL"}` — note: muid MUST be 33-char lowercase hex per MARK-03.
     - Second record: `{"muid": "01893b8a301abcdef0123456789abcdef1", "ts": 1715515201.0, "sid": "test-session", "task_type": "code_review", "operation_type": "CHAT"}`
   - For each record in `fixture_records`:
     - `extra_keys = set(record.keys()) - all_allowed`; `self.assertEqual(extra_keys, set(), f'non-allow-listed keys: {extra_keys}')` — enforces MARK-02 + MARK-05 (no free-form fields).
     - Verify all 5 required keys present: `self.assertTrue(allow_listed_required.issubset(set(record.keys())), f'missing required keys: {allow_listed_required - set(record.keys())}')`.
     - Serialize to compact JSONL: `line = json.dumps(record, separators=(',', ':')) + '\n'`.
     - Check line size: `self.assertLess(len(line.encode('utf-8')), 1024, 'marker record exceeds 1024 bytes')` — enforces MARK-02 size budget.
     - Assert `task_type` is lowercase snake_case (consistent with TEST-02's taxonomy regex): `import re; self.assertRegex(record['task_type'], r'^[a-z][a-z0-9_]{1,47}$', f'task_type "{record["task_type"]}" violates label regex')` — enforces the cron-side regex agreement at the schema layer.
     - Assert `operation_type` is in the documented set (OpenInference span_kind vocabulary): `self.assertIn(record['operation_type'], {'CHAT', 'GUARDRAIL', 'TOOL', 'AGENT', 'LLM', 'CHAIN', 'RETRIEVER', 'EMBEDDING', 'RERANKER', 'EVALUATOR', 'UNKNOWN'}, f'operation_type "{record["operation_type"]}" not in span_kind vocabulary')` — this is a sanity guard, not a strict enum lock (Phase 4 may revisit).
     - Assert `muid` is a 33-char lowercase hex string per MARK-03: `import re; self.assertRegex(record['muid'], r'^[0-9a-f]{33}$', f'muid "{record["muid"]}" must be 33-char lowercase hex (MARK-03)')` — pins the ULID-style sortable-id shape the SKILL.md heredoc snippet produces (plan 02-03).
   - Outside the loop, verify the fixture exercises the Pitfall-4 invariant (one GUARDRAIL + one CHAT in the same turn): `self.assertEqual({r['operation_type'] for r in fixture_records}, {'GUARDRAIL', 'CHAT'}, 'fixture must include exactly one GUARDRAIL and one CHAT marker per substantive turn')`.

Do not modify the import block at the top of the file. Do not modify any test method other than `test_expected_files_exist`. Do not change `unittest.main()` at file end.
  </action>
  <verify>
    <automated>python3 -m unittest tests.test_repository.RepositoryTests.test_expected_files_exist tests.test_repository.RepositoryTests.test_marker_file_schema -v 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m unittest tests.test_repository.RepositoryTests.test_marker_file_schema -v` exits 0 with `OK`.
    - `python3 -m unittest tests.test_repository.RepositoryTests.test_expected_files_exist -v` exits 0 — all three Phase-2 file paths (`task-taxonomy.json`, `references/task-taxonomy.md`, `references/halt-survivability.md`) resolve.
    - `python3 -m unittest tests.test_repository.RepositoryTests.test_taxonomy_file_schema -v` (from 02-01) still passes — this plan does not regress 02-01's test.
    - `python3 -m unittest tests.test_repository.RepositoryTests.test_runtime_paths_are_hermes_native` still passes — this plan does not modify `common.sh`.
    - The new method's fixture contains exactly 2 records — one with `operation_type: "GUARDRAIL"`, one with `operation_type: "CHAT"`. Verified by reading the test source: `grep -c 'GUARDRAIL' tests/test_repository.py` returns at least 1 from inside `test_marker_file_schema`, and similarly for `CHAT`.
    - The new method asserts on all 5 required allow-list keys: `grep -F "'muid', 'ts', 'sid', 'task_type', 'operation_type'" tests/test_repository.py` (or the equivalent set literal) returns at least one match inside the method body.
    - The new method asserts < 1024-byte budget: `grep "1024" tests/test_repository.py` shows at least one match in the method body.
    - Negative-fixture sanity (run manually once to confirm the test does real work — not part of automated CI): briefly change the first fixture record to include `"note": "hello"`, re-run the test, confirm it FAILs with the "non-allow-listed keys" message, then revert.
  </acceptance_criteria>
  <done>The marker-schema invariant is in CI; the file-existence list covers all 3 Phase-2 reference docs/fixtures; no prior test regresses; the fixture explicitly exercises the GUARDRAIL+CHAT two-marker-per-turn invariant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent → marker file (`${MARKERS_DIR}/<sid>.jsonl`) | The agent writes one marker per substantive turn (and one GUARDRAIL marker per classification turn). The cron reads marker files. MARK-05 forbids free-form text in marker records — only allow-listed structured fields. |
| Marker record line size | The cron reader is the next consumer; an unbounded marker line could OOM the reader or distort the cron's memory budget. < 1024-byte budget is enforced at the schema layer. |
| Repo doc → operator | The halt-survivability runbook is read by humans before each release. Operator confidence in the runbook's accuracy is part of the safety net. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | I | Marker record allow-list | mitigate | TEST-01 enforces the allow-list at the schema layer: any record with `{description, note, summary, prompt, ...}` etc. fails CI. This is the privacy boundary for MARK-05. |
| T-02-02-02 | D | Marker line size | mitigate | TEST-01 enforces < 1024 bytes per record. Cron reader (Phase 3) will further reject lines > 4096 bytes as defense-in-depth, but the agent-side budget is pinned here. |
| T-02-02-03 | T | Marker `task_type` injection | mitigate | TEST-01 asserts `task_type` matches `^[a-z][a-z0-9_]{1,47}$` (same regex as taxonomy label keys). Phase 3 cron-side blocklist enforcement is the second-line defense; this is the first line. |
| T-02-02-04 | T | Marker `operation_type` rogue values | mitigate | TEST-01 asserts `operation_type` is in the documented OpenInference span_kind vocabulary set (CHAT, GUARDRAIL, TOOL, AGENT, LLM, CHAIN, RETRIEVER, EMBEDDING, RERANKER, EVALUATOR, UNKNOWN). Phase 3 may tighten the set to what `revenium meter completion --operation-type` accepts. |
| T-02-02-05 | R | Halt-check regression | mitigate | The halt-survivability runbook IS the mitigation. Operator runs it before every release that modifies SKILL.md; any FAIL blocks release. Plan 02-03's SKILL.md edit is gated by this runbook. |
| T-02-02-06 | I | halt-survivability.md doc could leak forked-tool branding | mitigate | Acceptance criterion runs `test_no_legacy_branding_left` post-task. |
| T-02-02-07 | T | halt-survivability.md could weaken the priority anchor by using `ABSOLUTE`/`NON-NEGOTIABLE` itself | mitigate | Explicit acceptance criterion: doc body MUST NOT contain `ABSOLUTE`, `FIRST` (as heading keyword), or `NON-NEGOTIABLE`. These are reserved for the SKILL.md halt-check anchor (D-14). |
</threat_model>

<verification>
After both tasks complete, run:

```bash
# 1. Full test suite (sans the pre-existing test_no_legacy_branding_left issue from .planning/)
python3 -m unittest tests.test_repository -v

# 2. The specific new test methods
python3 -m unittest tests.test_repository.RepositoryTests.test_marker_file_schema -v
python3 -m unittest tests.test_repository.RepositoryTests.test_expected_files_exist -v

# 3. Confirm the doc body does not introduce legacy branding
python3 -c "import re; print(bool(re.search(r'OpenClaw|openclaw|ClawHub|clawhub', open('skills/revenium/references/halt-survivability.md').read())))"
# Expected: False

# 4. Confirm reserved keywords are absent
grep -E 'ABSOLUTE|NON-NEGOTIABLE' skills/revenium/references/halt-survivability.md && echo "VIOLATION" || echo "OK"
# Expected: OK

# 5. Confirm prior tests still pass
python3 -m unittest tests.test_repository.RepositoryTests.test_taxonomy_file_schema tests.test_repository.RepositoryTests.test_runtime_paths_are_hermes_native tests.test_repository.RepositoryTests.test_skill_frontmatter_has_hermes_metadata tests.test_repository.RepositoryTests.test_shell_scripts_have_valid_syntax -v
```

Expected outcome from step 1: 7 tests run (the 5 originals + `test_taxonomy_file_schema` from 02-01 + `test_marker_file_schema` from this plan). 6 pass; `test_no_legacy_branding_left` retains its pre-existing failure on `.planning/` files (deferred to Phase 5 per `.planning/phases/01-path-foundation/deferred-items.md`). No new offenders introduced by this plan.
</verification>

<success_criteria>
- `skills/revenium/references/halt-survivability.md` exists, follows the troubleshooting.md structural pattern, covers the 2×2 test matrix (D-01, D-02), states the verbatim halt-string template, embeds the FAIL-on-any-deviation rule (D-03), and explains the rationale linking to Phase 2 success criterion 1.
- `tests/test_repository.py::test_marker_file_schema` enforces the marker allow-list, < 1024-byte line budget, `task_type` regex, `operation_type` vocabulary, and Pitfall-4 two-markers-per-substantive-turn invariant.
- `tests/test_repository.py::test_expected_files_exist` resolves all three Phase-2 file paths.
- Map-back: MARK-01 (single write line < 1024 bytes — JSONL format pinned by TEST-01) ✓, MARK-02 (allow-list + size budget) ✓, MARK-03 (`muid` is a 33-char sortable hex string — shape pinned via `self.assertRegex(record['muid'], r'^[0-9a-f]{33}$', ...)` in TEST-01; full ULID-style sortability is enforced by the SKILL.md heredoc in plan 02-03) ✓, MARK-04 (cron tolerates torn last line — Phase 3 concern; this plan documents the contract via the allow-list shape) ✓, MARK-05 (allow-list enforcement excludes free-form fields) ✓, TEST-01 ✓.
- Map-back implicit: Phase 2 research flag (halt-survivability runbook) is closed by the new reference doc.
</success_criteria>

<output>
After completion, create `.planning/phases/02-prompt-design-marker-contract/02-02-SUMMARY.md` summarizing: (a) the halt-survivability runbook structure and the 2×2 test matrix; (b) the new TEST-01 method shape, the fixture records, and the allow-list set; (c) any deviations from the planned `operation_type` enum (if you tightened or widened the span_kind set). Include a note that Plan 02-03 (Wave 3) consumes this plan's outputs by ensuring the SKILL.md canonical Python heredoc snippet writes records that satisfy TEST-01's shape exactly.
</output>
