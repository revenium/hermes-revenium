---
phase: 02-prompt-design-marker-contract
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - skills/revenium/SKILL.md
  - tests/test_repository.py
autonomous: true
requirements: [PROMPT-01, PROMPT-02, PROMPT-03, PROMPT-04, PROMPT-05, PROMPT-07]
tags: [skill-md, prompt-design, end-loaded, marker-write, hermes-session-id, prompt-invariant-test]

must_haves:
  truths:
    - "`skills/revenium/SKILL.md` ends with a new `## FINAL ACTION — TASK CLASSIFICATION` H2 section (em dash U+2014 in heading per D-14 and PATTERNS.md), positioned strictly after `## Verification` (D-13), and the existing `## ABSOLUTE FIRST — HALT CHECK (NON-NEGOTIABLE)` block at lines 24–46 is byte-unchanged."
    - "The new section embeds the D-09 hard rule verbatim, 4 canonical examples covering the D-10 balance (1 clear-substantive + 1 clear-trivial + 1 borderline-classify + 1 borderline-skip), the D-12 blocklist verbatim, and exactly ONE canonical Python heredoc marker-write snippet (D-16) that handles BOTH the GUARDRAIL marker (classification turn) AND the CHAT marker (work turn) — Pitfall 4 two-markers-per-substantive-turn invariant."
    - "The canonical marker-write snippet resolves the session ID with a documented mechanism (HERMES_SESSION_ID env var primary; fallback documented inline in the snippet comment), producing a marker filename that matches `${MARKERS_DIR}/<session_id>.jsonl`."
    - "Marker records written by the canonical snippet satisfy `test_marker_file_schema` (from plan 02-02): allow-listed keys only, `task_type` matches `^[a-z][a-z0-9_]{1,47}$`, < 1024 bytes, `operation_type` from the documented span_kind vocabulary."
    - "`tests/test_repository.py::test_prompt_ordering_invariant` enforces PROMPT-07: the halt-check anchor `ABSOLUTE FIRST — HALT CHECK (NON-NEGOTIABLE)` (em dash) appears in SKILL.md AND its `.index()` position is strictly less than the `.index()` position of `FINAL ACTION — TASK CLASSIFICATION`."
    - "`skills/revenium/SKILL.md` frontmatter (lines 1–20) is byte-unchanged — `test_skill_frontmatter_has_hermes_metadata` continues to pass."
  artifacts:
    - path: "skills/revenium/SKILL.md"
      provides: "End-loaded `## FINAL ACTION — TASK CLASSIFICATION` section: hard rule + 4 examples + blocklist + canonical marker-write Python heredoc"
      contains: "## FINAL ACTION — TASK CLASSIFICATION"
    - path: "tests/test_repository.py"
      provides: "test_prompt_ordering_invariant (PROMPT-07)"
      exports: ["test_prompt_ordering_invariant"]
  key_links:
    - from: "skills/revenium/SKILL.md `## FINAL ACTION` section"
      to: "${MARKERS_DIR}/<session_id>.jsonl"
      via: "Python heredoc using os.path.expanduser('~/.hermes/state/revenium/markers') + os.environ.get('HERMES_SESSION_ID', fallback)"
      pattern: "open\\(marker_path, .ab., buffering=0\\)"
    - from: "skills/revenium/SKILL.md `## FINAL ACTION` section"
      to: "${TAXONOMY_FILE}"
      via: "lookup-first reference to references/task-taxonomy.md cold-path doc + the live taxonomy at the documented path"
      pattern: "task-taxonomy"
    - from: "tests/test_repository.py::test_prompt_ordering_invariant"
      to: "skills/revenium/SKILL.md"
      via: "text.index(halt_anchor) < text.index(classify_anchor) with em-dash literals"
      pattern: "ABSOLUTE FIRST — HALT CHECK"
---

<objective>
Ship the SKILL.md `## FINAL ACTION — TASK CLASSIFICATION` section (end-loaded per D-13, closing-discipline counterpart to the opening-discipline halt-check block) and the PROMPT-07 prompt-invariant test that pins their structural ordering. This is the last plan in Phase 2 and depends on plans 02-01 (taxonomy artifacts) and 02-02 (marker schema test + halt-survivability runbook) — both of which are referenced from the new SKILL.md section.

This plan also resolves the Phase 2 research-flagged open question: **how does Hermes expose the current session ID to the agent for marker filename composition?** Resolution: primary mechanism is `os.environ.get("HERMES_SESSION_ID", ...)`; fallback is documented inline. If empirical testing during execution shows HERMES_SESSION_ID is not set, the executor MUST switch to a documented alternative (timestamp-based pseudo-session-id OR a documented Hermes config path) and update the canonical snippet to match. The decision is pinned in this plan, not deferred to Phase 3.

Purpose: Close Phase 2 by adding the prompt content that drives marker writes (the agent half of the agent ↔ cron contract) while leaving the halt-check anchor byte-unchanged. The PROMPT-07 test is the regression guard for the SKILL.md structural invariant.

Output: Modified `SKILL.md` (single appended section, frontmatter and existing body untouched), modified `tests/test_repository.py` (one new test method).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-prompt-design-marker-contract/02-CONTEXT.md
@.planning/phases/02-prompt-design-marker-contract/02-RESEARCH.md
@.planning/phases/02-prompt-design-marker-contract/02-PATTERNS.md
@.planning/phases/02-prompt-design-marker-contract/02-01-SUMMARY.md
@.planning/phases/02-prompt-design-marker-contract/02-02-SUMMARY.md
@CLAUDE.md
@skills/revenium/SKILL.md
@skills/revenium/task-taxonomy.json
@skills/revenium/references/task-taxonomy.md
@skills/revenium/references/halt-survivability.md
@skills/revenium/scripts/common.sh
@skills/revenium/scripts/budget-check.sh
@skills/revenium/scripts/hermes-report.sh
@tests/test_repository.py

<interfaces>
Required new SKILL.md section heading (D-14 — locked, em dash U+2014):
`## FINAL ACTION — TASK CLASSIFICATION`

Required hard rule, embedded verbatim (D-09 — locked):
`Classify the turn if ANY of: (a) you called a tool other than read-only file inspection; (b) you produced > 200 words of new content; (c) the user asked a question requiring multi-step reasoning. Skip the turn if your entire output is ≤ 2 sentences and called no tools.`

Required trivial-label blocklist, embedded verbatim (D-12 — locked):
`ack, acknowledgment, greeting, confirmation, hello, thanks`

Canonical marker-write snippet (RESEARCH.md Pattern 1; PATTERNS.md lines 189–213) — embed in SKILL.md as a fenced Python block, expanded with a write_marker(task_type, operation_type) helper called twice (GUARDRAIL then CHAT) per Pitfall 4:
- Imports: fcntl, json, os, secrets, time (stdlib only)
- session_id resolution: pinned by Task 1 decision
- markers_dir: os.path.expanduser("~/.hermes/state/revenium/markers")
- muid(): f"{int(time.time_ns() // 1_000_000):013x}" + secrets.token_hex(10)
- record dict keys: muid, ts, sid, task_type, operation_type (allow-list only)
- json.dumps(record, separators=(",", ":"), ensure_ascii=True) + "\n"
- open(marker_path, "ab", buffering=0); fcntl.flock(f, fcntl.LOCK_EX); f.write(encoded)
- Two calls: write_marker("<looked_up_label>", "GUARDRAIL") and write_marker("<looked_up_label>", "CHAT")

Existing halt anchor (DO NOT MODIFY) — em dash U+2014:
`## ABSOLUTE FIRST — HALT CHECK (NON-NEGOTIABLE)`

Required PROMPT-07 test signature (RESEARCH.md lines 605–620; PATTERNS.md lines 346–361):
- Method name: test_prompt_ordering_invariant
- halt_anchor literal: 'ABSOLUTE FIRST — HALT CHECK (NON-NEGOTIABLE)' (em dash U+2014)
- classify_anchor literal: 'FINAL ACTION — TASK CLASSIFICATION' (em dash U+2014)
- assertIn both anchors; assertLess on text.index() positions

From plan 02-02 (test_marker_file_schema) — the schema the canonical snippet's output must satisfy:
- Required record keys: muid, ts, sid, task_type, operation_type
- Optional keys: turn_seq, agent, trace_id, model
- Line < 1024 bytes UTF-8 encoded
- task_type matches `^[a-z][a-z0-9_]{1,47}$`
- operation_type from span_kind set (CHAT, GUARDRAIL, ...)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve HERMES_SESSION_ID open question — pin the session-id discovery mechanism</name>
  <files>skills/revenium/SKILL.md</files>
  <read_first>
    - .planning/phases/02-prompt-design-marker-contract/02-RESEARCH.md (Open Questions item 3, lines 722–725 — the assumed mechanism and its [ASSUMED] tag)
    - skills/revenium/scripts/hermes-report.sh (any pattern for session-id resolution on the cron side — note the cron resolves session IDs from sqlite3 rows in state.db, not from env vars; the agent has no such DB access at write-time)
    - skills/revenium/SKILL.md (frontmatter `required_environment_variables` list at lines 9–12 — confirm whether HERMES_SESSION_ID belongs there if Hermes sets it natively)
    - skills/revenium/scripts/common.sh (env-var-driven path resolution — the discovery mechanism MUST be consistent with the project's existing env-var-with-fallback idiom)
  </read_first>
  <action>
Resolve the [ASSUMED] flag on HERMES_SESSION_ID by performing this decision-and-document workflow:

1. Empirically check whether Hermes sets HERMES_SESSION_ID as an env var in the agent's execution environment. Use the available context: search the Hermes documentation references in `.planning/research/STACK.md`, search the local Hermes installation if accessible (e.g., `env | grep -i hermes` from any Hermes-spawned shell would show this), or query the Hermes docs via WebFetch (`https://hermes-agent.nousresearch.com/docs/` looking for env-var contracts). If empirical verification is not possible from inside the executor environment, fall through to step 2.

2. Pin one of three resolution mechanisms in the canonical snippet and document the choice inline. The three acceptable options, in order of preference:

   (a) PRIMARY: HERMES_SESSION_ID env var. Use `os.environ.get("HERMES_SESSION_ID")` with NO fallback (raise if absent). Document in the SKILL.md snippet comment that Hermes is expected to set this env var. If empirical verification in step 1 confirmed it IS set, choose this option.

   (b) FALLBACK: Timestamp-based pseudo-session-id. Use `os.environ.get("HERMES_SESSION_ID") or f"pseudo-{int(time.time())}"` as the value. Document in the snippet comment that this fallback produces a NEW pseudo-session-id every Python invocation, meaning markers written from the same Hermes session via separate Python invocations could land in different files. This is a tracked imperfection — the cron's session-id resolution from `state.db` (Phase 3 concern) is the authoritative answer; the marker filename only needs to be groupable per logical conversation, and a sticky-per-invocation pseudo-id is sufficient for the equal-split path. Choose this if step 1 confirms HERMES_SESSION_ID is NOT set and there is no other discoverable mechanism.

   (c) ALTERNATIVE: Read from a Hermes state file (e.g., `~/.hermes/state/current-session`). Use this only if step 1 confirms such a file is canonical. Document the file path and its lifecycle (created when? cleaned up when?) inline in the snippet comment.

3. Document the chosen mechanism in two places:
   - As a comment block at the top of the canonical Python heredoc in SKILL.md (Task 2 below embeds the snippet; this task pins the choice and pre-writes the comment).
   - As a note in the SKILL.md frontmatter's `required_environment_variables` list IF AND ONLY IF option (a) is chosen — add `HERMES_SESSION_ID` to that list with a one-line description. If option (b) or (c) is chosen, do NOT modify the frontmatter (`test_skill_frontmatter_has_hermes_metadata` is byte-sensitive on the `category: devops` line but tolerant of additions to `required_environment_variables`; nonetheless, do not add an env var that Hermes doesn't actually set).

4. Emit a decision record to be included in this plan's eventual SUMMARY: which option was chosen, what evidence supported the choice, what the fallback semantics are if the agent encounters a missing env var.

This task creates NO new SKILL.md content yet (Task 2 does the appending) — it pins the snippet's session-id resolution line so Task 2 can embed the snippet without re-deciding. If empirical verification cannot be completed from inside the executor environment AND no documentation conclusively answers the question, default to option (b) (fallback timestamp) and document the imperfection — never leave the snippet with an [ASSUMED] comment.

Frontmatter edit boundary: If option (a) requires adding `HERMES_SESSION_ID` to `required_environment_variables`, the edit MUST be a single line addition under that existing list (after `REVENIUM_TEAM_ID`), preserving all other frontmatter fields byte-for-byte. The `category: devops` line at line 19, the `name: revenium` line at line 2, and the `metadata.hermes` block at lines 16–19 MUST NOT be touched.
  </action>
  <verify>
    <automated>python3 -c "t=open('skills/revenium/SKILL.md').read(); assert 'name: revenium' in t and 'category: devops' in t and 'metadata:' in t and 'hermes:' in t, 'frontmatter regressed'; print('frontmatter intact')" && python3 -m unittest tests.test_repository.RepositoryTests.test_skill_frontmatter_has_hermes_metadata -v 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - A decision record exists (in the eventual SUMMARY, or noted in the task's commit message) stating which of options (a)/(b)/(c) was chosen and the supporting evidence.
    - If option (a) was chosen, `HERMES_SESSION_ID` appears in the SKILL.md frontmatter's `required_environment_variables` list AND `test_skill_frontmatter_has_hermes_metadata` still passes (verifies `name: revenium`, `metadata:`, `hermes:`, `category: devops` are all present).
    - If option (b) or (c) was chosen, the SKILL.md frontmatter is byte-unchanged from its pre-task state (`git diff -- skills/revenium/SKILL.md | grep -E '^[-+]' | grep -v '^[-+]{3}' | grep -E 'name:|metadata:|hermes:|category:|required_environment_variables'` returns empty for those keys).
    - The chosen mechanism is documented in language that downstream Task 2 can embed into the snippet's comment block (no further re-decision required).
    - No `[ASSUMED]` tag remains in the resolution comment — the choice is pinned with evidence (empirical or documented fallback rationale).
  </acceptance_criteria>
  <done>The session-id discovery mechanism is pinned with a documented rationale; the SKILL.md frontmatter is either updated (option a) or unchanged (options b/c); `test_skill_frontmatter_has_hermes_metadata` passes; Task 2 has an unambiguous specification for the snippet's session-id resolution line.</done>
</task>

<task type="auto">
  <name>Task 2: Append `## FINAL ACTION — TASK CLASSIFICATION` to SKILL.md (PROMPT-01..05)</name>
  <files>skills/revenium/SKILL.md</files>
  <read_first>
    - skills/revenium/SKILL.md (full file, especially lines 24–46 — the halt-check block; lines 272–278 — the current trailing `## Verification` section, where the new content appends after)
    - .planning/phases/02-prompt-design-marker-contract/02-CONTEXT.md (D-09 — hard rule verbatim; D-10 — 4 canonical examples; D-12 — blocklist; D-13 — insertion site; D-14 — section heading; D-15 — content split SKILL.md hot path vs reference cold path; D-16 — canonical Python heredoc)
    - .planning/phases/02-prompt-design-marker-contract/02-RESEARCH.md (Architecture Patterns Pattern 1, lines 199–237 — the canonical marker-write snippet; Pattern 2 referenced for lookup-first reuse description; section 8 (SKILL.md analysis), lines 478–506 — voice register difference and existing structural pattern; Pitfall 4 — two markers per substantive turn)
    - .planning/phases/02-prompt-design-marker-contract/02-PATTERNS.md (lines 136–227 — SKILL.md insertion site, structural pattern, embedded constants, voice register, formatting conventions)
    - skills/revenium/references/task-taxonomy.md (the cold-path reference doc shipped in plan 02-01 — SKILL.md must link to this for the long-form schema/normalization rules)
    - skills/revenium/references/halt-survivability.md (the runbook from plan 02-02 — SKILL.md References section may optionally cross-reference, but no edits required there)
    - skills/revenium/task-taxonomy.json (the seed file — the lookup-first instruction names this path)
    - skills/revenium/scripts/budget-check.sh (lines 43–93 — the canonical Python heredoc pattern shape this snippet mirrors)
    - The decision record from Task 1 (HERMES_SESSION_ID resolution — pinned)
  </read_first>
  <action>
Append a new section to `skills/revenium/SKILL.md` at the end of the file. The new section is the new file terminus per D-13. Do NOT modify any line above the insertion point. Specifically:

- Do NOT modify the frontmatter (lines 1–20) — exception: only if Task 1 chose option (a), the `required_environment_variables` list may include the single new entry as documented in Task 1.
- Do NOT modify the `## ABSOLUTE FIRST — HALT CHECK (NON-NEGOTIABLE)` block (lines 24–46) — byte-for-byte preserved.
- Do NOT modify any of the H2 sections between the halt block and `## Verification`.
- Do NOT modify the `## Verification` section content (currently lines 272–278) — only ADD a new section AFTER it.
- DO ensure the file ends with a single trailing newline after the new section.

The new section MUST contain, in this order:

1. Heading (exact, em dash U+2014): `## FINAL ACTION — TASK CLASSIFICATION`

2. Framing paragraph (completion-voice register, NOT prohibition-voice — D-14, RESEARCH.md section 8 voice analysis): one short paragraph stating that, after responding to the user, the agent classifies the just-completed turn and writes marker(s) for the cron to consume. Frame as closing discipline, not opening discipline. Do NOT use `ABSOLUTE`, `FIRST`, `NON-NEGOTIABLE`, `MANDATORY`, or `STOP`. Use completion language: "Before yielding back to the user...", "After your response...", "If the turn was substantive, write a marker."

3. Hard rule (D-09 verbatim, inside a blockquote or bullet for emphasis): `Classify the turn if ANY of: (a) you called a tool other than read-only file inspection; (b) you produced > 200 words of new content; (c) the user asked a question requiring multi-step reasoning. Skip the turn if your entire output is ≤ 2 sentences and called no tools.` The exact wording MUST appear verbatim in the file — no paraphrasing, no rewording.

4. `### Examples` subsection with the 4 canonical examples (D-10). Use H3 subheadings or bold labels for each. The 4 examples MUST cover:
   - Clear substantive (CLASSIFY): the agent reviewed a code diff and made 2 tool calls (read_file + grep). Write a marker with `task_type = code_review`.
   - Clear trivial (SKIP): the user said "what is 2+2?" and the agent replied "4." in one sentence, no tools. Skip — no marker.
   - Borderline classify (CLASSIFY): the user asked the agent to explain a concept; the agent produced a 5-paragraph explanation, no tool calls. Per rule (b) (> 200 words of new content), classify. Use `task_type = analysis` or `research` depending on the example wording.
   - Borderline skip (SKIP): the user said "good morning, can you confirm you're ready?" and the agent replied "Good morning — ready when you are." over 2 lines, no tools. Despite being multi-line, this is ≤ 2 sentences and called no tools — skip.
   Each example MUST state the agent's behavior, the rule that fires (or doesn't), and the resulting marker decision in a structured way (e.g., "Trigger: rule (a) — called tools. Decision: classify with `task_type = code_review`.").

5. `### Lookup-first reuse` subsection (PROMPT-03, D-08): one short paragraph plus a fenced Python block. The paragraph states that before minting a new label, the agent MUST read `~/.hermes/state/revenium/task-taxonomy.json` and reuse any existing label that fits the turn's semantics. New labels are minted ONLY when no existing label clearly fits. Reuse is aggressive — fragmentation (`code_review` vs `code-review`) is permanent harm. The fenced block shows a short lookup expression that opens the taxonomy and iterates `taxonomy['labels']` to find a candidate. Cross-reference `references/task-taxonomy.md` for the long-form schema, normalization rules, and atomic mint pattern.

6. `### Trivial-label blocklist` subsection (D-12 verbatim) — a single short paragraph stating the cron rejects markers carrying any of these `task_type` values, followed by the bulleted blocklist:
   - `ack`
   - `acknowledgment`
   - `greeting`
   - `confirmation`
   - `hello`
   - `thanks`
   State that this list is closed-set for v1 — new entries require a release.

7. `### Marker write` subsection (PROMPT-05 — marker write is the FINAL ACTION, last subsection in the block per D-15). One short paragraph stating that the marker write is the agent's last action before yielding back to the user, and it is the easiest step to skip — placed here intentionally so it's hardest to overlook. Then exactly ONE Python heredoc fenced block (D-16 — exactly one snippet, no alternatives, no variations) embedding the canonical marker-write code. The snippet body MUST:
   - Use the session-id resolution mechanism pinned by Task 1 (with the chosen comment text).
   - Use `os.path.expanduser("~/.hermes/state/revenium/markers")` for the markers directory (state-path discipline allows this in SKILL.md per RESEARCH.md anti-patterns section: the agent context resolves `~` server-side, and `test_runtime_paths_are_hermes_native` does not scan SKILL.md body).
   - Define `muid()` using `int(time.time_ns() // 1_000_000):013x` + `secrets.token_hex(10)` — the stdlib ULID-style recipe from RESEARCH.md section 4.
   - Define `write_marker(task_type, operation_type)` as a helper that builds the record, json-dumps with `separators=(",", ":"), ensure_ascii=True`, encodes UTF-8, opens with `"ab", buffering=0`, calls `fcntl.flock(LOCK_EX)`, and `f.write(encoded)`.
   - Demonstrate Pitfall-4 two-markers-per-substantive-turn by calling `write_marker(...)` TWICE at the end of the snippet: once with `operation_type="GUARDRAIL"` (the classification turn itself — PROMPT-04) and once with `operation_type="CHAT"` (the work turn). Use a placeholder `task_type` like `"code_review"` and a code comment that tells the agent to replace it with the looked-up label.
   - Stay under ~30 lines (the snippet is a hot-path reference; verbosity defeats the purpose).
   - Use only stdlib imports: `fcntl, json, os, secrets, time` — no third-party imports.

8. Closing line (one sentence, completion register): state that if the turn was a skip (≤ 2 sentences, no tools), no marker is written; the cron will fall back to `--task-type unclassified` for that delta (PROMPT-02 + backward-compat hint, no need to elaborate on cron behavior).

Voice register reminder (D-14, RESEARCH.md section 8): Use completion framing throughout. Forbidden keywords inside the new section: `ABSOLUTE`, `NON-NEGOTIABLE`, `MANDATORY`, `MUST STOP`, `Do NOT make any tool calls`, `Do NOT continue reading`. The token `FIRST` and `(a)/(b)/(c)` rule labels in the hard rule are fine (they're part of D-09 verbatim text); just don't use `FIRST` as a heading keyword. Permitted directive language: `Before yielding...`, `After your response...`, `If the turn was substantive...`, `Reuse...`, `Mint only when...`.

Length budget: The full new section should be 90–180 lines. Beyond 200 lines, the section starts to compete with the halt block on content gravity — split into subsections per the structure above to manage density.

Em dash discipline (Pitfall 1): The heading uses `—` (U+2014), not `-`. Verify the heading you write contains the same em dash character used in the existing halt-check heading at line 24. PROMPT-07 (Task 3) tests this with a literal em-dash match.
  </action>
  <verify>
    <automated>python3 -c "
text = open('skills/revenium/SKILL.md').read()
assert 'ABSOLUTE FIRST — HALT CHECK (NON-NEGOTIABLE)' in text, 'halt anchor missing/modified'
assert 'FINAL ACTION — TASK CLASSIFICATION' in text, 'classify anchor missing'
assert text.index('ABSOLUTE FIRST — HALT CHECK') < text.index('FINAL ACTION — TASK CLASSIFICATION'), 'ordering wrong'
hard_rule_a = 'Classify the turn if ANY of: (a) you called a tool other than read-only file inspection; (b) you produced > 200 words of new content; (c) the user asked a question requiring multi-step reasoning. Skip the turn if your entire output is ≤ 2 sentences and called no tools.'
assert hard_rule_a in text, 'hard rule not verbatim'
for tok in ['ack', 'acknowledgment', 'greeting', 'confirmation', 'hello', 'thanks']:
    assert tok in text, f'blocklist token missing: {tok}'
for tok in ['import fcntl', 'import json', 'import os', 'import secrets', 'import time', 'fcntl.flock', 'time.time_ns', 'secrets.token_hex', 'buffering=0', 'GUARDRAIL', 'CHAT', 'json.dumps', 'separators=']:
    assert tok in text, f'snippet token missing: {tok}'
new_section = text[text.index('FINAL ACTION — TASK CLASSIFICATION'):]
for forbidden in ['ABSOLUTE', 'NON-NEGOTIABLE']:
    assert forbidden not in new_section, f'reserved keyword leaked into new section: {forbidden}'
assert 'task-taxonomy' in new_section, 'lookup-first reference missing'
assert 'name: revenium' in text and 'category: devops' in text and 'metadata:' in text, 'frontmatter regressed'
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - `skills/revenium/SKILL.md` contains the exact string `## FINAL ACTION — TASK CLASSIFICATION` (em dash U+2014) on its own line, positioned AFTER `## Verification` in the file.
    - `skills/revenium/SKILL.md` still contains the exact string `## ABSOLUTE FIRST — HALT CHECK (NON-NEGOTIABLE)` (em dash) byte-unchanged at line 24.
    - `text.index('ABSOLUTE FIRST — HALT CHECK')` is strictly less than `text.index('FINAL ACTION — TASK CLASSIFICATION')`.
    - The full D-09 hard-rule sentence appears verbatim in the new section (with U+2264 `≤` for "less than or equal to 2 sentences").
    - All 6 D-12 blocklist tokens (`ack`, `acknowledgment`, `greeting`, `confirmation`, `hello`, `thanks`) appear in the new section.
    - The canonical Python snippet contains all stdlib imports (`fcntl, json, os, secrets, time`), the `muid()` recipe (`time.time_ns`, `secrets.token_hex`), the `buffering=0` open mode, `fcntl.flock`, `json.dumps(..., separators=(",", ":"), ensure_ascii=True)`, and the two `write_marker(...)` calls (one with `GUARDRAIL`, one with `CHAT`).
    - The new section does NOT contain `ABSOLUTE` or `NON-NEGOTIABLE` (those are reserved for the halt anchor).
    - The new section references `task-taxonomy.json` (the live file) and `references/task-taxonomy.md` (the cold-path doc).
    - File ends with a single trailing newline.
    - `python3 -m unittest tests.test_repository.RepositoryTests.test_skill_frontmatter_has_hermes_metadata` passes (frontmatter untouched OR only one new env-var line added per Task 1 option (a)).
    - `python3 -m unittest tests.test_repository.RepositoryTests.test_no_legacy_branding_left` does NOT add `skills/revenium/SKILL.md` to the offenders list (the new section introduces no forbidden strings).
    - Total new content is between 90 and 200 lines (the length budget).
  </acceptance_criteria>
  <done>The `## FINAL ACTION — TASK CLASSIFICATION` section is appended to SKILL.md with hard rule, 4 examples, blocklist, and the canonical marker-write snippet (with the Task 1 session-id mechanism); halt block byte-unchanged; frontmatter byte-unchanged or only the one allowed addition; legacy-branding regex and frontmatter test both still pass.</done>
</task>

<task type="auto">
  <name>Task 3: Add test_prompt_ordering_invariant (PROMPT-07) — pin the SKILL.md structural ordering</name>
  <files>tests/test_repository.py</files>
  <read_first>
    - tests/test_repository.py (post-02-02 state — should include `test_taxonomy_file_schema` from 02-01 and `test_marker_file_schema` from 02-02 plus the three new entries in `expected`)
    - .planning/phases/02-prompt-design-marker-contract/02-RESEARCH.md (Pitfall 1, lines 511–518 — em-dash discipline; Draft Test Signatures PROMPT-07, lines 605–620)
    - .planning/phases/02-prompt-design-marker-contract/02-PATTERNS.md (lines 346–363 — PROMPT-07 method signature and the CRITICAL em-dash note)
    - skills/revenium/SKILL.md (post-Task-2 state — the file the test reads; confirm both anchors are present)
  </read_first>
  <action>
Add ONE new test method `test_prompt_ordering_invariant` to `class RepositoryTests` in `tests/test_repository.py`. Position the method after `test_marker_file_schema` (added by plan 02-02) and before `test_shell_scripts_have_valid_syntax`. The method MUST follow the signature shown in the `<interfaces>` block of this plan (sourced from RESEARCH.md lines 605–620 and PATTERNS.md lines 346–361):

- `def test_prompt_ordering_invariant(self):` with docstring `"""Halt-check anchor appears before the classification anchor in SKILL.md."""`.
- Read `text = (SKILL / 'SKILL.md').read_text()`.
- Define `halt_anchor = 'ABSOLUTE FIRST — HALT CHECK (NON-NEGOTIABLE)'` — the em dash is U+2014. Pitfall 1 is explicit: write the literal `—` character in the source, NOT `-` (ASCII hyphen). If unsure, the test source character at the position must produce 3 UTF-8 bytes (`0xE2 0x80 0x94`), not 1 byte (`0x2D`).
- Define `classify_anchor = 'FINAL ACTION — TASK CLASSIFICATION'` — same em dash discipline.
- `self.assertIn(halt_anchor, text, 'halt-check anchor missing from SKILL.md — do not remove or rename it')`.
- `self.assertIn(classify_anchor, text, 'classification anchor missing from SKILL.md — Phase 2 deliverable not present')`.
- `self.assertLess(text.index(halt_anchor), text.index(classify_anchor), 'halt-check anchor must appear before classification anchor in SKILL.md')`.

Do not modify the import block. Do not modify any other test method. Do not change `unittest.main()` at file end.

After adding the method, run `python3 -m unittest tests.test_repository.RepositoryTests.test_prompt_ordering_invariant -v` to confirm it passes. If it raises `ValueError: substring not found`, the em-dash in either the test or SKILL.md is wrong — fix by re-copying the literal em dash character from `skills/revenium/SKILL.md` line 24.

As a negative-fixture sanity check (manual, not part of automated CI): temporarily swap the order in your local file — move the new `## FINAL ACTION` section ABOVE the halt block — and confirm the test FAILs with the `assertLess` message. Then revert. This confirms the test is doing real work and not a vacuous pass.
  </action>
  <verify>
    <automated>python3 -m unittest tests.test_repository.RepositoryTests.test_prompt_ordering_invariant -v 2>&1 | tail -5 && python3 -c "import re; src=open('tests/test_repository.py').read(); idx=src.index('test_prompt_ordering_invariant'); method=src[idx:idx+1500]; assert 'ABSOLUTE FIRST — HALT CHECK (NON-NEGOTIABLE)' in method, 'em dash missing in halt_anchor literal'; assert 'FINAL ACTION — TASK CLASSIFICATION' in method, 'em dash missing in classify_anchor literal'; assert 'assertLess' in method and 'assertIn' in method, 'required asserts missing'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m unittest tests.test_repository.RepositoryTests.test_prompt_ordering_invariant -v` exits 0 with `OK`.
    - The test method's `halt_anchor` and `classify_anchor` string literals BOTH use the em-dash character U+2014 — verifiable by `python3 -c "src=open('tests/test_repository.py').read(); idx=src.index('halt_anchor'); print(repr(src[idx:idx+80]))"` showing `—` or the literal `—` between the keywords (not a `-`).
    - The test method contains `self.assertIn(halt_anchor, ...)`, `self.assertIn(classify_anchor, ...)`, and `self.assertLess(text.index(halt_anchor), text.index(classify_anchor), ...)`.
    - All previously-passing tests still pass: `test_expected_files_exist` (3 new paths from 02-01 and 02-02), `test_skill_frontmatter_has_hermes_metadata`, `test_runtime_paths_are_hermes_native`, `test_taxonomy_file_schema`, `test_marker_file_schema`, `test_shell_scripts_have_valid_syntax`. (`test_no_legacy_branding_left` retains its pre-existing `.planning/`-tree failure.)
    - Negative-fixture sanity (manual): temporarily reversing the section order in SKILL.md fails this test with the `assertLess` failure message; reverting passes. Confirms the test is not vacuous.
  </acceptance_criteria>
  <done>The PROMPT-07 invariant is in CI; the test uses literal U+2014 em dashes for both anchors; the structural ordering of SKILL.md (halt-block-first, classify-block-last) is permanently pinned against drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SKILL.md content → agent context | The new `## FINAL ACTION` section becomes part of the agent's working context (loaded via `skill_view()` per RESEARCH.md). Any weakening of the halt-check anchor here (even indirectly, via competing strong-framing keywords) is a safety regression. |
| Agent Python heredoc → marker file write | The canonical snippet writes to `${MARKERS_DIR}/<sid>.jsonl`. The snippet uses stdlib only (no pip), file mode `ab` with `buffering=0` (atomic O_APPEND), and `fcntl.flock(LOCK_EX)` (cooperative; advisory under POSIX). The cron reader does not need to flock (per RESEARCH.md). |
| HERMES_SESSION_ID env var → marker filename | If the env var is operator-controlled or unset, the snippet's session-id resolution falls back per Task 1's pinned mechanism. Markers with mis-attributed `sid` could conflate sessions; Phase 3's cron-side session reconciliation against `state.db` is the second-line defense. |
| New SKILL.md content → halt-check priority anchor | Adding 90–180 lines at end-of-file shifts the byte-position of the halt-check block but per the U-shaped "Lost in the Middle" curve, both ends remain high-attention. The PROMPT-07 test enforces structural-position invariant; the halt-survivability runbook (plan 02-02) is the empirical regression check. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | T | Halt-check anchor regression in SKILL.md | mitigate | PROMPT-07 (`test_prompt_ordering_invariant`) enforces structural ordering at CI time; halt-survivability runbook (plan 02-02) enforces behavioral verbatim-match before release. Two-layer defense. |
| T-02-03-02 | T | Voice-register dilution (new section using `ABSOLUTE`/`NON-NEGOTIABLE`) | mitigate | Acceptance criterion explicitly bans those tokens in the new section. PROMPT-07's `assertIn` on the halt-anchor's exact phrasing catches removal/rename; an additional grep check in Task 2's verify confirms reserved keywords are absent from the new section. |
| T-02-03-03 | I | Marker `sid` mis-attribution via fallback pseudo-session-id | mitigate | Task 1 pins the resolution mechanism and documents the fallback's failure mode inline in the snippet comment. Phase 3 cron-side reconciliation against `state.db` rows is the authoritative cross-check; the agent's marker filename only needs to be groupable. |
| T-02-03-04 | T | Marker record contains free-form fields (privacy boundary MARK-05) | mitigate | The canonical snippet's record dict literal contains ONLY the 5 allow-listed keys (`muid, ts, sid, task_type, operation_type`). TEST-01 (from plan 02-02) catches any drift in test fixtures; the canonical snippet shape pins what the agent actually emits. |
| T-02-03-05 | D | Marker record exceeds 1024-byte budget | mitigate | The canonical snippet uses `json.dumps(..., separators=(",", ":"), ensure_ascii=True)` (compact JSON). With allow-list fields only and `muid` capped at 33 chars, the typical record is ~110 bytes — well within the 1024-byte budget. TEST-01 enforces this on fixtures. |
| T-02-03-06 | T | `task_type` injection (e.g., via taxonomy file tampering) | mitigate | The agent reads the taxonomy via the lookup-first pattern; the cron-side regex `^[a-z][a-z0-9_]{1,47}$` (Phase 3) and the blocklist enforcement (Phase 3) reject malformed labels. The new SKILL.md content states the rules but does not enforce — enforcement is at the cron, by design. |
| T-02-03-07 | E | Snippet importing third-party packages | mitigate | Acceptance criterion enforces `fcntl, json, os, secrets, time` only — stdlib. Any `import ulid`, `import pydantic`, etc., in the snippet body fails the verify check. |
| T-02-03-08 | T | New SKILL.md content introduces legacy branding strings | mitigate | Acceptance criterion runs `test_no_legacy_branding_left` post-task — adding SKILL.md to the offenders list is a fail condition. |
</threat_model>

<verification>
After all three tasks complete, run the full test suite:

```bash
python3 -m unittest tests.test_repository -v
```

Expected outcome: 8 tests run.
- `test_expected_files_exist` — passes (3 new Phase-2 paths resolve)
- `test_skill_frontmatter_has_hermes_metadata` — passes (frontmatter intact or only HERMES_SESSION_ID added per Task 1 option a)
- `test_no_legacy_branding_left` — retains pre-existing `.planning/` failure (NOT extended by this plan; verify the offender list is unchanged)
- `test_runtime_paths_are_hermes_native` — passes (this plan does NOT modify `common.sh`)
- `test_taxonomy_file_schema` — passes (from 02-01)
- `test_marker_file_schema` — passes (from 02-02)
- `test_prompt_ordering_invariant` — passes (NEW in this plan)
- `test_shell_scripts_have_valid_syntax` — passes (this plan does NOT modify any shell script)

Additional checks:

```bash
# 1. Halt anchor byte-for-byte preserved
grep -F 'ABSOLUTE FIRST — HALT CHECK (NON-NEGOTIABLE)' skills/revenium/SKILL.md | wc -l
# Expected: 1

# 2. New classify anchor present
grep -F 'FINAL ACTION — TASK CLASSIFICATION' skills/revenium/SKILL.md | wc -l
# Expected: 1 (the H2 heading) or 2 (heading + cross-ref); should NOT be 0

# 3. Reserved keywords NOT in the new section
awk '/FINAL ACTION — TASK CLASSIFICATION/{flag=1} flag' skills/revenium/SKILL.md | grep -E 'ABSOLUTE|NON-NEGOTIABLE' && echo "VIOLATION" || echo "OK"
# Expected: OK

# 4. Em-dash in the test source (U+2014, not ASCII hyphen)
python3 -c "src = open('tests/test_repository.py').read(); idx = src.index('halt_anchor ='); print(repr(src[idx:idx+200]))"
# Expected: literal `—` or `—` between ABSOLUTE FIRST and HALT CHECK

# 5. Halt-survivability runbook is the empirical regression check — operator runs this before release per plan 02-02
ls -la skills/revenium/references/halt-survivability.md
# Expected: file exists (shipped in plan 02-02)
```

**Manual gate before merging this plan:** Run the halt-survivability runbook (`skills/revenium/references/halt-survivability.md`) on at least the Scenario 1 short-session × Claude Sonnet 4.6 cell of the test matrix. If FAIL, do not merge — revise the SKILL.md content until the halt string fires verbatim. This is the Phase 2 success criterion 1 — automated test (PROMPT-07) is necessary but not sufficient.
</verification>

<success_criteria>
- `skills/revenium/SKILL.md` ends with the `## FINAL ACTION — TASK CLASSIFICATION` section containing: framing paragraph (completion voice), D-09 hard rule verbatim, 4 canonical examples (1 clear-substantive + 1 clear-trivial + 1 borderline-classify + 1 borderline-skip), lookup-first reuse subsection, D-12 blocklist verbatim, one canonical marker-write Python heredoc snippet (with two write_marker calls — GUARDRAIL + CHAT per Pitfall 4).
- The halt-check block at SKILL.md lines 24–46 is byte-unchanged.
- The HERMES_SESSION_ID open question is pinned with a documented mechanism in Task 1; no `[ASSUMED]` tag remains in the snippet.
- `tests/test_repository.py::test_prompt_ordering_invariant` is in CI, uses literal U+2014 em dashes, asserts both anchors present, asserts halt anchor precedes classify anchor.
- All Phase 2 invariant tests pass: `test_taxonomy_file_schema` (02-01), `test_marker_file_schema` (02-02), `test_prompt_ordering_invariant` (this plan).
- `test_skill_frontmatter_has_hermes_metadata` continues to pass.
- Map-back: PROMPT-01 (end-loaded after halt block ✓), PROMPT-02 (substantive turn defined with hard rule + 4 examples + blocklist ✓), PROMPT-03 (lookup-first instruction with taxonomy reference ✓), PROMPT-04 (GUARDRAIL marker for classification turn — second write_marker call in the snippet ✓), PROMPT-05 (marker write as the FINAL ACTION subsection ✓), PROMPT-07 (test_prompt_ordering_invariant ✓).
- Implicit map-back: the canonical snippet shape satisfies MARK-01 (single write call, < 1024 bytes), MARK-02 (allow-list fields), MARK-03 (ULID-style sortable muid), MARK-05 (no free-form fields in the record dict literal).
- Phase 2 closure: all 19 phase requirement IDs (TAX-01..05, MARK-01..05, PROMPT-01..07, TEST-01..02) are addressed across plans 02-01, 02-02, 02-03.
</success_criteria>

<output>
After completion, create `.planning/phases/02-prompt-design-marker-contract/02-03-SUMMARY.md` summarizing:

1. The Task 1 decision record: which session-id resolution option (a/b/c) was chosen, the supporting evidence, and the documented fallback semantics.
2. The line-range diff in `skills/revenium/SKILL.md` — start/end line numbers of the new section, total lines added, halt-block byte-equality confirmed.
3. The `test_prompt_ordering_invariant` method signature and a snippet showing the literal em-dash bytes in the test source.
4. Confirmation that all Phase 2 requirement IDs (TAX-01..05, MARK-01..05, PROMPT-01..07, TEST-01..02) map to a passing test or shipped artifact across plans 02-01, 02-02, 02-03.
5. Pre-merge gate reminder: operator should run at least one cell of `references/halt-survivability.md` before merging this plan.
6. Hand-off note for Phase 3: the marker record shape (allow-list keys, < 1024 bytes, `task_type` regex, `operation_type` vocabulary) is pinned by the SKILL.md canonical snippet AND by `test_marker_file_schema`. Phase 3 cron-side reader can rely on this contract.
</output>
