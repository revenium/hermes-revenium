---
phase: 05-housekeeping-compat-hardening
plan: 01
type: execute
wave: 1
status: ready
depends_on: []
files_modified:
  - skills/revenium/scripts/common.sh
  - tests/test_repository.py
files_created:
  - skills/revenium/scripts/prune-markers.sh
  - .planning/phases/05-housekeeping-compat-hardening/05-01-SUMMARY.md
autonomous: true
requirements:
  - COMPAT-04
  - TEST-05
tags:
  - housekeeping
  - prune-markers
  - operator-script
  - common-sh-additions

must_haves:
  truths:
    - "D-26 / D-27 / D-28 / D-29 land atomically as a working prune pipeline: `bash skills/revenium/scripts/prune-markers.sh --dry-run` against a fixture with three marker files (one whose latest `HERMES:<sid>:…:<unix_ts>:<muid>` ledger row is 31 days old, one whose latest row is today, one orphan with no ledger row and mtime 31 days old) lists the 31-day-old + orphan as candidates without touching the filesystem; the same command without `--dry-run` removes those two files and leaves today's marker untouched; a third immediate re-run exits 0 with summary `scanned=1 kept=1 removed=0` (idempotent). Behavior driven by ledger field 4 (`<unix_ts>`) read via `grep \"^HERMES:${sid}:\" \"${LEDGER_FILE}\" | tail -1 | cut -d: -f4` per D-26; mtime fallback when no ledger row exists for the sid."
    - "`skills/revenium/scripts/common.sh` declares `MARKER_RETENTION_DAYS=\"${REVENIUM_MARKER_RETENTION_DAYS:-30}\"` and `PRUNE_LOCK_FILE=\"${STATE_DIR}/prune.lock\"` immediately after the existing `LOCK_FILE` declaration (line 20). Existing `MARKERS_DIR` / `MARKERS_READY_DIR` / `LOCK_FILE` lines remain byte-unchanged. `mkdir -p` block (line 22) is byte-unchanged — the new lock file is created on demand via `flock`, not preallocated. `test_runtime_paths_are_hermes_native` continues to pass (the regex assertions on `MARKERS_DIR` and `MARKERS_READY_DIR` and the literal `.hermes` / `state/revenium` / `task-taxonomy.json` substrings are all preserved)."
    - "`skills/revenium/scripts/prune-markers.sh` exists, is `chmod +x`, parses cleanly under `bash -n`, uses `set -euo pipefail` (hard-fail discipline; matches `clear-halt.sh` and `cron.sh` per `<plan_shape_suggestions>`), resolves `SCRIPT_DIR` via `BASH_SOURCE[0]`, sources `common.sh` with the `# shellcheck source=/dev/null` directive immediately above, and calls `ensure_path` before any work. The script accepts a single optional flag `--dry-run` (no other flags in v1; unknown flags exit 1 with usage on stderr). The script acquires `${PRUNE_LOCK_FILE}` via the same `exec 9>\"${PRUNE_LOCK_FILE}\"` + `python3 - <<'PY'` + `fcntl.flock(9, fcntl.LOCK_EX | fcntl.LOCK_NB)` pattern that `cron.sh` uses (`<canonical_refs>` + `<code_context>`); on lock contention it logs `warn \"prior prune still active, skipping\"` and exits 0 (NOT 1). Lock is acquired BEFORE the markers-dir scan so two concurrent operator invocations cannot race on the same file set (D-29)."
    - "Per-deletion logging discipline (D-29): every removal and every dry-run candidate is logged via the `info` helper from `common.sh` exactly as `info \"prune: removed sid=<sid> marker=<basename> last_ledger_ts=<iso> age_days=<n>\"` (live mode) or `info \"prune: dry-run, would remove sid=<sid> marker=<basename> last_ledger_ts=<iso> age_days=<n>\"` (dry-run). For orphan markers (no ledger row), the log line substitutes `last_ledger_ts=mtime` and reports `age_days` computed from `os.path.getmtime`. A single `info \"prune: summary, scanned=<n> kept=<n> removed=<n>\"` line is emitted at the end of every run regardless of dry-run. NEVER use bare `echo` for these logged events — `info` is the only correct helper per CLAUDE.md `## Logging` conventions."
    - "New test method `test_prune_markers_dry_run_and_live` appended to `RepositoryTests` in `tests/test_repository.py` mirrors the canonical scaffold from `test_cron_marker_split_end_to_end` (lines 373-491): `tempfile.TemporaryDirectory()` isolation; explicit `REVENIUM_*` env overrides (`HERMES_HOME`, `REVENIUM_STATE_DIR`, `REVENIUM_MARKERS_DIR`, `REVENIUM_MARKER_RETENTION_DAYS='30'`, `TZ='UTC'`) sourced from `**os.environ` plus the overrides — same hygiene the WR-03 task (Plan 05-03) extends Phase 4 tests with. Three sub-cases (dry-run / live / idempotent re-run) match the 05-PATTERNS.md scaffold lines 402-477 verbatim in shape; the test asserts: (a) dry-run leaves the 31-day-old marker AND the orphan marker on disk; (b) live run removes the 31-day-old marker AND the orphan marker but keeps today's marker; (c) idempotent re-run exits 0 with no further deletions. No subprocess timeout exceeds 30 seconds per call (the test runs in well under 60 seconds total)."
    - "`skills/revenium/scripts/prune-markers.sh` is added to the `expected` list in `tests/test_repository.py::test_expected_files_exist` (line 57 area). `test_shell_scripts_have_valid_syntax` automatically picks the new file up via `SKILL.rglob('scripts/*.sh')` so no edit is needed there — but the `bash -n` syntax check WILL run against the new file at every commit boundary."
    - "Pre-existing 41 tests continue to pass byte-unchanged across every commit in this plan: `python3 -m unittest discover -s tests -p 'test_*.py' -v` runs green at the close of T01 (common.sh + expected_files), T02 (prune script + test), T03 (SUMMARY only). Test count delta: 41 → 42 (one new method)."
    - "Out-of-scope discipline (locked by CONTEXT.md D-37 / D-38 / `<scope_locks>`): NO changes to `skills/revenium/plugins/revenium-classifier/classifier.py` (Plan 05-02 owns mint-back + recency sort — strict file-ownership boundary); NO changes to `skills/revenium/scripts/hermes-report.sh` (Plan 05-03 owns WR-01/WR-02); NO new state files outside `${STATE_DIR}`; NO new test files (all tests live in `tests/test_repository.py` per CLAUDE.md `### Module Design`); NO changes to `clear-halt.sh`, `cron.sh`, `install-cron.sh`, `uninstall-cron.sh`, `budget-check.sh`, `hermes-report.sh`, `split_strategies.py`; NO modification of `_count_tools_in_current_turn` or its 4 tests (D-37 — explicit KEEP); NO doc changes to README.md / setup.md / task-taxonomy.md (Plan 05-04 owns the documentation pass, intentionally docs-last so it cites the prune script's actual shipped flag surface)."
  artifacts:
    - path: "skills/revenium/scripts/common.sh"
      provides: "Two new lines inserted after `LOCK_FILE=\"${STATE_DIR}/cron.lock\"` (line 20): `MARKER_RETENTION_DAYS=\"${REVENIUM_MARKER_RETENTION_DAYS:-30}\"` and `PRUNE_LOCK_FILE=\"${STATE_DIR}/prune.lock\"`. New env var name is `REVENIUM_MARKER_RETENTION_DAYS` (screaming-snake + `REVENIUM_` prefix matches every other overridable var per `<established_patterns>` in 05-PATTERNS.md). Colon-dash (`:-`) fallback per common.sh discipline. No other change to common.sh. `mkdir -p` block at line 22 is byte-unchanged — the lock file is created on demand by `flock` via fd 9, not preallocated."
      contains: "MARKER_RETENTION_DAYS"
    - path: "skills/revenium/scripts/prune-markers.sh"
      provides: "New operator-invoked script. Reads `${MARKERS_DIR}/*.jsonl`, derives stale-criteria from `${LEDGER_FILE}` field-4 (`<unix_ts>`) per D-26 with `os.path.getmtime` fallback for orphans (no ledger row), and `rm`s files older than `${MARKER_RETENTION_DAYS}` days (default 30). Single optional flag: `--dry-run` (logs candidates without removing). `set -euo pipefail` discipline, sources `common.sh`, calls `ensure_path`, acquires `${PRUNE_LOCK_FILE}` via the cron.sh-style flock pattern (`exec 9>\"${PRUNE_LOCK_FILE}\"` + `python3 - <<'PY' fcntl.flock(9, LOCK_EX | LOCK_NB) PY`). Per-deletion `info` log line includes sid, basename, `last_ledger_ts` (ISO) or `mtime`, and `age_days`. Final `info` summary line reports `scanned=<n> kept=<n> removed=<n>` regardless of dry-run."
      contains: "MARKER_RETENTION_DAYS"
    - path: "tests/test_repository.py"
      provides: "Two edits: (1) `prune-markers.sh` added to the `expected` list in `test_expected_files_exist` (after the existing `clear-halt.sh` entry, line ~73-74); (2) new test method `test_prune_markers_dry_run_and_live` appended to `RepositoryTests` after the existing `test_cron_marker_split_end_to_end` family (any clean position works — placement is not load-bearing). The new method uses `tempfile.TemporaryDirectory()`, seeds a 3-marker fixture (old / fresh / orphan), invokes the prune script via `subprocess.run(['bash', str(PRUNE_SCRIPT), ...], env=env, ...)` with explicit `REVENIUM_*` overrides, and asserts the three sub-cases (dry-run preserve, live delete, idempotent re-run). Reuses NO existing helpers beyond stdlib imports (local imports per CLAUDE.md test conventions)."
      contains: "test_prune_markers_dry_run_and_live"
    - path: ".planning/phases/05-housekeeping-compat-hardening/05-01-SUMMARY.md"
      provides: "Summary mirroring the 04-01-SUMMARY.md frontmatter + body shape: `phase`, `plan`, `type`, `executed`, `requirements_completed: [COMPAT-04, TEST-05]`, `files_modified`, `files_created`, `tests_added: 1`, `tests_modified: 0`, `tests_total_after: 45` (end-of-phase count assuming all Wave 1 plans land — 41 baseline + 1 from 05-01 + 2 from 05-02 + 1 from 05-03; if 05-01 executes in isolation it alone produces 42), `commits` (3 atomic commits expected: T01 common.sh + expected_files, T02 prune script + test, T03 SUMMARY), `tags`. Body sections: # Plan 05-01 Summary — Marker File Pruning + common.sh Retention Knob; ## Outcome (one paragraph); ## Decisions Honored (### D-26 / D-27 / D-28 / D-29 sub-headers); ## Key Files (bulleted with one-line summaries); ## Verification (test output + bash -n exit code); ## Operator Verification (manual UAT triple-case from CONTEXT.md `<specifics>`); ## Out of Scope Carried Forward."
      contains: "D-26, D-27, D-28, D-29"
  key_links:
    - from: "skills/revenium/scripts/prune-markers.sh (Python heredoc that scans markers and reads ledger)"
      to: "skills/revenium/scripts/common.sh (MARKERS_DIR, LEDGER_FILE, MARKER_RETENTION_DAYS, PRUNE_LOCK_FILE, LOG_FILE)"
      via: "source `${SCRIPT_DIR}/common.sh` with `# shellcheck source=/dev/null` directive; consumes all five variables. NEVER hardcodes paths inline."
      pattern: "source \"\\$\\{SCRIPT_DIR\\}/common\\.sh\""
    - from: "skills/revenium/scripts/prune-markers.sh (lock acquisition)"
      to: "PRUNE_LOCK_FILE in common.sh"
      via: "exec 9>\"${PRUNE_LOCK_FILE}\" + `python3 - <<'PY' fcntl.flock(9, LOCK_EX | LOCK_NB) PY` — mirrors cron.sh:19-30 exactly. On EAGAIN: `warn \"prior prune still active, skipping\"` + exit 0 (NOT 1 — operator-friendly when running while a prior invocation is mid-pass). Lock file path = `${PRUNE_LOCK_FILE}` (= `${STATE_DIR}/prune.lock`), NOT shared with `${LOCK_FILE}` (= cron.lock) — D-29 lock-scope choice prevents operator prune runs from blocking cron ticks."
      pattern: "exec 9>\"\\$\\{PRUNE_LOCK_FILE\\}\""
    - from: "skills/revenium/scripts/prune-markers.sh (staleness check)"
      to: "skills/revenium/scripts/common.sh (LEDGER_FILE) + filesystem mtime"
      via: "For each `${MARKERS_DIR}/*.jsonl` file: derive sid from basename (strip .jsonl), query `grep \"^HERMES:${sid}:\" \"${LEDGER_FILE}\" 2>/dev/null | tail -1 | cut -d: -f4` for the latest reported timestamp; if non-empty, compare to `(now - retention_days*86400)`; if empty (orphan), fall back to `os.path.getmtime`. Both branches log `last_ledger_ts=<iso>` or `last_ledger_ts=mtime` for operator audit per D-29."
      pattern: "grep \"\\^HERMES:\\$\\{sid\\}:\""
    - from: "tests/test_repository.py::test_prune_markers_dry_run_and_live"
      to: "skills/revenium/scripts/prune-markers.sh"
      via: "subprocess.run(['bash', str(PRUNE_SCRIPT), '--dry-run'/None], env=env, capture_output=True, text=True, timeout=30). Env carries `REVENIUM_MARKERS_DIR`, `REVENIUM_MARKER_RETENTION_DAYS='30'`, `HERMES_HOME`, `REVENIUM_STATE_DIR`, `TZ='UTC'`. Three sub-cases assert: (a) `os.path.exists(old_marker)` after dry-run; (b) `not os.path.exists(old_marker) and not os.path.exists(orphan_path) and os.path.exists(fresh_marker)` after live run; (c) `r.returncode == 0` after idempotent re-run."
      pattern: "test_prune_markers_dry_run_and_live"
    - from: "tests/test_repository.py::test_expected_files_exist"
      to: "skills/revenium/scripts/prune-markers.sh"
      via: "Append `SKILL / 'scripts' / 'prune-markers.sh'` to the `expected` list (one-line insertion after the existing `clear-halt.sh` entry at line ~73). Test then covers existence + `bash -n` syntax validation automatically via `test_shell_scripts_have_valid_syntax`."
      pattern: "prune-markers\\.sh"

threat_model:
  trust_boundaries:
    - "Marker files in `${MARKERS_DIR}` — writers: classifier.py + SKILL.md FINAL ACTION + future agents. Reader: prune-markers.sh (DELETES). The prune script never reads marker file CONTENTS; it only resolves the sid from the basename via `${path%.jsonl}` and reads the ledger by exact-prefix grep `^HERMES:${sid}:`. No JSON parsing of marker bodies. No injection surface from marker content."
    - "Ledger file `${LEDGER_FILE}` — single-writer (hermes-report.sh per CRON-06), multi-reader (prune-markers.sh is a new read-only consumer). The prune script never writes the ledger. Read-only access via `grep`/`tail`/`cut`. Idempotency invariant preserved (D-26 reads field 4 timestamp, never reorders or rewrites lines)."
    - "Operator-invoked CLI surface — prune-markers.sh is operator-only (D-28); no cron entry installed. Lock file `${PRUNE_LOCK_FILE}` prevents two concurrent operator invocations from racing. The lock file path is distinct from `cron.lock` so an operator prune cannot block the metering cron."
  stride_register:
    - id: "T-05-01"
      category: "Tampering (filesystem)"
      component: "prune-markers.sh `rm` operation"
      disposition: "mitigate"
      mitigation: "Hard-fail discipline (`set -euo pipefail`) ensures the script aborts loudly on any single `rm` failure rather than continuing with a partially-pruned state. The `--dry-run` flag gives operators a safe preview before live deletion (D-29). `flock(LOCK_EX)` on `${PRUNE_LOCK_FILE}` serializes operator invocations against themselves so two concurrent prunes cannot race on the same file set. Per-deletion `info` log lines write to `${LOG_FILE}` so every removal is auditable. Lock is acquired BEFORE the directory scan — there is no TOCTOU window where another invocation could remove a file this run already classified as kept."
    - id: "T-05-02"
      category: "Denial of Service (lock contention)"
      component: "${PRUNE_LOCK_FILE} flock acquisition"
      disposition: "mitigate"
      mitigation: "Non-blocking flock (`fcntl.LOCK_EX | fcntl.LOCK_NB`) with `warn + exit 0` on EAGAIN — same shape cron.sh uses. Operator running prune-markers.sh twice in rapid succession (or while a previous invocation hangs) does not block; the second run reports `prior prune still active, skipping` and exits cleanly. The lock is scoped to its own file (`prune.lock`, NOT shared with `cron.lock`) so a stuck prune cannot block the metering cron — the operator can `rm ${PRUNE_LOCK_FILE}` to recover if a kill -9 leaves a stale lock (advisory locks release on file-descriptor close so this is rare)."
    - id: "T-05-03"
      category: "Information Disclosure (operator audit trail)"
      component: "per-deletion `info` log line content"
      disposition: "accept"
      mitigation: "Log lines contain `sid`, marker basename (= `<sid>.jsonl`), `last_ledger_ts`, and `age_days`. No marker file body content is logged — the script never opens the marker JSONL for reading. Sids are operator-owned data (already visible in `${LOG_FILE}` from hermes-report.sh per CRON-09 and in `${LEDGER_FILE}` per CRON-05); the prune script discloses nothing new. Privacy boundary per MARK-05 (markers never contain free-form user prompts) means there is no PII risk even if a future change accidentally added marker-body logging."
---

# Plan 05-01: prune-markers.sh + common.sh retention knob + E2E prune test

<files_to_read>
@.planning/phases/05-housekeeping-compat-hardening/05-CONTEXT.md
@.planning/phases/05-housekeeping-compat-hardening/05-PATTERNS.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@skills/revenium/scripts/common.sh
@skills/revenium/scripts/clear-halt.sh
@skills/revenium/scripts/cron.sh
@skills/revenium/scripts/hermes-report.sh
@tests/test_repository.py
</files_to_read>

<objective>
**Goal (verbatim slice from ROADMAP.md Phase 5):** "Marker files do not grow unbounded on long-running hosts" + "Running `bash ~/.hermes/skills/revenium/scripts/prune-markers.sh` (added in this phase) against a fixture with marker files whose latest-reported ledger row is older than N days removes those files and leaves files for active sessions untouched; behavior is idempotent across repeated runs."

**Scope:** Plan 05-01 ships the prune-markers.sh operator script + its retention-knob declarations in common.sh + the E2E unit test that pins SC1 of Phase 5. Three locked decisions (D-26 / D-27 / D-28 / D-29) drive every line of code in this plan; the multi-source coverage audit shows all four covered exclusively here. The plan does NOT touch classifier.py (Plan 05-02), hermes-report.sh (Plan 05-03), or any doc file (Plan 05-04 — docs-last so the operator runbook in setup.md cites the actual shipped flag surface).

**Why now / why this shape:**
- **D-26** locks the staleness criteria to the ledger timestamp (field 4 of `HERMES:<sid>:<total_tokens>:<unix_ts>:<muid>`) so the prune script is authoritative against the existing idempotency contract. Markers whose tokens haven't been fully reported yet are never deleted. Orphan markers (no ledger row) fall back to `os.path.getmtime` — safe because if the file has existed past the retention window with no ledger record, it cannot become "reported" retroactively.
- **D-27** locks the default retention to 30 days, overridable via `$REVENIUM_MARKER_RETENTION_DAYS`. Declared in common.sh adjacent to MARKERS_DIR to preserve single-source-of-truth path discipline (`test_runtime_paths_are_hermes_native` continues to pass).
- **D-28** locks operator-invoked only — no crontab entry. The script ships as a manual runbook step in setup.md (added in Plan 05-04). Future iteration may add `install-prune-cron.sh` per CONTEXT.md `<deferred>` if operators report forgetting.
- **D-29** locks the safety surface: `--dry-run` flag for preview; `info` log helper for every deletion; `flock`-serialized lock file. Two concurrent operator invocations cannot race on the same marker set.

**Atomic-commit invariants:**
- T01 (common.sh + test_expected_files_exist): the common.sh additions land in the SAME commit as the `expected` list update — otherwise the test would briefly fail when prune-markers.sh is listed but does not yet exist, or pass when common.sh references PRUNE_LOCK_FILE that nothing consumes. Actually — the cleanest split is T01 = common.sh additions alone (those don't reference prune-markers.sh; test still passes), T02 = prune-markers.sh + test_expected_files_exist update + test_prune_markers_dry_run_and_live. T02 lands as ONE atomic commit because the prune script's existence AND the test_expected_files_exist update AND the new test_prune_markers test all reference one another.
- T03 (SUMMARY): docs-only, single commit.

**Out of scope (locked):** classifier.py (Plan 05-02), hermes-report.sh (Plan 05-03), setup.md / task-taxonomy.md / README.md / PROJECT.md / REQUIREMENTS.md / ROADMAP.md (Plan 05-04), any other script, any new state file outside `${STATE_DIR}`, _count_tools_in_current_turn (D-37 explicit KEEP).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
**Phase 5 carry-forward (from CONTEXT.md `<decisions>`):**
- D-26: stale criteria = ledger row older than N days; orphan fallback = mtime.
- D-27: default 30 days, env override `REVENIUM_MARKER_RETENTION_DAYS`.
- D-28: operator-invoked only — no `cron.sh` integration in v1.
- D-29: `--dry-run` flag + per-deletion `info` log + `flock` lock file.

**Path discipline (load-bearing — `<canonical_refs>`):**
- All new state paths declared in `common.sh`, never inline. The new lines are `MARKER_RETENTION_DAYS` (config default, not a path) and `PRUNE_LOCK_FILE` (a path). Both declared adjacent to the existing `LOCK_FILE` at common.sh:20.
- `test_runtime_paths_are_hermes_native` (test_repository.py:117-130) asserts `MARKERS_DIR` and `MARKERS_READY_DIR` regex shapes survive; neither line changes here.

**Interfaces and contracts the executor needs:**

`skills/revenium/scripts/common.sh` (lines 1-43, single-source path registry):
- Existing block at lines 17-20:
  ```bash
  TAXONOMY_FILE="${REVENIUM_TAXONOMY_FILE:-${STATE_DIR}/task-taxonomy.json}"
  MARKERS_DIR="${REVENIUM_MARKERS_DIR:-${STATE_DIR}/markers}"
  MARKERS_READY_DIR="${REVENIUM_MARKERS_READY_DIR:-${STATE_DIR}/markers/.ready}"
  LOCK_FILE="${STATE_DIR}/cron.lock"
  ```
- Phase 5 additions (insert after `LOCK_FILE`, before the blank line and `mkdir -p` block at line 22):
  ```bash
  MARKER_RETENTION_DAYS="${REVENIUM_MARKER_RETENTION_DAYS:-30}"
  PRUNE_LOCK_FILE="${STATE_DIR}/prune.lock"
  ```
- `mkdir -p` block at line 22 is byte-unchanged. `prune.lock` is created on demand by `flock` via fd 9 (operator scripts typically have low frequency; creating a stale lock dir entry on every common.sh source is unnecessary).

`skills/revenium/scripts/clear-halt.sh` (the role-match analog for prune-markers.sh skeleton):
- Lines 1-11 are the canonical operator-script header pattern: shebang + `set -euo pipefail` + `SCRIPT_DIR` via `BASH_SOURCE[0]` + `# shellcheck source=/dev/null` + `source "${SCRIPT_DIR}/common.sh"`. Prune-markers.sh follows the SAME shape. NOTE: clear-halt.sh does NOT call `ensure_path` — but prune-markers.sh DOES because it runs `python3` for the flock heredoc and the ledger-timestamp math. Pattern source: `cron.sh:8`.

`skills/revenium/scripts/cron.sh` (the analog for the flock acquisition pattern):
- Lines 19-30 are the canonical `flock(LOCK_FILE)` pattern:
  ```bash
  exec 9>"${LOCK_FILE}"
  if ! python3 - <<'PY'
  import fcntl, sys
  try:
      fcntl.flock(9, fcntl.LOCK_EX | fcntl.LOCK_NB)
  except (OSError, BlockingIOError):
      sys.exit(11)
  PY
  then
    warn "prior tick still active, skipping this minute"
    exit 0
  fi
  ```
- For prune-markers.sh: substitute `${LOCK_FILE}` → `${PRUNE_LOCK_FILE}` and the warn message → `prior prune still active, skipping`. All other details (the `python3 - <<'PY'` heredoc shape, the `if ! python3 ... ; then ... fi` form that neutralizes `set -e` for the contention branch, the bare `exit 0` on EAGAIN) MUST be copied verbatim. Per CLAUDE.md `## Module Design`, every new script must source common.sh and add itself to the `expected` list — this plan does both.

`skills/revenium/scripts/hermes-report.sh` ledger-grep convention (conceptual analog — D-26 staleness check):
- The ledger row format is `HERMES:<sid>:<total_tokens>:<unix_ts>:<muid>` per CRON-05 / D-12. To read the latest ledger timestamp for a sid:
  ```bash
  last_ts=$(grep "^HERMES:${sid}:" "${LEDGER_FILE}" 2>/dev/null | tail -1 | cut -d: -f4 || true)
  ```
- The 4th colon-segment is the `<unix_ts>` (float, e.g. `1715515099.123`). The `|| true` tolerates a missing ledger file (fresh install with markers but no ledger yet — every marker is then an orphan and falls to mtime).

`tests/test_repository.py` scaffolding to reuse (line ranges in the current file):
- `test_expected_files_exist` (line 56-85): a flat list of expected paths; tests `path.exists()`. New entry slot is between line 73 (`clear-halt.sh`) and line 75 (`split_strategies.py`) — alphabetical ordering is not enforced but adjacent to `clear-halt.sh` is the natural place.
- `test_shell_scripts_have_valid_syntax`: globs `SKILL / 'scripts' / '*.sh'` and runs `bash -n` per file. Prune-markers.sh is picked up automatically — no test code change needed.
- `test_runtime_paths_are_hermes_native` (line 117-130): regex assertions on `MARKERS_DIR` and `MARKERS_READY_DIR` lines. Both regex anchors (`\$\{REVENIUM_MARKERS_DIR:-\$\{STATE_DIR\}/markers\}` and `\$\{REVENIUM_MARKERS_READY_DIR:-\$\{STATE_DIR\}/markers/\.ready\}`) are unchanged in this plan — they appear at common.sh:18-19, before the new lines at 21-22.
- `_setup_plugin_env` / `_restore_plugin_env` (lines 21-52): NOT needed for the prune test — that test does not load the classifier module. Use a plain `tempfile.TemporaryDirectory()` + explicit env dict, the same shape `test_cron_marker_split_end_to_end` uses.

`PRUNE_LOCK_FILE` lock-scope choice (D-29 + Claude's Discretion in CONTEXT.md):
- Scoped to its OWN file (`prune.lock`) rather than sharing the cron's `cron.lock`. Rationale: an operator running prune-markers.sh manually should NOT block a cron tick. The two operations are read-write-different on disjoint file sets (prune writes-deletes marker files + reads ledger; cron writes-appends ledger + writes-appends marker readiness sentinels). Sharing the lock would serialize them needlessly.

**Reference implementation sketch (informative, executor finalizes shape):**

```bash
#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
# shellcheck source=/dev/null
source "${SCRIPT_DIR}/common.sh"

ensure_path

DRY_RUN=false
for arg in "$@"; do
  case "${arg}" in
    --dry-run) DRY_RUN=true ;;
    *) echo "Unknown flag: ${arg}" >&2; echo "Usage: prune-markers.sh [--dry-run]" >&2; exit 1 ;;
  esac
done

exec 9>"${PRUNE_LOCK_FILE}"
if ! python3 - <<'PY'
import fcntl, sys
try:
    fcntl.flock(9, fcntl.LOCK_EX | fcntl.LOCK_NB)
except (OSError, BlockingIOError):
    sys.exit(11)
PY
then
  warn "prior prune still active, skipping"
  exit 0
fi

DRY_RUN="${DRY_RUN}" python3 - <<'PY'
import json, os, sys, time, datetime, pathlib

markers_dir = pathlib.Path(os.environ.get("MARKERS_DIR") or os.environ["REVENIUM_MARKERS_DIR"])
ledger_file = pathlib.Path(os.environ.get("LEDGER_FILE") or os.environ["REVENIUM_STATE_DIR"]) # ...
retention_days = int(os.environ.get("MARKER_RETENTION_DAYS", "30"))
dry_run = os.environ.get("DRY_RUN") == "true"
# ... (per-file scan, ledger grep, mtime fallback, info log, summary)
PY
```
(Note: the executor will use the actual `${MARKERS_DIR}` / `${LEDGER_FILE}` / `${MARKER_RETENTION_DAYS}` bash variables — passed via env to the python3 heredoc — not the speculative env-var lookups in the sketch above. The sketch is informative; the canonical pattern source is `hermes-report.sh:512-538` which already shows env-passthrough into a python3 heredoc.)

The `info` / `warn` helpers from common.sh (line 40-42) write `[<utc-iso>] [INFO ] [revenium] <msg>` to `${LOG_FILE}` and stderr. Per-deletion log format (D-29 specifics, CONTEXT.md `<specifics>`):
```
info "prune: removed sid=${sid} marker=${basename} last_ledger_ts=${iso_ts} age_days=${age_days}"
info "prune: dry-run, would remove sid=${sid} marker=${basename} last_ledger_ts=${iso_ts} age_days=${age_days}"
info "prune: summary, scanned=${scanned} kept=${kept} removed=${removed}"
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Declare MARKER_RETENTION_DAYS and PRUNE_LOCK_FILE in common.sh</name>
  <files>skills/revenium/scripts/common.sh</files>
  <read_first>
    - skills/revenium/scripts/common.sh (full file, lines 1-43)
    - 05-PATTERNS.md `### skills/revenium/scripts/common.sh (config, path registry)` section (lines 144-165)
    - tests/test_repository.py::test_runtime_paths_are_hermes_native (lines 117-130) — confirm which regex assertions must continue to pass after the edit
    - CLAUDE.md `## Single Source of Truth: common.sh` and `## Naming Patterns` (env var prefix + screaming-snake conventions)
  </read_first>
  <action>
    Insert TWO new lines into `skills/revenium/scripts/common.sh` immediately after the existing `LOCK_FILE="${STATE_DIR}/cron.lock"` declaration (line 20) and BEFORE the blank line that precedes the `mkdir -p ...` block at line 22.

    Lines to add (exact text, preserving leading-whitespace style of the surrounding lines — no leading indent, single space around `=` is not present in this file so do NOT add it):

    ```bash
    MARKER_RETENTION_DAYS="${REVENIUM_MARKER_RETENTION_DAYS:-30}"
    PRUNE_LOCK_FILE="${STATE_DIR}/prune.lock"
    ```

    Hard rules (any of these violated → revert and re-do):
    1. Colon-dash (`:-`) fallback syntax, NOT bare-dash (`-`). Bare-dash would not fire when the env var is set-but-empty.
    2. The env var name is `REVENIUM_MARKER_RETENTION_DAYS` (singular `MARKER_RETENTION_DAYS`, NOT `MARKERS_RETENTION_DAYS` — matches the SKILL.md / 05-CONTEXT.md / 05-PATTERNS.md spelling).
    3. New path variable `PRUNE_LOCK_FILE` does NOT have a `REVENIUM_*` env override (intentional — operators do not need to override the lock path; D-29 keeps the surface minimal). The colon-dash override only applies to the retention-days knob.
    4. NO change to the existing `MARKERS_DIR`, `MARKERS_READY_DIR`, `LOCK_FILE`, `TAXONOMY_FILE` lines (lines 17-20 byte-unchanged).
    5. NO change to the `mkdir -p` block at line 22 — `prune.lock` is NOT preallocated; flock creates it on demand via fd 9.
    6. NO change to `ensure_path`, `log`, `info`, `warn`, `error` helpers (lines 24-42).
    7. `set -uo pipefail` flag at line 4 is byte-unchanged — common.sh is the soft-fail file per CLAUDE.md `### `set -uo pipefail` (soft-fail) preserved`.

    Self-check before commit:
    - `bash -n skills/revenium/scripts/common.sh` exits 0.
    - `grep -n "MARKER_RETENTION_DAYS\|PRUNE_LOCK_FILE" skills/revenium/scripts/common.sh` shows exactly two lines, both immediately following the `LOCK_FILE=` line.
    - `python3 -m unittest tests.test_repository.RepositoryTests.test_runtime_paths_are_hermes_native -v` passes (the file still contains `.hermes`, `state/revenium`, `task-taxonomy.json`, `TAXONOMY_FILE=`, the MARKERS_DIR regex, the MARKERS_READY_DIR regex, `markers/.ready`, `markers`, `LOCK_FILE=`).
    - `python3 -m unittest discover -s tests -p 'test_*.py' -v` reports OK with 41 tests (no count change yet).

    This task lands in ONE commit: `feat(05-01): declare MARKER_RETENTION_DAYS + PRUNE_LOCK_FILE in common.sh`. No other files touched.
  </action>
  <acceptance_criteria>
    - `bash -n skills/revenium/scripts/common.sh` exits 0.
    - `grep -c '^MARKER_RETENTION_DAYS="\$\{REVENIUM_MARKER_RETENTION_DAYS:-30\}"$' skills/revenium/scripts/common.sh` returns 1.
    - `grep -c '^PRUNE_LOCK_FILE="\$\{STATE_DIR\}/prune\.lock"$' skills/revenium/scripts/common.sh` returns 1.
    - `grep -c '^LOCK_FILE="\$\{STATE_DIR\}/cron\.lock"$' skills/revenium/scripts/common.sh` returns 1 (existing line preserved byte-unchanged).
    - `grep -c 'MARKERS_RETENTION_DAYS\|REVENIUM_MARKER_RETENTION_DAYS:-\$\{MARKER_RETENTION_DAYS' skills/revenium/scripts/common.sh` returns 0 (no typos, no double-substitution).
    - `python3 -m unittest tests.test_repository.RepositoryTests.test_runtime_paths_are_hermes_native -v` passes.
    - `python3 -m unittest discover -s tests -p 'test_*.py' -v` reports OK with 41 tests, unchanged count.
    - Pre-existing 41 tests are byte-unchanged.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/johndemic/Development/projects/revenium/hermes-revenium && bash -n skills/revenium/scripts/common.sh && grep -c '^MARKER_RETENTION_DAYS=' skills/revenium/scripts/common.sh && grep -c '^PRUNE_LOCK_FILE=' skills/revenium/scripts/common.sh && python3 -m unittest tests.test_repository.RepositoryTests.test_runtime_paths_are_hermes_native -v 2>&1 | tail -5 && python3 -m unittest discover -s tests -p 'test_*.py' 2>&1 | tail -3</automated>
  </verify>
  <done>
    `common.sh` declares both new variables adjacent to `LOCK_FILE` with the canonical colon-dash fallback for the env-overridable retention knob; path-discipline test continues to pass; full suite green at 41 tests.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ship prune-markers.sh + test_expected_files_exist update + new test_prune_markers_dry_run_and_live</name>
  <files>skills/revenium/scripts/prune-markers.sh, tests/test_repository.py</files>
  <read_first>
    - skills/revenium/scripts/common.sh (lines 1-43, especially the new MARKER_RETENTION_DAYS + PRUNE_LOCK_FILE additions from T01, plus the existing MARKERS_DIR/LEDGER_FILE/LOG_FILE and info/warn helpers)
    - skills/revenium/scripts/clear-halt.sh (lines 1-11 — operator-script skeleton: shebang, `set -euo pipefail`, SCRIPT_DIR via BASH_SOURCE[0], # shellcheck source=/dev/null, source common.sh)
    - skills/revenium/scripts/cron.sh (lines 8, 19-30 — `ensure_path` call AND the canonical flock pattern: `exec 9>"${LOCK_FILE}"` + `python3 - <<'PY' fcntl.flock(9, LOCK_EX | LOCK_NB) PY`)
    - skills/revenium/scripts/hermes-report.sh lines 512-538 (Python heredoc env-passthrough pattern: env vars set on the python3 invocation line, read inside the heredoc via `os.environ['VAR']`)
    - tests/test_repository.py lines 56-85 (test_expected_files_exist — the `expected` list)
    - tests/test_repository.py lines 355-500 (test_cron_marker_split_end_to_end — scaffold to mirror: tempdir, env dict, subprocess.run shape)
    - 05-PATTERNS.md `### skills/revenium/scripts/prune-markers.sh` (lines 30-138 — full script skeleton + flock gate + dry-run parsing + ledger-timestamp read + logging-per-deletion pattern)
    - 05-PATTERNS.md `#### New test: prune script` (lines 397-477 — full test method scaffold with three sub-cases)
  </read_first>
  <behavior>
    Behavior the new test pins (TDD — test written alongside production code; both land in one commit so the test would be RED without the prune-markers.sh script):

    1. **Dry-run preserves files:** `subprocess.run(['bash', PRUNE_SCRIPT, '--dry-run'], env=env, timeout=30)` against a 3-marker fixture exits 0 with all three marker files still present on disk.

    2. **Live run removes stale + orphan, keeps fresh:** `subprocess.run(['bash', PRUNE_SCRIPT], env=env, timeout=30)` against the same fixture exits 0; the 31-day-old marker (sid in ledger with `<unix_ts>` 31 days ago) is removed; the orphan marker (no ledger row, mtime 31 days ago) is removed; the fresh marker (sid in ledger with `<unix_ts>` today) is preserved.

    3. **Idempotent re-run:** Immediately re-running the live command after sub-case 2 exits 0 with no further filesystem changes (scanned=1 / kept=1 / removed=0). The script does not error out on the now-empty stale set.

    4. **Unknown flag rejected:** `subprocess.run(['bash', PRUNE_SCRIPT, '--bogus'], env=env, timeout=10)` exits non-zero with a usage-style message on stderr. (Lightweight extra sub-case; optional if it stretches the test method length.)
  </behavior>
  <action>
    PART A — Create `skills/revenium/scripts/prune-markers.sh` (new file).

    File layout (mirror clear-halt.sh and cron.sh patterns; see 05-PATTERNS.md lines 30-138):

    1. Shebang: `#!/usr/bin/env bash`
    2. `set -euo pipefail` (hard-fail; matches clear-halt.sh and cron.sh — partial deletion is worse than no deletion)
    3. `SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"`
    4. `# shellcheck source=/dev/null` directive on the line IMMEDIATELY above `source`
    5. `source "${SCRIPT_DIR}/common.sh"`
    6. `ensure_path` (required because the script invokes python3 for the flock heredoc and the per-file scan)
    7. Flag parsing loop — single `--dry-run` flag accepted; any other arg exits 1 with `Unknown flag: <arg>` and `Usage: prune-markers.sh [--dry-run]` on stderr.
    8. Flock acquisition: `exec 9>"${PRUNE_LOCK_FILE}"` followed by the EXACT `if ! python3 - <<'PY' ... PY then warn ... exit 0 fi` pattern from cron.sh:19-30, substituting:
       - `${LOCK_FILE}` → `${PRUNE_LOCK_FILE}`
       - `warn "prior tick still active, skipping this minute"` → `warn "prior prune still active, skipping"`
    9. Main work — pass `${MARKERS_DIR}`, `${LEDGER_FILE}`, `${MARKER_RETENTION_DAYS}`, and a `DRY_RUN` bash variable into a python3 heredoc via env (same env-passthrough shape as hermes-report.sh:512-538):

       ```bash
       MARKERS_DIR="${MARKERS_DIR}" \
       LEDGER_FILE="${LEDGER_FILE}" \
       MARKER_RETENTION_DAYS="${MARKER_RETENTION_DAYS}" \
       DRY_RUN="${DRY_RUN}" \
       LOG_FILE="${LOG_FILE}" \
       python3 - <<'PY'
       import datetime, json, os, pathlib, sys
       # ... see Python implementation below ...
       PY
       ```

    Python heredoc body (stdlib only — `os`, `pathlib`, `datetime`, `time`, `re`):

    - Read env: `MARKERS_DIR`, `LEDGER_FILE`, `MARKER_RETENTION_DAYS` (int), `DRY_RUN` (string "true"/"false"), `LOG_FILE`.
    - Compute `now = time.time()`, `cutoff = now - retention_days * 86400`.
    - Counters: `scanned = kept = removed = 0`.
    - For each `.jsonl` file in `MARKERS_DIR` (use `pathlib.Path(MARKERS_DIR).glob('*.jsonl')`):
      - `scanned += 1`
      - Derive `sid = path.stem` (basename without .jsonl).
      - Read latest ledger timestamp: open `LEDGER_FILE` if it exists, iterate lines that start with `f"HERMES:{sid}:"`, take the last one, split on `:` and take field index 3 (0-indexed; the 4th colon segment). Float-parse it. If file is missing OR no matching line → `last_ts = None` (orphan branch).
      - If `last_ts is not None`:
        - `age_seconds = now - last_ts`; `age_days = age_seconds / 86400`.
        - If `last_ts < cutoff`: mark for removal with `iso_ts = datetime.datetime.fromtimestamp(last_ts, datetime.timezone.utc).strftime('%Y-%m-%dT%H:%M:%SZ')`.
        - Else: keep.
      - If `last_ts is None` (orphan):
        - `mtime = path.stat().st_mtime`; `age_seconds = now - mtime`; `age_days = age_seconds / 86400`.
        - If `mtime < cutoff`: mark for removal with `iso_ts = 'mtime'` (sentinel string).
        - Else: keep (orphan but recent — leave it; future ledger row may yet land).
      - For files marked for removal: format `info`-shape log line (see below), and if `DRY_RUN != 'true'` call `path.unlink(missing_ok=True)`. Increment `removed`. (Dry-run path increments `removed` too — semantically "would-remove-count" in dry-run mode; the summary line's `removed=` represents "files acted upon".)
      - For kept files: increment `kept`.
    - Print to stdout (and append to `LOG_FILE`) using the `info` log format from common.sh:
      ```
      [<utc-iso>] [INFO ] [revenium] prune: removed sid=<sid> marker=<basename> last_ledger_ts=<iso> age_days=<n>
      ```
      Or in dry-run mode:
      ```
      [<utc-iso>] [INFO ] [revenium] prune: dry-run, would remove sid=<sid> marker=<basename> last_ledger_ts=<iso> age_days=<n>
      ```
      Final summary:
      ```
      [<utc-iso>] [INFO ] [revenium] prune: summary, scanned=<n> kept=<n> removed=<n>
      ```

      Implementation hint: Replicate the `log()` function from common.sh inside the Python heredoc (it's 4 lines — `datetime.datetime.now(datetime.timezone.utc).strftime(...)`, format with `[<utc-iso>] [<level>] [revenium] <msg>`, `print(line); open(LOG_FILE, 'a').write(line + '\n')`). The bash-shell `info` helper IS the canonical path but calling it from within a python3 heredoc per-file would re-spawn python3 for each call, which is silly. The Python-side log function MUST produce a byte-identical line shape to what `info` would emit — operators tailing `${LOG_FILE}` should not see a stylistic difference between cron-side and prune-side INFO lines.

      Pattern note: this Python-side log helper exists ONLY inside the prune-markers.sh heredoc — it is NOT a refactor of common.sh's bash-side helpers. CLAUDE.md `## Logging` rules apply: timestamped, UTC, ISO-8601, tee to LOG_FILE and stderr. The `[INFO ]` 5-char field width (4 + trailing space) MUST match common.sh:40 exactly so log-line greps work uniformly.

    - Sys.exit code: `0` on success (zero or more deletions). Non-zero only on hard failures (e.g., `MARKERS_DIR` doesn't exist — treat as success with `scanned=0` because the directory not existing means there are no markers to prune; log an `info` line `prune: markers dir not found, nothing to prune` and exit 0). Actual hard failures (e.g., permission denied on `unlink`) propagate via `set -e` from the surrounding shell — `set -euo pipefail` is intentionally aggressive here per D-29 safety framing.

    Final bash-side line after the python3 heredoc: nothing — the script exits implicitly with the python3 exit code (or earlier from the flock contention branch). Do NOT add `exit 0` at the bottom; `set -e` + last-command-success is the canonical termination.

    `chmod +x` the file before the commit lands (`git update-index --chmod=+x skills/revenium/scripts/prune-markers.sh` or simply `chmod +x` followed by `git add`).

    PART B — Edit `tests/test_repository.py::test_expected_files_exist`:

    Locate the `expected` list (line 56-83). Insert ONE new entry immediately after the existing `SKILL / 'scripts' / 'clear-halt.sh',` entry (line 73), preserving 4-space indentation and trailing comma:

    ```python
        SKILL / 'scripts' / 'prune-markers.sh',
    ```

    No other edit to this method. The 22 entries become 23; the assertion loop is byte-unchanged.

    PART C — Append new test method `test_prune_markers_dry_run_and_live` to `RepositoryTests` in `tests/test_repository.py`.

    Placement: anywhere clean in `RepositoryTests`. Suggested location — immediately after `test_cron_marker_split_end_to_end` ends (the canonical analog test method) so subprocess-scaffolded tests live together. Exact location is not load-bearing; the suite discovers methods regardless of order.

    Method shape (mirror 05-PATTERNS.md lines 402-477 verbatim in structure; adapt variable names and assertions to be clearly tied to the three sub-cases):

    1. Local imports inside the method body (matching the existing pattern of `test_cron_marker_split_end_to_end` and other subprocess tests): `import json, os, subprocess, tempfile, time`.
    2. `PRUNE_SCRIPT = SKILL / 'scripts' / 'prune-markers.sh'`.
    3. `with tempfile.TemporaryDirectory() as tmpdir:` wrapper for automatic cleanup.
    4. Build the tmp directory layout: `hermes_home = os.path.join(tmpdir, 'hh')`; `state_dir = os.path.join(hermes_home, 'state', 'revenium')`; `markers_dir = os.path.join(state_dir, 'markers')`; `os.makedirs(markers_dir, mode=0o700)`; `ledger_file = os.path.join(state_dir, 'revenium-hermes.ledger')`.
    5. Seed three marker files + corresponding ledger rows (mirror 05-PATTERNS.md fixture):
       - **Old** sid: `'old-session-31d'`, ledger row: `f'HERMES:{old_sid}:1000:{int(time.time()) - 31*86400}:aaa\n'`, marker file: one JSON line `{muid, ts, sid, task_type, operation_type}`.
       - **Fresh** sid: `'fresh-session-today'`, ledger row: `f'HERMES:{fresh_sid}:500:{int(time.time())}:bbb\n'`, marker file: same shape.
       - **Orphan** sid: `'orphan-no-ledger'`, NO ledger row, marker file present with `os.utime(path, (old_ts, old_ts))` to backdate mtime to 31 days ago.
    6. Build env dict — explicit overrides only, do NOT use `**os.environ` (the test should be hermetic; if a developer has `REVENIUM_MARKERS_DIR` set in their shell it MUST be overridden, not leaked):
       ```python
       env = {
           'HOME': tmpdir,  # neutralize any operator-shell pollution
           'HERMES_HOME': hermes_home,
           'REVENIUM_STATE_DIR': state_dir,
           'REVENIUM_MARKERS_DIR': markers_dir,
           'REVENIUM_MARKER_RETENTION_DAYS': '30',
           'PATH': os.environ.get('PATH', ''),  # need python3 + bash on PATH
           'TZ': 'UTC',
       }
       ```
    7. Sub-case 1 — dry-run preserves:
       ```python
       r = subprocess.run(['bash', str(PRUNE_SCRIPT), '--dry-run'], env=env, capture_output=True, text=True, timeout=30)
       self.assertEqual(r.returncode, 0, f'dry-run exit {r.returncode}: stderr={r.stderr}')
       self.assertTrue(os.path.exists(os.path.join(markers_dir, f'{old_sid}.jsonl')), 'dry-run must not delete old marker')
       self.assertTrue(os.path.exists(os.path.join(markers_dir, f'{orphan_sid}.jsonl')), 'dry-run must not delete orphan marker')
       self.assertTrue(os.path.exists(os.path.join(markers_dir, f'{fresh_sid}.jsonl')), 'dry-run must not delete fresh marker')
       ```
    8. Sub-case 2 — live run removes stale + orphan, keeps fresh:
       ```python
       r = subprocess.run(['bash', str(PRUNE_SCRIPT)], env=env, capture_output=True, text=True, timeout=30)
       self.assertEqual(r.returncode, 0, f'live run exit {r.returncode}: stderr={r.stderr}')
       self.assertFalse(os.path.exists(os.path.join(markers_dir, f'{old_sid}.jsonl')), 'old marker must be deleted')
       self.assertFalse(os.path.exists(os.path.join(markers_dir, f'{orphan_sid}.jsonl')), 'orphan marker must be deleted')
       self.assertTrue(os.path.exists(os.path.join(markers_dir, f'{fresh_sid}.jsonl')), 'fresh marker must be kept')
       ```
    9. Sub-case 3 — idempotent re-run:
       ```python
       r = subprocess.run(['bash', str(PRUNE_SCRIPT)], env=env, capture_output=True, text=True, timeout=30)
       self.assertEqual(r.returncode, 0, f'idempotent run exit {r.returncode}: stderr={r.stderr}')
       self.assertTrue(os.path.exists(os.path.join(markers_dir, f'{fresh_sid}.jsonl')), 'fresh marker still present')
       ```

    Optional Sub-case 4 — unknown flag rejected:
    ```python
    r = subprocess.run(['bash', str(PRUNE_SCRIPT), '--bogus'], env=env, capture_output=True, text=True, timeout=10)
    self.assertNotEqual(r.returncode, 0, 'unknown flag must exit non-zero')
    self.assertIn('Unknown flag', r.stderr)
    ```

    PART D — commit all three edits (prune-markers.sh creation + test_expected_files_exist update + test_prune_markers_dry_run_and_live new method) in ONE atomic commit. The test_expected_files_exist update REQUIRES the prune-markers.sh file to exist; landing them separately would mean an intermediate state where the test is red. The new test method REQUIRES the production script to exist AND `chmod +x` AND functional — landing them separately would also be red.

    Commit message: `feat(05-01): ship prune-markers.sh operator script + E2E test (D-26..D-29)`. Files in the commit: `skills/revenium/scripts/prune-markers.sh` (new, `chmod +x`), `tests/test_repository.py` (modified).

    Self-check before commit:
    - `bash -n skills/revenium/scripts/prune-markers.sh` exits 0.
    - `test -x skills/revenium/scripts/prune-markers.sh` (executable bit set; if `git diff --staged --summary` shows `chmod` row).
    - `python3 -m unittest tests.test_repository.RepositoryTests.test_expected_files_exist -v` passes (24 expected files now).
    - `python3 -m unittest tests.test_repository.RepositoryTests.test_prune_markers_dry_run_and_live -v` passes.
    - `python3 -m unittest tests.test_repository.RepositoryTests.test_shell_scripts_have_valid_syntax -v` passes (bash -n on the new file).
    - `python3 -m unittest tests.test_repository.RepositoryTests.test_no_legacy_branding_left -v` passes (no forbidden tokens in the new file).
    - `python3 -m unittest discover -s tests -p 'test_*.py' -v` reports OK with 42 tests total (41 + 1 new).
  </action>
  <acceptance_criteria>
    - `test -f skills/revenium/scripts/prune-markers.sh && test -x skills/revenium/scripts/prune-markers.sh` (file exists and is executable).
    - `bash -n skills/revenium/scripts/prune-markers.sh` exits 0.
    - `grep -c '^set -euo pipefail$' skills/revenium/scripts/prune-markers.sh` returns 1.
    - `grep -c 'source "\$\{SCRIPT_DIR\}/common\.sh"' skills/revenium/scripts/prune-markers.sh` returns 1.
    - `grep -c '^ensure_path$' skills/revenium/scripts/prune-markers.sh` returns 1.
    - `grep -c 'exec 9>"\$\{PRUNE_LOCK_FILE\}"' skills/revenium/scripts/prune-markers.sh` returns 1.
    - `grep -c 'prior prune still active, skipping' skills/revenium/scripts/prune-markers.sh` returns 1.
    - `grep -c '\-\-dry-run' skills/revenium/scripts/prune-markers.sh` returns >= 1 (flag accepted at least once in the parser).
    - `grep -c "prune: removed\|prune: dry-run\|prune: summary" skills/revenium/scripts/prune-markers.sh` returns >= 3 (all three log-line shapes present).
    - `grep -v '^#' skills/revenium/scripts/prune-markers.sh | grep -c "\\.openclaw\|OpenClaw\|ClawHub\|clawhub"` returns 0 (no legacy branding; `grep -v '^#'` excludes comments per planner-antipatterns.md hygiene).
    - `grep -c "prune-markers\.sh'" tests/test_repository.py` returns >= 1 (entry added to test_expected_files_exist).
    - `grep -c '^    def test_prune_markers_dry_run_and_live' tests/test_repository.py` returns 1.
    - `python3 -m unittest tests.test_repository.RepositoryTests.test_expected_files_exist -v` passes.
    - `python3 -m unittest tests.test_repository.RepositoryTests.test_prune_markers_dry_run_and_live -v` passes.
    - `python3 -m unittest tests.test_repository.RepositoryTests.test_shell_scripts_have_valid_syntax -v` passes.
    - `python3 -m unittest tests.test_repository.RepositoryTests.test_no_legacy_branding_left -v` passes.
    - `python3 -m unittest tests.test_repository.RepositoryTests.test_runtime_paths_are_hermes_native -v` passes (regression check against T01).
    - `python3 -m unittest discover -s tests -p 'test_*.py' -v` reports OK with 42 tests total.
    - All other 41 pre-existing tests are byte-unchanged.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/johndemic/Development/projects/revenium/hermes-revenium && bash -n skills/revenium/scripts/prune-markers.sh && test -x skills/revenium/scripts/prune-markers.sh && python3 -m unittest tests.test_repository.RepositoryTests.test_prune_markers_dry_run_and_live tests.test_repository.RepositoryTests.test_expected_files_exist tests.test_repository.RepositoryTests.test_shell_scripts_have_valid_syntax tests.test_repository.RepositoryTests.test_no_legacy_branding_left tests.test_repository.RepositoryTests.test_runtime_paths_are_hermes_native -v 2>&1 | tail -15 && python3 -m unittest discover -s tests -p 'test_*.py' 2>&1 | tail -3</automated>
  </verify>
  <done>
    `skills/revenium/scripts/prune-markers.sh` is the canonical operator prune script (D-26..D-29 honored); `test_prune_markers_dry_run_and_live` pins SC1 of Phase 5 with three sub-cases (dry-run preserves / live deletes stale+orphan / idempotent re-run); full suite green at 42 tests; legacy-branding + runtime-path + shell-syntax invariants pass; all changes land in ONE atomic commit.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write 05-01-SUMMARY.md</name>
  <files>.planning/phases/05-housekeeping-compat-hardening/05-01-SUMMARY.md</files>
  <read_first>
    - .planning/phases/04-wire-enrichment/04-01-SUMMARY.md (the canonical SUMMARY shape — frontmatter + 7 body sections including # Plan title, ## Outcome, ## Decisions Honored sub-headers, ## Key Files, ## Verification, ## Operator Verification, ## Out of Scope Carried Forward)
    - .planning/phases/05-housekeeping-compat-hardening/05-CONTEXT.md (D-26..D-29 + `<specifics>` for the operator UAT triple-case)
    - 05-PATTERNS.md (the implementation reference text — for citing in Decisions Honored)
    - the prune-markers.sh + tests/test_repository.py changes from T01 + T02 (so the SUMMARY accurately cites what shipped)
  </read_first>
  <action>
    Create `.planning/phases/05-housekeeping-compat-hardening/05-01-SUMMARY.md` with the canonical frontmatter + body shape used by `.planning/phases/04-wire-enrichment/04-01-SUMMARY.md`.

    Frontmatter (YAML, between `---` fences) — fields in this exact order:
    - `phase: 05-housekeeping-compat-hardening`
    - `plan: 01`
    - `type: execute`
    - `executed: 2026-05-14` (planner-supplied; executor may overwrite on completion)
    - `requirements_completed: [COMPAT-04, TEST-05]` (both are broad invariants Plan 05-01 contributes toward; final flip happens in Plan 05-04)
    - `files_modified:` — listed as a YAML block-list, members exactly: `skills/revenium/scripts/common.sh`, `tests/test_repository.py`
    - `files_created:` — listed as a YAML block-list, members exactly: `skills/revenium/scripts/prune-markers.sh`, `.planning/phases/05-housekeeping-compat-hardening/05-01-SUMMARY.md`
    - `tests_added: 1` (test_prune_markers_dry_run_and_live)
    - `tests_modified: 1` (test_expected_files_exist — one entry added)
    - `tests_total_after: 45` (end-of-phase count assuming Plans 05-02 + 05-03 also land; if 05-01 executes in isolation it alone produces 42 — 41 baseline + 1 new test)
    - `commits:` — block-list with 3 entries (executor fills in actual hashes):
      ```
        - T01 <hash> — feat(05-01): declare MARKER_RETENTION_DAYS + PRUNE_LOCK_FILE in common.sh
        - T02 <hash> — feat(05-01): ship prune-markers.sh operator script + E2E test (D-26..D-29)
        - T03 <hash> — docs(05-01): write 05-01-SUMMARY.md
      ```
    - `tags: [housekeeping, prune-markers, operator-script, common-sh-additions]`

    Body sections (in order):

    1. `# Plan 05-01 Summary — Marker File Pruning + common.sh Retention Knob` (h1)

    2. `## Outcome` — one paragraph stating that Plan 05-01 ships the operator-invoked `prune-markers.sh` script honoring D-26 (stale criteria = latest ledger row older than N days, mtime fallback for orphans), D-27 (default retention 30 days via `REVENIUM_MARKER_RETENTION_DAYS`), D-28 (operator-only — no cron integration), and D-29 (`--dry-run` flag + per-deletion `info` log + dedicated `${PRUNE_LOCK_FILE}` flock). The script + its E2E regression test land in one commit (T02) following common.sh additions in T01. SC1 of Phase 5 ROADMAP is pinned by `test_prune_markers_dry_run_and_live` covering dry-run preserve / live delete-stale+orphan / idempotent re-run sub-cases. Full suite: 41 → 42 tests.

    3. `## Decisions Honored` — four sub-headers, one per decision:
       - `### D-26 — Staleness = latest ledger row older than N days` — describe the `grep "^HERMES:${sid}:" "${LEDGER_FILE}" | tail -1 | cut -d: -f4` read pattern (Python-side inside the heredoc), the float-parse, the cutoff comparison, and the mtime fallback for orphans (no ledger row). Note that markers with active ledger rows newer than the cutoff are NEVER deleted — preserves the idempotency contract.
       - `### D-27 — Default retention 30 days; env override` — quote the new common.sh line `MARKER_RETENTION_DAYS="${REVENIUM_MARKER_RETENTION_DAYS:-30}"`. Note that overriding via the env var works for both cron-time and operator-time invocations (single-source-of-truth path discipline preserved; `test_runtime_paths_are_hermes_native` continues to pass).
       - `### D-28 — Operator-invoked only; no cron integration` — state that no crontab line is added, no `install-prune-cron.sh` is created. The script is shipped as a manual runbook entry (Plan 05-04 adds the operator runbook to `references/setup.md`). Per CONTEXT.md `<deferred>`, an opt-in installer may follow in v1.1.
       - `### D-29 — Safety: --dry-run + info-log + flock` — describe the `--dry-run` flag (`bash prune-markers.sh --dry-run` lists candidates without removing), the per-deletion `info` log shape (`prune: removed sid=<sid> marker=<basename> last_ledger_ts=<iso> age_days=<n>`), and the dedicated `${PRUNE_LOCK_FILE}` (= `${STATE_DIR}/prune.lock`) flock that prevents two concurrent operator invocations from racing. Lock scope choice: NOT shared with `${LOCK_FILE}` (= cron.lock) so operator prune cannot block the metering cron (Claude's Discretion in CONTEXT.md).

    4. `## Key Files` — bulleted list mirroring the `must_haves.artifacts` block in this plan's frontmatter (4 files; one-line summary per file).

    5. `## Verification` — three checkpoints:
       - `python3 -m unittest discover -s tests -p 'test_*.py' -v` → **42/42 OK** (41 pre-existing + 1 new: `test_prune_markers_dry_run_and_live`).
       - `bash -n skills/revenium/scripts/prune-markers.sh` → **exit 0**.
       - `test_runtime_paths_are_hermes_native` passes against the updated common.sh (assertion regex for `MARKERS_DIR` and `MARKERS_READY_DIR` is byte-unchanged; the new `MARKER_RETENTION_DAYS` + `PRUNE_LOCK_FILE` lines do not displace the existing path-discipline literals).

    6. `## Operator Verification` — verbatim from CONTEXT.md `<specifics>` operator UAT triple-case:
       > Seed `${MARKERS_DIR}` with one marker whose latest ledger row is 31 days old, one whose latest is today, plus one orphan (no ledger row, mtime 31 days ago). Run `bash ~/.hermes/skills/revenium/scripts/prune-markers.sh --dry-run`. Confirm the 31-day-old + orphan markers are in the dry-run candidate list and today's marker is not. Run without `--dry-run`. Confirm the same two markers are removed and today's is preserved. Run again immediately. Confirm exit 0 with `removed=0` (idempotent).
       Then a placeholder for operator's session id + completion timestamp: `Operator session id: _pending_` / `Verified at: _pending_`.

    7. `## Out of Scope Carried Forward` — bulleted reminder that Plan 05-01 deliberately did NOT touch:
       - `skills/revenium/plugins/revenium-classifier/classifier.py` (Plan 05-02 owns D-32 mint-back + D-33 recency sort)
       - `skills/revenium/scripts/hermes-report.sh` (Plan 05-03 owns WR-01 sanitization + WR-02 dead-var removal)
       - `tests/test_repository.py` Phase 4 wire test `base_env` extensions (Plan 05-03 owns WR-03)
       - `README.md`, `skills/revenium/references/setup.md`, `skills/revenium/references/task-taxonomy.md`, `.planning/PROJECT.md` (Plan 05-04 owns the docs pass, intentionally docs-last)
       - `.planning/REQUIREMENTS.md` checkbox flips and `.planning/ROADMAP.md` Plans-list updates (Plan 05-04 owns these)
       - `_count_tools_in_current_turn` and its 4 tests (D-37 explicit KEEP; reconsidered in v1.1 if zero callers remain)
       - Any new state files outside `${STATE_DIR}` (D-38 explicit defer)

    This task lands in ONE commit: `docs(05-01): write 05-01-SUMMARY.md`. The SUMMARY does not affect any test, so it ships in its own atomic commit per the canonical SUMMARY-last pattern (matches 04-01's T05 docs-only commit).

    Sanity checks before commit:
    - `test_no_legacy_branding_left` is scoped to exclude `.planning/` per test_repository.py:110-111 (the `if rel.parts and rel.parts[0] == '.planning': continue` line). So SUMMARY content is exempt from the regex scan — but for hygiene the SUMMARY MUST still not contain any forbidden token (`OpenClaw`, `openclaw`, `ClawHub`, `clawhub`).
    - The frontmatter is parseable YAML — use proper indentation for block-lists (`  - ` for list items under `files_modified:` etc.).
    - All 4 decisions (D-26 / D-27 / D-28 / D-29) get sub-headers; no decision is implicitly conflated with another.
  </action>
  <acceptance_criteria>
    - `test -f .planning/phases/05-housekeeping-compat-hardening/05-01-SUMMARY.md` succeeds.
    - `grep -c '^phase: 05-housekeeping-compat-hardening$' .planning/phases/05-housekeeping-compat-hardening/05-01-SUMMARY.md` returns 1.
    - `grep -c '^plan: 01$' .planning/phases/05-housekeeping-compat-hardening/05-01-SUMMARY.md` returns 1.
    - `grep -c '^## Outcome$\|^## Decisions Honored$\|^### D-26\|^### D-27\|^### D-28\|^### D-29\|^## Key Files$\|^## Verification$\|^## Operator Verification$\|^## Out of Scope Carried Forward$' .planning/phases/05-housekeeping-compat-hardening/05-01-SUMMARY.md` returns 10 (all 4 sub-headers + 6 top-level sections present).
    - `grep -c 'requirements_completed: \[COMPAT-04, TEST-05\]' .planning/phases/05-housekeeping-compat-hardening/05-01-SUMMARY.md` returns 1.
    - `grep -c 'tests_total_after: 45' .planning/phases/05-housekeeping-compat-hardening/05-01-SUMMARY.md` returns 1.
    - `grep -v '^#' .planning/phases/05-housekeeping-compat-hardening/05-01-SUMMARY.md | grep -c 'OpenClaw\|openclaw\|ClawHub\|clawhub'` returns 0 (no legacy branding even though .planning/ is exempt).
    - `python3 -m unittest discover -s tests -p 'test_*.py' -v` reports OK with 42 tests.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/johndemic/Development/projects/revenium/hermes-revenium && test -f .planning/phases/05-housekeeping-compat-hardening/05-01-SUMMARY.md && grep -c '^### D-26\|^### D-27\|^### D-28\|^### D-29\|^## Outcome$\|^## Decisions Honored$\|^## Key Files$\|^## Verification$\|^## Operator Verification$\|^## Out of Scope Carried Forward$' .planning/phases/05-housekeeping-compat-hardening/05-01-SUMMARY.md && grep -c 'requirements_completed: \[COMPAT-04, TEST-05\]' .planning/phases/05-housekeeping-compat-hardening/05-01-SUMMARY.md && python3 -m unittest discover -s tests -p 'test_*.py' 2>&1 | tail -3</automated>
  </verify>
  <done>
    `05-01-SUMMARY.md` exists with full frontmatter + 7 body sections including all 4 decisions (D-26 / D-27 / D-28 / D-29) as sub-headers, operator UAT triple-case verbatim from CONTEXT.md, out-of-scope reminder citing the other three plans; legacy-branding clean; suite still green at 42 tests.
  </done>
</task>

</tasks>

<verification>
**Per-task automated verification** is specified in each task's `<verify>` block. Plan-level verification:

1. **Full test suite green after every commit:** `python3 -m unittest discover -s tests -p 'test_*.py' -v` reports OK after T01 (41 tests), T02 (42 tests — 1 new), T03 (42 tests). The per-commit green-test mandate is non-negotiable (CLAUDE.md `## Common commands`).

2. **Test count delta:** Pre-Plan-05-01 = 41 tests. Post-Plan-05-01 = 42 tests (+1: `test_prune_markers_dry_run_and_live`). The test_expected_files_exist method gains one entry but counts as the same test method.

3. **Bash syntax check:** `bash -n skills/revenium/scripts/prune-markers.sh` exits 0 (covered by `test_shell_scripts_have_valid_syntax`). `bash -n skills/revenium/scripts/common.sh` also still exits 0 after T01.

4. **Legacy-branding guard:** `test_no_legacy_branding_left` passes against every file edited or created in this plan. The new prune-markers.sh contains no forbidden token; the new SUMMARY is in `.planning/` (exempt from the test scope) but the planner audited for hygiene anyway.

5. **Path-discipline guard:** `test_runtime_paths_are_hermes_native` passes after T01 — the existing assertions on `.hermes`, `state/revenium`, `task-taxonomy.json`, `TAXONOMY_FILE=`, the MARKERS_DIR regex, the MARKERS_READY_DIR regex, `markers/.ready`, `markers`, and `LOCK_FILE=` all continue to match.

6. **Frontmatter guard:** `test_skill_frontmatter_has_hermes_metadata` is unaffected (no SKILL.md change in this plan).

7. **Three atomic commits expected:**
   - T01: `feat(05-01): declare MARKER_RETENTION_DAYS + PRUNE_LOCK_FILE in common.sh` — common.sh only.
   - T02: `feat(05-01): ship prune-markers.sh operator script + E2E test (D-26..D-29)` — prune-markers.sh (new, +x) + tests/test_repository.py (modified: +1 entry in test_expected_files_exist, +1 new test method). These three pieces are interdependent and MUST land together — otherwise the test_expected_files_exist update is red against a missing script, or the new test method is red against an absent script.
   - T03: `docs(05-01): write 05-01-SUMMARY.md` — SUMMARY only.

8. **Operator UAT (manual; documented in SUMMARY):** Mac Studio operator triple-case from CONTEXT.md `<specifics>`. NOT a blocking gate for plan completion — recorded for post-execute-phase verification.
</verification>

<success_criteria>
Plan 05-01 is complete when ALL of:

- [ ] T01: `skills/revenium/scripts/common.sh` declares `MARKER_RETENTION_DAYS="${REVENIUM_MARKER_RETENTION_DAYS:-30}"` and `PRUNE_LOCK_FILE="${STATE_DIR}/prune.lock"` immediately after the existing `LOCK_FILE` line; `bash -n` exits 0; `test_runtime_paths_are_hermes_native` passes; full suite green at 41 tests.
- [ ] T02: `skills/revenium/scripts/prune-markers.sh` is the canonical operator prune script — set -euo pipefail, sources common.sh, ensure_path, --dry-run flag, ${PRUNE_LOCK_FILE} flock, info-log per deletion, summary line at end. `test_expected_files_exist` updated with one new entry. `test_prune_markers_dry_run_and_live` appended to RepositoryTests with three sub-cases (dry-run preserve / live delete-stale+orphan / idempotent re-run). All three pieces in ONE atomic commit. `chmod +x` on the new script. Full suite green at 42 tests.
- [ ] T03: `05-01-SUMMARY.md` exists with full frontmatter + 7 body sections including ## Outcome, ## Decisions Honored (4 sub-headers for D-26 / D-27 / D-28 / D-29), ## Key Files, ## Verification, ## Operator Verification, ## Out of Scope Carried Forward. SUMMARY-only commit.
- [ ] Full test suite green after EVERY commit (41 / 42 / 42 across T01 / T02 / T03).
- [ ] No commits skip hooks (no `--no-verify`); legacy-branding + shell-syntax + path-discipline + frontmatter invariants pass throughout.
- [ ] Plan 05-01 ships within ~50% context budget across the 3 atomic commits.
- [ ] Plan 05-01 does NOT touch classifier.py / hermes-report.sh / README / setup.md / task-taxonomy.md / PROJECT.md / REQUIREMENTS.md / ROADMAP.md / SKILL.md / split_strategies.py / clear-halt.sh / cron.sh / install-cron.sh / uninstall-cron.sh / budget-check.sh / setup-local.sh / any HOOK-* fixture (strict file-ownership boundary against parallel plans 05-02 and 05-03 in Wave 1; against Plan 05-04 in Wave 2).

Then the plan is ready for `/gsd-verify-work 5` (after the other 3 plans land) followed by operator UAT triple-case on Mac Studio.
</success_criteria>

<output>
After completion, the executor creates `.planning/phases/05-housekeeping-compat-hardening/05-01-SUMMARY.md` (as part of Task 3) capturing:
- Requirements pinned (COMPAT-04, TEST-05 — broad invariants; final flip in Plan 05-04)
- Decisions honored (D-26 / D-27 / D-28 / D-29)
- Files modified (`common.sh`, `tests/test_repository.py`) + files created (`prune-markers.sh`, `05-01-SUMMARY.md`)
- Tests added: 1 (`test_prune_markers_dry_run_and_live`); tests modified: 1 (`test_expected_files_exist` got +1 entry)
- Verification evidence (42/42 OK; bash -n exit 0; runtime-paths test passes)
- Operator UAT triple-case (Mac Studio fixture seed + dry-run + live + idempotent re-run)
- Out-of-scope reminder citing the parallel plans 05-02 / 05-03 and the docs-last Plan 05-04
</output>
