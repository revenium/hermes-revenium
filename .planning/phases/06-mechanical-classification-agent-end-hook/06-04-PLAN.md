---
phase: 06-mechanical-classification-agent-end-hook
plan: 04
type: execute
wave: 4
gap_closure: true
status: ready
depends_on:
  - 06-03-PLAN.md
files_modified:
  - skills/revenium/scripts/common.sh
  - skills/revenium/scripts/hermes-report.sh
  - skills/revenium/plugins/revenium-classifier/__init__.py
  - skills/revenium/plugins/revenium-classifier/classifier.py
  - tests/test_repository.py
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/06-mechanical-classification-agent-end-hook/06-VERIFICATION.md
files_created:
  - .planning/phases/06-mechanical-classification-agent-end-hook/06-04-SUMMARY.md
autonomous: true
requirements:
  - HOOK-01
  - HOOK-02
  - HOOK-03
  - HOOK-04
  - HOOK-05
  - HOOK-06
  - HOOK-07
  - HOOK-08
  - HOOK-09
  - HOOK-10
  - HOOK-11
  - HOOK-12
  - HOOK-13
tags:
  - gap-closure
  - sentinel
  - cron-settle
  - race-fix
  - g-03

must_haves:
  truths:
    - "HOOK-13 (NEW, locked by D-21): `skills/revenium/scripts/common.sh` declares `MARKERS_READY_DIR=\"${REVENIUM_MARKERS_READY_DIR:-${STATE_DIR}/markers/.ready}\"` immediately AFTER the existing `MARKERS_DIR` declaration (currently at line 18), and adds `\"${MARKERS_READY_DIR}\"` to the existing `mkdir -p` line at line 21 so the directory is created on first source. The env-override-with-fallback shape matches the established pattern used by `MARKERS_DIR` and `TAXONOMY_FILE`; no other script ever inlines this path."
    - "Plugin sentinel write — substantive + skip + inheritance + halt paths (D-21 primary): in `skills/revenium/plugins/revenium-classifier/__init__.py::_on_session_end`, AFTER `run_classification(...)` returns inside the existing `try` block, the callback computes `MARKERS_READY_DIR / session_id` and calls `Path(...).touch(exist_ok=True)` — writing the empty sentinel unconditionally on every outcome `run_classification` produces (marker pair written, trivial-skip taken, parent inheritance adopted, halt-unclassified written). Sentinel write is the LAST observable side effect of the happy path."
    - "Plugin sentinel write — error path (D-04 belt extension to sentinel): the sentinel write ALSO fires inside the existing outer `except Exception` handler in `_on_session_end`, AFTER the `logger.warning(...)` call. If `run_classification` raises, the operator's session must STILL be reportable by the next cron tick — otherwise a classifier crash freezes the session forever. The sentinel write itself is wrapped in its own `try/except Exception` (logger.warning on failure, then swallow) so a permission error on the sentinel directory cannot escape `_on_session_end`. D-04 invariant extended: `_on_session_end` STILL never raises."
    - "`classifier.py` imports/exports `MARKERS_READY_DIR` — single-line addition alongside the existing `MARKERS_DIR` constant at line 39 (mirror the env-override-with-fallback pattern). Plugin `__init__.py` imports `MARKERS_READY_DIR` from `.classifier` (NOT from anywhere else) so the path-discipline pattern is preserved end-to-end. No other helper, no other constant, no logic in `classifier.py` changes — this is a one-line addition by hand-count."
    - "Cron sentinel-or-aged filter (D-21 primary path + safety net): in `skills/revenium/scripts/hermes-report.sh`, AFTER the existing sqlite `sessions=$(sqlite3 ...)` SELECT at line 45-53 (which produces the pipe-delimited candidate list including `started_at`), insert a Python heredoc that re-emits the filtered candidate list. For each candidate row the heredoc keeps the row IFF `(MARKERS_READY_DIR / sid).exists()` OR `(now - int(started_at)) >= SETTLE_SECONDS`. `SETTLE_SECONDS = int(os.environ.get('REVENIUM_CRON_SETTLE_SECONDS', '120'))`. Filtered (skipped) sessions emit exactly one `info` log line via the existing `info` helper: `skipping <sid> — awaiting plugin sentinel (age=Ns < settle=120s)`. Output format is byte-identical to the existing `sqlite3` output (same pipe delimiters, same column order, same row order) so the downstream `while IFS='|' read -r ...` loop at line 63 is UNCHANGED."
    - "Cron filter — stdlib-only heredoc (CLAUDE.md tech-stack constraint): the new Python heredoc imports ONLY from the stdlib (`os`, `sys`, `pathlib.Path`). No `sqlite3` import is needed in the heredoc (the SELECT already ran in bash); the heredoc consumes the bash variable `${sessions}` and the env vars `REVENIUM_CRON_SETTLE_SECONDS` + `MARKERS_READY_DIR` (the latter is exported by sourcing `common.sh` at the top of the script — already done at line 9). Soft-fail discipline: the heredoc tolerates a malformed row by passing it through unchanged (`continue` after `try: ... except Exception: print(row); continue`) so cron behavior never regresses if a future schema column shifts."
    - "Cron filter — no behavior change for sessions WITH a sentinel: the filter is additive on the in-bash side. Every existing test that synthesizes a state.db row AND writes a sentinel must continue to ship the metering call exactly as before — same delta math, same provider inference, same per-marker emission, same ledger writes. The G-03 fix MUST NOT touch any of the cron's downstream logic (split_strategies, ledger format, transaction-id shape, fail-open per-session tolerance, cron.lock flock). Diff is bounded to lines ~54-62 (the new filter heredoc inserted between the existing SELECT and the existing while-loop)."
    - "Test — sentinel write on happy path (positive): a new unit test invokes `_on_session_end(session_id='sid-happy', completed=True, interrupted=False, model='qwen3.6-plus', platform='cli')` with `classifier.run_classification` mocked (or with a minimal env so the real `run_classification` runs through the heuristic-skip path); assert `(MARKERS_READY_DIR / 'sid-happy').exists()` AND `(MARKERS_READY_DIR / 'sid-happy').stat().st_size == 0` after the call returns. Pins D-21 primary-path invariant."
    - "Test — sentinel write on error path (D-04 belt): a new unit test patches `classifier.run_classification` to raise `RuntimeError('synthetic')`; assert (1) `_on_session_end(...)` returns cleanly (no exception escapes — D-04 invariant), AND (2) `(MARKERS_READY_DIR / 'sid-error').exists()` AFTER the call returns. Pins the D-04 belt extension: classifier crash MUST NOT freeze a session in the cron's race window."
    - "Test — cron filter skips recent-no-sentinel (negative): a new unit test seeds a synthetic state.db with one session row whose `started_at` is `int(time.time()) - 30` (30 seconds ago, well under the 120s settle window) AND no sentinel at `${MARKERS_READY_DIR}/<sid>`; invokes `hermes-report.sh` against tmp `HERMES_HOME` + `REVENIUM_STATE_DIR`; asserts the resulting log line contains `skipping <sid> — awaiting plugin sentinel` AND the session was NEVER shipped to the mocked `revenium meter completion`."
    - "Test — cron filter includes aged-no-sentinel (safety net): a new unit test seeds a synthetic state.db with one session row whose `started_at` is `int(time.time()) - 200` (200 seconds ago, well over the 120s settle window) AND no sentinel; invokes `hermes-report.sh`; asserts the session WAS shipped to `revenium meter completion` (D-18 default applies — `--task-type unclassified` because no marker file exists). Pins the safety-net path: plugin-failure cases eventually report."
    - "Test — cron filter includes any-age-with-sentinel (sentinel primary): a new unit test seeds a synthetic state.db with one session row whose `started_at` is `int(time.time()) - 5` (5 seconds ago) AND a sentinel file at `${MARKERS_READY_DIR}/<sid>` (zero-byte, touch-style); invokes `hermes-report.sh`; asserts the session WAS shipped to `revenium meter completion` despite being well under the settle window. Pins the sentinel-driven primary path."
    - "End-to-end CI regression guard for G-03: a new behavioral test seeds (a) a synthetic state.db with a session row (id='cli-sid', tool_call_count=2, started_at=now-5), (b) a marker file at `${MARKERS_DIR}/cli-sid.jsonl` with a GUARDRAIL+CHAT pair carrying `task_type='generation'`, (c) a sentinel at `${MARKERS_READY_DIR}/cli-sid`; invokes `hermes-report.sh` against tmp `HERMES_HOME` with a stubbed `revenium` shim that records its argv to a log file; asserts the recorded `revenium meter completion` invocations carry `--task-type generation` (NOT `--task-type unclassified`). This is the regression guard that would have caught G-03 on 2026-05-14 before operator UAT had to surface it."
    - "HOOK-13 added to `.planning/REQUIREMENTS.md` under `### Mechanical Classification Hook (Phase 6)` as the 13th entry; new traceability row maps HOOK-13 → Phase 6 → Pending; Coverage block increments v1 requirements 49 → 50 and Mapped-to-phases 49 → 50; Categories `HOOK (12)` → `HOOK (13)`."
    - "ROADMAP Phase 6 Requirements line appends `, HOOK-13`; SC5 wording is updated to: \"the next cron tick after on_session_end ships `--task-type <meaningful-label>` to Revenium — verified by an end-to-end fixture that creates a session, fires on_session_end (writing both the marker AND the sentinel), then invokes `hermes-report.sh` and asserts the resulting `revenium meter completion` call carries the marker's `task_type`, NOT `unclassified`.\" Plans list appends `06-04-PLAN.md`. Progress Table row moves to `3/4 In progress — G-03 gap closure planned in 06-04-PLAN.md`. Categories line updates `HOOK (12)` → `HOOK (13)`. Coverage counts increment 49 → 50."
    - "`.planning/phases/06-mechanical-classification-agent-end-hook/06-VERIFICATION.md` G-03 entry transitions `gap_closure: pending` → `gap_closure: executed`; `gap_closure_plan: TBD (06-04-PLAN.md)` → `gap_closure_plan: 06-04-PLAN.md`; a new `gap_closure_outcome:` field is appended documenting the sentinel + cron-filter code-side closure. Frontmatter `status: requires_rerun_uat` is PRESERVED VERBATIM — G-03 is closed by code but the gap remains in `re_verification.gaps_remaining: [G-03]` until UAT round 4 records G-03 as `pass` on Mac Studio."
    - "`06-04-SUMMARY.md` contains verbatim the string: `Phase 6 cannot be marked verified until 06-HUMAN-UAT.md G-01, G-02, AND G-03 result move from \"failed\" to \"pass\"`. The manual-UAT gate language references ALL THREE gaps for traceability — G-01 and G-02 are code-closed and were operator-confirmed in UAT round 3, but their `gaps_remaining` listing trails until UAT round 4 retires G-03 alongside, at which point the verifier collapses all three closures into a single 3/4 → 4/4 Phase 6 Progress Table transition."
    - "D-04 belt around the sentinel write itself: the sentinel `Path(...).touch(exist_ok=True)` call in `_on_session_end` is wrapped in a local `try/except Exception` that logs `logger.warning(...)` and swallows. A permission error on the sentinel directory, a filesystem-full error, or any other unexpected OSError MUST NEVER raise out of `_on_session_end`. The sentinel is best-effort signalling; the safety-net (started_at age > SETTLE_SECONDS) covers any case where it fails to land. D-04 invariant END-TO-END preserved: `_on_session_end` returns None on every code path."
    - "Per-commit green-test mandate: full test suite (`python3 -m unittest discover -s tests -p 'test_*.py' -v`) passes end-to-end after EVERY commit in this plan. No regression in any Phase 1-3 test, no regression in any pre-existing HOOK-* test (12 from 06-02 + 4 from 06-03 = 16), plus the new HOOK-13 unit tests (2 sentinel-write + 3 cron-filter + 1 end-to-end = 6) pass. `test_no_legacy_branding_left` passes against every file edited in this plan."
    - "Out-of-scope discipline (locked by CONTEXT.md addendum #3 'Out of scope (deliberately)'): NO changes to `classifier.py` LOGIC (only the one-line `MARKERS_READY_DIR` constant addition); NO changes to heuristic-skip threshold or shape; NO changes to response-text signal handling; NO addressing of CR-01/CR-02/CR-03 from 06-02-REVIEW.md; NO migration of the existing 12 HOOK-* tests or 4 HOOK-12 tests (they preserve verbatim); NO changes to `setup-local.sh`, `references/setup.md`, SKILL.md, or HOOK-01..HOOK-12 entry strings."
  artifacts:
    - path: "skills/revenium/scripts/common.sh"
      provides: "New module-level constant `MARKERS_READY_DIR=\"${REVENIUM_MARKERS_READY_DIR:-${STATE_DIR}/markers/.ready}\"` inserted immediately after the existing `MARKERS_DIR` declaration; `\"${MARKERS_READY_DIR}\"` added to the existing `mkdir -p` line so the directory is created on first source. Path-discipline single-source-of-truth invariant preserved end-to-end."
      contains: "MARKERS_READY_DIR"
    - path: "skills/revenium/plugins/revenium-classifier/classifier.py"
      provides: "One-line addition: `MARKERS_READY_DIR = Path(os.environ.get(\"REVENIUM_MARKERS_READY_DIR\", str(MARKERS_DIR / \".ready\")))` placed IMMEDIATELY AFTER the existing `MARKERS_DIR` constant at line 39. No other change. Exports the constant for import by `__init__.py`."
      contains: "MARKERS_READY_DIR"
    - path: "skills/revenium/plugins/revenium-classifier/__init__.py"
      provides: "Sentinel write after `run_classification` returns in the happy-path try block AND after `logger.warning` in the outer except handler. Sentinel write itself is wrapped in a nested `try/except Exception` (logger.warning on failure, swallow) so D-04 invariant (`_on_session_end` never raises) is preserved. Imports `MARKERS_READY_DIR` from `.classifier` alongside the existing `run_classification` import."
      contains: "MARKERS_READY_DIR"
    - path: "skills/revenium/scripts/hermes-report.sh"
      provides: "Python heredoc filter inserted between the existing sqlite SELECT (line ~45-53) and the existing `while IFS='|' read` loop (line ~63). Filter consumes `${sessions}` from bash, keeps rows IFF `(MARKERS_READY_DIR / sid).exists() OR (now - started_at >= SETTLE_SECONDS)`, emits one `info` log line per skipped session, re-emits the kept rows in the SAME pipe-delimited format the downstream loop expects. Stdlib-only heredoc (os, sys, pathlib). SETTLE_SECONDS sourced from `REVENIUM_CRON_SETTLE_SECONDS` env with default 120."
      contains: "REVENIUM_CRON_SETTLE_SECONDS"
    - path: "tests/test_repository.py"
      provides: "5 new HOOK-13 unit tests (2 sentinel-write — happy + error-path; 3 cron-filter — skip-recent-no-sentinel, include-aged-no-sentinel, include-any-age-with-sentinel) + 1 new HOOK-13 end-to-end test (cron + sentinel produces non-unclassified shipping). One extension to `test_runtime_paths_are_hermes_native`: assert `MARKERS_READY_DIR=` substring is present in common.sh. Existing 16 HOOK-* test methods preserved verbatim."
      contains: "test_revenium_classifier_sentinel_written_on_happy_path"
    - path: ".planning/REQUIREMENTS.md"
      provides: "New HOOK-13 entry + traceability row + Coverage counts updated 49 → 50 + Categories HOOK (12) → HOOK (13)."
      contains: "HOOK-13"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 6 Requirements line appends HOOK-13; SC5 wording adds the cron-sentinel-synchronization invariant; Categories breakdown HOOK (12) → HOOK (13); Plans list appends 06-04-PLAN.md; Progress Table row updates."
      contains: "06-04-PLAN.md"
    - path: ".planning/phases/06-mechanical-classification-agent-end-hook/06-VERIFICATION.md"
      provides: "G-03 entry transitions gap_closure from pending to executed; gap_closure_plan flips from TBD to 06-04-PLAN.md; a new gap_closure_outcome field describes the sentinel + cron-filter code-side closure. Frontmatter status: requires_rerun_uat PRESERVED VERBATIM. re_verification.gaps_remaining: [G-03] PRESERVED VERBATIM."
      contains: "06-04-PLAN.md"
    - path: ".planning/phases/06-mechanical-classification-agent-end-hook/06-04-SUMMARY.md"
      provides: "Summary documenting the sentinel + cron-filter fix, the 6 new tests, and the manual UAT-4 gate language referencing G-01, G-02, AND G-03."
      contains: "G-01, G-02, AND G-03"
  key_links:
    - from: "skills/revenium/plugins/revenium-classifier/__init__.py::_on_session_end (happy path)"
      to: "${MARKERS_READY_DIR}/<sid>"
      via: "Path(MARKERS_READY_DIR / session_id).touch(exist_ok=True) after run_classification returns; nested try/except Exception swallows + logger.warnings on failure so D-04 invariant is preserved"
      pattern: "MARKERS_READY_DIR.*touch\\(exist_ok=True\\)"
    - from: "skills/revenium/plugins/revenium-classifier/__init__.py::_on_session_end (error path)"
      to: "${MARKERS_READY_DIR}/<sid>"
      via: "Same Path(...).touch(exist_ok=True) call placed AFTER the existing logger.warning in the outer except Exception handler; D-04 belt extension — classifier crash never freezes a session in the cron's race window"
      pattern: "except Exception.*MARKERS_READY_DIR.*touch"
    - from: "skills/revenium/scripts/hermes-report.sh (session SELECT filter)"
      to: "${MARKERS_READY_DIR}/<sid>"
      via: "Python heredoc reads MARKERS_READY_DIR env (exported by sourcing common.sh), checks os.path.exists() per candidate sid, falls through to age-vs-SETTLE_SECONDS check if absent; emits filtered pipe-delimited rows back to the downstream while-loop"
      pattern: "REVENIUM_CRON_SETTLE_SECONDS.*MARKERS_READY_DIR"
    - from: "skills/revenium/scripts/common.sh"
      to: "${STATE_DIR}/markers/.ready"
      via: "MARKERS_READY_DIR=${REVENIUM_MARKERS_READY_DIR:-${STATE_DIR}/markers/.ready} declaration + addition to the mkdir -p line; single source of truth, env-overridable for tests, default created on first source"
      pattern: "MARKERS_READY_DIR=\\$\\{REVENIUM_MARKERS_READY_DIR:-\\$\\{STATE_DIR\\}/markers/\\.ready\\}"
    - from: "tests/test_repository.py::test_revenium_classifier_cron_filter_end_to_end (G-03 regression guard)"
      to: "skills/revenium/scripts/hermes-report.sh"
      via: "Seed tmp HERMES_HOME with state.db row + marker file + sentinel; stub revenium CLI to record argv; invoke hermes-report.sh; assert recorded argv contains --task-type <marker-label> not --task-type unclassified"
      pattern: "task_type=generation|--task-type generation"

threat_model:
  trust_boundaries:
    - "plugin __init__.py::_on_session_end → ${MARKERS_READY_DIR}/<sid> (in-process write of a zero-byte sentinel file; same trust surface as the existing marker-pair write to MARKERS_DIR/<sid>.jsonl — owner-only directory inherited from STATE_DIR's 0o700 mode)"
    - "hermes-report.sh (cron-owned bash) → ${MARKERS_READY_DIR}/<sid> (in-process read via Python heredoc; same trust surface as the existing ledger read at LEDGER_FILE — owner-only directory)"
    - "REVENIUM_CRON_SETTLE_SECONDS env (cron config — operator-controlled via crontab entry or ${ENV_FILE}; tampering with the value only changes the cron's settle window, not the integrity of any data flow)"
  stride_register:
    - id: "T-06-16"
      category: "Tampering"
      component: "${MARKERS_READY_DIR}/<sid> sentinel file"
      disposition: "accept"
      mitigation: "Sentinel is a zero-byte file in an owner-only directory (mode 0o700 inherited from STATE_DIR — established by the existing `mkdir -p` call in common.sh and inherited by the new `mkdir -p \"${MARKERS_READY_DIR}\"` addition). The sentinel carries no payload — its mere existence is the signal. A local attacker who can write to MARKERS_READY_DIR can already write to MARKERS_DIR (same parent ownership), so the threat surface is unchanged from the existing marker file. The cron-side filter treats sentinel presence as 'plugin signalled ready'; a tampered (pre-created) sentinel would only cause the cron to ship `--task-type unclassified` ONE tick earlier — bounded by Revenium's per-transaction-id dedup at the wire level."
    - id: "T-06-17"
      category: "DoS"
      component: "${REVENIUM_CRON_SETTLE_SECONDS} env-driven settle window"
      disposition: "accept"
      mitigation: "Operator can tune the settle window via the cron env file (${ENV_FILE} — sourced by cron.sh at line 10-15). Setting SETTLE_SECONDS to a very large value (e.g., 86400) would delay reporting for any session without a sentinel by up to one day; the plugin's primary-path sentinel write covers the normal case so the delay only affects plugin-failure cases. Setting SETTLE_SECONDS to 0 collapses the filter to the legacy behavior (no settle) — useful for emergency rollback. The operator already owns the cron env; this env is not a security boundary."
    - id: "T-06-18"
      category: "Information Disclosure"
      component: "sentinel file existence on disk"
      disposition: "accept"
      mitigation: "Sentinel files are zero bytes — they carry no information except `existence`. A directory listing of MARKERS_READY_DIR reveals the same set of session IDs that MARKERS_DIR already exposes (a marker file is written alongside the sentinel for every substantive turn). No new information surface. Owner-only directory mode (0o700) is the same defense that protects MARKERS_DIR today."
---

# Plan 06-04: Gap-closure — Sentinel-driven cron settle filter (closes G-03)

<files_to_read>
@.planning/phases/06-mechanical-classification-agent-end-hook/06-CONTEXT.md
@.planning/phases/06-mechanical-classification-agent-end-hook/06-RESEARCH.md
@.planning/phases/06-mechanical-classification-agent-end-hook/06-PATTERNS.md
@.planning/phases/06-mechanical-classification-agent-end-hook/06-VERIFICATION.md
@.planning/phases/06-mechanical-classification-agent-end-hook/06-HUMAN-UAT.md
@.planning/phases/06-mechanical-classification-agent-end-hook/06-01-PLAN.md
@.planning/phases/06-mechanical-classification-agent-end-hook/06-02-PLAN.md
@.planning/phases/06-mechanical-classification-agent-end-hook/06-03-PLAN.md
@.planning/phases/06-mechanical-classification-agent-end-hook/06-03-SUMMARY.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@skills/revenium/scripts/common.sh
@skills/revenium/scripts/hermes-report.sh
@skills/revenium/plugins/revenium-classifier/__init__.py
@skills/revenium/plugins/revenium-classifier/classifier.py
@tests/test_repository.py
</files_to_read>

<objective>
**Gap (G-03, from 06-VERIFICATION.md / 06-HUMAN-UAT.md):** UAT round 3 on Mac Studio (172.16.1.175 against `gsd/phase-6-uat @ e49611b`, plans 06-01 + 06-02 + 06-03 deployed) confirmed G-01 + G-02 closure: marker file for CLI sid `20260514_031132_a7aa8e` shows `task_type: "generation"` (non-unclassified). But the cron pipeline shipped `task_type=unclassified` to Revenium **6.47 seconds BEFORE** the marker was written. The classifier's `on_session_end` callback does an LLM call to assign `task_type`. For `qwen3.6-plus` this takes ~5-10 seconds. The cron ticker runs every minute against `~/.hermes/state.db`. Sessions whose final turn straddles a minute boundary race the classifier — if the cron ticks AFTER state.db has the token delta but BEFORE `on_session_end` has written the marker, the cron takes the D-18 default (`--task-type unclassified`). The cron's ledger is idempotent — once a session is reported, future ticks skip it forever even if a marker arrives 6 seconds later.

Concrete UAT round 3 timeline:
- 07:11:32.51 — CLI session row created in state.db
- 07:12:02.08 — cron tick reads state.db, no marker exists yet, ships `task_type=unclassified`, writes ledger
- 07:12:08.55 — `on_session_end` fires, classifier writes marker with `task_type=generation` (6.47s late)
- Result: Revenium received `unclassified`; marker exists on disk but unreachable by the ledger-idempotent cron.

`state.db.ended_at` is NULL for CLI sessions (`hermes_cli/oneshot.py` doesn't populate it), so the cron can't filter on `ended_at IS NOT NULL` to wait for session completion. The cron's existing SELECT at `hermes-report.sh:45-53` has no settle-window or freshness filter.

**Locked decision (D-21, in 06-CONTEXT.md gap-closure addendum #3):** Plugin writes a per-session sentinel at `~/.hermes/state/revenium/markers/.ready/<sid>` after every `on_session_end` invocation completes (substantive, trivial-skip, inheritance, halt, AND error paths). Cron's session SELECT filters: report only if `(sentinel exists for sid) OR (started_at < now - SETTLE_SECONDS)`. SETTLE_SECONDS = 120 default, overridable via `REVENIUM_CRON_SETTLE_SECONDS` env. The sentinel is the deterministic primary path; the aged-safety-net handles plugin-failure cases.

Why sentinel (not settle-only, not state.db writes, not re-shipping):
- **(a) settle-delay only**: fails for long-running sessions (>N min) — they're still in flight when cron runs.
- **(b) sentinel — chosen.** Deterministic. Plugin signals readiness explicitly.
- **(c) plugin writes state.db.ended_at**: violates the "no writes to state.db" invariant (locked in CLAUDE.md).
- **(d) re-shipping with `--cost 0` rows**: doubles Revenium row count, complicates analytics math.

**This plan is the FOURTH plan in Phase 6 (06-04-PLAN.md) and runs in the gap-closure execution wave** (`--gaps-only` invocation). It supersedes ONLY:
- The path declarations in `common.sh` (one new constant + one mkdir extension)
- The plugin entrypoint in `__init__.py` (sentinel write after `run_classification` returns, in both happy + error paths)
- The cron session SELECT filter in `hermes-report.sh` (new Python heredoc inserted between SELECT and while-loop)
- `classifier.py` (one-line constant addition — no logic change)

Every other prior commit's invariant is preserved.

**Adds HOOK-13** to REQUIREMENTS.md and ROADMAP.md: "Cron pipeline MUST synchronize with the plugin via a per-session sentinel file at `${MARKERS_READY_DIR}/<sid>`. Sessions WITHOUT a sentinel AND younger than `REVENIUM_CRON_SETTLE_SECONDS` (default 120s) MUST be skipped this tick to give the plugin time to finish classification. Sessions older than the settle window MAY be reported even without a sentinel (D-18 default) to handle plugin-failure cases."

**Out of scope (deliberately, per CONTEXT.md gap-closure addendum #3):**
- Anything in `classifier.py` beyond the one-line `MARKERS_READY_DIR` constant addition (no logic changes to ANY helper).
- Heuristic-skip / response-text / parent-inheritance / budget-halt logic.
- CR-01/CR-02/CR-03 from 06-02-REVIEW.md.
- `setup-local.sh` changes (the new directory is created by `common.sh` sourcing, same pattern as `MARKERS_DIR`).
- HOOK-01..HOOK-12 entry strings (carried unchanged).
- SKILL.md, the existing 12 HOOK-* tests, the 4 HOOK-12 tests (preserved verbatim).
- The cron pipeline's other logic — splits, ledger writes, transaction-id shape, cron.lock flock, fail-open per-session tolerance. ONLY the session-selection SELECT filter changes.
- VERIFICATION.md `status:` field — STAYS at `requires_rerun_uat` because G-03 closes by code in this plan but UAT round 4 retires it operationally.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<commit_strategy>
This plan ships in 8 atomic commits — one per task. Each commit MUST leave `python3 -m unittest discover -s tests -p 'test_*.py' -v` green. Ordering is load-bearing:

1. **T01** lands the path constant (`common.sh` + `classifier.py` import + test_runtime_paths_are_hermes_native assertion) FIRST so all downstream tasks have a stable constant to reference. No behavior change on the cron or plugin yet — only the path declaration. Tests still pass because `MARKERS_READY_DIR` is unused.
2. **T02** lands the plugin sentinel write (`__init__.py` — happy path + error path + D-04 belt around the sentinel write itself). After T02 lands, sentinel writes start happening but the cron has not yet started reading them — fully backward-compatible.
3. **T03** lands the cron filter (`hermes-report.sh` heredoc between SELECT and while-loop). After T03 lands, the cron starts honoring sentinels AND the aged-safety-net. Without T02, T03 would silently freeze every session for 120s until aged-safety-net kicks in — so T02 MUST precede T03.
4. **T04** lands the planning artifacts (REQUIREMENTS HOOK-13 + ROADMAP SC5/Plans/Categories/Progress Table updates) BEFORE the new tests so the trace is intact at every commit.
5. **T05** lands the 5 unit tests (2 sentinel-write + 3 cron-filter). Tests assert the behavior T01+T02+T03 already shipped.
6. **T06** lands the end-to-end behavioral test (cron + sentinel → non-unclassified shipping). CI regression guard for G-03.
7. **T07** updates `06-VERIFICATION.md` to flip G-03 to `gap_closure: executed` with the new outcome paragraph; PRESERVES `status: requires_rerun_uat` and `gaps_remaining: [G-03]`.
8. **T08** lands `06-04-SUMMARY.md` with the verbatim UAT-4 gate language referencing G-01, G-02, AND G-03.

Conventional-commit prefixes: `feat(06):` for production code (T01 path constant, T02 plugin sentinel, T03 cron filter), `docs(06):` for planning artifacts + VERIFICATION + SUMMARY (T04, T07, T08), `test(06):` for tests (T05, T06).

Final commit T08 lands `06-04-SUMMARY.md`.
</commit_strategy>

<tasks>

<task id="06-04-T01" type="auto">
  <name>T01: Declare MARKERS_READY_DIR in common.sh + classifier.py + extend test_runtime_paths_are_hermes_native</name>
  <files>
    skills/revenium/scripts/common.sh
    skills/revenium/plugins/revenium-classifier/classifier.py
    tests/test_repository.py
  </files>
  <read_first>
    @skills/revenium/scripts/common.sh
    @skills/revenium/plugins/revenium-classifier/classifier.py
    @tests/test_repository.py
    @.planning/phases/06-mechanical-classification-agent-end-hook/06-CONTEXT.md
  </read_first>
  <action>
    Step 1 — `skills/revenium/scripts/common.sh`:

    Locate the existing `MARKERS_DIR` declaration at line 18: `MARKERS_DIR="${REVENIUM_MARKERS_DIR:-${STATE_DIR}/markers}"`. Insert IMMEDIATELY after it (between line 18 and line 19's `LOCK_FILE=` declaration) a new line:

      `MARKERS_READY_DIR="${REVENIUM_MARKERS_READY_DIR:-${STATE_DIR}/markers/.ready}"`

    This follows the established env-override-with-fallback shape used by `MARKERS_DIR`, `TAXONOMY_FILE`, `HERMES_HOME`, and `REVENIUM_STATE_DIR`. The env override `REVENIUM_MARKERS_READY_DIR` exists for test redirection (mirror `REVENIUM_MARKERS_DIR`'s role in the existing test fixtures).

    Then update the existing `mkdir -p` line at line 21 from:

      `mkdir -p "${STATE_DIR}" "${MARKERS_DIR}"`

    to:

      `mkdir -p "${STATE_DIR}" "${MARKERS_DIR}" "${MARKERS_READY_DIR}"`

    The `mkdir -p` is idempotent and inherits the parent directory mode — STATE_DIR is already created at 0o700-equivalent by the chain of setup helpers, and `.ready` inherits the same restrictive mode. No new `chmod` is required because POSIX inherits the umask, and the existing setup chain already produces an owner-only state directory tree.

    DO NOT touch any other line in common.sh. Preserve `set -uo pipefail`, the `HERMES_HOME` / `REVENIUM_STATE_DIR` defaults, the `STATE_DIR` / `CONFIG_FILE` / `BUDGET_STATUS_FILE` / `LEDGER_FILE` / `LOG_FILE` / `ENV_FILE` / `STATE_DB` / `TAXONOMY_FILE` / `MARKERS_DIR` / `LOCK_FILE` block, the `ensure_path` helper, and the `log` / `info` / `warn` / `error` helpers — byte-identical.

    Step 2 — `skills/revenium/plugins/revenium-classifier/classifier.py`:

    Locate the existing `MARKERS_DIR` constant at line 39: `MARKERS_DIR = Path(os.environ.get("REVENIUM_MARKERS_DIR", str(STATE_DIR / "markers")))`. Insert IMMEDIATELY after it (between line 39 and line 40's `TAXONOMY_FILE = ...` declaration) a new line:

      `MARKERS_READY_DIR = Path(os.environ.get("REVENIUM_MARKERS_READY_DIR", str(MARKERS_DIR / ".ready")))`

    Mirrors the env-override-with-fallback pattern used by every other Path constant in the module. The constant is exported at module level so `__init__.py` (T02) can import it via `from .classifier import run_classification, MARKERS_READY_DIR`.

    DO NOT touch anything else in `classifier.py`. No helper changes. No logic changes. No new imports — `os` and `pathlib.Path` are already imported. The diff for this file is +1/-0 by hand-count.

    Step 3 — `tests/test_repository.py`:

    Locate `test_runtime_paths_are_hermes_native` (currently at lines 117-129). Inside the method body, AFTER the existing `self.assertRegex(text, r'MARKERS_DIR="\$\{REVENIUM_MARKERS_DIR:-\$\{STATE_DIR\}/markers\}"')` assertion at line 124, INSERT one new assertion (mirror the existing regex shape):

      `self.assertRegex(text, r'MARKERS_READY_DIR="\$\{REVENIUM_MARKERS_READY_DIR:-\$\{STATE_DIR\}/markers/\.ready\}"')`

    Then INSERT one more positive substring assertion:

      `self.assertIn('markers/.ready', text)`

    The assertion shape mirrors the existing `MARKERS_DIR=` regex assertion and the surrounding `assertIn('markers', text)` assertion. DO NOT change any other assertion in the method. DO NOT change any other test. Append-only — no test modification.

    LEGACY BRANDING: the new lines must not contain forbidden tokens (none of `MARKERS_READY_DIR`, `markers/.ready`, or the regex assertion contains any forbidden product name).
  </action>
  <verify>
    <automated>cd /Users/johndemic/Development/projects/revenium/hermes-revenium &amp;&amp; grep -q 'MARKERS_READY_DIR="\${REVENIUM_MARKERS_READY_DIR:-\${STATE_DIR}/markers/\.ready}"' skills/revenium/scripts/common.sh &amp;&amp; grep -q 'mkdir -p "\${STATE_DIR}" "\${MARKERS_DIR}" "\${MARKERS_READY_DIR}"' skills/revenium/scripts/common.sh &amp;&amp; bash -n skills/revenium/scripts/common.sh &amp;&amp; grep -q 'MARKERS_READY_DIR = Path(os.environ.get("REVENIUM_MARKERS_READY_DIR"' skills/revenium/plugins/revenium-classifier/classifier.py &amp;&amp; python3 -c "import sys; sys.path.insert(0, 'skills/revenium/plugins/revenium-classifier'); import classifier; assert hasattr(classifier, 'MARKERS_READY_DIR'); from pathlib import Path; assert isinstance(classifier.MARKERS_READY_DIR, Path); print('classifier.MARKERS_READY_DIR =', classifier.MARKERS_READY_DIR)" &amp;&amp; grep -q "self.assertRegex(text, r'MARKERS_READY_DIR=" tests/test_repository.py &amp;&amp; python3 -m unittest tests.test_repository.RepositoryTests.test_runtime_paths_are_hermes_native -v 2>&amp;1 | tail -3 | grep -E '^OK( |$)' &amp;&amp; python3 -m unittest tests.test_repository.RepositoryTests.test_no_legacy_branding_left -v 2>&amp;1 | tail -3 | grep -E '^OK( |$)' &amp;&amp; python3 -m unittest discover -s tests -p 'test_*.py' -v 2>&amp;1 | tail -3 | grep -E '^OK( |$)'</automated>
  </verify>
  <acceptance_criteria>
    `common.sh` declares `MARKERS_READY_DIR` with env-override-with-fallback shape immediately after `MARKERS_DIR`; the `mkdir -p` line includes the new directory; `bash -n` passes. `classifier.py` exports `MARKERS_READY_DIR` as a `Path` constant immediately after `MARKERS_DIR` (no other change). `test_runtime_paths_are_hermes_native` asserts the new path substring is present in `common.sh` (regex assertion mirrors the existing `MARKERS_DIR=` assertion). Full test suite green. Legacy-branding test green.
  </acceptance_criteria>
  <requirements_satisfied>HOOK-13 (path-declaration half — the path constant is established before any consumer references it). Path-discipline single-source-of-truth invariant preserved.</requirements_satisfied>
  <done>`common.sh` + `classifier.py` declare `MARKERS_READY_DIR`; path-discipline test extended; full test suite still green.</done>
  <commit_message>feat(06): declare MARKERS_READY_DIR in common.sh + classifier.py; extend path-discipline test (HOOK-13 prep)</commit_message>
</task>

<task id="06-04-T02" type="auto">
  <name>T02: Plugin __init__.py writes sentinel after run_classification — happy path + error path + D-04 belt</name>
  <files>
    skills/revenium/plugins/revenium-classifier/__init__.py
  </files>
  <read_first>
    @skills/revenium/plugins/revenium-classifier/__init__.py
    @skills/revenium/plugins/revenium-classifier/classifier.py
    @.planning/phases/06-mechanical-classification-agent-end-hook/06-CONTEXT.md
  </read_first>
  <action>
    Edit `skills/revenium/plugins/revenium-classifier/__init__.py`. The file currently has:
    - Module docstring (lines 1-11)
    - `from __future__ import annotations` (line 12)
    - `import logging` (line 14)
    - `from .classifier import run_classification` (line 16)
    - `logger = logging.getLogger("revenium_classifier")` (line 18)
    - `def _on_session_end(...)` (lines 21-60)
    - `def register(ctx) -> None:` (lines 63-74)

    Step 1 — Add `Path` import. After `import logging` (line 14), insert:

      `from pathlib import Path`

    Step 2 — Extend the `.classifier` import. Change line 16 from `from .classifier import run_classification` to:

      `from .classifier import run_classification, MARKERS_READY_DIR`

    The `MARKERS_READY_DIR` constant was added to `classifier.py` in T01; this is its first consumer.

    Step 3 — Define a private helper `_write_sentinel(session_id)` between the `logger = ...` line (currently line 18) and `def _on_session_end(...)` (currently line 21). The helper centralizes the sentinel write so the happy-path call site and the error-path call site share one implementation, AND so the D-04 belt (the inner try/except that prevents the sentinel write itself from raising) lives in one place instead of being duplicated.

    The helper's behavior:
    - Build `sentinel_path = MARKERS_READY_DIR / session_id`.
    - Ensure the parent directory exists: `MARKERS_READY_DIR.mkdir(parents=True, exist_ok=True)` — defensive against test fixtures that didn't pre-create it.
    - Call `sentinel_path.touch(exist_ok=True)` — empty file, touch-style. `exist_ok=True` so a stale sentinel from a previous run does not cause an error.
    - Wrap the ENTIRE body in a `try/except Exception as exc:` that logs `logger.warning("revenium-classifier sentinel write failed for sid=%s: %s", session_id, exc)` and returns. The sentinel is best-effort signalling; the cron's aged-safety-net (settle_seconds) covers any case where the sentinel fails to land. D-04 invariant END-TO-END preserved: this helper NEVER raises.
    - Docstring (one paragraph): "D-21 sentinel write: emits an empty file at MARKERS_READY_DIR / session_id to signal to the cron pipeline that the plugin has completed processing for this session. Cron's session-SELECT filter at hermes-report.sh treats sentinel presence as 'plugin signalled ready' and reports the session this tick; sentinel absence defers reporting until the session's started_at ages past REVENIUM_CRON_SETTLE_SECONDS (default 120s). D-04 belt: any IOError / OSError / PermissionError on the sentinel write is logged and swallowed — the sentinel is best-effort, and the cron's aged-safety-net handles silent failures."

    Step 4 — Modify `_on_session_end` to call `_write_sentinel(session_id)` in two places:

    Place A — happy path: AFTER `run_classification(...)` returns (currently line 52) AND BEFORE the closing `except Exception as exc:` clause (currently line 53). Within the existing `try:` block:

      `_write_sentinel(session_id)`

    Place this call as the LAST statement inside the try-block. It must execute regardless of whether `run_classification` took the substantive-marker-write path, the trivial-skip path, the inheritance path, or the halt-unclassified path — all of those paths return normally from `run_classification` and reach this point.

    Place B — error path: AFTER the existing `logger.warning(...)` block in `except Exception as exc:` (currently lines 53-60). Add a single statement at the END of the except clause:

      `_write_sentinel(session_id)`

    This is the D-04 belt extension to the sentinel: classifier crash MUST NOT freeze a session in the cron's race window. The sentinel says "the plugin has finished with this session — cron may proceed" — and "finished" includes "finished with an exception". Without this line, a classifier crash would leave the session sentinel-less, and the cron would defer reporting until SETTLE_SECONDS elapses (aged-safety-net). With this line, the cron reports on the very next tick — same latency as the happy path.

    Critical placement detail: the `_write_sentinel` call in the except clause MUST come AFTER the existing `logger.warning(exc)` call so the warning log captures the original exception's context first. If `_write_sentinel` itself were to throw (it cannot — D-04 belt), the warning log already captured the root cause.

    Step 5 — Defensive `session_id` check inside the `_on_session_end` callback: the existing `if not session_id: return` guard at line 44 must NOT be bypassed. The sentinel write inside the `try` block is fine because it executes AFTER the guard. But the sentinel write inside the `except` block could in principle run when `session_id` is falsy (e.g., if `run_classification` was called with a falsy session_id — which the guard at line 44 prevents — OR if the early return at line 45 itself raises in some bizarre future world). Add a guard inside `_write_sentinel`: `if not session_id: return` as the first statement (before the try/except). The helper becomes a no-op for falsy session_id; the cron never sees a phantom sentinel.

    DO NOT change `register(ctx)` (currently lines 63-74). DO NOT change the docstrings of `_on_session_end` or `register`. DO NOT change the `**kwargs` signature of `_on_session_end`. The diff for this file is approximately +18/-1 lines by hand-count, contained entirely within the import block, the new `_write_sentinel` helper, and two single-line additions inside the existing `_on_session_end` body.

    LEGACY BRANDING: the new helper's docstring and inline comments must not contain forbidden tokens.
  </action>
  <verify>
    <automated>cd /Users/johndemic/Development/projects/revenium/hermes-revenium &amp;&amp; grep -q 'from pathlib import Path' skills/revenium/plugins/revenium-classifier/__init__.py &amp;&amp; grep -q 'from .classifier import run_classification, MARKERS_READY_DIR' skills/revenium/plugins/revenium-classifier/__init__.py &amp;&amp; grep -q 'def _write_sentinel' skills/revenium/plugins/revenium-classifier/__init__.py &amp;&amp; grep -c '_write_sentinel(session_id)' skills/revenium/plugins/revenium-classifier/__init__.py | grep -E '^2$' &amp;&amp; python3 -c "import sys, os, tempfile, importlib; tmp = tempfile.mkdtemp(); os.environ['HERMES_HOME'] = tmp; os.environ['REVENIUM_STATE_DIR'] = tmp + '/state/revenium'; os.environ['REVENIUM_MARKERS_DIR'] = tmp + '/state/revenium/markers'; os.environ['REVENIUM_MARKERS_READY_DIR'] = tmp + '/state/revenium/markers/.ready'; sys.path.insert(0, 'skills/revenium/plugins/revenium-classifier'); import classifier; importlib.reload(classifier); import __init__ as plugin; importlib.reload(plugin); assert callable(plugin._write_sentinel); plugin._write_sentinel('test-sid'); from pathlib import Path; assert Path(tmp + '/state/revenium/markers/.ready/test-sid').exists(), 'sentinel not written'; print('sentinel write smoke test OK')" &amp;&amp; python3 -m unittest tests.test_repository.RepositoryTests.test_no_legacy_branding_left -v 2>&amp;1 | tail -3 | grep -E '^OK( |$)' &amp;&amp; python3 -m unittest discover -s tests -p 'test_*.py' -v 2>&amp;1 | tail -3 | grep -E '^OK( |$)'</automated>
  </verify>
  <acceptance_criteria>
    `__init__.py` imports `MARKERS_READY_DIR` from `.classifier`; new `_write_sentinel(session_id)` helper exists with D-04 belt (try/except Exception → logger.warning → swallow); `_on_session_end` calls `_write_sentinel(session_id)` in BOTH the happy-path try-block AND the error-path except-block (grep -c == 2). Smoke test (sentinel write to tmp dir) passes. Full test suite green. Legacy-branding test green.
  </acceptance_criteria>
  <requirements_satisfied>HOOK-13 (sentinel-write half — plugin signals readiness on every outcome). D-04 (extended to sentinel write: helper never raises).</requirements_satisfied>
  <done>Plugin `__init__.py` writes the sentinel after every `run_classification` outcome (happy + error) without raising; full test suite still green.</done>
  <commit_message>feat(06): plugin __init__.py writes sentinel at MARKERS_READY_DIR/sid after run_classification (HOOK-13)</commit_message>
</task>

<task id="06-04-T03" type="auto">
  <name>T03: hermes-report.sh — insert Python heredoc filter between sqlite SELECT and while-loop (sentinel-or-aged)</name>
  <files>
    skills/revenium/scripts/hermes-report.sh
  </files>
  <read_first>
    @skills/revenium/scripts/hermes-report.sh
    @skills/revenium/scripts/common.sh
    @.planning/phases/06-mechanical-classification-agent-end-hook/06-CONTEXT.md
  </read_first>
  <action>
    Edit `skills/revenium/scripts/hermes-report.sh`. The file currently has at lines 41-63:

    ```
    main() {
      info "=== Hermes Metering Reporter starting ==="

      local sessions
      sessions=$(sqlite3 "${STATE_DB}" "
        SELECT id, model, source, input_tokens, output_tokens,
               cache_read_tokens, cache_write_tokens, reasoning_tokens,
               estimated_cost_usd, api_call_count, started_at, ended_at,
               billing_provider
        FROM sessions
        WHERE (input_tokens > 0 OR output_tokens > 0)
        ORDER BY started_at DESC;
      " 2>/dev/null) || { warn "Failed to query state.db"; exit 0; }

      if [[ -z "${sessions}" ]]; then
        info "No sessions with token usage found."
        return
      fi

      local reported_count=0
      local skipped_count=0

      while IFS='|' read -r sid model source input_tokens output_tokens       cache_read cache_write reasoning_tokens estimated_cost       api_calls started_at ended_at billing_provider; do
    ```

    Insert the new filter BETWEEN the existing `if [[ -z "${sessions}" ]]; then info "No sessions with token usage found."; return; fi` block (currently lines 55-58) and the `local reported_count=0` declaration (currently line 60). Concretely: the heredoc reads `${sessions}` from bash via stdin, applies the sentinel-or-aged filter, and re-emits the kept rows back to bash via stdout — reassigned into the same `sessions` variable. The downstream `while IFS='|' read -r ...` loop at line 63 is UNCHANGED.

    Insertion shape (the Python heredoc is the only new code):

    1. After the `if [[ -z "${sessions}" ]]; then ... fi` block, add a comment line: `# G-03 sentinel-or-aged filter (D-21): drop sessions younger than SETTLE_SECONDS that have no plugin sentinel.`
    2. Then a single bash assignment that pipes `${sessions}` through the Python heredoc and reassigns:

       ```
       sessions=$(printf '%s\n' "${sessions}" | python3 - <<'PY' 2>/dev/null || echo "${sessions}"
       <heredoc body — see below>
       PY
       )
       ```

       The `2>/dev/null || echo "${sessions}"` fallback preserves soft-fail discipline (`set -uo pipefail` is in effect): if the heredoc crashes for any unexpected reason (Python missing, MARKERS_READY_DIR unset, etc.), the cron falls through to the legacy unfiltered behavior. This is consistent with the pre-existing fail-open pattern at line 53 (`|| { warn ...; exit 0; }`) but softer — a filter failure should warn-and-passthrough, not abort the cron tick.

    3. Python heredoc body (stdlib-only — `os`, `sys`, `time`, `pathlib.Path`):
       - Read `SETTLE_SECONDS = int(os.environ.get('REVENIUM_CRON_SETTLE_SECONDS', '120'))` with a try/except ValueError fallback to 120 (operator typo protection).
       - Read `MARKERS_READY_DIR = Path(os.environ.get('REVENIUM_MARKERS_READY_DIR', os.environ.get('STATE_DIR', '') + '/markers/.ready'))`. Note `STATE_DIR` is exported by common.sh via the bash environment but may not be exported by default — defensive fallback chain handles both cases.
       - `now = int(time.time())`.
       - Read stdin line by line (`for raw_line in sys.stdin:`). For each non-empty line:
         - Strip newline. Split on `|` (matches the bash IFS).
         - Field index 0 is `sid`; field index 10 is `started_at` (matching the column order in the SELECT: `id, model, source, input_tokens, output_tokens, cache_read_tokens, cache_write_tokens, reasoning_tokens, estimated_cost_usd, api_call_count, started_at, ended_at, billing_provider` — `started_at` is the 11th column = index 10).
         - Try `int(parts[10])`. On ValueError (malformed row), pass the row through unchanged with `print(raw_line.rstrip())` and `continue` — soft-fail discipline mirrors CLAUDE.md guidance.
         - Compute `age = now - started_at_int`. Compute `has_sentinel = (MARKERS_READY_DIR / sid).exists()`.
         - If `has_sentinel OR age >= SETTLE_SECONDS`: `print(raw_line.rstrip())` (keep the row).
         - Else: skip (do NOT print). Note the skipped sid for logging.
       - After the loop, emit ONE log line per skipped session via `sys.stderr` so the existing `info` helper-style log target captures it. Shape: `sys.stderr.write(f"[INFO] [revenium] skipping {sid} — awaiting plugin sentinel (age={age}s < settle={SETTLE_SECONDS}s)\n")` for each skipped sid. Use `sys.stderr.write` (not `print(..., file=sys.stderr)`) so the formatting precisely matches the existing `info` log helper's `[INFO ] [revenium]` prefix (note: the existing `log` helper writes via `tee -a ${LOG_FILE} >&2` so stderr is the captured stream). Actually — the existing `log` helper produces `[2026-05-14T07:11:32Z] [INFO ] [revenium] ...` with a timestamp. The heredoc cannot replicate that timestamp prefix without re-implementing the formatter. Simpler approach: skip-line emission is done in BASH (not Python), so the existing `info` helper formats the line correctly. Therefore: the Python heredoc only emits the FILTERED rows to stdout AND emits a single tab-separated `SKIPPED\t<sid>\t<age>` line per skipped session to stderr; the bash code AFTER the heredoc grep-reads stderr (captured via `2>` redirect) for `SKIPPED` lines and re-emits them through `info` for proper log formatting.

       Simpler, cleaner architecture: split the filter into TWO subshell pipes — first compute kept-rows + skipped-rows separately, then bash-side `info` log each skipped row.

       The cleanest concrete shape is:
       - Bash builds a comma-separated list of all candidate sids in bash (or pipes `${sessions}` once to extract the (sid, started_at) tuple set via `awk` or `cut`).
       - Python heredoc reads the (sid, started_at) tuples and emits ONLY a list of sids to DROP, one per line, on stdout.
       - Bash reads the drop-list, computes the filtered sessions by `grep -v` against the drop list, and emits one `info "skipping ..."` line per dropped sid.

       Hand the executor the freedom to choose between the two architectures (single-pipe with stderr-tagged skip lines parsed back into `info` calls, OR two-pipe with bash-side log emission). Both satisfy: (a) Python heredoc is stdlib-only, (b) skip log lines go through the existing `info` helper for consistent log formatting, (c) downstream `while IFS='|' read -r` loop is unchanged.

       Reference test for the heredoc: invoke `python3 -c "<heredoc body>" <<< "sid1|m|s|10|5|0|0|0|0|0|$(($(date +%s)-30))|0|none"` with `REVENIUM_CRON_SETTLE_SECONDS=120` env set and `${MARKERS_READY_DIR}/sid1` absent — must emit nothing on stdout (sid1 dropped, age=30s < 120s, no sentinel). Then re-invoke with `${MARKERS_READY_DIR}/sid1` touched — must emit the row on stdout (sentinel present overrides age check).

    4. ALL OTHER LINES in `hermes-report.sh` from line 60 onward (the `local reported_count=0`, the `while IFS='|' read ...` loop, the per-marker emission logic, the ledger writes, the `info "=== Done ..."` line at 575) are BYTE-IDENTICAL after this task. `git diff skills/revenium/scripts/hermes-report.sh` must show edits localized to a single contiguous block between the existing SELECT (line 53) and the existing `while` loop (line 63).

    5. PRESERVE `set -uo pipefail` at line 5. PRESERVE the `ensure_path` call at line 11. PRESERVE every preflight check at lines 13-32. PRESERVE the `touch "${LEDGER_FILE}"` at line 34. PRESERVE the `ORG_NAME` resolution at lines 36-39.

    6. The new heredoc must be stdlib-only. NO `sqlite3` import in the heredoc (the SELECT already ran in bash). NO `json` import. The heredoc body does not need network, subprocess, or any non-stdlib module.

    7. Soft-fail discipline: the heredoc must tolerate malformed rows (rows with fewer than 11 pipe-delimited fields, rows where started_at is not parseable as int) by passing them through unchanged and continuing — same pattern as the bash loop's `((skipped_count++)) || true` continue-on-error idiom.

    8. PRESERVE `set -uo pipefail` semantics across the new pipeline: the `printf | python3 || echo "${sessions}"` shape preserves the soft-fail invariant — if Python crashes, the original sessions list flows through unfiltered (matching the pre-existing soft-fail behavior on `sqlite3` failure at line 53).

    LEGACY BRANDING: the new heredoc, the new bash comments, and the new `info` log strings must not contain forbidden tokens.
  </action>
  <verify>
    <automated>cd /Users/johndemic/Development/projects/revenium/hermes-revenium &amp;&amp; bash -n skills/revenium/scripts/hermes-report.sh &amp;&amp; grep -q 'REVENIUM_CRON_SETTLE_SECONDS' skills/revenium/scripts/hermes-report.sh &amp;&amp; grep -q 'MARKERS_READY_DIR' skills/revenium/scripts/hermes-report.sh &amp;&amp; grep -q 'awaiting plugin sentinel' skills/revenium/scripts/hermes-report.sh &amp;&amp; grep -q 'G-03\|sentinel-or-aged\|D-21' skills/revenium/scripts/hermes-report.sh &amp;&amp; python3 -c "import subprocess, os, tempfile, time; tmp = tempfile.mkdtemp(); os.makedirs(os.path.join(tmp, 'state', 'revenium', 'markers', '.ready'), exist_ok=True); env = dict(os.environ); env['HERMES_HOME'] = tmp; env['REVENIUM_STATE_DIR'] = os.path.join(tmp, 'state', 'revenium'); env['REVENIUM_MARKERS_DIR'] = os.path.join(tmp, 'state', 'revenium', 'markers'); env['REVENIUM_MARKERS_READY_DIR'] = os.path.join(tmp, 'state', 'revenium', 'markers', '.ready'); env['REVENIUM_CRON_SETTLE_SECONDS'] = '120'; r = subprocess.run(['bash', '-n', 'skills/revenium/scripts/hermes-report.sh'], env=env, capture_output=True, text=True); assert r.returncode == 0, f'bash -n failed: {r.stderr}'; print('hermes-report.sh syntax OK')" &amp;&amp; python3 -m unittest tests.test_repository.RepositoryTests.test_shell_scripts_have_valid_syntax -v 2>&amp;1 | tail -3 | grep -E '^OK( |$)' &amp;&amp; python3 -m unittest tests.test_repository.RepositoryTests.test_no_legacy_branding_left -v 2>&amp;1 | tail -3 | grep -E '^OK( |$)' &amp;&amp; python3 -m unittest discover -s tests -p 'test_*.py' -v 2>&amp;1 | tail -3 | grep -E '^OK( |$)'</automated>
  </verify>
  <acceptance_criteria>
    `hermes-report.sh` passes `bash -n`; the new heredoc lives between the existing SELECT and the while-loop; `REVENIUM_CRON_SETTLE_SECONDS` and `MARKERS_READY_DIR` references present; `awaiting plugin sentinel` skip-log string present; heredoc is stdlib-only Python; all downstream lines (lines 60+ pre-edit, lines after the heredoc post-edit) are byte-identical to pre-edit. `test_shell_scripts_have_valid_syntax` passes. Full test suite green. Legacy-branding test green.
  </acceptance_criteria>
  <requirements_satisfied>HOOK-13 (cron-filter half — sentinel-or-aged primary + safety-net path). Path-discipline invariant preserved (MARKERS_READY_DIR resolved via env override that derives from common.sh).</requirements_satisfied>
  <done>Cron session SELECT filter ships; sessions without sentinels are deferred until SETTLE_SECONDS elapses; full test suite still green.</done>
  <commit_message>feat(06): hermes-report.sh sentinel-or-aged session filter (D-21, closes G-03 cron-race)</commit_message>
</task>

<task id="06-04-T04" type="auto">
  <name>T04: Add HOOK-13 to REQUIREMENTS.md + update ROADMAP.md Phase 6 (Requirements, SC5, Plans, Categories, Progress Table)</name>
  <files>
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
  </files>
  <read_first>
    @.planning/REQUIREMENTS.md
    @.planning/ROADMAP.md
    @.planning/phases/06-mechanical-classification-agent-end-hook/06-CONTEXT.md
    @.planning/phases/06-mechanical-classification-agent-end-hook/06-VERIFICATION.md
  </read_first>
  <action>
    Step 1 — REQUIREMENTS.md edits:

    1. In the `### Mechanical Classification Hook (Phase 6)` subsection (currently 12 entries after 06-03 added HOOK-12), APPEND a new entry as the 13th item, immediately after the existing HOOK-12 line:

       `- [ ] **HOOK-13**: Cron pipeline MUST synchronize with the plugin via a per-session sentinel file at ${MARKERS_READY_DIR}/<sid>. The plugin writes the sentinel from on_session_end after run_classification completes (substantive, trivial-skip, inheritance, halt, AND error paths). Cron's session SELECT in hermes-report.sh filters: report only if (sentinel exists for sid) OR (started_at < now - REVENIUM_CRON_SETTLE_SECONDS, default 120s). The sentinel is the deterministic primary path; the aged-safety-net handles plugin-failure cases. Closes G-03 surfaced by UAT round 3 on Mac Studio 2026-05-14 — cron-ticker raced the LLM classifier by ~6.47s and shipped task_type=unclassified before the marker was written.`

    2. Append a new traceability row to the `## Traceability` table at the bottom: `| HOOK-13 | Phase 6 | Pending |`. Insert it immediately after the `| HOOK-12 | Phase 6 | Pending |` row so HOOK-* rows stay contiguous and in numeric order.

    3. Update the Coverage block at the bottom:
       - `**v1 requirements:** 49 total` → `**v1 requirements:** 50 total`
       - `**Mapped to phases:** 49` → `**Mapped to phases:** 50`
       - `**Unmapped:** 0` (unchanged)

    Step 2 — ROADMAP.md edits:

    1. In `### Phase 6` section, on the `**Requirements**:` line, append `, HOOK-13`. Final line reads: `**Requirements**: HOOK-01, HOOK-02, HOOK-03, HOOK-04, HOOK-05, HOOK-06, HOOK-07, HOOK-08, HOOK-09, HOOK-10, HOOK-11, HOOK-12, HOOK-13`.

    2. Update Phase 6 success criterion SC5. The current SC5 (set by 06-01) is about the test suite passing. The G-03 closure invariant lives more naturally as a continuation of SC2 — but per CONTEXT.md gap-closure addendum #3, the load-bearing update is on SC5 (the test-suite-and-end-to-end-verification criterion). Update SC5 to:

       `python3 -m unittest discover -s tests -p 'test_*.py' -v passes including new tests for the classifier (heuristic skip-fast-path on trivial turns, LLM call invocation contract, parent_session_id inheritance via state.db join). No regressions in existing Phase 1-3 tests. Additionally, the next cron tick after on_session_end ships --task-type <meaningful-label> to Revenium — verified by an end-to-end fixture that creates a session, fires on_session_end (writing both the marker AND the sentinel at ${MARKERS_READY_DIR}/<sid>), then invokes hermes-report.sh and asserts the resulting revenium meter completion call carries the marker's task_type, NOT unclassified. The cron's session SELECT filter at hermes-report.sh respects (sentinel exists for sid) OR (started_at < now - REVENIUM_CRON_SETTLE_SECONDS, default 120s); sessions without sentinels that are younger than the settle window are deferred to a later tick.`

       Preserve SC1, SC2, SC3, SC4, SC6 verbatim — the G-03 invariant fits SC5's end-to-end verification scope. (SC2's CLI-no-JSONL clause set by 06-03 remains valid; G-03 is the cron-race fix, distinct from SC2's classifier-side fix.)

    3. Update the `**Plans**: 3 plans (...)` line to `**Plans**: 4 plans (06-01 ships the original gateway-hook implementation, 06-02 closes G-01 with the hermes_cli plugin migration, 06-03 closes G-02 with the state.db tool-count source-of-truth swap, 06-04 closes G-03 with the plugin-sentinel + cron settle-window filter)`.

    4. In the Plans checklist (currently has `- [x] 06-01-PLAN.md ...`, `- [x] 06-02-PLAN.md ...`, `- [x] 06-03-PLAN.md ...`), append a new entry below 06-03:

       `- [ ] 06-04-PLAN.md — Gap closure: plugin writes per-session sentinel at MARKERS_READY_DIR/<sid> after every on_session_end outcome; hermes-report.sh session SELECT filters by (sentinel exists OR started_at older than REVENIUM_CRON_SETTLE_SECONDS, default 120s); add 5 unit tests + 1 end-to-end test; add HOOK-13 (HOOK-01..HOOK-12 carried, HOOK-13 added)`

    5. In the Progress Table, change Phase 6's row text. The current row (set by 06-03) reads `3/3 | G-01 + G-02 closed (UAT-3 confirmed marker writes correctly); G-03 surfaced — cron race ships unclassified ahead of marker; pending 06-04 settle fix` with completed `2026-05-14`. Update to: `3/4 | In progress — G-03 gap closure planned in 06-04-PLAN.md`. Leave the `Completed` column unchanged at `2026-05-14`.

    6. Update the top-of-file Phase 6 bullet (currently mentions UAT round 3 surfacing G-03 — text last touched by 06-03). Append at the end of the parenthetical: `; gap closure in 06-04-PLAN.md adds a plugin sentinel + cron settle-window filter so cron only reports sessions whose plugin has finished classification`. Do not rewrite the bullet's earlier text.

    7. Update the `## Coverage` section: `**v1 requirements:** 49` → `**v1 requirements:** 50`; `**Mapped to phases:** 49` → `**Mapped to phases:** 50`; Categories line `HOOK (12)` → `HOOK (13)`. Final categories line: `**Categories:** PATH (3), TAX (5), MARK (5), PROMPT (7), CRON (9), WIRE (4), COMPAT (4), TEST (5), HOOK (13)`.

    NO other lines in REQUIREMENTS.md or ROADMAP.md change. Preserve all other content verbatim including HOOK-01..HOOK-12 entries (they remain valid — HOOK-13 EXTENDS the requirement set, it does not supersede any prior entry). SC1, SC2, SC3, SC4, SC6 are preserved verbatim.

    LEGACY BRANDING: the new HOOK-13 description + the new SC5 wording + the new Plans list entry must not contain any forbidden tokens. `test_no_legacy_branding_left` scans `.planning/`... actually it EXCLUDES `.planning/` per the test scope (`tests/test_repository.py:110-111`), but the legacy-branding test on shipped artifacts must still pass.
  </action>
  <verify>
    <automated>cd /Users/johndemic/Development/projects/revenium/hermes-revenium &amp;&amp; grep -q 'HOOK-13.*sentinel\|HOOK-13.*MARKERS_READY_DIR\|HOOK-13.*Cron pipeline' .planning/REQUIREMENTS.md &amp;&amp; grep -c '| HOOK-' .planning/REQUIREMENTS.md | grep -E '^13$' &amp;&amp; grep -q 'v1 requirements:.*50' .planning/REQUIREMENTS.md &amp;&amp; grep -q 'HOOK-01, HOOK-02, HOOK-03, HOOK-04, HOOK-05, HOOK-06, HOOK-07, HOOK-08, HOOK-09, HOOK-10, HOOK-11, HOOK-12, HOOK-13' .planning/ROADMAP.md &amp;&amp; grep -q '06-04-PLAN.md' .planning/ROADMAP.md &amp;&amp; grep -q 'HOOK (13)' .planning/ROADMAP.md &amp;&amp; grep -q 'REVENIUM_CRON_SETTLE_SECONDS\|MARKERS_READY_DIR' .planning/ROADMAP.md &amp;&amp; grep -q '3/4' .planning/ROADMAP.md &amp;&amp; python3 -m unittest tests.test_repository.RepositoryTests.test_no_legacy_branding_left -v 2>&amp;1 | tail -3 | grep -E '^OK( |$)' &amp;&amp; python3 -m unittest discover -s tests -p 'test_*.py' -v 2>&amp;1 | tail -3 | grep -E '^OK( |$)'</automated>
  </verify>
  <acceptance_criteria>
    REQUIREMENTS.md has the new HOOK-13 entry + 13th traceability row + Coverage counts 50/50; ROADMAP.md Phase 6 Requirements line appends HOOK-13; SC5 contains the new sentinel/settle-window invariant; Plans list includes 06-04-PLAN.md; Categories shows HOOK (13); Progress Table row reflects the in-progress 06-04 state (3/4); full test suite still green; legacy branding test still green.
  </acceptance_criteria>
  <requirements_satisfied>HOOK-13 (planning trace — implementation evidence lands in T01, T02, T03; this task is the planning-artifact half).</requirements_satisfied>
  <done>REQUIREMENTS.md + ROADMAP.md updated to declare HOOK-13 and reference the sentinel + settle-window invariant; existing tests remain green.</done>
  <commit_message>docs(06): add HOOK-13 requirement and update Phase 6 SC5 for cron sentinel + settle-window</commit_message>
</task>

<task id="06-04-T05" type="auto">
  <name>T05: Add 5 unit tests — sentinel write (happy + error) + cron filter (skip-recent-no-sentinel, include-aged-no-sentinel, include-any-age-with-sentinel)</name>
  <files>
    tests/test_repository.py
  </files>
  <read_first>
    @tests/test_repository.py
    @skills/revenium/plugins/revenium-classifier/__init__.py
    @skills/revenium/plugins/revenium-classifier/classifier.py
    @skills/revenium/scripts/hermes-report.sh
    @skills/revenium/scripts/common.sh
  </read_first>
  <action>
    In `tests/test_repository.py`, append FIVE new test methods to the `RepositoryTests` class. Place them as a contiguous block IMMEDIATELY AFTER the existing `test_revenium_classifier_tool_count_end_to_end_cli_substantive` method added by 06-03-T04, and BEFORE `test_setup_md_has_mechanical_classification_hook_section`. The new methods sit alongside the existing HOOK-* tests so future readers find the full G-03 coverage in one place.

    All five tests use the SAME scaffold pattern as the existing HOOK-* tests (mirror `test_revenium_classifier_tool_count_uses_state_db` and `test_revenium_classifier_substantive_uses_session_jsonl_tool_count`):
    - `tmpdir = tempfile.mkdtemp(prefix='gsd-hook-<short-name>-')`
    - `snap, added, hh, sd, md = _setup_plugin_env(tmpdir)` — the helper already sets up HERMES_HOME / REVENIUM_STATE_DIR / REVENIUM_MARKERS_DIR. Note: `_setup_plugin_env` does NOT currently set `REVENIUM_MARKERS_READY_DIR`. The new tests must additionally set `os.environ['REVENIUM_MARKERS_READY_DIR'] = os.path.join(sd, 'markers', '.ready')` and `os.makedirs(os.path.join(sd, 'markers', '.ready'), mode=0o700, exist_ok=True)` BEFORE the `importlib.reload(classifier)` call so the module's `MARKERS_READY_DIR` constant picks up the redirected path.
    - `try: ... finally: _restore_plugin_env(snap, added); shutil.rmtree(tmpdir, ignore_errors=True)`.

    Test method 1 — `test_revenium_classifier_sentinel_written_on_happy_path` (HOOK-13 sentinel primary):
    - Docstring: "HOOK-13 / D-21: when _on_session_end completes via the happy path (run_classification returns normally for ANY outcome — substantive marker write, trivial-skip, inheritance, or halt-unclassified), the plugin writes an empty sentinel file at MARKERS_READY_DIR / session_id. The sentinel is the cron's primary readiness signal."
    - Imports: importlib, os, shutil, sys, tempfile, unittest.mock.
    - Body:
      - Set up plugin env + REVENIUM_MARKERS_READY_DIR as described.
      - `importlib.reload(sys.modules['classifier'])` if cached, then `import __init__ as plugin; importlib.reload(plugin)`.
      - Patch `plugin.run_classification` with `unittest.mock.MagicMock(return_value=None)` so the helper short-circuits to a no-op (we are testing the sentinel write, not the classifier).
      - Invoke `plugin._on_session_end(session_id='sid-happy', completed=True, interrupted=False, model='qwen3.6-plus', platform='cli')`.
      - Assertions:
        - `sentinel_path = os.path.join(sd, 'markers', '.ready', 'sid-happy')`.
        - `self.assertTrue(os.path.exists(sentinel_path), 'sentinel not written for happy path')`.
        - `self.assertEqual(os.path.getsize(sentinel_path), 0, 'sentinel must be zero-byte')`.
      - try/finally cleanup.

    Test method 2 — `test_revenium_classifier_sentinel_written_on_error_path` (HOOK-13 / D-04 belt):
    - Docstring: "HOOK-13 / D-21 / D-04 belt: when run_classification raises an exception, _on_session_end catches it, logs a warning, AND STILL writes the sentinel. The sentinel write inside the except handler prevents a classifier crash from freezing a session in the cron's race window. D-04 invariant: _on_session_end never raises out."
    - Body:
      - Set up plugin env + REVENIUM_MARKERS_READY_DIR.
      - Import + reload classifier + plugin as above.
      - Patch `plugin.run_classification` with a side_effect that raises `RuntimeError('synthetic classifier crash')`.
      - Invoke `plugin._on_session_end(session_id='sid-error', completed=True, interrupted=False, model='qwen3.6-plus', platform='cli')` — must NOT raise.
      - Assertions:
        - `sentinel_path = os.path.join(sd, 'markers', '.ready', 'sid-error')`.
        - `self.assertTrue(os.path.exists(sentinel_path), 'sentinel not written on error path — D-04 belt broken')`.
        - `self.assertEqual(os.path.getsize(sentinel_path), 0)`.
      - try/finally cleanup.

    Test method 3 — `test_revenium_classifier_cron_filter_skips_recent_no_sentinel` (HOOK-13 cron primary):
    - Docstring: "HOOK-13 / D-21: a session whose started_at is 30s ago (within the 120s settle window) AND has NO sentinel file is SKIPPED by hermes-report.sh's session SELECT filter; the session is NOT shipped to revenium meter completion this tick. Pins the cron's race-defense primary path."
    - Imports: importlib, os, shutil, sqlite3, subprocess, sys, tempfile, time.
    - Body:
      - Set up tmp HERMES_HOME + STATE_DIR + MARKERS_DIR + MARKERS_READY_DIR via the existing helper pattern. Do NOT use `_setup_plugin_env` directly here — this test invokes `hermes-report.sh` as a subprocess, not the plugin Python. Instead: build the tmpdir structure manually and pass env vars to `subprocess.run`.
      - `state_db = os.path.join(hh, 'state.db')`. Create the sessions table with the canonical schema columns the SELECT expects: `CREATE TABLE sessions (id TEXT PRIMARY KEY, model TEXT, source TEXT, input_tokens INTEGER, output_tokens INTEGER, cache_read_tokens INTEGER, cache_write_tokens INTEGER, reasoning_tokens INTEGER, estimated_cost_usd REAL, api_call_count INTEGER, started_at INTEGER, ended_at INTEGER, billing_provider TEXT)`.
      - `recent_ts = int(time.time()) - 30`. Insert one row: `('sid-recent', 'qwen3.6-plus', 'cli', 100, 50, 0, 0, 0, 0.01, 1, recent_ts, None, 'openai')`.
      - Do NOT create a sentinel file at `${MARKERS_READY_DIR}/sid-recent`.
      - Create a stubbed `revenium` shim on PATH: a small bash script at `${tmpdir}/bin/revenium` that records its argv to `${tmpdir}/revenium-invocations.log` and exits 0. `chmod +x` it.
      - Run `subprocess.run(['bash', str(SKILL / 'scripts' / 'hermes-report.sh')], env={..., 'PATH': f"{tmpdir}/bin:{os.environ['PATH']}", 'HERMES_HOME': hh, 'REVENIUM_STATE_DIR': sd, 'REVENIUM_MARKERS_DIR': md, 'REVENIUM_MARKERS_READY_DIR': os.path.join(sd, 'markers', '.ready'), 'REVENIUM_CRON_SETTLE_SECONDS': '120'}, capture_output=True, text=True, timeout=30)`.
      - Assertions:
        - Confirm `revenium-invocations.log` does NOT exist (the stub was never called) OR is empty (the stub was never called). `self.assertFalse(os.path.exists(invocations_log) and os.path.getsize(invocations_log) > 0, 'cron unexpectedly shipped a recent-no-sentinel session')`.
        - Confirm the cron's log output (stderr + ${LOG_FILE}) contains the skip message: `self.assertIn('awaiting plugin sentinel', result.stderr + open(os.path.join(sd, 'revenium-metering.log')).read())`.
      - try/finally cleanup.

    Test method 4 — `test_revenium_classifier_cron_filter_includes_aged_no_sentinel` (HOOK-13 safety net):
    - Docstring: "HOOK-13 / D-21 safety net: a session whose started_at is 200s ago (older than the 120s settle window) AND has NO sentinel is INCLUDED in the cron's session list — D-18 default applies (--task-type unclassified) because no marker file exists. Pins the safety-net path for plugin-failure cases."
    - Body: same scaffold as method 3 but with `aged_ts = int(time.time()) - 200`. Do NOT create a sentinel. Stub `revenium` records argv.
    - Assertions:
      - `invocations_log` EXISTS and is non-empty: `self.assertTrue(os.path.exists(invocations_log) and os.path.getsize(invocations_log) > 0)`.
      - Recorded argv contains `--task-type unclassified` (the D-18 default for no-marker sessions): `self.assertIn('--task-type unclassified', open(invocations_log).read())`.
      - The skip-log line is NOT in the cron log: `self.assertNotIn('skipping sid-aged', result.stderr + open(...).read())`.
    - try/finally cleanup.

    Test method 5 — `test_revenium_classifier_cron_filter_includes_any_age_with_sentinel` (HOOK-13 sentinel-wins):
    - Docstring: "HOOK-13 / D-21 sentinel-wins: a session whose started_at is 5s ago (well within the 120s settle window) BUT has a sentinel file at MARKERS_READY_DIR/<sid> is INCLUDED in the cron's session list. Pins the sentinel-driven primary path: sentinel presence overrides the age check."
    - Body: same scaffold as method 3 but with `recent_ts = int(time.time()) - 5`. ALSO create a sentinel file at `os.path.join(sd, 'markers', '.ready', 'sid-with-sentinel')` via `Path(...).touch()`. Stub `revenium` records argv.
    - Assertions:
      - `invocations_log` EXISTS and is non-empty (cron shipped despite the age).
      - The skip-log line is NOT in the cron log.
    - try/finally cleanup.

    Style invariants — same as the surrounding HOOK-* tests:
    - 4-space indent, double-quoted strings inside Python bodies.
    - Use `_setup_plugin_env` / `_restore_plugin_env` where applicable (methods 1, 2 — the in-process plugin tests). Use manual tmpdir + env-passing for the subprocess tests (methods 3, 4, 5).
    - `import classifier as handler` if a method needs to touch classifier directly (methods 1, 2 do not need this — they patch `plugin.run_classification`).
    - DO NOT delete, rename, or modify any existing test method. Append-only.

    LEGACY BRANDING: docstrings and inline comments in the new tests must not contain forbidden tokens.
  </action>
  <verify>
    <automated>cd /Users/johndemic/Development/projects/revenium/hermes-revenium &amp;&amp; grep -q 'def test_revenium_classifier_sentinel_written_on_happy_path' tests/test_repository.py &amp;&amp; grep -q 'def test_revenium_classifier_sentinel_written_on_error_path' tests/test_repository.py &amp;&amp; grep -q 'def test_revenium_classifier_cron_filter_skips_recent_no_sentinel' tests/test_repository.py &amp;&amp; grep -q 'def test_revenium_classifier_cron_filter_includes_aged_no_sentinel' tests/test_repository.py &amp;&amp; grep -q 'def test_revenium_classifier_cron_filter_includes_any_age_with_sentinel' tests/test_repository.py &amp;&amp; python3 -m unittest tests.test_repository.RepositoryTests.test_revenium_classifier_sentinel_written_on_happy_path tests.test_repository.RepositoryTests.test_revenium_classifier_sentinel_written_on_error_path tests.test_repository.RepositoryTests.test_revenium_classifier_cron_filter_skips_recent_no_sentinel tests.test_repository.RepositoryTests.test_revenium_classifier_cron_filter_includes_aged_no_sentinel tests.test_repository.RepositoryTests.test_revenium_classifier_cron_filter_includes_any_age_with_sentinel -v 2>&amp;1 | tail -3 | grep -E '^OK( |$)' &amp;&amp; python3 -m unittest tests.test_repository.RepositoryTests.test_no_legacy_branding_left -v 2>&amp;1 | tail -3 | grep -E '^OK( |$)' &amp;&amp; python3 -m unittest discover -s tests -p 'test_*.py' -v 2>&amp;1 | tail -3 | grep -E '^OK( |$)'</automated>
  </verify>
  <acceptance_criteria>
    Five new test methods exist; each runs in isolation and passes; the broader suite still passes; legacy branding test still passes. The sentinel-write tests exercise both the happy path and the error path (D-04 belt extension). The cron-filter tests exercise the three branches: recent-no-sentinel (skip), aged-no-sentinel (include via safety net), any-age-with-sentinel (include via sentinel primary).
  </acceptance_criteria>
  <requirements_satisfied>HOOK-13 (unit-test coverage for the 5 invariants of D-21: sentinel write on happy + error paths, cron filter primary + safety-net + sentinel-override).</requirements_satisfied>
  <done>Five new unit tests for the sentinel write + cron filter land; full test suite still green.</done>
  <commit_message>test(06): add 5 unit tests for sentinel write + cron sentinel-or-aged filter (HOOK-13)</commit_message>
</task>

<task id="06-04-T06" type="auto">
  <name>T06: Add end-to-end test — synthetic state.db + marker + sentinel → cron ships marker's task_type (NOT unclassified)</name>
  <files>
    tests/test_repository.py
  </files>
  <read_first>
    @tests/test_repository.py
    @skills/revenium/scripts/hermes-report.sh
    @skills/revenium/plugins/revenium-classifier/classifier.py
  </read_first>
  <action>
    In `tests/test_repository.py`, append a SIXTH new test method to the `RepositoryTests` class. Place it IMMEDIATELY AFTER the five unit tests added by T05 and BEFORE `test_setup_md_has_mechanical_classification_hook_section`. This is the end-to-end CI regression guard for G-03.

    Test method — `test_revenium_classifier_cron_filter_end_to_end_ships_marker_task_type` (HOOK-13 behavioral / G-03 regression guard):

    Docstring (2-3 lines): "HOOK-13 / G-03 regression guard: when state.db has a CLI session row (tool_call_count=2, recent started_at) AND a marker file exists at MARKERS_DIR/<sid>.jsonl with task_type='generation' AND a sentinel exists at MARKERS_READY_DIR/<sid>, hermes-report.sh's session SELECT INCLUDES the session AND the downstream revenium meter completion calls carry --task-type generation (NOT --task-type unclassified). This is the test that would have caught G-03 on 2026-05-14 — the cron-race shipping unclassified ahead of the plugin's marker."

    Imports inside the method: importlib, json, os, shutil, sqlite3, subprocess, sys, tempfile, time.

    Scaffold (mirror the shape of `test_cron_marker_split_end_to_end` from Phase 3 + the cron-filter tests added in T05):
    - `tmpdir = tempfile.mkdtemp(prefix='gsd-hook-e2e-cron-sentinel-')`
    - Build the tmp HERMES_HOME tree manually: `hh = os.path.join(tmpdir, 'hh')`; `sd = os.path.join(hh, 'state', 'revenium')`; `md = os.path.join(sd, 'markers')`; `mrd = os.path.join(md, '.ready')`; `os.makedirs(md); os.makedirs(mrd)`.
    - Stage the sources state-db, marker file, sentinel, and revenium-cli stub:

    Test body, in order:

    1. **Build state.db row** (synthetic CLI session shape — same as 06-03's test fixture, plus a recent started_at):
       - `state_db = os.path.join(hh, 'state.db')`.
       - `conn = sqlite3.connect(state_db)`.
       - Create the sessions table with the full canonical schema the cron's SELECT expects (id, model, source, input_tokens, output_tokens, cache_read_tokens, cache_write_tokens, reasoning_tokens, estimated_cost_usd, api_call_count, started_at, ended_at, billing_provider) — plus `tool_call_count` for forward-compatibility with the classifier (06-03's lookup queries this column).
       - `started_at = int(time.time()) - 5` (well within the 120s settle window — the sentinel must override the age check for the test to be meaningful).
       - Insert one row: `('cli-sid', 'qwen3.6-plus', 'cli', 1000, 500, 0, 0, 0, 0.05, 1, started_at, None, 'openai')`. Add `tool_call_count=2` as a separate column if the schema supports it; otherwise omit (the cron does not consult tool_call_count — only the classifier does).
       - `conn.commit(); conn.close()`.

    2. **Write the marker file** at `os.path.join(md, 'cli-sid.jsonl')` — exactly TWO records, the GUARDRAIL+CHAT pair the classifier would produce. Use the muid format from MARK-03 (33-char lowercase hex). Both records carry `task_type='generation'` and `operation_type` values 'GUARDRAIL' and 'CHAT' respectively. Each record < 1024 bytes, single line. Use `json.dumps(...) + '\n'` written via `open(marker_path, 'a')` (append-binary not required for the fixture).

    3. **Write the sentinel** at `os.path.join(mrd, 'cli-sid')` via `Path(...).touch(exist_ok=True)` — zero-byte file.

    4. **Stub the `revenium` CLI**: create `os.path.join(tmpdir, 'bin', 'revenium')` as a 3-line bash script:
       ```
       #!/usr/bin/env bash
       printf '%q ' "$@" >> "{tmpdir}/revenium-invocations.log"
       printf '\n' >> "{tmpdir}/revenium-invocations.log"
       exit 0
       ```
       (Embed the `tmpdir` path via Python string interpolation when writing the file.) Chmod +x. Also stub `revenium config show` to exit 0 silently — the cron's preflight check at `hermes-report.sh:29` calls this. The simplest stub handles both: if `$1 == "config"` and `$2 == "show"`, exit 0 silently; otherwise log and exit 0. Even simpler: have the stub always exit 0 + always log argv. The preflight will log a `config show` invocation in the log but the meter completion invocations will also be logged — the test asserts on the meter completion lines.

    5. **Seed an empty ledger** at `os.path.join(sd, 'revenium-hermes.ledger')` via `open(...).close()` so the cron's `touch ${LEDGER_FILE}` line at hermes-report.sh:34 finds it.

    6. **Invoke hermes-report.sh** via subprocess:
       ```
       env = {
           'PATH': f"{os.path.join(tmpdir, 'bin')}:{os.environ['PATH']}",
           'HOME': os.environ.get('HOME', tmpdir),
           'HERMES_HOME': hh,
           'REVENIUM_STATE_DIR': sd,
           'REVENIUM_MARKERS_DIR': md,
           'REVENIUM_MARKERS_READY_DIR': mrd,
           'REVENIUM_CRON_SETTLE_SECONDS': '120',
       }
       result = subprocess.run(['bash', str(SKILL / 'scripts' / 'hermes-report.sh')], env=env, capture_output=True, text=True, timeout=60)
       ```

    7. **Assertions**:
       - `self.assertEqual(result.returncode, 0, f'hermes-report.sh failed: stderr={result.stderr}')`.
       - `invocations_log = os.path.join(tmpdir, 'revenium-invocations.log')`.
       - `self.assertTrue(os.path.exists(invocations_log), 'revenium stub was never invoked — cron may have skipped the session')`.
       - `log_text = open(invocations_log).read()`.
       - Assert the `meter completion` invocation is present: `self.assertIn('meter completion', log_text)`.
       - Assert the recorded argv contains `--task-type generation`: `self.assertIn('--task-type generation', log_text)` (the marker's task_type, not unclassified).
       - Assert the recorded argv does NOT contain `--task-type unclassified`: `self.assertNotIn('--task-type unclassified', log_text)`.

    8. try/finally: `shutil.rmtree(tmpdir, ignore_errors=True)`.

    Style invariants — same as the surrounding HOOK-* tests:
    - 4-space indent, double-quoted strings.
    - Append-only — no existing test modified.

    LEGACY BRANDING: docstring + comments must not contain forbidden tokens.
  </action>
  <verify>
    <automated>cd /Users/johndemic/Development/projects/revenium/hermes-revenium &amp;&amp; grep -q 'def test_revenium_classifier_cron_filter_end_to_end_ships_marker_task_type' tests/test_repository.py &amp;&amp; python3 -m unittest tests.test_repository.RepositoryTests.test_revenium_classifier_cron_filter_end_to_end_ships_marker_task_type -v 2>&amp;1 | tail -3 | grep -E '^OK( |$)' &amp;&amp; python3 -m unittest tests.test_repository.RepositoryTests.test_no_legacy_branding_left -v 2>&amp;1 | tail -3 | grep -E '^OK( |$)' &amp;&amp; python3 -m unittest discover -s tests -p 'test_*.py' -v 2>&amp;1 | tail -3 | grep -E '^OK( |$)'</automated>
  </verify>
  <acceptance_criteria>
    `test_revenium_classifier_cron_filter_end_to_end_ships_marker_task_type` exists; it seeds tmp HERMES_HOME with state.db row + marker file + sentinel; stubs `revenium` CLI; invokes `hermes-report.sh`; asserts the stub log contains `--task-type generation` AND does NOT contain `--task-type unclassified`; test runs in isolation; broader test suite still passes; legacy branding test still passes.
  </acceptance_criteria>
  <requirements_satisfied>HOOK-13 (behavioral / end-to-end CI regression guard for G-03). HOOK-11 (universal-coverage invariant — proves the plugin → marker → sentinel → cron chain ships the meaningful task_type for sessions without JSONL).</requirements_satisfied>
  <done>End-to-end test landed; the full plugin → marker → sentinel → cron chain ships --task-type generation (NOT unclassified); full test suite still green.</done>
  <commit_message>test(06): add end-to-end cron+sentinel test — ships marker task_type, not unclassified (HOOK-13 / G-03)</commit_message>
</task>

<task id="06-04-T07" type="auto">
  <name>T07: Update 06-VERIFICATION.md — transition G-03 gap_closure pending → executed; PRESERVE status: requires_rerun_uat AND gaps_remaining: [G-03]</name>
  <files>
    .planning/phases/06-mechanical-classification-agent-end-hook/06-VERIFICATION.md
  </files>
  <read_first>
    @.planning/phases/06-mechanical-classification-agent-end-hook/06-VERIFICATION.md
    @.planning/phases/06-mechanical-classification-agent-end-hook/06-CONTEXT.md
  </read_first>
  <action>
    Edit `.planning/phases/06-mechanical-classification-agent-end-hook/06-VERIFICATION.md` in the frontmatter only — do not touch any body content below the frontmatter's closing `---`.

    Step 1 — Update the G-03 entry in the `gaps:` block. The current G-03 entry (lines 24-29) reads:
    ```
      - id: G-03
        severity: high
        gap_closure: pending
        gap_closure_plan: TBD (06-04-PLAN.md)
        summary: "..."
        relates_to: [HOOK-12, SC2, SC5, Phase-3-cron-pipeline]
    ```

    Apply these edits:
    - Change `gap_closure: pending` to `gap_closure: closed` (matches the shape used for G-01 and G-02 after their respective code-side closure — both currently read `gap_closure: closed`). NOTE: the project uses `closed` (not `executed`) for the code-side terminal state on G-01 and G-02 because the operator UAT round 3 has already retired them. For G-03, the code is closed but UAT round 4 has NOT yet run — so the correct value is `executed` (matches the in-flight terminology used in CONTEXT.md addendum #3 "VERIFICATION.md G-03 transition" — `gap_closure: pending → executed`). Use `executed`.
    - Change `gap_closure_plan: TBD (06-04-PLAN.md)` to `gap_closure_plan: 06-04-PLAN.md`.
    - APPEND a new field `gap_closure_outcome:` after `gap_closure_plan:` with the value: `"code fix landed (plugin __init__.py writes per-session sentinel at MARKERS_READY_DIR/<sid> after every on_session_end outcome — happy path + D-04 error-path belt; hermes-report.sh session SELECT filters by sentinel-or-aged with default 120s settle window via REVENIUM_CRON_SETTLE_SECONDS). G-03 is closed by code; behaviorally closed only after UAT round 4 records a CLI substantive turn straddling a minute boundary producing a marker file AND that marker reaching Revenium with the correct task_type (NOT unclassified). The six new HOOK-13 tests (2 sentinel-write + 3 cron-filter + 1 end-to-end) pin the regression guard in CI."`
    - Update `relates_to: [HOOK-12, SC2, SC5, Phase-3-cron-pipeline]` to `relates_to: [HOOK-13, SC5, Phase-3-cron-pipeline]`. (HOOK-12 was the G-02-relevant requirement; HOOK-13 is the G-03-relevant one. SC2 was already covered by HOOK-12 / G-02. Keeping SC5 + Phase-3-cron-pipeline as the relates-to scope is accurate.)
    - DO NOT change `severity`, `summary` on G-03 — those fields document the historical evidence and remain valid.

    Step 2 — DO NOT change the frontmatter `status:` field. It MUST remain `status: requires_rerun_uat`. The verify command in this task explicitly asserts the file still contains `status: requires_rerun_uat`. Rationale: G-03 is code-closed by this plan but operator-confirmed only after UAT round 4 on Mac Studio. Until that operator UAT runs, G-03 stays in `re_verification.gaps_remaining: [G-03]` and the file status stays at `requires_rerun_uat`.

    Step 3 — DO NOT change the `re_verification.gaps_remaining: [G-03]` line. It MUST remain `[G-03]` even though G-03 is now code-closed. The phase verifier reads `gaps_remaining` as the authoritative source for the phase-Verified gate, and UAT round 4 retires G-03 by transitioning it to a pass result in `06-HUMAN-UAT.md`. After UAT round 4, a separate update (NOT part of this plan) will move `gaps_remaining: [G-03]` → `gaps_remaining: []` and flip `status: requires_rerun_uat` → `status: verified` (or whatever terminal status the phase verifier uses). This plan does NOT make that transition — that is the phase verifier's job after UAT round 4 records `pass`.

    Step 4 — DO NOT change the `gaps:` G-01 or G-02 entries. Both are already at `gap_closure: closed` per the UAT round 3 retirement (confirmed in CONTEXT.md addendum #3: "UAT round 3 confirmed G-01 + G-02 closure"). Their entries remain valid.

    Step 5 — Update the top-level frontmatter `updated:` field. The file currently has `updated: 2026-05-14T07:20:00Z`. Bump to `updated: 2026-05-14T08:00:00Z` or any later same-day timestamp matching the ISO-8601 style — anything later than the current value, indicating this plan's edits landed on 2026-05-14.

    Step 6 — DO NOT change the top-of-frontmatter `gap_closure_plan: 06-02-PLAN.md` pointer at line 6. That pointer documents the FIRST gap-closure plan (06-02 for G-01). The G-03 closure is recorded under G-03's own entry. Leave the top-level pointer unchanged.

    Step 7 — Update the `re_verification.gaps_closed: [G-01, G-02]` line. After this plan lands code-closure for G-03, the field should become `gaps_closed: [G-01, G-02, G-03]` — because the code-side closure has happened. The behavioral retirement of G-03 is still gated by UAT round 4 (recorded in `gaps_remaining`, not `gaps_closed`), but the code-side closure is in `gaps_closed`. This matches the precedent set by 06-03: when 06-03 code-closed G-02, the file's `gaps_closed` was updated to `[G-01, G-02]` even though `gaps_remaining: [G-01, G-02]` was preserved until UAT round 3 retired them. Apply the same precedent here: `gaps_closed: [G-01, G-02, G-03]` AND `gaps_remaining: [G-03]` (unchanged). The verify command must assert BOTH.

    Step 8 — DO NOT touch any body content (the markdown sections below the frontmatter's closing `---`). The "## Goal Achievement", "## Gap closure (added ...)" and other body sections are historical/narrative and reading-only for this task.

    LEGACY BRANDING: the new gap_closure_outcome string must not contain forbidden tokens. Note: `.planning/` is EXCLUDED from `test_no_legacy_branding_left`'s scope (`tests/test_repository.py:110-111`), but the shipping-artifacts legacy test must continue to pass.
  </action>
  <verify>
    <automated>cd /Users/johndemic/Development/projects/revenium/hermes-revenium &amp;&amp; grep -q 'status: requires_rerun_uat' .planning/phases/06-mechanical-classification-agent-end-hook/06-VERIFICATION.md &amp;&amp; python3 -c "import re; t = open('.planning/phases/06-mechanical-classification-agent-end-hook/06-VERIFICATION.md').read(); fm = t.split('---', 2)[1]; assert 'id: G-03' in fm; g03_block = fm.split('id: G-03', 1)[1].split('id: ', 1)[0] if 'id: G-03' in fm else fm.split('id: G-03', 1)[1]; assert 'gap_closure: executed' in g03_block, 'G-03 gap_closure not flipped to executed: ' + g03_block[:500]; assert 'gap_closure_plan: 06-04-PLAN.md' in g03_block, '06-04-PLAN.md pointer not added'; assert 'gap_closure_outcome' in g03_block, 'gap_closure_outcome field missing'; assert 'status: requires_rerun_uat' in fm, 'status incorrectly mutated'; assert 'gaps_remaining: [G-03]' in fm, 'gaps_remaining was mutated — G-03 must stay until UAT round 4'; assert 'gaps_closed: [G-01, G-02, G-03]' in fm, 'gaps_closed should include G-03 after code-side closure'; print('VERIFICATION.md frontmatter shape OK')" &amp;&amp; python3 -m unittest tests.test_repository.RepositoryTests.test_no_legacy_branding_left -v 2>&amp;1 | tail -3 | grep -E '^OK( |$)' &amp;&amp; python3 -m unittest discover -s tests -p 'test_*.py' -v 2>&amp;1 | tail -3 | grep -E '^OK( |$)'</automated>
  </verify>
  <acceptance_criteria>
    06-VERIFICATION.md frontmatter has G-03 `gap_closure: executed` (was `pending`); G-03 has `gap_closure_plan: 06-04-PLAN.md` (was `TBD`); G-03 has a new `gap_closure_outcome` field describing the code-side closure; `relates_to: [HOOK-13, SC5, Phase-3-cron-pipeline]` updated; frontmatter `status: requires_rerun_uat` is PRESERVED (the verify command explicitly checks this); `re_verification.gaps_remaining: [G-03]` is unchanged (G-03 stays in remaining until UAT round 4 retires it); `re_verification.gaps_closed` updated from `[G-01, G-02]` to `[G-01, G-02, G-03]`; `updated:` field bumped to a 2026-05-14 timestamp later than the existing one; no body content modified; full test suite still green; legacy branding test still green.
  </acceptance_criteria>
  <requirements_satisfied>HOOK-13 (verification-trace half — the artifact that records code-side closure of G-03). Closes the planning-trace loop for G-03 without prematurely flipping the phase status.</requirements_satisfied>
  <done>06-VERIFICATION.md updated to record G-03 code-side closure; phase status remains requires_rerun_uat pending UAT round 4.</done>
  <commit_message>docs(06): mark G-03 gap_closure: executed in 06-VERIFICATION.md (status preserved at requires_rerun_uat)</commit_message>
</task>

<task id="06-04-T08" type="auto">
  <name>T08: Write 06-04-SUMMARY.md — manual UAT-4 gate language referencing G-01, G-02, AND G-03</name>
  <files>
    .planning/phases/06-mechanical-classification-agent-end-hook/06-04-SUMMARY.md
  </files>
  <read_first>
    @.planning/phases/06-mechanical-classification-agent-end-hook/06-03-SUMMARY.md
    @.planning/phases/06-mechanical-classification-agent-end-hook/06-VERIFICATION.md
    @.planning/phases/06-mechanical-classification-agent-end-hook/06-HUMAN-UAT.md
    @.planning/phases/06-mechanical-classification-agent-end-hook/06-04-PLAN.md
  </read_first>
  <action>
    Create `.planning/phases/06-mechanical-classification-agent-end-hook/06-04-SUMMARY.md` using the same frontmatter shape as `06-03-SUMMARY.md` (read it as the canonical template). The summary documents what this plan shipped and explicitly flags the UAT round 4 gate.

    Required frontmatter (YAML):
    - `phase: 06-mechanical-classification-agent-end-hook`
    - `plan: 04`
    - `subsystem: cron+plugins`
    - `tags: [plugins, hermes-cli, on-session-end, cron, sentinel, settle-window, race-fix, gap-closure]`
    - `requires:` block listing:
      - `- plan: 06-03-PLAN.md` with `provides:` "Tool-count source-of-truth swap (state.db.sessions.tool_call_count primary, JSONL fallback) — the classifier correctly identifies substantive CLI turns. Sentinel-write contract in this plan layers on top: the classifier produces a marker; the plugin then signals readiness via sentinel so the cron does not race the LLM."
    - `provides:` block listing:
      - "Plugin sentinel write — per-session empty file at MARKERS_READY_DIR/<sid> written from _on_session_end after run_classification returns (every outcome) AND in the outer except handler (D-04 belt extension). Classifier crash no longer freezes a session in the cron's race window."
      - "Cron settle-window filter — hermes-report.sh session SELECT filters by (sentinel exists for sid) OR (started_at < now - REVENIUM_CRON_SETTLE_SECONDS, default 120s). Sentinel-driven primary path; aged-safety-net for plugin-failure cases."
      - "Closes G-03 in code: cron-race ship-unclassified-before-marker no longer happens. CLI sessions whose final turn straddles a minute boundary are deferred to the next tick after the plugin's sentinel lands."
      - "Six new HOOK-13 tests pin the regression guard: sentinel write happy + error paths, cron filter skip-recent-no-sentinel + include-aged-no-sentinel + include-any-age-with-sentinel, end-to-end cron+sentinel ships marker's task_type."
      - "ROADMAP Phase 6 SC5 explicitly calls out the cron sentinel-or-aged synchronization invariant via the new MARKERS_READY_DIR + REVENIUM_CRON_SETTLE_SECONDS clauses."
    - `affects: [phase-04-wire-enrichment, phase-05-housekeeping, hermes-uat]` (same as 06-03-SUMMARY.md's affects line).
    - `key-files:` block:
      - `modified:` listing the 7 files (skills/revenium/scripts/common.sh, skills/revenium/scripts/hermes-report.sh, skills/revenium/plugins/revenium-classifier/__init__.py, skills/revenium/plugins/revenium-classifier/classifier.py, tests/test_repository.py, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/phases/06-mechanical-classification-agent-end-hook/06-VERIFICATION.md). Note: count is 8 files modified (the 7 production/test/planning files + VERIFICATION.md).
      - `created:` listing this SUMMARY.md.
      - `deleted:` empty list.
    - `key-decisions:` block listing D-21 (plugin sentinel + cron settle-window filter, sentinel deterministic primary + age-based safety net) and the scope-discipline decision (out-of-scope: classifier logic, heuristic shape, response-text, CR-01/CR-02/CR-03, setup-local.sh, references/setup.md, HOOK-01..HOOK-12 entry strings, existing 16 HOOK-* tests).
    - `patterns-established:` block with 2 entries:
      - "Empty-file sentinel signalling pattern: the plugin writes a zero-byte file in a path-discipline-managed directory; the cron-side filter checks existence to gate downstream behavior. No payload, no parsing, no schema — pure existence semantics. Reusable for future cron/plugin synchronization needs without re-litigating the IPC shape."
      - "Settle-window safety net pattern: a sentinel-driven primary path layered on an age-based fallback. The sentinel is the deterministic happy-path signal; the age check handles plugin-absent / plugin-crashed / plugin-uninstalled cases. Cron eventually reports every session (no infinite deferral) while honoring the explicit signal when present."
    - `requirements-completed: [HOOK-01, HOOK-02, HOOK-03, HOOK-04, HOOK-05, HOOK-06, HOOK-07, HOOK-08, HOOK-09, HOOK-10, HOOK-11, HOOK-12, HOOK-13]`.
    - `duration: ~15min` (estimate — 8 tasks, 8 atomic commits).
    - `completed: 2026-05-14`.

    Body content (markdown sections after frontmatter close):

    1. `# Phase 06 Plan 04: Gap-closure — Plugin sentinel + cron settle-window filter (closes G-03)` heading.

    2. **One-sentence lead** describing what shipped: "Added a per-session sentinel file at `${MARKERS_READY_DIR}/<sid>` written by the plugin's `on_session_end` after every classification outcome; `hermes-report.sh` now filters its session SELECT by sentinel-or-aged with a 120s default settle window, closing G-03 (cron-ticker racing the LLM classifier) surfaced by Mac Studio UAT round 3."

    3. `## Performance` section with task count (8), commit count (8), files modified/created/deleted counts (8 modified, 1 created, 0 deleted).

    4. `## Accomplishments` section bullets covering: the path declaration (common.sh + classifier.py + path-discipline test), the plugin sentinel write (happy + error paths), the cron filter (sentinel-or-aged with stdlib Python heredoc), HOOK-13 in REQUIREMENTS, ROADMAP SC5 update, VERIFICATION G-03 transition (status preserved at requires_rerun_uat), the 6 new tests (5 unit + 1 end-to-end), and the per-commit-green test-suite mandate.

    5. `## Task Commits` section listing the 8 commits with their conventional-commit prefixes:
       - T01 `feat(06):` declare MARKERS_READY_DIR in common.sh + classifier.py + path-discipline test
       - T02 `feat(06):` plugin __init__.py writes sentinel at MARKERS_READY_DIR/sid after run_classification
       - T03 `feat(06):` hermes-report.sh sentinel-or-aged session filter
       - T04 `docs(06):` add HOOK-13 requirement and update Phase 6 SC5
       - T05 `test(06):` add 5 unit tests for sentinel write + cron filter
       - T06 `test(06):` add end-to-end cron+sentinel test — ships marker task_type
       - T07 `docs(06):` mark G-03 gap_closure: executed in 06-VERIFICATION.md
       - T08 `docs(06):` this SUMMARY.md

    6. `## Decisions Honored` section: D-21 (plugin sentinel + cron settle-window primary, aged-safety-net secondary); D-04 (extended to sentinel write — _write_sentinel never raises, _on_session_end never raises); scope-discipline (no classifier logic changes, no heuristic threshold changes, no other cron pipeline changes beyond the session SELECT filter).

    7. **Mandatory `## Manual UAT gate` section** with VERBATIM the string:

       `Phase 6 cannot be marked verified until 06-HUMAN-UAT.md G-01, G-02, AND G-03 result move from "failed" to "pass"`

       Then a numbered-list operator runbook (mirror the shape of 06-03-SUMMARY.md's "Manual UAT gate" section):
       1. Pull `gsd/phase-6-uat` to Mac Studio: `git pull origin gsd/phase-6-uat`.
       2. Re-run install (idempotent): `bash examples/setup-local.sh`. Confirm `~/.hermes/state/revenium/markers/.ready/` directory exists with mode 0o700 (or owner-only).
       3. Restart Hermes gateway: `hermes gateway restart` — confirm plugin still loads.
       4. Drive a substantive turn via CLI: `hermes chat -q "Review src/foo.py for race conditions"`. Confirm:
          - `~/.hermes/state/revenium/markers/<sid>.jsonl` contains a GUARDRAIL+CHAT pair with non-`unclassified` task_type (G-01 + G-02 verification, preserved from UAT round 3).
          - `~/.hermes/state/revenium/markers/.ready/<sid>` exists (sentinel written).
       5. Wait for the next cron tick (~60s); confirm `~/.hermes/state/revenium/revenium-metering.log` shows `revenium meter completion` invoked with `--task-type <meaningful-label>` (NOT `--task-type unclassified`). This retires G-03.
       6. (Optional) For the long-running-session edge case: drive a substantive turn that takes > 120s (the settle window). Confirm the cron tick that lands DURING the in-flight session DOES skip with `awaiting plugin sentinel (age=Ns < settle=120s)` log line; confirm the next tick AFTER on_session_end fires DOES ship the meaningful task_type.
       7. (Optional) For the plugin-failure edge case: temporarily mis-configure the plugin (e.g., remove from `plugins.enabled` in `~/.hermes/config.yaml`), drive a substantive turn, wait for `started_at + 120s`, confirm the cron eventually reports the session with `--task-type unclassified` (D-18 safety-net default).

       Closing paragraph: "Until UAT round 4 records G-03 result = `pass` in `06-HUMAN-UAT.md`: `06-VERIFICATION.md` frontmatter remains `status: requires_rerun_uat`; `re_verification.gaps_remaining` still lists `[G-03]`; Phase 6 Progress Table row stays at `3/4 In progress — G-03 gap closure planned in 06-04-PLAN.md` (NOT `Verified`). The Phase 6 row transitions to `4/4 Verified` only after operator UAT round 4 retires G-03. Note that G-01 + G-02 are already operator-confirmed (UAT round 3, recorded in 06-HUMAN-UAT.md); they appear in the verbatim UAT-4 gate string above for traceability — UAT round 4 confirms the full chain by retiring the remaining gap."

    8. `## Backward compatibility` section — preserve the standard D-17 + D-18 zero-diff invariants framing. Note: D-18 (no-marker fallthrough → --task-type unclassified) STILL applies for aged-no-sentinel sessions (the safety-net path). New invariant: D-21 (sentinel + settle-window) layers on top of D-18 — sentinels accelerate the happy path, the age check preserves the legacy behavior for plugin-absent installs.

    9. `## Self-Check` section — verify the 8 files were edited, the 8 commits landed, the 6 new tests pass, the test suite is green, the VERIFICATION.md status is still `requires_rerun_uat`, the `gaps_remaining: [G-03]` is unchanged.

    LEGACY BRANDING: every line of this summary must be clean. (`.planning/` is excluded from the legacy-branding test, but the shipping-artifacts test must still pass.)

    The `## Manual UAT gate` section's lead sentence (item 7 above) is load-bearing — the quality-gate self-check asserts the literal string `Phase 6 cannot be marked verified until 06-HUMAN-UAT.md G-01, G-02, AND G-03 result move from "failed" to "pass"` is present in the file. The presence of G-01 + G-02 + G-03 in the gate string is the traceability anchor — it documents that ALL three closures must be operator-confirmed before Phase 6 is Verified, even though G-01 and G-02 were already retired in UAT round 3.
  </action>
  <verify>
    <automated>cd /Users/johndemic/Development/projects/revenium/hermes-revenium &amp;&amp; test -f .planning/phases/06-mechanical-classification-agent-end-hook/06-04-SUMMARY.md &amp;&amp; grep -q 'plan: 04' .planning/phases/06-mechanical-classification-agent-end-hook/06-04-SUMMARY.md &amp;&amp; grep -q 'HOOK-13' .planning/phases/06-mechanical-classification-agent-end-hook/06-04-SUMMARY.md &amp;&amp; grep -q 'MARKERS_READY_DIR\|sentinel\|settle-window' .planning/phases/06-mechanical-classification-agent-end-hook/06-04-SUMMARY.md &amp;&amp; grep -F 'Phase 6 cannot be marked verified until 06-HUMAN-UAT.md G-01, G-02, AND G-03 result move from "failed" to "pass"' .planning/phases/06-mechanical-classification-agent-end-hook/06-04-SUMMARY.md &amp;&amp; grep -q 'Manual UAT gate' .planning/phases/06-mechanical-classification-agent-end-hook/06-04-SUMMARY.md &amp;&amp; python3 -m unittest tests.test_repository.RepositoryTests.test_no_legacy_branding_left -v 2>&amp;1 | tail -3 | grep -E '^OK( |$)' &amp;&amp; python3 -m unittest discover -s tests -p 'test_*.py' -v 2>&amp;1 | tail -3 | grep -E '^OK( |$)'</automated>
  </verify>
  <acceptance_criteria>
    06-04-SUMMARY.md exists with the canonical frontmatter shape (plan: 04, requirements-completed includes HOOK-13, key-files modified/created lists); body contains the load-bearing "Manual UAT gate" section with the VERBATIM string `Phase 6 cannot be marked verified until 06-HUMAN-UAT.md G-01, G-02, AND G-03 result move from "failed" to "pass"`; operator runbook documents (a) sentinel directory presence check, (b) CLI substantive turn produces marker + sentinel, (c) next cron tick ships meaningful task_type (NOT unclassified) as the test that retires G-03, (d) optional long-running-session and plugin-failure edge cases; full test suite still green; legacy branding test still green.
  </acceptance_criteria>
  <requirements_satisfied>HOOK-13 (summary trace — the artifact that retro-documents the closure narrative). Phase 6 manual-UAT gate explicitly referenced for all three gaps (G-01, G-02, G-03) — UAT round 4 retires G-03 while preserving the traceability anchor for the full Phase 6 closure.</requirements_satisfied>
  <done>06-04-SUMMARY.md landed with the full closure narrative; UAT-4 gate is unambiguously flagged for G-01, G-02, AND G-03; full test suite still green.</done>
  <commit_message>docs(06): summarize 06-04 — G-03 closure via plugin sentinel + cron settle-window; UAT-4 gate references all 3 gaps</commit_message>
</task>

</tasks>

<verification>

After all 8 tasks complete:

1. **Full test suite green** — `python3 -m unittest discover -s tests -p 'test_*.py' -v` passes with the 6 new HOOK-13 tests (5 unit + 1 end-to-end) added to the existing 16 HOOK-* tests from 06-02 + 06-03 (12 + 4 = 16). The exact total is not load-bearing; the load-bearing assertion is OK status + no regressions.

2. **Per-commit green-test mandate** — every one of the 8 commits leaves the test suite green. T01's commit (the path constant) is a no-op for any existing code path. T02 adds the sentinel write but no consumer reads it yet — backward-compatible. T03 starts honoring sentinels but the safety-net (started_at >= SETTLE_SECONDS) ensures the existing test fixtures that don't set sentinels still ship (they synthesize aged or recently-aged sessions and rely on the existing pre-G-03 behavior; the new filter passes them through after SETTLE_SECONDS, but the existing tests don't time-gate so the integration is bounded). Verify that the existing `test_cron_marker_split_end_to_end` and other Phase 3 cron tests still pass — their fixtures synthesize sessions with various `started_at` values; the filter must not falsely skip them. If the existing fixtures use `started_at = 0` (epoch) or anything > 120s old, they pass through unmodified. If any existing fixture seeds a session with `started_at = now`, that fixture will need a sentinel — but the existing fixtures should already use historical timestamps for test determinism.

3. **G-03 code-side closure recorded** — `06-VERIFICATION.md::gaps[G-03].gap_closure` is `executed`, `gap_closure_plan` is `06-04-PLAN.md`, and a `gap_closure_outcome` paragraph describes the sentinel + cron-filter fix. `re_verification.gaps_closed: [G-01, G-02, G-03]` reflects the code-side closure.

4. **Phase status preservation** — `06-VERIFICATION.md::status: requires_rerun_uat` is unchanged. `re_verification.gaps_remaining: [G-03]` is unchanged. The phase is NOT marked Verified by this plan; that happens only after UAT round 4 retires G-03 in `06-HUMAN-UAT.md`.

5. **Manual UAT-4 gate language** — `06-04-SUMMARY.md` contains the verbatim string `Phase 6 cannot be marked verified until 06-HUMAN-UAT.md G-01, G-02, AND G-03 result move from "failed" to "pass"`.

6. **Out-of-scope discipline** — no edits to `classifier.py` LOGIC (only one-line constant addition), no edits to setup-local.sh, references/setup.md, SKILL.md, split_strategies.py, cron.sh, task-taxonomy.json, HOOK-01..HOOK-12 entry strings, or any of the existing 16 HOOK-* test methods (other than the implicit "still pass" assertion).

7. **D-04 invariants preserved** — `_on_session_end` returns None on every code path; the new `_write_sentinel` helper has its own try/except Exception belt and NEVER raises. `_count_tools_in_current_turn` (06-03's helper) still returns int on every code path.

8. **D-21 invariants pinned** — five unit tests + one end-to-end test prove the sentinel-write happy-path, sentinel-write error-path, cron filter skip-recent-no-sentinel, cron filter include-aged-no-sentinel, cron filter include-any-age-with-sentinel, and end-to-end (marker + sentinel → meaningful --task-type) semantics. The end-to-end test is the CI regression guard that would have caught G-03 on 2026-05-14.

9. **Path-discipline single-source-of-truth invariant preserved** — `MARKERS_READY_DIR` lives in `common.sh` (canonical) and is mirrored in `classifier.py` via env-override-with-fallback; `__init__.py` imports it from `.classifier` (NOT inlining the path); `hermes-report.sh` reads it via env (sourced via common.sh).

</verification>

<success_criteria>

- 06-04-PLAN.md (this file) exists at `.planning/phases/06-mechanical-classification-agent-end-hook/06-04-PLAN.md` with `gap_closure: true` in the frontmatter.
- 8 atomic commits landed (one per task), each leaving the full test suite green.
- `common.sh` declares `MARKERS_READY_DIR` and includes it in the `mkdir -p` line.
- `classifier.py` exports `MARKERS_READY_DIR` as a Path constant alongside `MARKERS_DIR` (one-line addition; no logic change).
- `__init__.py` writes a per-session sentinel via `_write_sentinel` helper in both the happy-path try block AND the error-path except handler; `_write_sentinel` itself has D-04 belt (try/except + log warning + swallow).
- `hermes-report.sh` filters its session SELECT by `(sentinel exists for sid) OR (started_at < now - SETTLE_SECONDS)` via a stdlib Python heredoc inserted between the SELECT and the while-loop.
- 5 new unit tests + 1 new end-to-end test added to `tests/test_repository.py`.
- `test_runtime_paths_are_hermes_native` extended with the MARKERS_READY_DIR assertion.
- HOOK-13 added to REQUIREMENTS.md (entry + traceability row + Coverage 49→50).
- ROADMAP Phase 6 Requirements line includes HOOK-13; SC5 wording adds the cron-sentinel-or-aged invariant; Plans list adds 06-04-PLAN.md; Categories HOOK (12)→(13); Progress Table row reflects in-progress 06-04 (3/4).
- 06-VERIFICATION.md G-03 entry: `gap_closure: executed`, `gap_closure_plan: 06-04-PLAN.md`, new `gap_closure_outcome` field. Frontmatter `status: requires_rerun_uat` PRESERVED. `gaps_remaining: [G-03]` PRESERVED. `gaps_closed: [G-01, G-02, G-03]` reflects the code-side closure.
- 06-04-SUMMARY.md exists with verbatim "Phase 6 cannot be marked verified until 06-HUMAN-UAT.md G-01, G-02, AND G-03 result move from \"failed\" to \"pass\"".
- All 8 commits' pre-commit + post-commit `python3 -m unittest discover -s tests -p 'test_*.py' -v` runs return OK.
- `test_no_legacy_branding_left` passes against every edited shipping file.

</success_criteria>

<output>

After completion, this plan produces:

- 4 modified production files (`skills/revenium/scripts/common.sh`, `skills/revenium/scripts/hermes-report.sh`, `skills/revenium/plugins/revenium-classifier/__init__.py`, `skills/revenium/plugins/revenium-classifier/classifier.py`)
- 1 modified test file (`tests/test_repository.py`) — 6 new tests appended + 2 new assertions in `test_runtime_paths_are_hermes_native`, 0 existing tests modified
- 3 modified planning artifacts (`.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/phases/06-mechanical-classification-agent-end-hook/06-VERIFICATION.md`)
- 1 created planning artifact (`.planning/phases/06-mechanical-classification-agent-end-hook/06-04-SUMMARY.md`)

The plan closes G-03 by code and pins the regression in CI. The phase remains at `status: requires_rerun_uat` until operator UAT round 4 on Mac Studio retires G-03 (G-01 + G-02 were already operator-confirmed in UAT round 3 but appear in the UAT-4 gate string for full-Phase-6 traceability). The Phase 6 row in ROADMAP's Progress Table will move to `4/4 Verified` only after that operator pass.

</output>
