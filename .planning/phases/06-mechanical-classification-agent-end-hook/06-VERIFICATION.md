---
phase: 06-mechanical-classification-agent-end-hook
verified: 2026-05-13T18:59:09Z
updated: 2026-05-13T19:38:00Z
status: gaps_found
score: 11/11 must-haves verified (automated); operator UAT on Mac Studio surfaced a Phase 6 goal gap (see 06-HUMAN-UAT.md G-01)
overrides_applied: 0
gaps:
  - id: G-01
    severity: high
    summary: "agent:end hook does not fire for non-platform-served sessions (CLI `hermes chat -q`, cron-ticker invocations). The Phase 6 goal ('a Hermes lifecycle hook deterministically writes a marker record ... independent of whether the agent loaded the revenium skill or executed FINAL ACTION') is not achieved for CLI sessions, which is the dominant dev-time path and the original adoption-gap Phase 6 was created to close. Evidence: two CLI substantive turns on 2026-05-13T19:32-19:33 produced no marker file and no hook-related log entries despite the hook being loaded at gateway startup. See 06-HUMAN-UAT.md for full evidence."
    relates_to: [HOOK-01, HOOK-02, HOOK-05, HOOK-06, SC1, SC2, SC6]
re_verification:
  previous_status: human_needed
  previous_score: 11/11 must-haves verified (automated)
  gaps_closed: []
  gaps_remaining: [G-01]
  regressions: []
human_verification:
  - test: "Operator-side UAT — load hook, restart gateway, exercise a fresh substantive Hermes session WITHOUT loading the revenium skill, and confirm marker pair appears with a non-`unclassified` task_type."
    expected: |
      (1) `bash examples/setup-local.sh` succeeds and reports `Installed hook to ~/.hermes/hooks/revenium-classifier`.
      (2) `hermes gateway restart` succeeds.
      (3) Gateway startup log emits: `[hooks] Loaded hook 'revenium-classifier' for events: ['agent:end']`.
      (4) After a substantive turn in a fresh Hermes session (no skill_view("revenium"), no manual classification), `~/.hermes/state/revenium/markers/<sid>.jsonl` contains a GUARDRAIL+CHAT marker pair with a meaningful (non-`unclassified`) `task_type`.
      (5) On the next cron tick, `revenium meter completion` is invoked with `--task-type <meaningful-label>` (not `unclassified`).
    why_human: |
      Requires a running Hermes gateway process, a real substantive turn through an LLM, and Revenium CLI configuration — cannot be exercised in CI. This is the manual UAT gate the SUMMARY.md flags before Phase 6 is marked Verified (see "Manual UAT gate" in 06-01-SUMMARY.md and ROADMAP SC1/SC2/SC6).
  - test: "Subagent inheritance UAT — drive a `delegate_task`-style child session and confirm the child's marker file inherits the parent's `task_type`."
    expected: |
      A subagent session whose `state.db.sessions.parent_session_id` points at a root session with task_type=research produces a marker file at `~/.hermes/state/revenium/markers/<child-sid>.jsonl` carrying `task_type: research` on both records, with NO LLM call recorded against the subagent budget.
    why_human: |
      Hermes' subagent dispatch (delegate_task lineage, real state.db.sessions parent chain) cannot be exercised without running Hermes end-to-end. Synthetic test (`test_revenium_classifier_subagent_inherits`) proves the helpers correctly, but operator confirmation of real-world `parent_session_id` shape is required (ROADMAP SC3).
---

# Phase 6: Mechanical Classification via Hermes agent:end Hook Verification Report

**Phase Goal:** When a Hermes session — primary or subagent — completes a turn, a Hermes lifecycle hook deterministically writes a marker record at `~/.hermes/state/revenium/markers/<sid>.jsonl` with a meaningful `task_type` and `operation_type`, independent of whether the agent loaded the `revenium` skill or executed the FINAL ACTION self-classification code. Token attribution by activity in Revenium becomes observable without depending on agent compliance.
**Verified:** 2026-05-13T18:59:09Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

The plan's `must_haves.truths` block (11 invariants) — every truth verified against the actual codebase:

| #  | Truth                                                                                                                                              | Status              | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| -- | -------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1  | After setup-local.sh + gateway restart, ~/.hermes/hooks/revenium-classifier/ contains HOOK.yaml + handler.py and gateway log emits load line       | VERIFIED (partial)  | `examples/setup-local.sh:27-33` performs the copy; `hooks/revenium-classifier/HOOK.yaml` exists with valid name+events+description (Python YAML round-trip verified). The gateway-log emission cannot be observed in CI — covered by human verification item 1.                                                                                                                                                                              |
| 2  | Synthetic substantive agent:end fixture dispatched to handler.handle produces exactly two records (GUARDRAIL + CHAT) at markers/<sid>.jsonl        | VERIFIED            | `test_revenium_classifier_marker_pair` + `test_revenium_classifier_llm_label` both pass; both assert exactly 2 records with `{GUARDRAIL, CHAT}` operation_types and Phase-2-schema keys. Confirmed via end-to-end behavioral spot-check (substantive-turn.json → 2 records with task_type=code_review).                                                                                                                                          |
| 3  | Synthetic trivial agent:end fixture creates NO marker file                                                                                          | VERIFIED            | `test_revenium_classifier_trivial_skip` passes; the trivial-turn.json fixture (response=13 chars) plus an absent session jsonl yields `_count_tools_in_current_turn → 0` and short response → `handler.handle` returns before any write; test asserts `MARKERS_DIR/<sid>.jsonl` does NOT exist.                                                                                                                                                  |
| 4  | When state.db has parent→root with marker file carrying task_type=research, subagent's marker pair inherits and LLM is NOT called                  | VERIFIED (with WARN) | `test_revenium_classifier_subagent_inherits` passes; mock-asserts `call_llm.assert_not_called()`. Inheritance branch correctly walks chain via `_walk_to_root_session` (handler.py:57-76) and reads parent task_type via `_read_latest_task_type` (handler.py:112-130). **Warning:** the inheritance branch returns before the D-13 dedup check (CR-01) — see Warnings below. Happy path verified; CR-01 edge case noted.            |
| 5  | When budget-status.json carries halted:true, LLM is NOT called and marker pair carries task_type=unclassified with WARN log entry                  | VERIFIED            | `test_revenium_classifier_halt_unclassified` passes; mock-asserts `call_llm.assert_not_called()`; both records carry task_type=`unclassified`; WARN line observed in test output: `revenium-classifier: budget halted, wrote unclassified for sid=20260513_120100_testsubstantive`. Fail-open on missing file verified by `test_revenium_classifier_halt_failopen_on_missing_file`.                                                              |
| 6  | When call_llm returns a label in trivial blocklist or fails regex, marker pair carries task_type=unclassified                                       | VERIFIED            | `test_revenium_classifier_llm_blocklist_fallthrough` passes. Direct asserts on `_validate_label`: `"ack" → "unclassified"`, `"code-review" → "unclassified"` (hyphen violates regex), `"" → "unclassified"`, `"research" → "research"`, `"CODE_REVIEW" → "code_review"`. End-to-end: LLM mock returns `"thanks"` → marker file has only `"unclassified"`.                                                                                          |
| 7  | When marker file already contains a GUARDRAIL+CHAT pair within 30s of time.time(), the hook skips the write                                          | VERIFIED (partial)  | `test_revenium_classifier_dedupe` passes for the root-session path: pre-seeded fresh pair → `mock_llm.assert_not_called()` AND no new lines appended; aged-pair (120s) → hook does write 2 new lines. **Warning:** the subagent inheritance branch bypasses this dedup (CR-01 — `handle` lines 304-310 return before `_recent_marker_pair_exists` runs). For substantive non-subagent turns this truth is verified.                                |
| 8  | Hook handler NEVER raises out of handle() — every exception path is caught and logged                                                              | VERIFIED            | `test_revenium_classifier_never_raises` passes; exercises five distinct failure modes (call_llm raising, _write_marker_pair raising, wrong event_type, missing session_id, garbage object response). All paths fall through to logger.warning. Outer try/except at `handler.py:301-341` covers the body. D-04 invariant pinned.                                                                                                            |
| 9  | Full test suite passes (`python3 -m unittest discover -s tests -p 'test_*.py' -v`)                                                                  | VERIFIED            | Ran 26 tests in 3.994s → OK. 12 new HOOK-* test methods all pass. Pre-existing Phase 1-3 tests (skill frontmatter, runtime paths, marker schema, taxonomy schema, taxonomy atomic write, split conservation, S2 50/50 bias, halt-check anchor, prompt ordering, cron marker split end-to-end, etc.) all pass.                                                                                                                                  |
| 10 | references/setup.md carries `## Mechanical classification hook` section after `## How attribution works`                                            | VERIFIED            | `test_setup_md_has_mechanical_classification_hook_section` passes. Section is at line 86, after `## How attribution works` at line 73. Contains gateway-restart instruction (line 94), `~/.hermes/hooks/revenium-classifier/` path (line 88), subagent mention (line 90), and the "Do NOT use `hermes hooks list`" anti-pattern callout (line 116).                                                                                              |
| 11 | test_no_legacy_branding_left passes against every new file                                                                                          | VERIFIED            | Direct grep of `OpenClaw\|openclaw\|ClawHub\|clawhub` across all new files (HOOK.yaml, handler.py, test-payloads/*.json, setup.md additions, setup-local.sh) returns zero matches. `test_no_legacy_branding_left` passes in the full test run.                                                                                                                                                                                                |

**Score:** 11/11 must-haves verified by automated means; truth #1 and the Phase 6 ROADMAP SC1/SC2/SC3/SC6 manual operator path require human UAT to fully verify.

### Required Artifacts

| Artifact                                                                       | Expected                                                                              | Status     | Details                                                                                                                                                                                                                                                                                            |
| ------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `skills/revenium/hooks/revenium-classifier/HOOK.yaml`                          | Manifest matching `discover_and_load()` contract: name + description + events:[agent:end] | VERIFIED   | 4-line YAML file. `name: revenium-classifier`, `events: [agent:end]`, description present. `yaml.safe_load` round-trips with expected shape.                                                                                                                                                       |
| `skills/revenium/hooks/revenium-classifier/handler.py`                         | `async def handle` + 10 private helpers + module-level constants                       | VERIFIED   | 342 lines. All 10 expected helpers present (`_walk_to_root_session`, `_count_tools_in_current_turn`, `_read_latest_task_type`, `_recent_marker_pair_exists`, `_budget_halted`, `_read_taxonomy_labels`, `_classify_via_llm`, `_validate_label`, `_muid`, `_write_marker_pair`). `inspect.iscoroutinefunction(handle) == True`. |
| `skills/revenium/hooks/revenium-classifier/test-payloads/trivial-turn.json`    | session_id + short response                                                            | VERIFIED   | response=13 chars ("Good morning!"). Used by trivial_skip test.                                                                                                                                                                                                                                    |
| `skills/revenium/hooks/revenium-classifier/test-payloads/substantive-turn.json`| session_id + > 200 char response                                                       | VERIFIED   | response ≈ 365 chars (race-condition review prose). Used by halt/llm/dedupe tests.                                                                                                                                                                                                                  |
| `skills/revenium/hooks/revenium-classifier/test-payloads/subagent-turn.json`   | child-sid + state.db parent chain                                                      | VERIFIED   | session_id=20260513_120200_testchild + > 200 char response. Used by subagent_inherits scenario.                                                                                                                                                                                                     |
| `examples/setup-local.sh`                                                      | Copies hook into `~/.hermes/hooks/revenium-classifier/` idempotently + gateway-restart next-step line | VERIFIED   | Lines 22-33 add the hook block: prunes the stale `${TARGET_DIR}/hooks` duplicate (B1 fix), creates `${HOOKS_DIR}/revenium-classifier`, `rm -rf` + `cp -R` semantics, prints `Installed hook to ${HOOK_TARGET}`. Line 40 echoes `Restart Hermes gateway to load the classifier hook: hermes gateway restart`. |
| `skills/revenium/references/setup.md`                                          | `## Mechanical classification hook` section after `## How attribution works`           | VERIFIED   | Section at line 86, 31 lines, covers installation/restart/3 verification mechanisms/anti-pattern callout. Order assertion (hook section after attribution section) covered in test.                                                                                                              |
| `tests/test_repository.py`                                                     | Extended test_expected_files_exist + 12 new HOOK-* methods                              | VERIFIED   | 12 new methods discovered: `test_revenium_classifier_{trivial_skip, never_raises, dedupe, llm_label, llm_blocklist_fallthrough, halt_unclassified, halt_failopen_on_missing_file, subagent_inherits, walk_to_root, substantive_uses_session_jsonl_tool_count, marker_pair}` + `test_setup_md_has_mechanical_classification_hook_section`. |
| `.planning/REQUIREMENTS.md`                                                    | HOOK-01..HOOK-10 entries + traceability rows + coverage counts 37 → 47                  | VERIFIED   | Section "### Mechanical Classification Hook (Phase 6)" at line 76 contains 10 HOOK-* entries. Traceability table contains 10 HOOK-* rows. Coverage: `v1 requirements: 47 total`, `Mapped to phases: 47`, `Unmapped: 0`.                                                                                  |
| `.planning/ROADMAP.md`                                                         | Phase 6 Requirements line replaces 'TBD' with HOOK-01..HOOK-10; 06-01-PLAN.md tracked   | VERIFIED   | ROADMAP line 93: `Requirements: HOOK-01, HOOK-02, HOOK-03, HOOK-04, HOOK-05, HOOK-06, HOOK-07, HOOK-08, HOOK-09, HOOK-10`. Plan row marked `[x] 06-01-PLAN.md` at line 104. Categories line includes `HOOK (10)`. Phase 6 marked `[x]` at line 17. SC1/SC2 wording reflects Conflict C1/C2 fix.   |

### Key Link Verification

| From                                       | To                                                                       | Via                                                                                                                                                                       | Status   | Details                                                                                                                                                                                                                                                                                          |
| ------------------------------------------ | ------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `handler.py`                               | `~/.hermes/sessions/<sid>.jsonl`                                         | `_count_tools_in_current_turn` reads with `try/except OSError → 0`                                                                                                       | WIRED    | Lines 79-109. Reads `SESSIONS_DIR / f"{sid}.jsonl"`, scans backward for last `role:user`, counts `role:tool` after. Verified by `test_revenium_classifier_substantive_uses_session_jsonl_tool_count` (returns 2 for fixture with 2 tool entries).                                                |
| `handler.py`                               | `~/.hermes/state.db`                                                     | `_walk_to_root_session` uses `sqlite3.connect(f'file:{STATE_DB}?mode=ro', uri=True)` and recurses                                                                          | WIRED (partial) | Lines 57-76. Read-only URI mode + `WHERE id = ?` parameterized query + max_depth=10. **Warning:** depth-cap path returns `current` (mid-chain sid) not `sid` (input) — silent mis-attribution on cycles (CR-02). Happy path verified by `test_revenium_classifier_walk_to_root` Cases A/B/C.    |
| `handler.py`                               | `~/.hermes/state/revenium/budget-status.json`                            | `_budget_halted` reads file in try/except → returns False on any failure (fail-open per D-08)                                                                              | WIRED    | Lines 166-173. `json.loads(BUDGET_STATUS_FILE.read_text())` + `bool(data.get("halted", False))`. Verified by `test_revenium_classifier_halt_unclassified` (halted=True path) and `test_revenium_classifier_halt_failopen_on_missing_file` (missing file → False).                                  |
| `handler.py`                               | `~/.hermes/state/revenium/markers/<sid>.jsonl`                           | `_write_marker_pair` opens append-binary with `fcntl.LOCK_EX`, writes GUARDRAIL + CHAT under single lock                                                                  | WIRED    | Lines 267-291. `MARKERS_DIR.mkdir(mode=0o700)` + `open(marker_path, "ab", buffering=0)` + `fcntl.flock(f, fcntl.LOCK_EX)` + two `f.write()`. Verified by `test_revenium_classifier_marker_pair` (2 records, ≤1024 bytes each, 33-char hex muid, allow-listed keys).                                  |
| `handler.py`                               | `agent.auxiliary_client.call_llm`                                        | Module-level `try/except ImportError → call_llm = None`; invocation wraps with `await asyncio.to_thread(call_llm, messages=[...], temperature=0.0, max_tokens=64, timeout=10.0)`, NO `task=` argument | WIRED    | Lines 31-34 (lazy import) + 213-246 (async invocation). `test_revenium_classifier_llm_label` asserts `kwargs.get('task')` is absent, `temperature=0.0`, `max_tokens=64`.                                                                                                                          |
| `examples/setup-local.sh`                  | `~/.hermes/hooks/revenium-classifier/`                                   | `mkdir -p` + `rm -rf` + `cp -R` + `hermes gateway restart` echo line                                                                                                       | WIRED    | Lines 22-33 (hook install) + line 40 (echo line). B1 fix (prune of `${TARGET_DIR}/hooks` duplicate) at line 27. `bash -n` syntax check passes.                                                                                                                                                  |
| `tests/test_repository.py`                 | `skills/revenium/hooks/revenium-classifier/handler.py`                   | `sys.path.insert(str(HOOK_DIR))` + `import handler` + `asyncio.run(handler.handle(...))`; `unittest.mock.patch('handler.call_llm')`                                       | WIRED    | `_setup_hook_env` helper (lines 21-40) handles env redirect + sys.path + tmpdir. Re-import via `importlib.reload` on cached `handler` module ensures path constants pick up the redirected env. All 12 hook tests use this pattern.                                                                |
| `test_revenium_classifier_subagent_inherits` | tmp state.db with parent_session_id                                   | Reuse `build_state_db` shape with parent_session_id column; seed parent + child rows; seed parent marker file with task_type=research                                       | WIRED    | Lines 1325-1387. Creates state.db with `parent_session_id` column; inserts root + child rows; seeds root's marker file; asserts child inherits and LLM was not called.                                                                                                                            |

### Data-Flow Trace (Level 4)

| Artifact                       | Data Variable             | Source                                                                                              | Produces Real Data    | Status      |
| ------------------------------ | ------------------------- | --------------------------------------------------------------------------------------------------- | --------------------- | ----------- |
| `handler._write_marker_pair`   | `task_type`               | `_classify_via_llm` → `call_llm` (real LLM) OR `_read_latest_task_type` (parent marker) OR `unclassified` (halt/blocklist/error) | Yes (with LLM) / inherited / static-on-fallback | FLOWING     |
| `handler._classify_via_llm`    | `prompt`                  | `_build_classification_prompt` from `context['message']` + `response_preview` + `_read_taxonomy_labels()` | Yes — pulls live taxonomy from disk, real turn content from `context` payload | FLOWING     |
| `handler._read_latest_task_type` | inherited `task_type`     | `MARKERS_DIR / f"{root_sid}.jsonl"` — actual parent marker file written by previous hook invocation | Yes — reads real marker file | FLOWING     |
| `handler._budget_halted`       | `halted` boolean          | `BUDGET_STATUS_FILE` — written by the cron `budget-check.sh` script (existing infra)                | Yes — reads real cron-maintained status file | FLOWING     |

No hollow props or static-only data sources detected. The handler's outputs flow from real disk state + live LLM call + payload context — no hardcoded labels survive validation.

### Behavioral Spot-Checks

| Behavior                                                                                            | Command                                                                                       | Result                                                                                                                                                                          | Status |
| --------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| Hook module imports cleanly + all expected helpers/constants present + `handle` is async            | `python3 -c "...import handler; ..."`                                                         | All 9 helpers exist; LABEL_RE pattern matches expected; TRIVIAL_BLOCKLIST has 6 elements; `iscoroutinefunction(handle)=True`; `_muid()` produces 33-char hex                       | PASS   |
| HOOK.yaml is valid YAML with name=revenium-classifier, events=[agent:end], description present     | `python3 -c "import yaml; d=yaml.safe_load(open(...HOOK.yaml));..."`                          | name='revenium-classifier'; events=['agent:end']; description begins "Classifies each Hermes agent:end turn..."                                                                  | PASS   |
| End-to-end synthetic substantive turn produces GUARDRAIL+CHAT marker pair with mocked code_review LLM | Direct invocation of `asyncio.run(handler.handle("agent:end", context))` with `substantive-turn.json` and mocked call_llm | Marker file exists at `MARKERS_DIR/20260513_120100_testsubstantive.jsonl`; exactly 2 lines; `{GUARDRAIL, CHAT}` ops; both records carry `task_type=code_review`; muids are 33-char hex | PASS   |
| Full test suite passes                                                                              | `python3 -m unittest discover -s tests -p 'test_*.py' -v`                                     | Ran 26 tests in 3.994s — OK. 12 HOOK-* tests + 14 pre-existing Phase 1-3 tests all pass.                                                                                         | PASS   |
| D-17/D-18 zero-diff invariants                                                                       | `git diff aba6d0c -- skills/revenium/SKILL.md skills/revenium/scripts/hermes-report.sh skills/revenium/scripts/split_strategies.py skills/revenium/scripts/cron.sh skills/revenium/scripts/common.sh skills/revenium/task-taxonomy.json` | 0 lines of diff                                                                                                                                                                  | PASS   |
| All Phase-6-modified files free of unresolved debt markers (TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER)    | grep across handler.py, HOOK.yaml, setup-local.sh, setup.md additions, test_repository.py    | Zero matches                                                                                                                                                                     | PASS   |
| `bash -n examples/setup-local.sh` passes                                                            | `bash -n examples/setup-local.sh`                                                              | OK                                                                                                                                                                                | PASS   |
| Gateway startup log emission `[hooks] Loaded hook 'revenium-classifier' for events: ['agent:end']` observed in a real Hermes gateway | (operator must run `hermes gateway restart` and grep the gateway log)                         | Cannot run in CI                                                                                                                                                                  | SKIP — routed to human verification |
| Real subagent turn (delegate_task lineage) inherits parent task_type in production state.db          | Operator must dispatch a delegate_task, then read child sid's marker file                     | Cannot run in CI                                                                                                                                                                  | SKIP — routed to human verification |

### Probe Execution

No probes declared in PLAN/SUMMARY for Phase 6. Phase 6 is a hook subsystem phase, not a migration phase; the gating is the full test suite which passes 26/26.

| Probe       | Command       | Result                  | Status        |
| ----------- | ------------- | ----------------------- | ------------- |
| (none)      | n/a           | n/a                     | N/A           |

### Requirements Coverage

| Requirement | Source Plan      | Description (truncated)                                                                                              | Status      | Evidence                                                                                                                                                                                                                                                                                                                          |
| ----------- | ---------------- | -------------------------------------------------------------------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| HOOK-01     | 06-01-PLAN.md    | HOOK.yaml + handler.py ship at canonical path; handler exposes `async def handle(event_type, context)`               | SATISFIED   | Both files exist; HOOK.yaml has `name`, `events: [agent:end]`, `description`; handler.py has `async def handle` (verified `inspect.iscoroutinefunction`). Gateway-log verification routed to human UAT.                                                                                                                              |
| HOOK-02     | 06-01-PLAN.md    | Heuristic skip-fast-path: 0 tools AND len(response) < 200 → handler returns without write                            | SATISFIED   | `handler.py:312-316`. Pinned by `test_revenium_classifier_trivial_skip` + `test_revenium_classifier_substantive_uses_session_jsonl_tool_count`.                                                                                                                                                                                  |
| HOOK-03     | 06-01-PLAN.md    | Walk state.db.parent_session_id read-only, depth-capped at 10; subagent inherits root's task_type; LLM NOT called    | SATISFIED (with WARN) | `handler.py:57-76` (walk) + `:112-130` (read latest) + `:302-310` (handle inheritance branch). Pinned by `test_revenium_classifier_subagent_inherits` + `test_revenium_classifier_walk_to_root`. **CR-02 noted**: depth-cap returns mid-chain sid; only `assertIsInstance(result, str)` covers that case (silent mis-attribution risk on cycles). |
| HOOK-04     | 06-01-PLAN.md    | Handler reads budget-status.json; halted:true → LLM skipped, write task_type=unclassified + WARN; fail-open on missing file | SATISFIED   | `handler.py:166-173` (`_budget_halted`) + `:322-328` (handle gate). Pinned by `test_revenium_classifier_halt_unclassified` + `test_revenium_classifier_halt_failopen_on_missing_file`. WARN observed in test output.                                                                                                            |
| HOOK-05     | 06-01-PLAN.md    | Lazy import call_llm; invoke via asyncio.to_thread without `task=`; validate against regex + TRIVIAL_BLOCKLIST       | SATISFIED   | `handler.py:31-34` (lazy import) + `:213-259` (invocation + validate). Pinned by `test_revenium_classifier_llm_label` (asserts `task` kwarg absent) + `test_revenium_classifier_llm_blocklist_fallthrough`.                                                                                                                       |
| HOOK-06     | 06-01-PLAN.md    | Exactly 2 markers per substantive turn (GUARDRAIL + CHAT) via O_APPEND + fcntl.LOCK_EX; Phase 2 schema; muid 33-char hex; < 1024 bytes | SATISFIED   | `handler.py:267-291`. Pinned by `test_revenium_classifier_marker_pair` (asserts 2 records, GUARDRAIL+CHAT, 33-char hex muid, allow-listed keys, < 1024 bytes/line).                                                                                                                                                              |
| HOOK-07     | 06-01-PLAN.md    | Tail-check 30s: if recent GUARDRAIL+CHAT pair exists, skip write                                                     | SATISFIED (with WARN) | `handler.py:133-163` (`_recent_marker_pair_exists`) + `:319-320` (handle dedup branch). Pinned by `test_revenium_classifier_dedupe`. **CR-01 noted**: subagent inheritance branch returns BEFORE this check, allowing a hook double-write when SKILL.md FINAL ACTION runs inside a subagent. Default substantive (non-subagent) path verified. |
| HOOK-08     | 06-01-PLAN.md    | examples/setup-local.sh installs hook into ~/.hermes/hooks/ on every run + documents `hermes gateway restart`        | SATISFIED   | `examples/setup-local.sh:22-33` (install block with B1 prune) + line 40 (gateway restart echo). `bash -n` passes.                                                                                                                                                                                                              |
| HOOK-09     | 06-01-PLAN.md    | test_repository.py extends `test_expected_files_exist` + 6+ new HOOK-* test methods; tempdir + env redirect pattern  | SATISFIED   | 5 new hook files added to `test_expected_files_exist` list (lines 76-81); 12 new test methods added (exceeds 6+ lower bound). `_setup_hook_env` / `_restore_hook_env` helpers (lines 21-52) match the cron-test tempdir + env-var redirect pattern.                                                                              |
| HOOK-10     | 06-01-PLAN.md    | references/setup.md has `## Mechanical classification hook` section AFTER `## How attribution works` + Conflict C1/C2 callout | SATISFIED   | Section at line 86 (after attribution at line 73); covers install path, gateway restart, gateway-log verification line, and `Do NOT use hermes hooks list` anti-pattern (line 116). Pinned by `test_setup_md_has_mechanical_classification_hook_section`.                                                                       |

**Coverage: 10/10 HOOK-* requirements satisfied.** Two requirements (HOOK-03, HOOK-07) carry WARNINGs from the code review on specific edge cases (depth-cap cycle handling; subagent + agent same-turn race). No requirements ORPHANED. No requirements BLOCKED.

### Anti-Patterns Found

| File                                                          | Line     | Pattern                                                          | Severity | Impact                                                                                                                                                                                                              |
| ------------------------------------------------------------- | -------- | ---------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| (none)                                                        | n/a      | TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER                              | n/a      | Zero unresolved debt markers found across handler.py, HOOK.yaml, test-payloads/*.json, examples/setup-local.sh, references/setup.md additions, tests/test_repository.py.                                                |
| `handler.py:65-72`                                            | 65-72    | Returns `current` (mid-chain sid) when depth cap trips           | WARNING  | CR-02 — silent mis-attribution on `parent_session_id` cycles. Test (`test_revenium_classifier_walk_to_root` Case D) only checks `assertIsInstance(result, str)` — does NOT verify cycle detection. Edge case, low risk in practice. |
| `handler.py:301-310`                                          | 301-310  | Subagent inheritance branch bypasses `_recent_marker_pair_exists` check | WARNING  | CR-01 — if SKILL.md FINAL ACTION runs inside a subagent that the hook then classifies, the hook can produce a second pair → 4 records → double-attribution. Cron's per-muid wire-level dedup is the safety net.                                          |
| `handler.py:86-89, 119-121, 141-144`                          | (several) | Narrow `OSError`-only catch (no UnicodeDecodeError handling)     | INFO     | WR-02 — non-UTF-8 bytes in a session jsonl would raise UnicodeDecodeError out of the helper. Caught by outer D-04 belt; downstream effect is "unclassified" instead of "trivial-skip". Very unlikely in practice.                                                  |
| `handler.py:38-44`                                            | 38-44    | Path constants duplicated from common.sh with no parity test     | INFO     | WR-05 — handler.py's `MARKERS_DIR`, `STATE_DIR`, etc. mirror common.sh but no test enforces parity. Future common.sh path rename would silently diverge.                                                                                                       |
| `handler.py:133-163`                                          | 133-163  | `_recent_marker_pair_exists` performs lock-free read against an EX-locked writer | INFO     | WR-04 — splitlines() tolerates truncated final line; failure mode is "miss a fresh pair → write duplicate", caught by cron per-muid dedup.                                                                                                                  |
| `handler.py:226-236`                                          | 226-236  | `timeout=10.0` is a call_llm kwarg only — no `asyncio.wait_for` enforcement | INFO     | WR-08 — if upstream `call_llm` ignores its own timeout, the hook could stall.                                                                                                                                                                                |
| `handler.py:147-163`                                          | 147-163  | Partial-pair race uncovered — only fresh GUARDRAIL with no CHAT triggers double-write | INFO     | WR-06 — `test_revenium_classifier_dedupe` does NOT cover the partial-pair case.                                                                                                                                                                              |
| `examples/setup-local.sh:31-32`                               | 31-32    | `rm -rf` + `cp -R` runs without checking whether gateway is running | INFO     | WR-07 — destructive operation, but `hermes gateway restart` is in the Next Steps echo. Acceptable for v1.                                                                                                                                                    |

**Categorization:** 0 BLOCKERS. 2 WARNINGs (CR-01, CR-02). 7 INFOs from code review.

### Human Verification Required

Two items requiring operator UAT before Phase 6 is fully marked Verified (matches the SUMMARY's "Manual UAT gate before Phase 6 is marked Verified" statement and ROADMAP SC1/SC2/SC3/SC6):

#### 1. Gateway Startup + Substantive Turn UAT (ROADMAP SC1, SC2, SC6)

**Test:** On the Mac Studio, run:
```bash
bash examples/setup-local.sh
hermes gateway restart
# Then start a fresh Hermes session, ask a substantive question that requires
# actual work (e.g., "Review src/foo.py for race conditions") — do NOT load
# the revenium skill, do NOT execute any FINAL ACTION classification code.
```

**Expected:**
1. `bash examples/setup-local.sh` reports `Installed hook to ~/.hermes/hooks/revenium-classifier`.
2. `hermes gateway restart` succeeds.
3. Gateway startup log emits: `[hooks] Loaded hook 'revenium-classifier' for events: ['agent:end']`.
4. After the substantive turn yields back, `~/.hermes/state/revenium/markers/<sid>.jsonl` contains a GUARDRAIL+CHAT marker pair with a meaningful (non-`unclassified`) `task_type` matching the live taxonomy.
5. The next cron tick (within 60s) invokes `revenium meter completion` with `--task-type <meaningful-label>` (not `unclassified`).

**Why human:** Requires a running Hermes gateway process, a real LLM-driven substantive turn, and Revenium CLI configuration. Cannot be exercised in CI. This is the operator UAT gate explicitly called out in 06-01-SUMMARY.md.

#### 2. Subagent Inheritance UAT (ROADMAP SC3)

**Test:** Dispatch a `delegate_task`-style subagent turn from a parent Hermes session whose marker file already carries a real task_type, then inspect the child sid's marker file.

**Expected:**
1. `state.db.sessions.parent_session_id` for the child row is set to the root sid (verified via `sqlite3 ~/.hermes/state.db "SELECT id, parent_session_id FROM sessions WHERE id = '<child-sid>'"`).
2. `~/.hermes/state/revenium/markers/<child-sid>.jsonl` exists.
3. Both records in the child file carry the SAME `task_type` as the most recent valid record in `~/.hermes/state/revenium/markers/<root-sid>.jsonl`.
4. No new LLM call was charged against the subagent budget (operator can compare Revenium-side billing for the parent vs. child time windows).

**Why human:** Hermes' `delegate_task` lineage, real `state.db.sessions.parent_session_id` writing, and Revenium billing observability cannot be exercised in CI. Synthetic test pins the helper-function correctness; operator confirms production behavior.

### Gaps Summary

No BLOCKERS — every plan-declared must-have truth is observable in the codebase and verified by either tests or behavioral spot-checks. All 10 HOOK-* requirements have implementation evidence; all 26 unit tests pass; D-17/D-18 zero-diff backward-compat invariants hold; no debt markers in modified files; no legacy branding.

Two WARNINGs (CR-01 and CR-02 from 06-REVIEW.md) flag correctness bugs in the subagent inheritance branch and the depth-cap path of `_walk_to_root_session`. These do NOT block the phase goal under normal operation — the happy-path subagent flow inherits correctly, and depth-cap-overflow / parent-chain cycles are anomalous data conditions. Both are documented in the Anti-Patterns table for follow-up consideration in a Phase 6.5 / Phase 7 hardening pass.

The phase status is `human_needed` because two ROADMAP success criteria (SC1: gateway-load log emission; SC2/SC3/SC6: production substantive + subagent UAT) cannot be verified without a running Hermes gateway and a real LLM turn. The SUMMARY explicitly flags this as the "Manual UAT gate before Phase 6 is marked Verified."

---

_Verified: 2026-05-13T18:59:09Z_
_Verifier: Claude (gsd-verifier)_
