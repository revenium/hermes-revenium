---
phase: 04-wire-enrichment
plan: 01
type: execute
wave: 1
status: ready
depends_on: []
files_modified:
  - skills/revenium/scripts/hermes-report.sh
  - tests/test_repository.py
  - skills/revenium/references/setup.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
files_created:
  - .planning/phases/04-wire-enrichment/04-01-SUMMARY.md
autonomous: true
requirements:
  - WIRE-01
  - WIRE-02
  - WIRE-03
  - WIRE-04
  - COMPAT-01
tags:
  - wire-enrichment
  - argv-passthrough
  - operation-type-default
  - provider-regression

must_haves:
  truths:
    - "WIRE-01 (D-22 gate DISCHARGED — PASSES per 04-RESEARCH.md): the zero-marker fallthrough cmd array in `skills/revenium/scripts/hermes-report.sh` at line 635 emits `--operation-type \"CHAT\"` immediately AFTER the existing `--task-type \"unclassified\"` line. The pre-existing block comment at lines 610-612 (`Do NOT emit --operation-type: Phase 4 owns the WIRE-01 default decision`) is replaced with a single-line note that D-22 gate passed (Revenium server-side defaults operationType to CHAT — verified via 50+ historical records + CLI dry-run + cost parity). No other change to the fallthrough cmd array."
    - "COMPAT-01 atomic test flip — MUST land in the SAME commit as the WIRE-01 production change to preserve per-commit green: in `tests/test_repository.py::test_cron_marker_split_end_to_end` at line 614, the existing `self.assertNotIn('--operation-type', flags, 'zero-marker fallthrough must NOT emit --operation-type (Phase 4 WIRE-01 owns that decision)')` is replaced with `self.assertEqual(flags.get('--operation-type'), 'CHAT', 'zero-marker fallthrough must emit --operation-type CHAT (WIRE-01 / D-22)')`. The surrounding sub-case 2 structure (the only zero-marker sub-case in this test method) is unchanged. The other 36 tests in the suite continue to pass byte-unchanged."
    - "WIRE-02 + WIRE-03 pipe-format extension — MUST land atomically in ONE commit (Pitfall 1 from 04-RESEARCH.md: pipe-field count mismatch silently corrupts `d_cost`): in `skills/revenium/scripts/hermes-report.sh`, the split_rows Python heredoc at lines 517-534 is extended with `m_agent = marker.get('agent', '')` and `m_trace = marker.get('trace_id', '')` reads before the `print(...)`, AND the print statement is extended to append `|{m_agent}|{m_trace}` as fields 10 + 11. SIMULTANEOUSLY, the bash `while IFS='|' read` declaration at line 546-547 is extended to add `m_agent m_trace` as the last two read variables (BOTH the `local row muid ...` declaration AND the `while IFS='|' read -r muid ...` line). Both edits are part of the same atomic change — no intermediate state where the Python emits 11 fields but bash reads 9."
    - "WIRE-02 + WIRE-03 cmd-array fallback — same commit as the pipe extension: in the marker-driven cmd array at lines 564 and 566, `--agent \"Hermes\"` is replaced with `--agent \"${m_agent:-Hermes}\"` and `--trace-id \"${sid}\"` is replaced with `--trace-id \"${m_trace:-${sid}}\"`. Colon-dash (`:-`) is mandatory (Pitfall 3) so empty-string-from-pipe correctly triggers the fallback. The `--transaction-id \"${sid}-${total_tokens}-${muid}\"` line between them is UNCHANGED. Phase 4 ships the wire ready for upstream writers to populate `agent`/`trace_id`; D-23 explicitly defers any classifier.py / SKILL.md writer changes — in production today every marker falls back to `Hermes` / `${sid}` because no upstream writer populates these fields."
    - "WIRE-02 + WIRE-03 passthrough test (new test method `test_wire_agent_trace_passthrough` in `tests/test_repository.py::RepositoryTests`): seeds ONE session in synthetic state.db with `started_at = 1715514000.0` (bypasses G-03 sentinel filter per Pitfall 6), writes a GUARDRAIL+CHAT marker pair to `${MARKERS_DIR}/<sid>.jsonl` where BOTH markers carry `agent='revenium-skill'` and `trace_id='trace-abc-001'`, invokes `bash hermes-report.sh` via the stubbed-revenium harness, asserts every captured invocation's argv contains `--agent revenium-skill` AND `--trace-id trace-abc-001` (NOT the hardcoded fallbacks). Second sub-case in the same test method: rerun with a fresh tmpdir and the SAME marker pair but with the `agent` and `trace_id` keys omitted, assert every invocation carries `--agent Hermes` AND `--trace-id <sid>` (the colon-dash fallback). Both sub-cases use the identical `build_state_db` / `revenium` shim / `argv_to_flags` helpers already in the file."
    - "WIRE-04 8-provider regression test (new test method `test_wire_no_provider_regression_per_class` in `tests/test_repository.py::RepositoryTests`): single DRY loop over an 8-tuple PROVIDER_CASES list copied verbatim from 04-PATTERNS.md lines 280-298. Each iteration creates a fresh `tempfile.mkdtemp(prefix='gsd-wire-provider-e2e-')`, seeds ONE state.db row with that case's `billing_provider` + `model` (and `started_at = 1715514000.0`), writes a GUARDRAIL+CHAT marker pair (forcing the marker-driven split path so N=2 invocations per case), invokes the cron, and asserts every captured invocation carries the expected `--provider`, `--model`, and `--model-source` flags. The `meta` case is special: `billing_provider=''` (empty string) → asserts `--model-source` is ABSENT from the argv. `openrouter-special` case: `clean_model` strips `anthropic/` prefix → `--model claude-sonnet-4-5` AND `--provider anthropic` AND `--model-source openrouter`. `bedrock-special` case: `clean_model` strips `anthropic.` prefix → `--model claude-3-5-sonnet-20241022-v2:0` (the `:0` suffix is preserved) AND `--provider anthropic` AND `--model-source bedrock`. Descriptive `label` strings appear in every assertion message so CI output identifies the failing case."
    - "Pre-existing 37 tests continue to pass byte-unchanged: `python3 -m unittest discover -s tests -p 'test_*.py' -v` runs green end-to-end after EVERY commit in this plan. The 35 non-touched test methods (everything except `test_cron_marker_split_end_to_end`) are not modified. `test_no_legacy_branding_left`, `test_shell_scripts_have_valid_syntax`, `test_runtime_paths_are_hermes_native`, `test_skill_frontmatter_has_hermes_metadata` MUST pass against every file edited in this plan."
    - "D-25 attribution-semantics one-paragraph note appended to `skills/revenium/references/setup.md` inside the existing `## How attribution works` section (line 73 in the current file): verbatim text per CONTEXT.md `<specifics>` — `When markers carry different agent or trace_id values across a session, each Revenium meter call records the per-turn attribution; per-session aggregation happens dashboard-side.` No new section header; the paragraph attaches to the existing section's end. No other docs change."
    - "`.planning/REQUIREMENTS.md` updates: WIRE-01, WIRE-02, WIRE-03, WIRE-04, COMPAT-01 checkboxes (`- [ ]` → `- [x]`) under their respective subsections; traceability table rows for those 5 IDs move from `Pending` → `Verified (Phase 4)`. CRON-07 description prose is updated in place to reflect the WIRE-01 / D-22 discharge: the existing `with --task-type unclassified and no explicit --operation-type` text is replaced with `with --task-type unclassified and --operation-type CHAT (WIRE-01 / D-22 discharged in Phase 4)`. This is a one-line prose update, not a checkbox flip; CRON-07's checkbox state is unchanged (Phase 3 remains the owning phase per Traceability). No new requirements added — Phase 4 ships WIRE-01..04 + COMPAT-01 exactly. Coverage counts (50 total, 50 mapped) unchanged."
    - "`.planning/ROADMAP.md` Phase 4 row: `Plans: TBD` → `Plans: 1 plan`; Plans list under Phase 4 receives `- [ ] 04-01-PLAN.md — Wire enrichment (WIRE-01..04 + COMPAT-01) — pass-through agent/trace_id from markers, CHAT default for zero-marker fallthrough, 8-provider regression guard`; Progress Table Phase 4 row updates `0/0 / Not started / -` → `0/1 / Executed / 2026-05-14` after execute-phase lands (the planner writes the row with `0/1 In progress`; execute-plan transitions it to `1/1 Executed` on completion). SC1-SC4 wording is unchanged."
    - "Out-of-scope discipline (locked by CONTEXT.md D-22/D-23/D-24/D-25 + `<scope_locks>`): NO changes to `skills/revenium/plugins/revenium-classifier/classifier.py` (no upstream agent/trace_id population — D-23); NO changes to `skills/revenium/SKILL.md` FINAL ACTION snippet (D-23); NO changes to `skills/revenium/plugins/revenium-classifier/__init__.py`; NO new state files; NO new scripts; NO changes to `split_strategies.py`; NO changes to `common.sh`; NO changes to `setup-local.sh`; NO new entry in `examples/`; NO changes to ledger format or marker schema; NO changes to HOOK-01..HOOK-13 entry strings; the existing 12 HOOK-* tests + 4 HOOK-12 tests + 6 HOOK-13 tests preserved verbatim."
  artifacts:
    - path: "skills/revenium/scripts/hermes-report.sh"
      provides: "Three edits in a single file, two atomic commits. (1) Marker-driven split path: split_rows Python heredoc emits 11 pipe-delimited fields (fields 10-11 are `m_agent` + `m_trace` from `marker.get('agent', '')` / `marker.get('trace_id', '')`); bash `while IFS='|' read` declaration consumes those two extra fields; cmd array `--agent` and `--trace-id` lines use `${m_agent:-Hermes}` and `${m_trace:-${sid}}` colon-dash fallbacks. (2) Zero-marker fallthrough cmd array: adds `--operation-type \"CHAT\"` after the existing `--task-type \"unclassified\"` line. Block comment at lines 610-612 reduced to a one-line D-22 gate-discharged note. set -uo pipefail discipline preserved; no flag change."
      contains: "operation-type \"CHAT\""
    - path: "tests/test_repository.py"
      provides: "Two new test methods (`test_wire_agent_trace_passthrough`, `test_wire_no_provider_regression_per_class`) appended to `RepositoryTests`; one assertion update in `test_cron_marker_split_end_to_end` at line 614 (COMPAT-01 flip). Both new methods reuse the existing `build_state_db`/`run_cron`/`argv_to_flags`/`revenium` shim helpers already in the file (no new helpers; no module-level imports added)."
      contains: "test_wire_no_provider_regression_per_class"
    - path: "skills/revenium/references/setup.md"
      provides: "One paragraph appended inside the existing `## How attribution works` section documenting D-25 per-marker attribution semantics. Plain prose; no new section header; no new headings."
      contains: "per-turn attribution"
    - path: ".planning/REQUIREMENTS.md"
      provides: "5 checkbox flips (WIRE-01, WIRE-02, WIRE-03, WIRE-04, COMPAT-01) and 5 traceability table row state updates from Pending → Verified (Phase 4). One additional prose-only edit to CRON-07's description text replacing `no explicit --operation-type` with `--operation-type CHAT (WIRE-01 / D-22 discharged in Phase 4)` so REQUIREMENTS.md stays internally consistent with the WIRE-01 wire change. No new entries; no coverage count change."
      contains: "WIRE-01"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 4 row: Plans count finalized (1 plan); Plans list receives 04-01-PLAN.md entry; Progress Table row transitions Not started → In progress (and to Executed once execute-phase lands)."
      contains: "04-01-PLAN.md"
    - path: ".planning/phases/04-wire-enrichment/04-01-SUMMARY.md"
      provides: "Summary with frontmatter (phase, plan, type, requirements_completed, files_modified, files_created); body sections: Decisions Honored (D-22 gate discharge, D-23 cron pass-through only, D-24 8-case regression scaffolding, D-25 attribution semantics note), Key Files, Verification (test suite green, manual operator action description for end-to-end Mac Studio confirmation)."
      contains: "WIRE-01, WIRE-02, WIRE-03, WIRE-04, COMPAT-01"
  key_links:
    - from: "skills/revenium/scripts/hermes-report.sh (split_rows Python heredoc, lines 517-534)"
      to: "marker JSON `agent` and `trace_id` optional fields"
      via: "marker.get('agent', '') / marker.get('trace_id', '') reads emit empty string when absent; pipe-delimited print appends `|{m_agent}|{m_trace}` as fields 10-11 with no trailing pipe"
      pattern: "marker\\.get\\(['\"]agent['\"]"
    - from: "skills/revenium/scripts/hermes-report.sh (marker-driven cmd array, lines 564-566)"
      to: "m_agent / m_trace bash variables from while-read pipe consumption"
      via: "--agent \"${m_agent:-Hermes}\" and --trace-id \"${m_trace:-${sid}}\" — colon-dash fallback handles empty-string-from-pipe correctly (Pitfall 3)"
      pattern: "\\$\\{m_agent:-Hermes\\}"
    - from: "skills/revenium/scripts/hermes-report.sh (zero-marker fallthrough cmd array, line 635)"
      to: "Revenium API server-side operationType default (verified per D-22 gate)"
      via: "--operation-type \"CHAT\" appended after --task-type \"unclassified\"; idempotent w.r.t. existing historical records (all show operationType: CHAT) and w.r.t. cost calculation (verified 28733-token pairs, $0.000001 floating-point rounding only)"
      pattern: "--operation-type \"CHAT\""
    - from: "tests/test_repository.py::test_wire_agent_trace_passthrough (new method)"
      to: "skills/revenium/scripts/hermes-report.sh (marker-driven cmd array)"
      via: "two sub-cases: (1) marker pair with agent/trace_id present → assert flags carry marker values; (2) marker pair with both keys omitted → assert flags carry Hermes/${sid} fallbacks; uses existing stubbed-revenium harness from test_cron_marker_split_end_to_end"
      pattern: "test_wire_agent_trace_passthrough"
    - from: "tests/test_repository.py::test_wire_no_provider_regression_per_class (new method)"
      to: "skills/revenium/scripts/hermes-report.sh (provider inference at lines 214-263)"
      via: "8-tuple PROVIDER_CASES loop; per-case fresh tmpdir + state.db row + GUARDRAIL+CHAT marker pair; assertions on --provider, --model, --model-source per case (meta case asserts --model-source ABSENT)"
      pattern: "PROVIDER_CASES.*meta.*openrouter-special.*bedrock-special"
    - from: "tests/test_repository.py::test_cron_marker_split_end_to_end (line 614 — COMPAT-01)"
      to: "skills/revenium/scripts/hermes-report.sh (zero-marker fallthrough cmd array)"
      via: "assertNotIn('--operation-type', flags) → assertEqual(flags.get('--operation-type'), 'CHAT') — flipped atomically in the same commit as the production fallthrough change"
      pattern: "assertEqual\\(flags\\.get\\(['\"]--operation-type['\"]\\), ['\"]CHAT['\"]"
    - from: ".planning/REQUIREMENTS.md (CRON-07 description prose)"
      to: "WIRE-01 wire-change semantics (zero-marker fallthrough emits --operation-type CHAT)"
      via: "one-line prose update inside CRON-07's bullet — `no explicit --operation-type` → `--operation-type CHAT (WIRE-01 / D-22 discharged in Phase 4)` — keeps REQUIREMENTS.md internally consistent with the WIRE-01 production change shipped by this plan"
      pattern: "--operation-type CHAT \\(WIRE-01 / D-22 discharged in Phase 4\\)"

threat_model:
  trust_boundaries:
    - "marker JSON file (writer: classifier.py + SKILL.md; reader: hermes-report.sh) — values for `agent` and `trace_id` are pass-through to revenium CLI argv. Today no upstream writer populates these fields (D-23); when one does, values become CLI flag arguments via the `cmd+=(--agent \"${value}\")` array pattern (no shell word splitting, no injection)."
    - "Revenium API server-side operationType default behavior (gated by D-22 research): the API treats absent and CHAT identically for cost calculation; switching the wire from absent to explicit CHAT is idempotent in production. Boundary is the revenium CLI → API HTTP body."
    - "8 provider-class state.db rows + marker fixtures (new test) — synthetic data only; no production state mutation. Test isolation via tempfile.mkdtemp + env redirect (HERMES_HOME, REVENIUM_STATE_DIR) preserves the existing test_cron_marker_split_end_to_end pattern."
  stride_register:
    - id: "T-04-01"
      category: "Tampering"
      component: "marker JSON `agent` field passed to --agent argv"
      disposition: "accept"
      mitigation: "Array-element argv pattern (`cmd+=(--agent \"${m_agent:-Hermes}\")`) passes the value as a single argv element to the revenium CLI binary — no shell word splitting, no string interpolation into a subshell, no eval. Established defense-in-depth pattern already used for every other marker-derived flag in hermes-report.sh. A maliciously-crafted marker (writer = classifier.py / SKILL.md, both operator-controlled in v1) could only cause the revenium CLI to receive an unusual --agent string; the CLI rejects flags it doesn't recognize and the operator owns the writer side. Defense surface is the 4096-byte per-line cap in the marker reader (already enforced at line 392 of hermes-report.sh) and the marker schema's 1024-byte total record cap (TEST-01 invariant). Per RESEARCH.md security domain: no new attack surface introduced by Phase 4."
    - id: "T-04-02"
      category: "Tampering"
      component: "marker JSON `trace_id` field passed to --trace-id argv"
      disposition: "accept"
      mitigation: "Identical to T-04-01: array-element argv pattern + per-line/per-record size caps + operator-controlled writer side. trace_id is a free string per the Phase 2 marker schema; no regex validation defined upstream. The colon-dash fallback (`${m_trace:-${sid}}`) ensures even an empty string from the pipe correctly produces a sid-derived value. No injection surface; accept per Phase 2 design."
    - id: "T-04-03"
      category: "Information Disclosure (Analytics)"
      component: "--operation-type CHAT in zero-marker fallthrough — re-bucketing risk for historical data"
      disposition: "mitigate (discharged by D-22 gate)"
      mitigation: "D-22 research gate verified three independent evidence paths: (a) `revenium meter completion --dry-run --json` confirms absent operationType emits `{operationType: ABSENT}` body; (b) `revenium metrics completions` over the 2026-04-01..2026-05-12 window returns 50 records ALL showing `operationType: CHAT` — server-side default for absent input is CHAT; (c) cost parity for GUARDRAIL vs CHAT pairs from the same session (28733-token pair, $0.000001 floating-point delta only) confirms operationType is analytics-dimension only, not a cost multiplier. Switching the zero-marker fallthrough from absent to explicit CHAT is idempotent for existing customers' dashboards and budgets. Disposition: mitigate — the mitigation is the research evidence itself + WIRE-04's argv regression test confirming no other argv field shifts in the fallthrough path."
---

# Plan 04-01: Wire Enrichment — pass-through agent/trace_id, CHAT default, 8-provider regression

<files_to_read>
@.planning/phases/04-wire-enrichment/04-CONTEXT.md
@.planning/phases/04-wire-enrichment/04-RESEARCH.md
@.planning/phases/04-wire-enrichment/04-PATTERNS.md
@.planning/phases/03-cron-marker-reader-equal-split-ledger-v2/03-01-PLAN.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@skills/revenium/scripts/hermes-report.sh
@skills/revenium/scripts/split_strategies.py
@skills/revenium/scripts/common.sh
@skills/revenium/references/setup.md
@tests/test_repository.py
</files_to_read>

<objective>
**Goal (verbatim from ROADMAP.md Phase 4):** Each split metering call carries the richest `--operation-type`, `--agent`, and `--trace-id` available from the marker, with a documented, conservative fallback to today's hardcoded values; provider inference and cost scaling never regress across any split call.

**Scope:** Phase 4 is a contained argv enrichment of `skills/revenium/scripts/hermes-report.sh`. Two code paths change: the marker-driven per-marker emission block (lines 517-571) and the zero-marker fallthrough block (lines 606-636). No new state files, no new scripts, no upstream writer changes (D-23). Optional marker fields `agent` and `trace_id` flow through the pipe-delimited split_rows format as new fields 10-11; the cmd array uses colon-dash fallbacks (`${m_agent:-Hermes}`, `${m_trace:-${sid}}`) so today's universal empty-from-pipe case continues to ship the existing hardcoded values byte-unchanged. The zero-marker fallthrough adds `--operation-type CHAT` per D-22 (gate DISCHARGED in 04-RESEARCH.md — server-side default; cost-parity verified). COMPAT-01 flips one test assertion line atomically in the same commit. D-24 ships an 8-provider regression test mirroring the existing `test_cron_marker_split_end_to_end` scaffolding. D-25 ships a one-paragraph attribution-semantics note in `setup.md`.

**Why now / why this shape:** All four user decisions are locked in 04-CONTEXT.md:
- **D-22 (WIRE-01):** Gate DISCHARGED via three independent evidence paths in 04-RESEARCH.md (CLI dry-run, historical records, cost parity). Ship `--operation-type CHAT` as the zero-marker default. No fallback needed.
- **D-23 (WIRE-02 + WIRE-03):** Cron pass-through ONLY. Phase 4 does NOT extend `classifier.py` or `SKILL.md` FINAL ACTION. The wire is built to carry `agent`/`trace_id` the moment a future writer chooses to populate them. Today the universal case is empty pipe field → colon-dash fallback fires.
- **D-24 (WIRE-04):** New test method `test_wire_no_provider_regression_per_class` with an 8-case DRY loop (anthropic, openai, google, xai, deepseek, meta, openrouter-special, bedrock-special) asserting per-marker argv carries the same `--provider`/`--model`/`--model-source` shape the legacy pre-Phase-3 single-call path would have produced. Mirrors `test_cron_marker_split_end_to_end` scaffolding exactly.
- **D-25:** Per-marker attribution as-is when markers in a session disagree on agent/trace_id. One-paragraph documentation note in `setup.md::## How attribution works`.

**Atomic-commit invariants (Pitfalls 1 + 2 from 04-RESEARCH.md):**
- Pipe Python emit (fields 10-11) AND bash while-read variables (`m_agent m_trace`) MUST land in ONE commit. Mismatched field counts silently corrupt `d_cost`.
- Zero-marker `--operation-type CHAT` production change AND the COMPAT-01 test assertion flip MUST land in ONE commit. Otherwise `test_cron_marker_split_end_to_end` fails immediately after the code edit.

**Out of scope (locked):** classifier.py logic, SKILL.md FINAL ACTION, split_strategies.py, ledger format, marker schema, common.sh paths, setup-local.sh, HOOK-01..HOOK-13 entry strings, existing 37 tests except the one COMPAT-01 line at test_repository.py:614.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
**Phase 4 carry-forward (from CONTEXT.md):**
- Phase 3 SC3 byte-diff invariant explicitly carved out `--operation-type` to be added by Phase 4 — the only legal argv change to the zero-marker fallthrough path.
- The 5-field ledger row format and `${sid}-${total_tokens}-${muid}` transaction-id shape are owned by Phase 3 — Phase 4 does NOT change them.
- Phase 6 marker writers (classifier.py + SKILL.md FINAL ACTION) write only the 5 required marker keys today — `agent` and `trace_id` are absent from every production marker. The colon-dash fallback IS the universal case in production until a future phase chooses to populate.
- The 4096-byte per-line cap in the marker reader (line 392 of hermes-report.sh) + 1024-byte per-record cap (Phase 2 TEST-01) bound the blast radius of any malformed marker.

**Interfaces and contracts the executor needs:**

`skills/revenium/scripts/hermes-report.sh::split_rows Python heredoc (lines 517-534)`:
- Current print format: `{muid}|{task_type}|{operation_type}|{input}|{output}|{cache_read}|{cache_write}|{total}|{cost}` (9 pipe-delimited fields).
- Phase 4 extension: append fields 10 (`m_agent`) and 11 (`m_trace`) from `marker.get('agent', '')` / `marker.get('trace_id', '')`. No trailing pipe.
- Stdlib-only (`json`, `os`, `sys`). `try/except Exception` wraps the body; errors `print(..., file=sys.stderr)` + `sys.exit(3)`. The existing `MARKERS_JSON` env var carries the parsed marker list — `marker.get('agent', '')` is safe whether or not the optional key is present (no schema re-validation needed).

`skills/revenium/scripts/hermes-report.sh::marker-driven while-read loop (line 546-547)`:
- Current declaration: `local row muid t_type op_type d_in d_out d_cr d_cw d_tot d_cost`
- Current loop header: `while IFS='|' read -r muid t_type op_type d_in d_out d_cr d_cw d_tot d_cost; do`
- Phase 4 extension: append `m_agent m_trace` to BOTH the `local` declaration AND the `while IFS='|' read -r` argument list (in the same commit; Pitfall 1).

`skills/revenium/scripts/hermes-report.sh::marker-driven cmd array (lines 564 and 566)`:
- Current: `--agent "Hermes"` and `--trace-id "${sid}"`.
- Phase 4 target: `--agent "${m_agent:-Hermes}"` and `--trace-id "${m_trace:-${sid}}"`. Colon-dash mandatory (Pitfall 3).
- The line between them — `--transaction-id "${sid}-${total_tokens}-${muid}"` — is UNCHANGED.
- `set -uo pipefail` (soft-fail) is preserved; no flag change.

`skills/revenium/scripts/hermes-report.sh::zero-marker fallthrough cmd array (line 635)`:
- Current tail (line 630-636): `--agent "Hermes" / --transaction-id "${sid}-${total_tokens}" / --trace-id "${sid}" / --is-streamed / --quiet / --task-type "unclassified"`.
- Phase 4 target: append `--operation-type "CHAT"` as the line immediately after `--task-type "unclassified"`. No other change. The pre-existing comment block at lines 610-612 (`Do NOT emit --operation-type: Phase 4 owns the WIRE-01 default decision per CONTEXT.md research_gates`) is replaced with a single-line note: `# WIRE-01 / D-22: emit --operation-type CHAT — gate discharged in 04-RESEARCH.md (server-side default; cost parity verified).`

`tests/test_repository.py::RepositoryTests::test_cron_marker_split_end_to_end (line 355) — COMPAT-01 flip at lines 614-616`:
- Current assertion:
  - `self.assertNotIn('--operation-type', flags, 'zero-marker fallthrough must NOT emit --operation-type (Phase 4 WIRE-01 owns that decision)')`
- Phase 4 replacement (ONE line — same indent, same triple-quoted style):
  - `self.assertEqual(flags.get('--operation-type'), 'CHAT', 'zero-marker fallthrough must emit --operation-type CHAT (WIRE-01 / D-22)')`
- No other change in this test method. The 36 other tests in the suite are not touched.

`tests/test_repository.py::RepositoryTests` — reusable helpers already in the file (DO NOT reimplement):
- `build_state_db(path, sessions)` at lines 373-395 — the 13-column INSERT shape including `billing_provider`.
- `run_cron(env, invocations_log)` at lines 409-428 — subprocess invocation + shlex parsing of the invocations log.
- `argv_to_flags(argv)` at lines 430-445 — converts flat argv to a `{flag: value}` dict for assertions; treats trailing boolean flags (`--is-streamed`, `--quiet`) as `True`.
- `revenium` shim at lines 461-481 — `$INVOCATIONS_LOG` captures `printf "%q "` argv per invocation.
- `base_env` at lines 483-491 — `HOME=shim_home`, `HERMES_HOME=hermes_home`, `REVENIUM_STATE_DIR=state_dir`, `PATH=bin_dir:$PATH`, `INVOCATIONS_LOG=invocations_log`, `TZ=UTC`.
- `started_at = 1715514000.0` — fixed 2024 timestamp that bypasses the G-03 sentinel filter (Pitfall 6).

`PROVIDER_CASES` 8-tuple list (copy verbatim from 04-PATTERNS.md lines 280-298):
```
(label, billing_provider, model, expected_provider, expected_clean_model, expected_model_source)
('anthropic',         'anthropic',  'claude-sonnet-4-6',                              'anthropic', 'claude-sonnet-4-6',                              'anthropic')
('openai',            'openai',     'gpt-4o',                                         'openai',    'gpt-4o',                                         'openai')
('google',            'google',     'gemini-1.5-pro',                                 'google',    'gemini-1.5-pro',                                 'google')
('xai',               'xai',        'grok-2',                                         'xai',       'grok-2',                                         'xai')
('deepseek',          'deepseek',   'deepseek-chat',                                  'deepseek',  'deepseek-chat',                                  'deepseek')
('meta',              '',           'llama-3.1-70b',                                  'meta',      'llama-3.1-70b',                                  None)
('openrouter-special','openrouter', 'anthropic/claude-sonnet-4-5',                    'anthropic', 'claude-sonnet-4-5',                              'openrouter')
('bedrock-special',   'bedrock',    'anthropic.claude-3-5-sonnet-20241022-v2:0',     'anthropic', 'claude-3-5-sonnet-20241022-v2:0',                'bedrock')
```
The `meta` case asserts `--model-source` is ABSENT from the captured argv (`self.assertNotIn('--model-source', flags, ...)`). Every other case asserts `--model-source == expected_model_source`.

`skills/revenium/references/setup.md` — `## How attribution works` is the existing section at line 73. The D-25 paragraph is appended to the END of that section's existing prose. No new section, no new heading.

`.planning/REQUIREMENTS.md` — WIRE-01..04 + COMPAT-01 are under `### Wire Enrichment (Adjacent-Flag Wins)` and `### Backward Compatibility & Invariants`. Flip checkboxes `- [ ]` → `- [x]`. The traceability table (last block) maps each ID to a Phase + Status row — change `Pending` → `Verified (Phase 4)` for those 5 IDs. Coverage counts (50 total, 50 mapped) are unchanged. Additionally, CRON-07's bullet at line ~50 contains a stale prose phrase about the fallthrough path emitting `no explicit --operation-type`; that phrase is updated in place to read `--operation-type CHAT (WIRE-01 / D-22 discharged in Phase 4)` so REQUIREMENTS.md stays self-consistent after Phase 4 lands. CRON-07's checkbox state is NOT touched — Phase 3 remains the owning phase per the Traceability table; only the description prose is corrected.

`.planning/ROADMAP.md` — Phase 4 row currently reads `**Plans**: TBD`. Update to `**Plans**: 1 plan`. Append the Plans bullet list under Phase 4 with the 04-01-PLAN.md entry. Progress Table Phase 4 row: planner writes `0/1 / In progress / -`; execute-phase transitions it to `1/1 / Executed / <date>` on completion.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Land WIRE-01 + COMPAT-01 atomically — zero-marker fallthrough emits --operation-type CHAT, test assertion flips in the same commit</name>
  <files>skills/revenium/scripts/hermes-report.sh, tests/test_repository.py</files>
  <read_first>
    - skills/revenium/scripts/hermes-report.sh lines 605-670 (the zero-marker fallthrough block + the `Do NOT emit --operation-type` comment)
    - tests/test_repository.py lines 605-635 (the COMPAT-01 sub-case 2 of test_cron_marker_split_end_to_end, including the assertNotIn line at 614-616 and the surrounding ledger-row assertions that MUST continue to pass)
    - 04-PATTERNS.md lines 79-108 (Block B: Zero-marker fallthrough — WIRE-01 target shape)
    - 04-RESEARCH.md `## D-22 Research Gate Verdict` and `### Wire Extension Points / Zero-Marker Fallthrough`
  </read_first>
  <action>
    In `skills/revenium/scripts/hermes-report.sh`, edit the zero-marker fallthrough block:
    1. Replace the pre-existing comment block at lines 610-612 (the three lines starting with `# Argv differs from the pre-Phase-3 legacy single-call ONLY by the addition of` through `# Phase 4 owns the WIRE-01 default decision per CONTEXT.md research_gates.`) with a single-line note: `# WIRE-01 / D-22: emit --operation-type CHAT — gate discharged in 04-RESEARCH.md (Revenium server-side default; cost parity verified across 50+ historical records).`
    2. In the cmd array starting at line 616, insert `--operation-type "CHAT"` as a new line IMMEDIATELY after the existing `--task-type "unclassified"` line (currently line 635) and BEFORE the closing `)` of the array.
    3. The `--agent "Hermes"`, `--transaction-id "${sid}-${total_tokens}"`, `--trace-id "${sid}"`, `--is-streamed`, `--quiet`, `--task-type "unclassified"` lines are NOT changed. Only the comment block and the one new --operation-type line are touched. The conditional appends below the array (--model-source, --total-cost, --organization-name, --environment) are unchanged.

    In `tests/test_repository.py::test_cron_marker_split_end_to_end`, edit lines 614-616 (the only zero-marker sub-case assertion on --operation-type):
    1. Replace `self.assertNotIn('--operation-type', flags, 'zero-marker fallthrough must NOT emit --operation-type ' '(Phase 4 WIRE-01 owns that decision)')` with `self.assertEqual(flags.get('--operation-type'), 'CHAT', 'zero-marker fallthrough must emit --operation-type CHAT (WIRE-01 / D-22)')`.
    2. Preserve the surrounding ledger-row assertions (lines 621-635) verbatim — they check the 5-field ledger v2 format and the `unclassified-` synthetic muid prefix; those invariants are unchanged.
    3. Do NOT touch any other test method or any other assertion in `test_cron_marker_split_end_to_end`.

    BOTH file edits land in ONE commit. The COMPAT-01 test assertion flip without the production change leaves the test red against the unmodified script; the production change without the assertion flip leaves the test red against the new --operation-type CHAT argv. Atomicity is mandatory.

    `bash -n skills/revenium/scripts/hermes-report.sh` MUST succeed before commit. `set -uo pipefail` discipline preserved.
  </action>
  <acceptance_criteria>
    - `bash -n skills/revenium/scripts/hermes-report.sh` exits 0.
    - `python3 -m unittest tests.test_repository.RepositoryTests.test_cron_marker_split_end_to_end -v` passes.
    - Full suite green: `python3 -m unittest discover -s tests -p 'test_*.py' -v` reports OK with no regressions.
    - `grep -n '\-\-operation-type \"CHAT\"' skills/revenium/scripts/hermes-report.sh` shows the new line inside the zero-marker fallthrough cmd array (one occurrence in this block; the marker-driven path's `--operation-type "${op_type}"` at line 570 is preserved).
    - `grep -n 'assertEqual.*--operation-type.*CHAT' tests/test_repository.py` shows the flipped assertion.
    - `grep -n 'assertNotIn.*--operation-type' tests/test_repository.py` returns zero matches.
    - The other 36 tests in the suite are byte-unchanged.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/johndemic/Development/projects/revenium/hermes-revenium && bash -n skills/revenium/scripts/hermes-report.sh && python3 -m unittest tests.test_repository.RepositoryTests.test_cron_marker_split_end_to_end -v 2>&1 | tail -20 && python3 -m unittest discover -s tests -p 'test_*.py' 2>&1 | tail -5</automated>
  </verify>
  <done>
    Zero-marker fallthrough emits `--operation-type CHAT` on the wire (verified by re-running test_cron_marker_split_end_to_end against the updated assertion); pre-existing 37 tests continue to pass byte-unchanged; WIRE-01 + COMPAT-01 committed atomically.
  </done>
</task>

<task type="auto">
  <name>Task 2: Land WIRE-02 + WIRE-03 atomically — split_rows pipe extension (fields 10-11) AND bash while-read variable extension AND cmd array colon-dash fallback</name>
  <files>skills/revenium/scripts/hermes-report.sh</files>
  <read_first>
    - skills/revenium/scripts/hermes-report.sh lines 508-580 (the split_rows Python heredoc at 517-534, the empty-split-rows tolerance at 537-544, the while-read declaration and loop header at 546-547, and the marker-driven cmd array at 550-580)
    - 04-PATTERNS.md lines 27-76 (Block A: Marker-driven split path — full WIRE-02 / WIRE-03 target shape)
    - 04-RESEARCH.md `### Marker-Driven Path (WIRE-02 / WIRE-03)` + Pitfalls 1, 3, 4, 5
    - skills/revenium/scripts/split_strategies.py (for the equal_split contract that the heredoc already uses — no change needed there)
  </read_first>
  <action>
    In `skills/revenium/scripts/hermes-report.sh`, edit the marker-driven split path. All three edits below land in ONE commit (Pitfall 1):

    1. Extend the split_rows Python heredoc at lines 517-534. Inside the `for marker, split in zip(markers, splits):` loop, before the existing `print(...)` call, add two reads:
       - `m_agent = marker.get('agent', '')   # optional Phase 2 field; empty string triggers bash :- fallback`
       - `m_trace = marker.get('trace_id', '')   # optional Phase 2 field; empty string triggers bash :- fallback`
       Add a comment above the print noting the pipe-safety constraint: `# NOTE: m_agent / m_trace values MUST NOT contain '|' (pipe-safety; today's only values are pipe-safe fallbacks per D-23).`
       Change the print to append fields 10 and 11 (no trailing pipe): `f"{split['cache_write']}|{split['total']}|{split['cost']}|{m_agent}|{m_trace}"`. The first 9 fields are UNCHANGED in order or content.

    2. Extend the bash while-read declaration at lines 546-547 (Pitfall 1: same commit as the Python edit):
       - Change `local row muid t_type op_type d_in d_out d_cr d_cw d_tot d_cost` to `local row muid t_type op_type d_in d_out d_cr d_cw d_tot d_cost m_agent m_trace`.
       - Change `while IFS='|' read -r muid t_type op_type d_in d_out d_cr d_cw d_tot d_cost; do` to `while IFS='|' read -r muid t_type op_type d_in d_out d_cr d_cw d_tot d_cost m_agent m_trace; do`.

    3. Extend the marker-driven cmd array at lines 564 and 566:
       - Change `--agent "Hermes"` (line 564) to `--agent "${m_agent:-Hermes}"`.
       - Change `--trace-id "${sid}"` (line 566) to `--trace-id "${m_trace:-${sid}}"`.
       - The `--transaction-id "${sid}-${total_tokens}-${muid}"` line between them (line 565) is UNCHANGED.
       - Colon-dash (`:-`) is mandatory per Pitfall 3 — `${m_agent-Hermes}` (bare dash) would NOT fire the fallback for an empty-string-from-pipe value.
       - All other cmd array lines (--model, --provider, --input-tokens, --output-tokens, --cache-read-tokens, --cache-creation-tokens, --total-tokens, --stop-reason, --request-time, --completion-start-time, --response-time, --request-duration, --is-streamed, --quiet, --task-type, --operation-type) are UNCHANGED.
       - The conditional appends below the array (--model-source, --total-cost, --organization-name, --environment) at lines 573-584 are UNCHANGED.

    `bash -n skills/revenium/scripts/hermes-report.sh` MUST succeed before commit. `set -uo pipefail` discipline preserved.

    Self-check after edit: confirm `test_cron_marker_split_end_to_end` STILL passes byte-unchanged — its existing marker fixtures DO NOT include `agent`/`trace_id` keys, so the marker.get(...) reads emit empty strings, fields 10-11 of the pipe are empty, m_agent and m_trace are empty, and the colon-dash fallback produces `--agent Hermes` and `--trace-id ${sid}` byte-identical to the pre-Phase-4 behavior. The conservation/idempotency/ledger assertions in that test are unaffected.
  </action>
  <acceptance_criteria>
    - `bash -n skills/revenium/scripts/hermes-report.sh` exits 0.
    - `grep -v '^#' skills/revenium/scripts/hermes-report.sh | grep -c 'm_agent\|m_trace'` shows at least 6 substantive (non-comment) occurrences (Python reads x2, local declaration x2, while-read header x2, cmd array x2 — totals depend on exact line counts but must be >= 6 non-comment).
    - `grep -n 'm_agent:-Hermes\|m_trace:-\${sid}' skills/revenium/scripts/hermes-report.sh` shows the colon-dash fallbacks in the cmd array.
    - `grep -n '\-\-agent \"Hermes\"' skills/revenium/scripts/hermes-report.sh` shows ONLY the zero-marker fallthrough occurrence at line ~630 — the marker-driven path's old hardcoded "Hermes" is gone.
    - `python3 -m unittest tests.test_repository.RepositoryTests.test_cron_marker_split_end_to_end -v` passes byte-unchanged (existing marker fixtures don't carry agent/trace_id; fallback fires; argv unchanged from operator's perspective).
    - `python3 -m unittest tests.test_repository.RepositoryTests.test_s2_bias_50_50_attribution -v` passes (this test also exercises the marker-driven path).
    - Full suite green: `python3 -m unittest discover -s tests -p 'test_*.py' -v` reports OK.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/johndemic/Development/projects/revenium/hermes-revenium && bash -n skills/revenium/scripts/hermes-report.sh && python3 -m unittest tests.test_repository.RepositoryTests.test_cron_marker_split_end_to_end tests.test_repository.RepositoryTests.test_s2_bias_50_50_attribution -v 2>&1 | tail -15 && python3 -m unittest discover -s tests -p 'test_*.py' 2>&1 | tail -5</automated>
  </verify>
  <done>
    Marker-driven split path carries optional marker `agent`/`trace_id` to `--agent`/`--trace-id` argv with colon-dash fallback to `Hermes` / `${sid}`; pipe-format extension (11 fields) and while-read variable extension landed atomically; all pre-existing 37 tests pass byte-unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add WIRE-02 + WIRE-03 passthrough regression test — test_wire_agent_trace_passthrough (positive + fallback sub-cases)</name>
  <files>tests/test_repository.py</files>
  <read_first>
    - tests/test_repository.py lines 355-748 (test_cron_marker_split_end_to_end full method — the scaffold to mirror)
    - tests/test_repository.py lines 373-481 (build_state_db / run_cron / argv_to_flags / revenium shim — the helpers to reuse)
    - 04-PATTERNS.md lines 236-270 (new method shape + assertion patterns for WIRE-02 / WIRE-03)
    - 04-RESEARCH.md `### Existing Pattern to Mirror` and `### Marker Field Semantics`
  </read_first>
  <action>
    Append a new test method `test_wire_agent_trace_passthrough` to `RepositoryTests` in `tests/test_repository.py`. Place it AFTER `test_cron_marker_split_end_to_end` (line 748 area) and BEFORE `test_s2_bias_50_50_attribution` (line 749), OR at any clean position in the class — the exact location is not load-bearing. Test method docstring states it pins WIRE-02 + WIRE-03 marker passthrough plus fallback behavior.

    Method structure — TWO sub-cases (positive + fallback) sharing the same per-call scaffolding:

    Sub-case A (positive — marker carries agent + trace_id):
    1. Use `tempfile.mkdtemp(prefix='gsd-wire-agent-trace-')` for tmpdir.
    2. Build the tmp directory layout exactly as test_cron_marker_split_end_to_end does: `hermes_home`, `state_dir`, `markers_dir` (mode 0o700), `state_db`, `ledger`, `shim_home`, `bin_dir`, `invocations_log`.
    3. Write the `revenium` shim script to `bin_dir/revenium` per the existing helper pattern (captures argv shell-escaped to `$INVOCATIONS_LOG`).
    4. Seed ONE state.db row using `build_state_db`: `id='wire-agent-trace-positive'`, `model='claude-sonnet-4-6'`, `source='test'`, `input_tokens=10000`, `output_tokens=4000`, `cache_read=200`, `cache_write=100`, `reasoning=0`, `estimated_cost='0.123456'`, `api_calls=2`, `started_at=1715514000.0` (Pitfall 6 — bypasses G-03 sentinel filter), `ended_at=1715515100.0`, `billing_provider='anthropic'`.
    5. Write a GUARDRAIL+CHAT marker pair to `${markers_dir}/<sid>.jsonl` (newline-separated JSON, one per line). Both markers carry `agent='revenium-skill'` and `trace_id='trace-abc-001'`:
       - GUARDRAIL marker: `{muid: '01893b8a300abcdef0123456789abc01', ts: 1715515001.0, sid: <sid>, task_type: 'code_review', operation_type: 'GUARDRAIL', agent: 'revenium-skill', trace_id: 'trace-abc-001'}`
       - CHAT marker: `{muid: '01893b8a300abcdef0123456789abc02', ts: 1715515002.0, sid: <sid>, task_type: 'code_review', operation_type: 'CHAT', agent: 'revenium-skill', trace_id: 'trace-abc-001'}`
    6. Build `base_env` per the existing pattern (HOME=shim_home, HERMES_HOME, REVENIUM_STATE_DIR, PATH=bin_dir:..., INVOCATIONS_LOG, TZ=UTC).
    7. Invoke `run_cron(base_env, invocations_log)` → expect rc==0, len(invocations)==2 (one per marker).
    8. For each captured invocation: `flags = argv_to_flags(argv)`; assert `flags.get('--agent') == 'revenium-skill'` (WIRE-02 message) AND `flags.get('--trace-id') == 'trace-abc-001'` (WIRE-03 message). Assertion messages reference WIRE-02 / WIRE-03 explicitly.
    9. `shutil.rmtree(tmpdir, ignore_errors=True)` in a finally block.

    Sub-case B (fallback — same scaffolding, fresh tmpdir, marker pair WITHOUT optional fields):
    1. Same tmp-dir layout as A.
    2. Seed ONE state.db row with `id='wire-agent-trace-fallback'` (different sid so no cross-talk); other fields identical.
    3. Write a GUARDRAIL+CHAT marker pair OMITTING the `agent` and `trace_id` keys — only the 5 required keys plus `operation_type`.
    4. Same `base_env` shape (with the new sid).
    5. Invoke `run_cron`. For each invocation: assert `flags.get('--agent') == 'Hermes'` (WIRE-02 fallback message) AND `flags.get('--trace-id') == '<sid>'` (WIRE-03 fallback message; use the sub-case's sid string).
    6. Cleanup.

    Both sub-cases can share helper-builder code via a nested function inside the test method, or run inline as two `with self.subTest(...)` blocks. The 04-RESEARCH.md / 04-PATTERNS.md sample assertion strings ("WIRE-02: --agent must carry marker agent field when present" etc.) are recommended verbatim for CI clarity.

    Do NOT add module-level imports — use the existing pattern of local imports inside the method (`import json, os, shutil, sqlite3, subprocess, tempfile`).
  </action>
  <acceptance_criteria>
    - `python3 -m unittest tests.test_repository.RepositoryTests.test_wire_agent_trace_passthrough -v` passes.
    - Test asserts BOTH sub-cases: marker carrying agent/trace_id → argv carries marker values; marker omitting them → argv carries Hermes / ${sid} fallbacks.
    - Full suite green: 38 tests total (37 pre-existing + 1 new); `python3 -m unittest discover -s tests -p 'test_*.py' -v` reports OK.
    - `test_no_legacy_branding_left` passes against the new test method (no legacy product names in fixtures or assertion strings).
    - `grep -c 'subTest\|sub_test\|sub-case' tests/test_repository.py` confirms the new method uses a clearly structured two-sub-case shape.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/johndemic/Development/projects/revenium/hermes-revenium && python3 -m unittest tests.test_repository.RepositoryTests.test_wire_agent_trace_passthrough -v 2>&1 | tail -15 && python3 -m unittest discover -s tests -p 'test_*.py' 2>&1 | tail -5</automated>
  </verify>
  <done>
    `test_wire_agent_trace_passthrough` pins WIRE-02 / WIRE-03 passthrough (markers carry agent/trace_id → argv reflects them) and fallback (markers omit them → argv carries Hermes / ${sid}); 38/38 suite green.
  </done>
</task>

<task type="auto">
  <name>Task 4: Add WIRE-04 8-provider regression test — test_wire_no_provider_regression_per_class (DRY 8-case loop)</name>
  <files>tests/test_repository.py</files>
  <read_first>
    - tests/test_repository.py lines 355-748 (test_cron_marker_split_end_to_end — scaffold to mirror)
    - tests/test_repository.py lines 373-481 (build_state_db / run_cron / argv_to_flags / revenium shim helpers)
    - skills/revenium/scripts/hermes-report.sh lines 214-263 (clean_model + provider inference + special-case branches for openrouter and bedrock)
    - 04-PATTERNS.md lines 274-315 (full WIRE-04 method shape + the 8-tuple PROVIDER_CASES list + per-case assertion pattern)
    - 04-RESEARCH.md `## Provider Class Enumeration (for D-24)` — the verified provider table with --provider / --model / --model-source expectations per case
  </read_first>
  <action>
    Append a new test method `test_wire_no_provider_regression_per_class` to `RepositoryTests` in `tests/test_repository.py`. Method docstring states it pins WIRE-04: every per-marker `revenium meter completion` argv preserves the same `--provider`, `--model`, `--model-source` flags the legacy pre-Phase-3 single-call path would have produced for each of the 8 provider classes (D-24).

    Method structure — single DRY loop over an 8-tuple `PROVIDER_CASES` list defined inline at the top of the method. The PROVIDER_CASES list is sourced VERBATIM from `.planning/phases/04-wire-enrichment/04-PATTERNS.md` lines 280-298 — copy that tuple list directly (8 entries, 6-element tuples shaped `(label, billing_provider, model, expected_provider, expected_clean_model, expected_model_source)`). The 8 entries in order are: `anthropic`, `openai`, `google`, `xai`, `deepseek`, `meta`, `openrouter-special`, `bedrock-special`. For each entry's exact `billing_provider` / `model` / `expected_provider` / `expected_clean_model` / `expected_model_source` values, read 04-PATTERNS.md lines 280-298 — do NOT improvise values; the test cases are determined by the verified provider inference table in 04-RESEARCH.md `## Provider Class Enumeration`.

    Per-case loop body (use `with self.subTest(case=label):` so each iteration's failure is reported separately in CI output):
    1. Fresh `tmpdir = tempfile.mkdtemp(prefix='gsd-wire-provider-e2e-')` per case. Cleanup in a try/finally.
    2. Build the tmp directory layout identically to test_cron_marker_split_end_to_end: `hermes_home`, `state_dir`, `markers_dir` (mode 0o700), `state_db`, `ledger`, `shim_home`, `bin_dir`, `invocations_log`.
    3. Write the `revenium` shim (capture argv shell-escaped to `$INVOCATIONS_LOG`).
    4. Seed ONE state.db row using `build_state_db`: `id=f'wire-provider-{label}'`, `model=<case model>`, `source='test'`, `input_tokens=10000`, `output_tokens=4000`, `cache_read=200`, `cache_write=100`, `reasoning=0`, `estimated_cost='0.123456'`, `api_calls=2`, `started_at=1715514000.0` (Pitfall 6 — bypasses G-03 sentinel filter), `ended_at=1715515100.0`, `billing_provider=<case billing_provider>`.
    5. Write a GUARDRAIL+CHAT marker pair to `${markers_dir}/<sid>.jsonl` (forces N=2 marker-driven split path so each case produces 2 invocations). Markers carry only the 5 required keys + operation_type — no agent/trace_id (Phase 4 D-23: writers don't populate these today).
    6. Build base_env per the existing pattern. Invoke `run_cron(base_env, invocations_log)` → expect rc==0, len(invocations)==2.
    7. Assertions per invocation:
       - `flags.get('--provider') == expected_provider` (assertion message includes the label for CI clarity)
       - `flags.get('--model') == expected_clean_model` (clean_model strips `owner/` or `anthropic.` / `openai.` / etc. prefixes per hermes-report.sh:214-222)
       - If `expected_model_source is not None`: `flags.get('--model-source') == expected_model_source`.
       - If `expected_model_source is None` (meta case only): `self.assertNotIn('--model-source', flags, f'{label}: --model-source must be ABSENT when billing_provider is empty')`.
    8. Cleanup via `shutil.rmtree(tmpdir, ignore_errors=True)`.

    Notes:
    - The meta case is the ONLY case with `expected_model_source = None` — assert ABSENCE of --model-source, not a specific value.
    - The openrouter-special case has `expected_provider = 'anthropic'` (not `openrouter`); the `--model-source openrouter` flag IS emitted; the `clean_model` strips the `anthropic/` prefix leaving `claude-sonnet-4-5`.
    - The bedrock-special case has `expected_provider = 'anthropic'` (not `bedrock`); the `--model-source bedrock` flag IS emitted; the `clean_model` strips the `anthropic.` prefix leaving `claude-3-5-sonnet-20241022-v2:0` (the `:0` version suffix is preserved — do not strip it).
    - All 8 cases share the same DRY loop body; structuring as 8 separate methods is not required (D-24 says either shape is acceptable; planner chose DRY loop for compactness).
  </action>
  <acceptance_criteria>
    - `python3 -m unittest tests.test_repository.RepositoryTests.test_wire_no_provider_regression_per_class -v` passes ALL 8 sub-tests.
    - Each sub-test asserts BOTH invocations (GUARDRAIL + CHAT) carry the expected --provider / --model / --model-source for that provider class.
    - The `meta` case asserts `--model-source` is ABSENT from the argv (negative assertion).
    - The `openrouter-special` case asserts `--provider anthropic` (not openrouter) AND `--model claude-sonnet-4-5` (anthropic/ prefix stripped) AND `--model-source openrouter`.
    - The `bedrock-special` case asserts `--provider anthropic` (not bedrock) AND `--model claude-3-5-sonnet-20241022-v2:0` (anthropic. prefix stripped; `:0` suffix preserved) AND `--model-source bedrock`.
    - Full suite green: 39 tests total (37 pre-existing + 2 new from Phase 4); `python3 -m unittest discover -s tests -p 'test_*.py' -v` reports OK.
    - Test runtime under 60 seconds (8 cases × ~2-3s each via run_cron subprocess).
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/johndemic/Development/projects/revenium/hermes-revenium && python3 -m unittest tests.test_repository.RepositoryTests.test_wire_no_provider_regression_per_class -v 2>&1 | tail -25 && python3 -m unittest discover -s tests -p 'test_*.py' 2>&1 | tail -5</automated>
  </verify>
  <done>
    `test_wire_no_provider_regression_per_class` pins WIRE-04 across all 8 provider classes (anthropic, openai, google, xai, deepseek, meta, openrouter-special, bedrock-special) including the meta `--model-source` ABSENCE case and the openrouter/bedrock prefix-stripping special cases; 39/39 suite green.
  </done>
</task>

<task type="auto">
  <name>Task 5: Finalize docs (REQUIREMENTS.md + ROADMAP.md + setup.md) and write 04-01-SUMMARY.md — flip checkboxes, update CRON-07 prose, finalize phase plans count, append D-25 attribution-semantics paragraph, create SUMMARY</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, skills/revenium/references/setup.md, .planning/phases/04-wire-enrichment/04-01-SUMMARY.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (the full file — focus on WIRE-01..04 + COMPAT-01 entries; CRON-07 description at line ~50 inside `### Cron Split + Ledger Migration`; the traceability table at the bottom)
    - .planning/ROADMAP.md lines 69-79 (Phase 4 section: Goal, Depends on, Requirements, Success Criteria, Plans: TBD)
    - .planning/ROADMAP.md lines 142-149 (Progress Table — Phase 4 row currently `0/0 / Not started / -`)
    - skills/revenium/references/setup.md (the full file — focus on the `## How attribution works` section starting at line 73 and ending before `## Mechanical classification hook` at line 86)
    - 04-CONTEXT.md `<decisions>` D-25 (the verbatim paragraph text) and `<specifics>` (D-25 documentation location)
    - .planning/phases/03-cron-marker-reader-equal-split-ledger-v2/03-01-SUMMARY.md (canonical SUMMARY shape for this project — frontmatter + body sections)
    - .planning/phases/06-mechanical-classification-agent-end-hook/06-04-SUMMARY.md (most recent SUMMARY in the project)
    - 04-CONTEXT.md (the four locked decisions D-22 / D-23 / D-24 / D-25)
    - 04-RESEARCH.md (the D-22 gate evidence — needs to appear in the Decisions Honored section)
  </read_first>
  <action>
    This task lands all docs-only changes in a single atomic commit. Four files are touched:

    --- (A) `.planning/REQUIREMENTS.md` edits ---

    1. In `### Wire Enrichment (Adjacent-Flag Wins)`: change `- [ ] **WIRE-01**` → `- [x] **WIRE-01**` (line ~56), and the same checkbox flip for `**WIRE-02**`, `**WIRE-03**`, `**WIRE-04**`.
    2. In `### Backward Compatibility & Invariants`: change `- [ ] **COMPAT-01**` → `- [x] **COMPAT-01**` (line ~63).
    3. **CRON-07 prose update (NIT 1 fix from plan-checker — required for REQUIREMENTS.md internal consistency after WIRE-01 ships):** In `### Cron Split + Ledger Migration`, the CRON-07 bullet at line ~50 currently reads (verbatim):
       `- [ ] **CRON-07**: When `N == 0` markers exist for a session delta (older install, agent didn't classify, missing file), the cron falls through to the existing single-call path with `--task-type unclassified` and no explicit `--operation-type``
       Replace the tail of that bullet — specifically the substring `with --task-type unclassified and no explicit --operation-type` — with `with --task-type unclassified and --operation-type CHAT (WIRE-01 / D-22 discharged in Phase 4)`. Keep the rest of the bullet (the leading `- [ ] **CRON-07**: When N == 0 markers exist for a session delta (older install, agent didn't classify, missing file), the cron falls through to the existing single-call path`) byte-unchanged. Do NOT flip CRON-07's checkbox — Phase 3 still owns that requirement per the Traceability table; only the description prose is updated for internal consistency.
    4. In the `## Traceability` table at the bottom: change the `Status` column for `WIRE-01`, `WIRE-02`, `WIRE-03`, `WIRE-04`, `COMPAT-01` rows from `Pending` to `Verified (Phase 4)`. CRON-07's row status is UNCHANGED.
    5. Coverage counts (50 total / 50 mapped) are UNCHANGED — Phase 4 adds NO new requirements.
    6. Update the `*Last updated:*` line at the bottom from `2026-05-12` to today's date (`2026-05-14`).

    --- (B) `.planning/ROADMAP.md` edits ---

    1. Phase 4 section line `**Plans**: TBD` → `**Plans**: 1 plan`.
    2. Below the Phase 4 Success Criteria block (after line 78, before `### Phase 5`), insert a Plans bullet list with one entry: a `Plans:` heading line followed by an unchecked-checkbox bullet (`- [ ]`) pointing to `04-01-PLAN.md` with a description summarizing the plan as `Wire enrichment (WIRE-01..04 + COMPAT-01) — pass-through agent/trace_id from markers, CHAT default for zero-marker fallthrough, 8-provider regression guard`. The exact shape mirrors the Plans block under Phase 3 in ROADMAP.md lines 66-67 (same heading + unchecked-checkbox + plan-filename + dash + description pattern).
    3. Progress Table row for `4. Wire Enrichment`: change `0/0` → `0/1` and `Not started` → `In progress`. (execute-plan will transition this to `1/1` / `Executed` / today's date on plan completion — leave the `Completed` cell as `-` for now since this is the planner's output, not execute-phase's.)
    4. Do NOT change SC1-SC4 wording; they are unchanged.

    --- (C) `skills/revenium/references/setup.md` edits ---

    1. Locate the existing `## How attribution works` section (line 73). Read the section's existing prose (it covers cron splits + marker reads + ledger semantics).
    2. At the END of that section's prose, BEFORE the next section header `## Mechanical classification hook` (line 86), append one new paragraph (verbatim per D-25 / 04-CONTEXT.md `<specifics>`):
       > When markers carry different `agent` or `trace_id` values across a session, each Revenium meter call records the per-turn attribution; per-session aggregation happens dashboard-side.
    3. No new section header, no new sub-section, no list. One plain-prose paragraph attached to the existing section's end.
    4. Preserve all other content of `setup.md` byte-unchanged. Do NOT touch `## Mechanical classification hook` (Phase 6's section) or any other section.

    --- (D) `.planning/phases/04-wire-enrichment/04-01-SUMMARY.md` create ---

    Create `.planning/phases/04-wire-enrichment/04-01-SUMMARY.md` with the canonical frontmatter + body shape used by 03-01-SUMMARY.md and 06-04-SUMMARY.md.

    Frontmatter (YAML, between `---` fences):
    - `phase: 04-wire-enrichment`
    - `plan: 01`
    - `type: execute`
    - `executed: 2026-05-14` (planner-supplied; execute-plan may overwrite on completion)
    - `requirements_completed: [WIRE-01, WIRE-02, WIRE-03, WIRE-04, COMPAT-01]`
    - `files_modified: [skills/revenium/scripts/hermes-report.sh, tests/test_repository.py, skills/revenium/references/setup.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md]`
    - `files_created: [.planning/phases/04-wire-enrichment/04-01-SUMMARY.md]`
    - `tests_added: 2` (test_wire_agent_trace_passthrough, test_wire_no_provider_regression_per_class)
    - `tests_modified: 1` (test_cron_marker_split_end_to_end — COMPAT-01 assertion flip)
    - `tests_total_after: 39`
    - `commits: <executor-fills-in>` (5 atomic commits expected: T01 WIRE-01+COMPAT-01, T02 WIRE-02+WIRE-03, T03 test_wire_agent_trace_passthrough, T04 test_wire_no_provider_regression_per_class, T05 docs + SUMMARY)
    - `tags: [wire-enrichment, argv-passthrough, operation-type-default, provider-regression]`

    Body sections:

    1. `# Plan 04-01 Summary — Wire Enrichment` (h1)

    2. `## Outcome` — one paragraph stating that Phase 4 enriches `revenium meter completion` argv with `--operation-type CHAT` on the zero-marker fallthrough (WIRE-01 / D-22), passes through marker `agent` / `trace_id` optional fields as `--agent` / `--trace-id` with colon-dash fallback to `Hermes` / `${sid}` (WIRE-02 + WIRE-03 / D-23), ships an 8-provider regression test (WIRE-04 / D-24), and documents per-marker attribution semantics in `setup.md` (D-25). 38 pre-existing tests preserved byte-unchanged; 2 new tests pass; 1 test assertion (COMPAT-01) flipped atomically with the WIRE-01 production change. REQUIREMENTS.md CRON-07 description prose updated in place for internal consistency.

    3. `## Decisions Honored` — four sub-headers, one per decision:
       - `### D-22 (WIRE-01) — Gate DISCHARGED; ship --operation-type CHAT default` — summarize the three evidence paths (CLI dry-run absent vs CHAT; 50+ historical records all showing CHAT server-side default; cost parity GUARDRAIL vs CHAT at 28733 tokens with $0.000001 floating-point delta). State that the D-22 fallback (permanently omit --operation-type) was NOT exercised.
       - `### D-23 (WIRE-02 + WIRE-03) — Cron pass-through ONLY; no upstream writer changes` — state that classifier.py and SKILL.md FINAL ACTION are byte-unchanged; the wire is built to carry agent/trace_id the moment a future writer populates them; in production today every marker hits the colon-dash fallback because no upstream writer emits these fields.
       - `### D-24 (WIRE-04) — 8-provider regression test landed` — list the 8 cases (anthropic, openai, google, xai, deepseek, meta, openrouter-special, bedrock-special) and note that the meta case asserts --model-source ABSENT (the only case without billing_provider), and that openrouter/bedrock special-cases preserve the existing prefix-stripping behavior.
       - `### D-25 — Per-marker attribution semantics documented` — quote the one-paragraph addition to setup.md.

    4. `## Key Files` — bulleted list of the 5 modified files with a one-line summary per file (mirrors the `must_haves.artifacts` block above).

    5. `## Verification` — three checkpoints:
       - `python3 -m unittest discover -s tests -p 'test_*.py' -v` → 39/39 OK (37 pre-existing + 2 new).
       - `bash -n skills/revenium/scripts/hermes-report.sh` → exit 0 (no syntax regression).
       - **Operator UAT (manual gate before phase verification):** Deploy the updated `hermes-report.sh` to a Mac Studio (or any host with active cron). Run a CLI substantive turn to generate one session with markers in `${MARKERS_DIR}/<sid>.jsonl` (Phase 6 classifier writes these mechanically). On the next cron tick, observe the Revenium dashboard for that session's metering rows: the rows should carry `taskType=<classified-label>` (from the marker) AND `operationType=CHAT` (from D-22 default in the zero-marker case, OR from the marker's operation_type in the marker-driven case). If markers are written with `agent`/`trace_id` populated by a future upstream writer phase, those columns should reflect the marker values; today they will reflect the Hermes / sid fallback. Operator records the session id in the SUMMARY's `## Operator Verification` section once completed.

    6. `## Operator Verification` — placeholder section with the verbatim text: `Operator action: run a CLI substantive turn against the deployed Phase 4 wire and confirm the Revenium dashboard shows agent + trace_id columns populated when markers carry them (today: Hermes / ${sid} fallback) and operationType=CHAT for the zero-marker case. Record session id and completion timestamp here once verified.` Leave space for the operator to fill in the session id + timestamp.

    7. `## Out of Scope Carried Forward` — bulleted reminder that Phase 4 deliberately did NOT touch classifier.py, SKILL.md FINAL ACTION, split_strategies.py, common.sh, ledger format, marker schema, HOOK-01..HOOK-13 entry strings, setup-local.sh, or any of the existing 35 non-touched tests. Phase 5 (Housekeeping) inherits any deferred D-23 ideas (upstream agent/trace_id population in classifier.py).

    Sanity checks across all four files:
    - `test_no_legacy_branding_left` must continue to pass against every edited file — no legacy product names introduced.
    - `test_setup_md_has_mechanical_classification_hook_section` (Phase 6 test) must continue to pass — the `## Mechanical classification hook` heading is byte-unchanged.
    - `test_skill_frontmatter_has_hermes_metadata` is unaffected (no SKILL.md change).
    - All four files commit together in ONE atomic commit (`docs(04): finalize REQUIREMENTS / ROADMAP / setup.md + 04-01-SUMMARY.md`).
  </action>
  <acceptance_criteria>
    - `grep -c '\- \[x\] \*\*WIRE-01\*\*\|\- \[x\] \*\*WIRE-02\*\*\|\- \[x\] \*\*WIRE-03\*\*\|\- \[x\] \*\*WIRE-04\*\*\|\- \[x\] \*\*COMPAT-01\*\*' .planning/REQUIREMENTS.md` returns 5.
    - `grep -c 'Verified (Phase 4)' .planning/REQUIREMENTS.md` returns 5.
    - `grep -n 'CRON-07.*--operation-type CHAT (WIRE-01 / D-22 discharged in Phase 4)' .planning/REQUIREMENTS.md` returns 1 match (the updated CRON-07 prose).
    - `grep -n 'no explicit --operation-type' .planning/REQUIREMENTS.md` returns zero matches (stale text replaced).
    - `grep -n 'Plans.*: 1 plan' .planning/ROADMAP.md | grep -B 2 'Phase 4: Wire Enrichment'` confirms the Phase 4 plans count line.
    - `grep -n '04-01-PLAN.md' .planning/ROADMAP.md` returns at least 1 match (the new Plans bullet list under Phase 4).
    - `grep -n 'per-turn attribution.*per-session aggregation happens dashboard-side' skills/revenium/references/setup.md` returns 1 match inside the `## How attribution works` section.
    - `grep -n '^## Mechanical classification hook' skills/revenium/references/setup.md` returns 1 match — Phase 6 section header byte-unchanged.
    - `test -f .planning/phases/04-wire-enrichment/04-01-SUMMARY.md` succeeds (file exists).
    - `grep -c '^## Decisions Honored\|^### D-22\|^### D-23\|^### D-24\|^### D-25\|^## Operator Verification' .planning/phases/04-wire-enrichment/04-01-SUMMARY.md` returns 6 (all section/sub-section headings present).
    - Frontmatter `requirements_completed` in SUMMARY lists exactly [WIRE-01, WIRE-02, WIRE-03, WIRE-04, COMPAT-01].
    - Full suite green: `python3 -m unittest discover -s tests -p 'test_*.py' -v` reports OK (specifically `test_no_legacy_branding_left` and `test_setup_md_has_mechanical_classification_hook_section` pass against all four edited files).
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/johndemic/Development/projects/revenium/hermes-revenium && grep -c 'Verified (Phase 4)' .planning/REQUIREMENTS.md && grep -n '\-\-operation-type CHAT (WIRE-01 / D-22 discharged in Phase 4)' .planning/REQUIREMENTS.md && (! grep -q 'no explicit --operation-type' .planning/REQUIREMENTS.md) && grep -n '04-01-PLAN.md' .planning/ROADMAP.md && grep -n 'per-turn attribution' skills/revenium/references/setup.md && test -f .planning/phases/04-wire-enrichment/04-01-SUMMARY.md && grep -c '^## Decisions Honored\|^### D-22\|^### D-23\|^### D-24\|^### D-25\|^## Operator Verification' .planning/phases/04-wire-enrichment/04-01-SUMMARY.md && python3 -m unittest tests.test_repository.RepositoryTests.test_no_legacy_branding_left tests.test_repository.RepositoryTests.test_setup_md_has_mechanical_classification_hook_section -v 2>&1 | tail -10 && python3 -m unittest discover -s tests -p 'test_*.py' 2>&1 | tail -5</automated>
  </verify>
  <done>
    WIRE-01..04 + COMPAT-01 checkboxes flipped and traceability rows updated in REQUIREMENTS.md; CRON-07 description prose updated in place (`no explicit --operation-type` → `--operation-type CHAT (WIRE-01 / D-22 discharged in Phase 4)`) for internal consistency; ROADMAP.md Phase 4 row finalized (1 plan, 04-01-PLAN.md listed, Progress Table updated to In progress); D-25 attribution-semantics paragraph appended to `setup.md::## How attribution works`; 04-01-SUMMARY.md created with full frontmatter + 7 body sections including all 4 decisions and operator UAT prompt; all four files commit together in one atomic commit; legacy-branding test + mechanical-classification-hook section invariants green.
  </done>
</task>

</tasks>

<verification>
**Per-task automated verification** is specified in each task's `<verify>` block. Plan-level verification:

1. **Full test suite green after every commit:** `python3 -m unittest discover -s tests -p 'test_*.py' -v` reports OK after T01, T02, T03, T04, T05 (five atomic commits expected; the per-commit green-test mandate is non-negotiable).

2. **Test count delta:** Pre-Phase-4 = 37 tests. Post-Phase-4 = 39 tests (2 new methods; 1 method has one assertion flipped but the method is still counted as 1 test).

3. **Bash syntax check:** `bash -n skills/revenium/scripts/hermes-report.sh` exits 0 (covered by `test_shell_scripts_have_valid_syntax` in the suite — pinned).

4. **Legacy-branding guard:** `test_no_legacy_branding_left` passes against every file edited in this plan (covered by the suite).

5. **Setup.md section invariant:** `test_setup_md_has_mechanical_classification_hook_section` passes — Phase 6's section header at `## Mechanical classification hook` is byte-unchanged after the D-25 paragraph append.

6. **REQUIREMENTS.md internal consistency:** CRON-07 description prose no longer contradicts WIRE-01. `grep -n 'no explicit --operation-type' .planning/REQUIREMENTS.md` returns zero matches after T05; the new text `--operation-type CHAT (WIRE-01 / D-22 discharged in Phase 4)` is present in CRON-07's bullet.

7. **Atomic-commit pitfalls verified:**
   - Pitfall 1 (pipe field count): split_rows Python emits 11 fields AND bash while-read consumes 11 variables in the SAME commit (T02). `d_cost` field continues to be exactly the 9th pipe-delimited token, byte-unchanged for the universal empty-agent/trace_id case.
   - Pitfall 2 (COMPAT-01 atomic flip): production --operation-type CHAT addition AND test assertion flip from assertNotIn → assertEqual land in the SAME commit (T01).
   - Pitfall 3 (colon-dash mandatory): grep confirms `${m_agent:-Hermes}` and `${m_trace:-${sid}}` (not bare-dash forms).
   - Pitfall 6 (G-03 sentinel filter bypass): new test fixtures all use `started_at = 1715514000.0` so the cron settle-window filter is bypassed and run_cron returns the expected invocation count per case.

8. **Operator UAT (manual; documented in SUMMARY):** Mac Studio cron tick produces marker-driven rows with `--operation-type CHAT` on the wire and zero-marker fallthrough rows with `--operation-type CHAT`; Revenium dashboard reflects the per-turn taskType for marker-driven rows.
</verification>

<success_criteria>
Plan 04-01 is complete when ALL of:

- [ ] T01 (WIRE-01 + COMPAT-01 atomic): `--operation-type "CHAT"` line present in zero-marker fallthrough cmd array AT line ~636 of hermes-report.sh; test_cron_marker_split_end_to_end's COMPAT-01 sub-case 2 asserts `flags.get('--operation-type') == 'CHAT'`. Both changes in one commit.
- [ ] T02 (WIRE-02 + WIRE-03 atomic): split_rows Python heredoc emits 11 pipe fields including `m_agent` and `m_trace`; bash while-read declaration consumes all 11; cmd array uses `${m_agent:-Hermes}` and `${m_trace:-${sid}}` colon-dash fallbacks. All three edits in one commit.
- [ ] T03: test_wire_agent_trace_passthrough method present with positive + fallback sub-cases; passes when run in isolation.
- [ ] T04: test_wire_no_provider_regression_per_class method present with 8-case DRY loop; all 8 cases pass including meta (--model-source ABSENT), openrouter-special (prefix strip + override), bedrock-special (prefix strip + override + colon preserved).
- [ ] T05: WIRE-01..04 + COMPAT-01 checkboxes flipped + CRON-07 description prose updated for WIRE-01 consistency in REQUIREMENTS.md; ROADMAP.md Phase 4 row shows `Plans: 1 plan` + 04-01-PLAN.md in Plans list + Progress Table In progress; D-25 attribution-semantics paragraph appended to setup.md::## How attribution works (Phase 6 section header byte-unchanged); 04-01-SUMMARY.md exists with frontmatter + 7 body sections including all 4 decisions, operator UAT prompt, and key-files list. All four files commit together in one atomic commit.
- [ ] Full test suite green after EVERY commit (39 tests after T03+T04 land).
- [ ] No commits skip hooks (no `--no-verify`); legacy-branding guard green throughout.
- [ ] Phase 4 ships within ~50% context budget across the 5 atomic commits.

Then the plan is ready for `/gsd-verify-work 4` followed by operator UAT on Mac Studio.
</success_criteria>

<output>
After completion, the executor creates `.planning/phases/04-wire-enrichment/04-01-SUMMARY.md` (as part of Task 5) capturing:
- Requirements verified (WIRE-01, WIRE-02, WIRE-03, WIRE-04, COMPAT-01)
- Decisions honored (D-22 / D-23 / D-24 / D-25 — with the D-22 gate-discharge evidence referenced)
- Files modified + tests added/modified
- Verification evidence (test suite output + bash -n exit code)
- Operator UAT prompt (Mac Studio end-to-end run + Revenium dashboard inspection)
</output>
