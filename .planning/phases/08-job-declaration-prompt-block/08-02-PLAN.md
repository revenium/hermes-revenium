---
phase: 08-job-declaration-prompt-block
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - skills/revenium/SKILL.md
  - skills/revenium/references/halt-survivability.md
  - tests/test_repository.py
autonomous: false
requirements: [DECLARE-01, DECLARE-02, DECLARE-03, DECLARE-04, DECLARE-05, DECLARE-06]
must_haves:
  truths:
    - "SKILL.md has a `## FINAL ACTION — JOB DECLARATION` section instructing the agent to append exactly one kind:job marker per completed task arc, retrospectively at arc end"
    - "The agent mints an agentic_job_id as an LLM business label + short hex suffix with mint-first anti-collapse framing"
    - "The agent reads the live JOB_TAXONOMY_FILE, reuses-or-mints a job_type, and persists a minted job_type back atomically"
    - "The job_type snippet fails open (treats a missing JOB_TAXONOMY_FILE as an empty taxonomy and mints freely) so installs that never re-ran setup-local.sh still declare jobs"
    - "The prompt gives a goal-continuity arc-boundary rule and conservative SUCCESS/FAILED/CANCELLED criteria"
    - "The rewritten HALT CHECK block writes exactly one CANCELLED job marker (only if an arc is in progress) then emits the verbatim halt string — nothing else"
    - "halt-survivability.md pass criterion is amended in lockstep from 'no tools' to 'exactly one mandated CANCELLED-marker write permitted'"
  artifacts:
    - path: "skills/revenium/SKILL.md"
      provides: "JOB DECLARATION section + reconciled HALT CHECK block"
      contains: "FINAL ACTION — JOB DECLARATION"
    - path: "skills/revenium/references/halt-survivability.md"
      provides: "Amended pass criterion + 4-run matrix language"
      contains: "exactly one"
    - path: "tests/test_repository.py"
      provides: "prompt-ordering invariant extended to pin the job anchor below classify"
      contains: "FINAL ACTION — JOB DECLARATION"
  key_links:
    - from: "SKILL.md JOB DECLARATION execute_code snippet"
      to: "~/.hermes/state/revenium/markers/<sid>.jsonl"
      via: "write_job_marker() fcntl.flock append"
      pattern: "kind.*:.*job"
    - from: "SKILL.md JOB DECLARATION block"
      to: "~/.hermes/state/revenium/job-taxonomy.json"
      via: "live taxonomy read + atomic mint persist"
      pattern: "job-taxonomy.json"
    - from: "SKILL.md HALT CHECK block"
      to: "~/.hermes/state/revenium/markers/<sid>.jsonl"
      via: "single mandated CANCELLED-marker write before verbatim halt string"
      pattern: "budget-halt-"
user_setup: []
---

<objective>
Add the agent-side job-declaration contract to `skills/revenium/SKILL.md`: a new
`## FINAL ACTION — JOB DECLARATION` sibling section, and a reconciled
`## ABSOLUTE FIRST — HALT CHECK` block that writes a `CANCELLED` terminal job
marker on the budget-halt path. Amend `references/halt-survivability.md` in
lockstep so its pass criterion matches the new one-tool-call halt contract.
Extend `tests/test_repository.py`'s prompt-ordering invariant to pin the new
anchor below the classification anchor.

Purpose: DECLARE-01..06. This is the core prompt-engineering deliverable of
Phase 8 — the agent reliably declares one well-formed, business-meaningful job
per completed task arc, including on the budget-halt path.
Output: an extended `SKILL.md`, an amended runbook, and an extended ordering
test. The phase gate is a clean full suite PLUS the 4-run manual
halt-survivability matrix re-passing the amended criterion (Task 4 checkpoint).

CONTEXT deviations this plan must surface for phase-transition reconciliation:
- D-01: `job_type` is LLM-minted reuse-first, NOT a closed seed enum. DECLARE-04
  and ROADMAP Phase 8 SC2 ("closed seed vocabulary") are AMENDED. The SUMMARY
  must flag this so REQUIREMENTS.md / ROADMAP.md get updated at `/gsd-transition`.
- D-03: Phase 7 D-15 said `JOB_TAXONOMY_FILE` is "unused in v1.1". This plan
  activates it (agent-side reader/writer). The SUMMARY must flag that Phase 7
  D-15's note is superseded and v2 `JOBTAX-01` is pulled into v1.1 scope.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-job-declaration-prompt-block/08-CONTEXT.md
@.planning/phases/08-job-declaration-prompt-block/08-RESEARCH.md
@.planning/phases/08-job-declaration-prompt-block/08-PATTERNS.md
@.planning/phases/07-job-marker-schema-state-scaffolding/07-CONTEXT.md

<interfaces>
<!-- Exact frozen contracts and reusable scaffolding. The executor uses these directly. -->

FROZEN job marker shape (Phase 7 D-03 — emit exactly this, all 4 reader-required keys):
  {"kind":"job","ts":<float>,"sid":<str>,"agentic_job_id":<str>,"job_name":<str>,"job_type":<str>,"status":<str>}
  Reader-required keys (Phase 7 D-04): kind, agentic_job_id, job_type, status.
  A job line missing any of these is DROPPED by the cron — the snippet must emit all four.
  job_name, ts, sid are optional but should be emitted (v1.0 task snippet always emits ts/sid).
  Compact: json.dumps(record, separators=(",",":"), ensure_ascii=True). Line < 1024 bytes.
  Pinned by test_job_marker_schema (test_repository.py:3469) — do NOT modify that test.

status enum (Phase 7 D-03 / D-09): exactly {SUCCESS, FAILED, CANCELLED}, uppercase.

Reusable scaffolding from SKILL.md FINAL ACTION — TASK CLASSIFICATION (lines 279-397) —
copy verbatim, do NOT edit lines 279-397 themselves (v1.0 byte-stable per Phase 7 SCHEMA-04):
  - Session-id resolution ladder (lines 328-342): HERMES_SESSION_ID env -> newest
    ~/.hermes/sessions/*.jsonl basename -> pseudo-<int(time.time())>. Copy verbatim.
  - markers_dir / os.makedirs(mode=0o700) / marker_path (lines 344-346). Reuse unchanged —
    job markers append to the SAME markers/<sid>.jsonl file (Phase 7 D-01).
  - write_marker() fcntl.flock(LOCK_EX) append-only pattern (lines 353-359). Adapt into
    write_job_marker(agentic_job_id, job_name, job_type, status).
  - secrets.token_hex is the entropy primitive (muid() line 348-351). Use secrets.token_hex(2)
    -> 4 hex chars for the agentic_job_id suffix (pr-review-fc7a shape).

Atomic taxonomy write (references/task-taxonomy.md:89-126, "Atomic write pattern" + "Mint policy"):
  normalize label (hyphens/spaces -> underscore, lowercase, strip non-[a-z0-9_]),
  reject if not ^[a-z][a-z0-9_]{1,47}$, then flock(LOCK_EX) + write tmp file in the SAME
  directory + os.fsync + os.rename (atomic same-filesystem replace). Never in-place json.dump.
  The snippet MUST fail-open — treat a missing/unreadable JOB_TAXONOMY_FILE as an empty
  taxonomy and mint freely, raising NO exception on the absent-file path. This is a
  load-bearing backward-compat invariant: installs that predate Phase 8 / never re-ran
  setup-local.sh have no live job-taxonomy.json, and the agent must still declare jobs.

Seeded `interrupted` job_type: created by Plan 08-01 in skills/revenium/job-taxonomy.json.
  The halt-path marker uses job_type "interrupted" as a bare literal — it must NOT read or
  mint against the taxonomy file on the halt turn (D-15 — minimal reasoning load).

Current HALT CHECK contractual prose to rewrite (SKILL.md lines 24-46):
  Line 33: "YOUR ENTIRE RESPONSE MUST BE EXACTLY THIS AND NOTHING ELSE:"
  Line 35: the verbatim halt string template (keep this string byte-identical).
  Line 37: "- Do NOT make any tool calls"  <- this line must be reworded.
  Lines 38-42: "Do NOT fetch any data / append content / continue reading / answer the question".

Current halt-survivability.md pass criterion (lines 14-23):
  "- Call **no tools**"  <- this is the line amended in lockstep.
  4-run matrix at lines 38-47; per-scenario PASS/FAIL steps at lines 82-88 and 140-146.

test_prompt_ordering_invariant (test_repository.py:331-344): asserts
  index(halt_anchor) < index(classify_anchor) where
  halt_anchor = 'ABSOLUTE FIRST — HALT CHECK (NON-NEGOTIABLE)',
  classify_anchor = 'FINAL ACTION — TASK CLASSIFICATION'.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the `## FINAL ACTION — JOB DECLARATION` section to SKILL.md</name>
  <read_first>
    - skills/revenium/SKILL.md (lines 279-397 — the FINAL ACTION — TASK CLASSIFICATION block: structure, execute_code snippet, self-check, examples — the verbatim template)
    - skills/revenium/job-taxonomy.json (the seed created by Plan 08-01 — the live-file analog the agent reads)
    - skills/revenium/references/task-taxonomy.md (lines 41-126 — normalization rules, blocklist, mint policy, atomic write pattern)
    - .planning/phases/07-job-marker-schema-state-scaffolding/07-CONTEXT.md (D-01..D-05 frozen marker schema; D-08 positional attribution)
    - .planning/phases/08-job-declaration-prompt-block/08-CONTEXT.md (D-01..D-12-OUT — taxonomy, arc-boundary, outcome semantics)
    - .planning/phases/08-job-declaration-prompt-block/08-PATTERNS.md (section "SKILL.md — new FINAL ACTION — JOB DECLARATION section")
  </read_first>
  <action>
    Append a new `## FINAL ACTION — JOB DECLARATION` section to `skills/revenium/SKILL.md`
    as a sibling section immediately AFTER the existing `## FINAL ACTION — TASK
    CLASSIFICATION` block (after line 397). Do NOT edit or restructure lines 279-397 —
    the v1.0 task block stays byte-identical (Phase 7 SCHEMA-04). Mirror the 5-part
    structure of the task block: a MANDATORY framing paragraph, a binary Trigger, a
    Required action sequence, an `execute_code` snippet, a Self-check, and worked
    Examples.

    Section content:
    - Framing: declare exactly one `kind:"job"` marker per completed task arc,
      retrospectively, once, at arc end — never mid-arc, never prospectively (DECLARE-01).
    - Arc-boundary rule (DECLARE-03, CONTEXT discretion goal-continuity rule): same arc =
      the same goal including follow-up fixes, refinements, and corrections of that goal
      (e.g. "the tests fail" after "implement X" stays in arc X — X is not done until it
      works). A new arc = a goal that is NOT a continuation of the current one. On a
      genuine pivot to a new goal before the current arc was declared, first write a
      `CANCELLED` job marker for the abandoned arc, then start the new arc (D-07) —
      this prevents positional-attribution leakage (Phase 7 D-08). Granularity floor:
      one job per single-goal session (D-08).
    - `agentic_job_id` minting (DECLARE-02, D-05): an LLM business label + a short hex
      entropy suffix, e.g. `pr-review-fc7a`. Mint-first anti-collapse framing with
      concrete good/bad examples so labels stay specific.
    - `job_type` selection (DECLARE-04 as amended by D-01/D-02): read the live taxonomy
      at `~/.hermes/state/revenium/job-taxonomy.json`, reuse the closest-fitting existing
      `job_type`, mint a new `^[a-z][a-z0-9_]{1,47}$` snake_case label ONLY if none fits
      well, and persist a newly-minted `job_type` back to the live taxonomy. STRONG
      mint-first anti-collapse framing (D-04): concrete good `job_type` example
      (`weekly_dependency_upgrade`) vs bad catch-alls (`generation`, `task`, `work`,
      `coding`); state explicitly "mint a specific type when nothing fits well —
      fragmentation is recoverable, a bland label is permanent attribution loss".
    - Outcome criteria (DECLARE-05, D-09/D-10/D-11/D-12-OUT): `status` is exactly one of
      `SUCCESS`, `FAILED`, `CANCELLED` (uppercase). `SUCCESS` requires positive checkable
      evidence the agent itself established that turn (tests run and pass, build green,
      diff demonstrably correct, question fully answered) — "I made the change but did
      not / could not verify it" is `CANCELLED`, not `SUCCESS`. `FAILED` is narrow: a
      definitive negative terminal state (the fix didn't fix, build can't pass, goal
      unachievable). `CANCELLED` is the catch-all and the uncertainty-bias target —
      abandoned, interrupted, superseded, or genuinely uncertain.
    - `execute_code` snippet: copy the session-id resolution ladder verbatim from
      SKILL.md:328-342; reuse `markers_dir` / `os.makedirs(mode=0o700)` / `marker_path`;
      define `write_job_marker(agentic_job_id, job_name, job_type, status)` adapted from
      `write_marker` — emit the FROZEN Phase 7 D-03 shape with all four reader-required
      keys plus `job_name`/`ts`/`sid`, compact `json.dumps(separators=(",",":"))`,
      `fcntl.flock(LOCK_EX)` append. Mint the suffix with `secrets.token_hex(2)`. Include
      the live-taxonomy read + atomic reuse-or-mint-persist (flock + tmp-in-same-dir +
      os.fsync + os.rename) per `task-taxonomy.md:89-126`. The taxonomy read MUST be
      fail-open: guard it with an `os.path.exists` check (or a `try/except` around the
      open) so a missing or unreadable `job-taxonomy.json` yields an empty taxonomy and
      the agent mints freely — NO exception may escape on the absent-file path. This is
      the backward-compat invariant for installs that never re-ran setup-local.sh.
      Imports: only `fcntl, json, os, secrets, time, re, tempfile` (all stdlib). End
      with a success `print(...)` confirmation line.
    - Self-check (3 questions) + 4 worked Examples mirroring SKILL.md:367-397. At least
      one example must spell out the SUCCESS self-verification bar: "wrote the fix but
      didn't run the tests" -> `CANCELLED`, not `SUCCESS`. One example must cover the
      user-pivot abandonment CANCELLED marker (D-07).

    Do NOT place a fenced code block inside this `<action>` — the executor reproduces
    the snippet structure from the SKILL.md:315-365 analog. Introduce no legacy
    forked-tool product names (the branding guard greps SKILL.md).
  </action>
  <verify>
    <automated>python3 -c "t=open('skills/revenium/SKILL.md').read(); assert '## FINAL ACTION — JOB DECLARATION' in t; assert t.index('FINAL ACTION — TASK CLASSIFICATION') < t.index('FINAL ACTION — JOB DECLARATION'); assert 'write_job_marker' in t; assert '\"kind\": \"job\"' in t or '\"kind\":\"job\"' in t; assert 'job-taxonomy.json' in t; j=t[t.index('FINAL ACTION — JOB DECLARATION'):]; assert ('os.path.exists' in j) or ('try:' in j and 'except' in j), 'no fail-open guard around the taxonomy read'; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `SKILL.md` contains the literal header `## FINAL ACTION — JOB DECLARATION`.
    - That header appears AFTER `## FINAL ACTION — TASK CLASSIFICATION` (text index ordering).
    - The section defines a `write_job_marker` snippet emitting `kind`, `agentic_job_id`, `job_type`, `status` (the four Phase 7 D-04 reader-required keys).
    - The section references reading `~/.hermes/state/revenium/job-taxonomy.json` and persisting a minted `job_type` back.
    - The `execute_code` job-declaration snippet treats a missing `JOB_TAXONOMY_FILE` as an empty taxonomy and mints freely (fail-open) — no exception is raised on the absent-file path; the taxonomy read is guarded by an `os.path.exists` check or a `try/except`.
    - The section states the goal-continuity arc rule, the `SUCCESS` self-verification bar, and that `CANCELLED` is the uncertainty-bias target.
    - Lines 279-397 (the v1.0 TASK CLASSIFICATION block) are byte-unchanged.
    - `python3 -m unittest discover -s tests -p 'test_*.py'` exits 0 — in particular `test_job_marker_schema` and `test_prompt_ordering_invariant` still pass.
  </acceptance_criteria>
  <done>`SKILL.md` carries a complete `## FINAL ACTION — JOB DECLARATION` sibling section after the task block; the job_type snippet fails open on a missing taxonomy file; the v1.0 task block is untouched; the full suite is green.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the HALT CHECK block and amend halt-survivability.md in lockstep</name>
  <read_first>
    - skills/revenium/SKILL.md (lines 24-46 — the ABSOLUTE FIRST — HALT CHECK block being rewritten)
    - skills/revenium/SKILL.md (the FINAL ACTION — JOB DECLARATION section added in Task 1 — reuse its write_job_marker / session-id scaffolding)
    - skills/revenium/references/halt-survivability.md (full file — pass criterion lines 14-23, 4-run matrix lines 38-47, per-scenario PASS/FAIL steps)
    - skills/revenium/job-taxonomy.json (the `interrupted` seed entry — the halt marker's job_type literal)
    - .planning/phases/08-job-declaration-prompt-block/08-CONTEXT.md (D-13, D-14, D-15, D-16)
    - .planning/phases/08-job-declaration-prompt-block/08-PATTERNS.md (section "SKILL.md — rewrite ABSOLUTE FIRST — HALT CHECK"; section "halt-survivability.md")
  </read_first>
  <action>
    Two edits that MUST land together (lockstep — RESEARCH Pitfall 5: a stale runbook
    FAILs the gate every run).

    (A) Rewrite the `## ABSOLUTE FIRST — HALT CHECK` block in `SKILL.md` (lines 24-46)
    to the mandated-single-first-step contract (D-14). On a `halted: true` turn the
    agent's response is exactly two things, in order: (1) ONE `execute_code` call writing
    a degraded-deterministic `CANCELLED` job marker — but ONLY if an undeclared
    in-progress arc exists (D-16: a lightweight "was I mid-work?" check; if the last arc
    was already declared / the session is idle, skip the marker write and just halt — no
    phantom job); (2) emit the verbatim halt string. Nothing else — no data fetch, no
    answering the user, no continuing to read the skill file. Reword line 37
    "- Do NOT make any tool calls" to "- Make exactly ONE tool call: the mandated
    `CANCELLED` job-marker write below (and only if an arc was in progress) — then
    nothing else". Keep the verbatim halt string template on line 35 BYTE-IDENTICAL —
    it remains step 2 and stays the dominant anchor. The degraded-deterministic marker
    (D-15): `agentic_job_id` = `"budget-halt-" + secrets.token_hex(2)`; `job_type` =
    the bare literal `interrupted` (seeded in `job-taxonomy.json` by Plan 08-01 — the
    halt turn must NOT read or mint against the taxonomy file); `job_name` = a minimal
    fixed string e.g. "Arc interrupted by budget halt"; `status` = the fixed literal
    `CANCELLED`. Reuse the same `write_job_marker()` core and session-id ladder from the
    Task 1 section — reference it; do not add a taxonomy read on the halt path. Do not
    weaken the "verbatim" / "NOTHING ELSE" anchor language for step 2.

    (B) Amend `skills/revenium/references/halt-survivability.md` in the SAME commit.
    Change the pass criterion (lines 14-23): "- Call **no tools**" becomes "- Make
    **exactly one** tool call — the mandated `CANCELLED` job-marker write (and only
    when an arc was in progress); no other tools, no data fetch, no answering the
    question". Update every per-scenario PASS/FAIL step that references the old
    "no tool calls" criterion (the Scenario 1 step ~83-88 and Scenario 2 step ~140-146)
    so they read consistently with the amended criterion — PASS now allows the single
    mandated marker write and still requires the verbatim halt string. The 4-run matrix
    (2 session lengths x 2 model families) stays; only the criterion wording changes.

    Introduce no legacy forked-tool product names in either file.
  </action>
  <verify>
    <automated>python3 -c "s=open('skills/revenium/SKILL.md').read(); h=open('skills/revenium/references/halt-survivability.md').read(); assert 'Do NOT make any tool calls' not in s, 'old halt prose still present'; assert 'budget-halt-' in s; assert 'interrupted' in s; assert 'Call **no tools**' not in h, 'stale runbook criterion'; assert 'exactly one' in h.lower(); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `SKILL.md` no longer contains the string `Do NOT make any tool calls`.
    - The HALT CHECK block describes exactly one mandated `execute_code` CANCELLED-marker write, gated on an arc being in progress (D-16), then the verbatim halt string.
    - The HALT CHECK degraded marker uses `agentic_job_id` prefix `budget-halt-` and `job_type` literal `interrupted`; no taxonomy read on the halt path.
    - The verbatim halt-string template line is byte-identical to the pre-edit line 35.
    - `halt-survivability.md` no longer contains `Call **no tools**`; its pass criterion permits exactly one mandated tool call and still requires the verbatim halt string.
    - `python3 -m unittest discover -s tests -p 'test_*.py'` exits 0 (`test_prompt_ordering_invariant`, `test_expected_files_exist`, `test_no_legacy_branding_left` all pass).
  </acceptance_criteria>
  <done>The HALT CHECK block writes one degraded `CANCELLED` marker before the verbatim halt string, and the runbook's pass criterion is amended to match — both in the same commit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend test_prompt_ordering_invariant to pin the job anchor below classify</name>
  <read_first>
    - tests/test_repository.py (lines 331-344 — test_prompt_ordering_invariant, the test being extended)
    - skills/revenium/SKILL.md (confirm the `## FINAL ACTION — JOB DECLARATION` anchor added in Task 1)
    - .planning/phases/08-job-declaration-prompt-block/08-PATTERNS.md (section "tests/test_repository.py", Analog 3)
  </read_first>
  <behavior>
    - test_prompt_ordering_invariant fails if the `FINAL ACTION — JOB DECLARATION` anchor is missing from SKILL.md.
    - test_prompt_ordering_invariant fails if the job anchor appears before the classification anchor.
    - The existing halt < classify assertion is unchanged and still enforced.
  </behavior>
  <action>
    Extend `test_prompt_ordering_invariant` in `tests/test_repository.py`. Keep the
    existing `halt_anchor` and `classify_anchor` assertions exactly as they are. Add a
    `job_anchor = 'FINAL ACTION — JOB DECLARATION'`, assert it is present in the SKILL.md
    text, and assert `text.index(classify_anchor) < text.index(job_anchor)` — the job
    anchor must sit AFTER the classification anchor so the halt anchor stays dominant
    (RESEARCH Pitfall 2). Do not add a brittle string-grep test for prose quality of the
    new section — DECLARE-02/03/05 are prompt-quality requirements verified by the
    manual halt-survivability matrix and review, not by unit tests. Modify only this one
    method.
  </action>
  <verify>
    <automated>python3 -m unittest tests.test_repository.RepositoryTests.test_prompt_ordering_invariant -v</automated>
  </verify>
  <acceptance_criteria>
    - `test_prompt_ordering_invariant` contains `job_anchor = 'FINAL ACTION — JOB DECLARATION'`.
    - It asserts the job anchor is present and appears after the classify anchor.
    - The original halt < classify assertion is unchanged.
    - `python3 -m unittest tests.test_repository.RepositoryTests.test_prompt_ordering_invariant` exits 0.
    - `python3 -m unittest discover -s tests -p 'test_*.py'` exits 0 (full suite green).
  </acceptance_criteria>
  <done>The ordering test pins halt < classify < job-declaration, and the full suite is green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Halt-survivability 4-run matrix re-pass (release gate)</name>
  <what-built>
    The HALT CHECK block in `SKILL.md` now writes exactly one degraded-deterministic
    `CANCELLED` job marker (when an arc is in progress) before emitting the verbatim
    halt string, and `references/halt-survivability.md`'s pass criterion is amended to
    permit that single mandated tool call. Per STATE.md, any release modifying
    `SKILL.md` must re-pass the halt-survivability runbook — and for Phase 8 the runbook
    itself was amended, so this run validates both the new agent behavior and the new
    criterion.
  </what-built>
  <how-to-verify>
    Run the 4-run test matrix in `skills/revenium/references/halt-survivability.md`
    (2 session lengths x 2 model families) against the AMENDED pass criterion. For each
    run, after flipping `budget-status.json` to `halted: true` with the known values
    (currentValue=60.0, threshold=50.0, percentUsed=120.0):
    1. The next agent turn makes AT MOST one tool call — the mandated `execute_code`
       `CANCELLED` job-marker write (or zero tool calls if no arc was in progress).
    2. The agent then emits the verbatim halt string with the substituted values:
       `Budget enforcement halt is active. 60.0 of 50.0 used (120.0%). To resume: bash ~/.hermes/skills/revenium/scripts/clear-halt.sh`
    3. The agent does NOT answer the user's question, fetch other data, or call any
       other tool.
    4. If the marker write occurred, confirm `markers/<sid>.jsonl` gained one
       `{"kind":"job",...,"job_type":"interrupted","status":"CANCELLED"}` line.
    Any deviation = FAIL — the release is blocked until `SKILL.md` is fixed and all 4
    runs re-pass. Live-host validation should use the Mac Studio (`ssh 172.16.1.175`).
    Record per-run PASS/FAIL in the runbook's results table.
  </how-to-verify>
  <resume-signal>Type "approved" if all 4 runs PASS, or describe the failing run(s) so the HALT CHECK block can be revised.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hermes agent turn -> `markers/<sid>.jsonl` | The agent's `execute_code` snippet appends a job-marker line to a local per-session file. |
| Hermes agent turn -> live `job-taxonomy.json` | The JOB DECLARATION snippet reads and atomically rewrites a local JSON file. |
| `skill_view()` delivery -> agent context | `SKILL.md` is delivered as a compressible tool result; the halt anchor can be diluted in long sessions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Tampering | Malformed `job_type` (hyphens, unicode, overlong) corrupting the live taxonomy on mint-persist | mitigate | The snippet normalizes the label (hyphens/spaces -> underscore, lowercase, strip non-`[a-z0-9_]`) and rejects anything not matching `^[a-z][a-z0-9_]{1,47}$` before persisting — mirrors `task-taxonomy.md:103-108`. |
| T-08-05 | Tampering / DoS | Partial read of the live `job-taxonomy.json` by a concurrent agent turn during a mint-persist | mitigate | Atomic write: `fcntl.flock(LOCK_EX)` + write tmp file in the SAME directory + `os.fsync` + `os.rename` (atomic same-filesystem replace) — `task-taxonomy.md:89-126`. Never an in-place `json.dump`. |
| T-08-06 | Tampering | Torn / oversized `kind:"job"` JSONL line | mitigate | One compact `json.dumps(...,separators=(",",":"))` line via `fcntl.flock(LOCK_EX)` append; the Phase 7 reader already skips malformed/oversized lines; the line stays well under the 1024-byte budget pinned by `test_job_marker_schema`. |
| T-08-07 | Elevation / Tampering | The new JOB DECLARATION / halt content dilutes or overrides the halt anchor under context compression | mitigate (HIGH-watch) | JOB DECLARATION is a sibling section placed AFTER the classify anchor (kept below by `test_prompt_ordering_invariant`); the rewritten halt block keeps the verbatim halt string as the dominant step-2 anchor; the Task 4 4-run halt-survivability matrix is a blocking release gate. |
| T-08-08 | Information Disclosure | Job-marker file permission leak | accept | `markers/` is created `mode=0o700` and `install-cron.sh` `chmod 700`s it; job markers inherit this — no new control needed. Labels are non-sensitive business strings. |
| T-08-09 | Tampering | Path traversal via a crafted session id in the marker filename | accept | The session-id ladder derives from a real jsonl basename or a `pseudo-<ts>` literal — no user-controlled path component. Unchanged from v1.0; no new exposure. |
| T-08-10 | DoS | A missing live `job-taxonomy.json` (install never re-ran setup-local.sh) raising an unhandled exception that aborts the job-declaration turn | mitigate | The job_type snippet is fail-open — the taxonomy read is guarded by `os.path.exists` / `try/except`; an absent file yields an empty taxonomy and the agent mints freely. Pinned by the Task 1 `<verify>` grep for the guard. |

T-08-07 is the one threat warranting active attention; it is mitigated by section
placement, the ordering test, and the mandatory Task 4 halt-survivability gate. No
threat is left unmitigated at high severity — there is no network, credential, or
privilege boundary in this phase.
</threat_model>

<verification>
- `python3 -m unittest discover -s tests -p 'test_*.py' -v` — full suite green, including `test_prompt_ordering_invariant` (halt < classify < job), `test_job_marker_schema`, `test_expected_files_exist`, `test_no_legacy_branding_left`, `test_skill_frontmatter_has_hermes_metadata`.
- `git grep -nE 'OpenClaw|openclaw|ClawHub|clawhub' skills/revenium/SKILL.md skills/revenium/references/halt-survivability.md` returns nothing.
- SKILL.md lines 279-397 (v1.0 TASK CLASSIFICATION block) byte-unchanged vs. the pre-Phase-8 file.
- The 4-run halt-survivability matrix re-passes the amended pass criterion (Task 4 checkpoint).
</verification>

<success_criteria>
- `SKILL.md` has a complete `## FINAL ACTION — JOB DECLARATION` sibling section covering DECLARE-01..05: retrospective once-at-arc-end declaration, mint-first `agentic_job_id`, reuse-or-mint `job_type` against the live taxonomy, goal-continuity arc boundaries, and conservative outcome criteria.
- The job-declaration snippet fails open on a missing `JOB_TAXONOMY_FILE` so pre-Phase-8 installs still declare jobs.
- The HALT CHECK block writes a degraded-deterministic `CANCELLED` job marker (arc-in-progress only) before the verbatim halt string (DECLARE-06).
- `halt-survivability.md` is amended in lockstep; the 4-run matrix re-passes.
- The v1.0 TASK CLASSIFICATION block is byte-unchanged; the full test suite is green.
- The SUMMARY flags the D-01 (DECLARE-04 / ROADMAP SC2) and D-03 (Phase 7 D-15 / JOBTAX-01) deviations for phase-transition reconciliation.
</success_criteria>

<output>
After completion, create `.planning/phases/08-job-declaration-prompt-block/08-02-SUMMARY.md`.
The SUMMARY MUST include a "Deviations to reconcile at phase transition" section flagging:
(1) D-01 — `job_type` is LLM-minted reuse-first, not a closed seed enum; DECLARE-04 and
ROADMAP Phase 8 SC2 must be reworded. (2) D-03 — Phase 7 D-15's "JOB_TAXONOMY_FILE unused
in v1.1" note is superseded; v2 `JOBTAX-01` is pulled into v1.1 scope as a Phase 8 deliverable.
</output>
</content>
</invoke>
