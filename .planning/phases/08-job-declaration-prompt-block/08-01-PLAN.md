---
phase: 08-job-declaration-prompt-block
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/revenium/job-taxonomy.json
  - examples/setup-local.sh
  - tests/test_repository.py
autonomous: true
requirements: [DECLARE-04]
must_haves:
  truths:
    - "A seed skills/revenium/job-taxonomy.json ships with the skill, mirroring the task-taxonomy.json schema"
    - "The seed taxonomy includes an `interrupted` entry so the budget-halt path never triggers a fresh mint"
    - "setup-local.sh copies the seed to the live JOB_TAXONOMY_FILE no-clobber on fresh install"
    - "test_repository.py presence-enforces job-taxonomy.json and pins its schema"
  artifacts:
    - path: "skills/revenium/job-taxonomy.json"
      provides: "Seed job-type vocabulary (exactly 11 entries: 10 business labels + interrupted)"
      contains: "labels"
    - path: "examples/setup-local.sh"
      provides: "Seed->live no-clobber copy of job-taxonomy.json"
      contains: "JOB_TAXONOMY_DEST"
    - path: "tests/test_repository.py"
      provides: "job-taxonomy.json presence + schema invariants"
      contains: "test_job_taxonomy_file_schema"
  key_links:
    - from: "examples/setup-local.sh"
      to: "~/.hermes/state/revenium/job-taxonomy.json"
      via: "no-clobber cp gated on [[ ! -f ]]"
      pattern: "cp .*job-taxonomy.json"
    - from: "tests/test_repository.py"
      to: "skills/revenium/job-taxonomy.json"
      via: "test_expected_files_exist + test_job_taxonomy_file_schema"
      pattern: "job-taxonomy.json"
---

<objective>
Ship the Phase 8 seed-data substrate: a new `skills/revenium/job-taxonomy.json`
seed file, the seed->live copy wiring in `examples/setup-local.sh`, and the
`tests/test_repository.py` invariants that presence-enforce and schema-pin the
seed. This is the Wave 0 / foundation plan — it creates the artifacts the
Wave 2 `SKILL.md` JOB DECLARATION block reads and references (the seeded
`interrupted` entry in particular).

Purpose: DECLARE-04 (amended by CONTEXT D-01) — the agent mints `job_type`
LLM-first, reuse-first against a live `job-taxonomy.json`. This plan provides
the shipped seed and its install path; the agent-side reader/writer is Wave 2.
Output: a test-pinned seed file plus an install-script seed-copy block, all
bash-3.2-safe and stdlib-only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-job-declaration-prompt-block/08-CONTEXT.md
@.planning/phases/08-job-declaration-prompt-block/08-RESEARCH.md
@.planning/phases/08-job-declaration-prompt-block/08-PATTERNS.md

<interfaces>
<!-- Exact analog structures the executor mirrors. No codebase exploration needed. -->

Seed task-taxonomy.json schema (skills/revenium/task-taxonomy.json) — the schema to mirror:
  Top-level single key "labels" -> object mapping label -> descriptor.
  Each descriptor has exactly two keys: "description" (string) and "examples" (list of strings).
  Label regex: ^[a-z][a-z0-9_]{1,47}$
  Blocklist (never seed): ack, acknowledgment, greeting, confirmation, hello, thanks

setup-local.sh task-taxonomy.json copy block (examples/setup-local.sh:14-22) — the analog:
  STATE_DIR_DEFAULT="${REVENIUM_STATE_DIR:-${HOME}/.hermes/state/revenium}"
  TAXONOMY_DEST="${REVENIUM_TAXONOMY_FILE:-${STATE_DIR_DEFAULT}/task-taxonomy.json}"
  mkdir -p "$(dirname "${TAXONOMY_DEST}")"
  if [[ ! -f "${TAXONOMY_DEST}" ]]; then cp ... ; echo "Seeded ..."; else echo "... not overwriting"; fi
  REPO_ROOT is already defined at setup-local.sh:4. STATE_DIR_DEFAULT is already defined at line 14.
  Script uses `set -euo pipefail` (line 2). New block must be bash-3.2-safe (cp + [[ ! -f ]] only).

common.sh env-var override for the live job taxonomy path (Phase 7 D-13, already declared):
  REVENIUM_JOB_TAXONOMY_FILE  -> live path default ${STATE_DIR}/job-taxonomy.json

test_repository.py analogs:
  - test_expected_files_exist (lines 56-86): a list `expected` of Path objects asserted to exist.
    SKILL / 'task-taxonomy.json' is at line 64 — add SKILL / 'job-taxonomy.json' beside it.
  - test_taxonomy_file_schema (lines 139-160): schema test for task-taxonomy.json. NOTE its
    lines 150-153 pin an EXACT ordered expected_labels list — the new job test MUST NOT do this
    (D-04: seed list is planner discretion). Pin schema + regex + blocklist + a minimum count only.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the job-taxonomy.json seed file</name>
  <read_first>
    - skills/revenium/task-taxonomy.json (the exact schema + descriptor shape to mirror)
    - skills/revenium/references/task-taxonomy.md (lines 17-72: Schema, Label normalization rules, Blocklist)
    - .planning/phases/08-job-declaration-prompt-block/08-RESEARCH.md (Code Examples — "Seed job-taxonomy.json shape"; D-04 entry list)
    - .planning/phases/08-job-declaration-prompt-block/08-PATTERNS.md (section "skills/revenium/job-taxonomy.json")
  </read_first>
  <behavior>
    - File parses as valid JSON with a single top-level "labels" object.
    - "labels" has exactly 11 entries: the 10 verbatim business labels seeded
      below plus the mandatory `interrupted` entry.
    - Every label key matches ^[a-z][a-z0-9_]{1,47}$.
    - No label is in the blocklist {ack, acknowledgment, greeting, confirmation, hello, thanks}.
    - Every descriptor has exactly the keys "description" (string) and "examples" (list).
    - An `interrupted` label is present (consumed by the Wave 2 halt-path marker per D-15/A4).
  </behavior>
  <action>
    Create `skills/revenium/job-taxonomy.json` mirroring the `task-taxonomy.json` schema
    exactly: a single top-level `labels` object mapping each `job_type` label to a
    descriptor with exactly two keys, `description` (string) and `examples` (list of
    strings). Seed exactly these 10 business-meaningful coding-agent job types per
    CONTEXT D-04 (the planner has already exercised the D-04 discretion — seed this
    list verbatim, do not substitute): `feature_development`, `bug_fix`, `code_review`,
    `refactoring`, `research`, `debugging`, `testing`, `documentation`, `devops`,
    `planning`. Add one more entry, `interrupted`, with a description noting it is the
    budget-halt / abandoned-arc terminal job type — this is mandated by D-15/A4 so the
    Wave 2 halt marker writes `job_type":"interrupted"` without touching the live
    taxonomy file. The seed file therefore has exactly 11 label entries total.
    Every label key must match `^[a-z][a-z0-9_]{1,47}$` and must NOT be a blocklisted
    label. Give each entry a one-sentence `description` and a 2-item `examples` list of
    realistic phrasings. Indent with 2 spaces, trailing newline. Do not introduce any
    legacy forked-tool product names (the branding guard greps this file).
  </action>
  <verify>
    <automated>python3 -c "import json,re; d=json.load(open('skills/revenium/job-taxonomy.json')); L=d['labels']; assert len(L)==11, len(L); rx=re.compile(r'^[a-z][a-z0-9_]{1,47}\$'); assert all(rx.match(k) for k in L); assert 'interrupted' in L; assert all(set(v)=={'description','examples'} for v in L.values()); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `skills/revenium/job-taxonomy.json` exists and parses as JSON.
    - Top-level object has exactly one key `labels`.
    - `labels` has exactly 11 entries (the 10 verbatim business labels + `interrupted`); all keys match `^[a-z][a-z0-9_]{1,47}$`; none blocklisted.
    - An `interrupted` entry is present.
    - Each descriptor has exactly `description` (str) and `examples` (list).
    - `python3 -m unittest tests.test_repository` does not regress (file is additive).
  </acceptance_criteria>
  <done>The seed `job-taxonomy.json` ships beside `task-taxonomy.json`, schema-identical, with exactly 11 label entries including `interrupted`, and the verify command prints `ok`.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the job-taxonomy.json seed->live copy into setup-local.sh</name>
  <read_first>
    - examples/setup-local.sh (lines 1-22 — REPO_ROOT, STATE_DIR_DEFAULT, the task-taxonomy.json copy block analog)
    - skills/revenium/scripts/common.sh (line ~25 — JOB_TAXONOMY_FILE / REVENIUM_JOB_TAXONOMY_FILE declaration)
    - .planning/phases/08-job-declaration-prompt-block/08-PATTERNS.md (section "examples/setup-local.sh")
  </read_first>
  <action>
    Add a parallel no-clobber seed-copy block to `examples/setup-local.sh` immediately
    after the existing `task-taxonomy.json` block (after line 22). Reuse the already-defined
    `REPO_ROOT` and `STATE_DIR_DEFAULT` variables — do not redeclare them. Define
    `JOB_TAXONOMY_DEST="${REVENIUM_JOB_TAXONOMY_FILE:-${STATE_DIR_DEFAULT}/job-taxonomy.json}"`
    (the `REVENIUM_JOB_TAXONOMY_FILE` env var matches the override declared in
    `common.sh`). Guard the copy with `if [[ ! -f "${JOB_TAXONOMY_DEST}" ]]`: on the
    true branch `cp "${REPO_ROOT}/skills/revenium/job-taxonomy.json" "${JOB_TAXONOMY_DEST}"`
    and `echo "Seeded ${JOB_TAXONOMY_DEST}"`; on the else branch echo a "not overwriting"
    message. Preserve the script's `set -euo pipefail` mode. Use only `cp` and
    `[[ ! -f ]]` — bash 3.2-safe, no bash-4 syntax (the live Mac Studio host is bash
    3.2.57). Do not add this copy to `install-cron.sh` — `setup-local.sh` is the only
    v1.0 seed-copy path (RESEARCH A1).
  </action>
  <verify>
    <automated>bash -n examples/setup-local.sh && grep -q 'JOB_TAXONOMY_DEST' examples/setup-local.sh && grep -q 'cp .*job-taxonomy.json' examples/setup-local.sh && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n examples/setup-local.sh` exits 0 (valid syntax).
    - The script contains a `JOB_TAXONOMY_DEST` assignment using `REVENIUM_JOB_TAXONOMY_FILE` with a `:-` fallback.
    - A `cp ... job-taxonomy.json ...` line gated by `[[ ! -f "${JOB_TAXONOMY_DEST}" ]]` is present.
    - `set -euo pipefail` on line 2 is unchanged; no bash-4 syntax introduced.
    - `python3 -m unittest tests.test_repository.RepositoryTests.test_shell_scripts_have_valid_syntax` still passes (note: this test globs `skills/revenium/scripts/*.sh`; `setup-local.sh` validity is covered by the `bash -n` above).
  </acceptance_criteria>
  <done>`setup-local.sh` performs a no-clobber seed->live copy of `job-taxonomy.json` to the `REVENIUM_JOB_TAXONOMY_FILE` path, bash-3.2-safe, and `bash -n` passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add job-taxonomy.json presence + schema invariants to test_repository.py</name>
  <read_first>
    - tests/test_repository.py (lines 56-86 test_expected_files_exist; lines 139-160 test_taxonomy_file_schema — the analog)
    - skills/revenium/job-taxonomy.json (the file under test, created in Task 1)
    - .planning/phases/08-job-declaration-prompt-block/08-PATTERNS.md (section "tests/test_repository.py", Analog 1 and Analog 2)
  </read_first>
  <behavior>
    - test_expected_files_exist fails if skills/revenium/job-taxonomy.json is removed.
    - test_job_taxonomy_file_schema passes for the Task 1 seed file.
    - test_job_taxonomy_file_schema fails if a label violates the regex, is blocklisted, or a descriptor is missing description/examples.
    - test_job_taxonomy_file_schema does NOT pin an exact ordered label list (D-04: seed list is planner discretion) — it asserts a minimum count of 8 instead.
  </behavior>
  <action>
    Edit `tests/test_repository.py`. (1) In `test_expected_files_exist`, add
    `SKILL / 'job-taxonomy.json'` to the `expected` list, immediately after the
    `SKILL / 'task-taxonomy.json'` entry. (2) Add a new method
    `test_job_taxonomy_file_schema` mirroring `test_taxonomy_file_schema` but for
    `SKILL / 'job-taxonomy.json'`: assert the file exists, parses as JSON, has a
    top-level `labels` dict, every label key matches `^[a-z][a-z0-9_]{1,47}$`, no label
    is in the forbidden set `{ack, acknowledgment, greeting, confirmation, hello,
    thanks}`, and every descriptor has a `description` (str) and `examples` (list).
    CRITICAL — unlike `test_taxonomy_file_schema` lines 150-153, do NOT assert an exact
    ordered `expected_labels` list; instead assert `len(labels) >= 8` (D-04 makes the
    seed list planner discretion — the test stays tolerant even though Task 1 ships
    exactly 11; the test guards a floor, not the exact seed). Place the new method
    directly after `test_taxonomy_file_schema`. Do not modify `test_taxonomy_file_schema`,
    `test_runtime_paths_are_hermes_native`, `test_job_marker_schema`, or any other
    existing test.
  </action>
  <verify>
    <automated>python3 -m unittest tests.test_repository.RepositoryTests.test_expected_files_exist tests.test_repository.RepositoryTests.test_job_taxonomy_file_schema -v</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_repository.py` contains `SKILL / 'job-taxonomy.json'` in `test_expected_files_exist`.
    - A method `test_job_taxonomy_file_schema` exists and asserts `labels` dict, label regex, blocklist, descriptor shape, and `len(labels) >= 8`.
    - `test_job_taxonomy_file_schema` does NOT contain an exact ordered `expected_labels` equality assertion.
    - `python3 -m unittest tests.test_repository.RepositoryTests.test_expected_files_exist tests.test_repository.RepositoryTests.test_job_taxonomy_file_schema` exits 0.
    - `python3 -m unittest discover -s tests -p 'test_*.py'` exits 0 (full suite green).
  </acceptance_criteria>
  <done>Both the presence test and the new schema test pass against the Task 1 seed, and the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo seed file -> live state file | `setup-local.sh` copies a shipped JSON seed into `~/.hermes/state/revenium/` on the operator's host. |
| test harness -> shipped artifacts | `test_repository.py` reads shipped files; no privilege crossing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | `job-taxonomy.json` seed content | accept | Static JSON shipped in-repo; reviewed at commit; `test_job_taxonomy_file_schema` enforces label regex and blocklist so a malformed seed fails the build. Low risk — no untrusted input, no network. |
| T-08-02 | Tampering | `setup-local.sh` seed-copy block clobbering an operator's customized live taxonomy | mitigate | No-clobber `[[ ! -f "${JOB_TAXONOMY_DEST}" ]]` guard — an existing live file is never overwritten, matching the v1.0 `task-taxonomy.json` block. |
| T-08-03 | Information Disclosure | live `job-taxonomy.json` written by `cp` into state dir | accept | `cp` preserves the parent dir's permissions; the state dir is already mode-controlled by v1.0 install (`install-cron.sh` `chmod`). No new secret material — job-type labels are non-sensitive. |

No high-severity threats. The plan ships a static JSON data file and a no-clobber
copy line; there is no network, credential, privilege boundary, or untrusted input
in scope.
</threat_model>

<verification>
- `python3 -m unittest discover -s tests -p 'test_*.py' -v` — full suite green, including the new `test_job_taxonomy_file_schema`.
- `bash -n examples/setup-local.sh` exits 0.
- `git grep -nE 'OpenClaw|openclaw|ClawHub|clawhub' skills/revenium/job-taxonomy.json examples/setup-local.sh` returns nothing (branding guard).
</verification>

<success_criteria>
- `skills/revenium/job-taxonomy.json` exists, schema-mirrors `task-taxonomy.json`, has exactly 11 entries (10 business labels + `interrupted`).
- `examples/setup-local.sh` performs a bash-3.2-safe no-clobber seed->live copy of the job taxonomy.
- `test_repository.py` presence-enforces and schema-pins the seed without pinning an exact label list.
- Full test suite is green; no legacy branding introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/08-job-declaration-prompt-block/08-01-SUMMARY.md`.
</output>
</content>
</invoke>
